# Changes vs upstream Calibre-Web-Automated

Tracks every divergence between this fork and `crocodilestick/Calibre-Web-Automated@main` since the fork point. Updated per release.

Format: each row is one fork-PR, mapped to its upstream PR or issue (if any), with a one-line description and the squash-merge SHA.

## Backports (upstream PRs we merged ahead of upstream review)

| Fork PR | Upstream | Author | Description | SHA | Release |
|---|---|---|---|---|---|
| #4 | [#1313](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1313) | @ikuma-hiroyuki | i18n(ja) fill empty msgstr + fix existing | `f5fc59a` | v4.0.6 |
| #5 | [#1291](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1291) | @sinyawskiy | i18n update messages.po | `67dce95` | v4.0.6 |
| #7 | [#1274](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1274) | @cu0uz | i18n update messages.po | `65ffc90` | v4.0.6 |
| #8 | [#1305](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1305) | @RFrens | i18n update messages.po | `f1dc937` | v4.0.6 |
| #9 | [#1267](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1267) | @area57 | i18n update messages.po | `4ebb7ae` | v4.0.6 |
| #11 | [#1296](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1296) | @julien-noblet | i18n(fr) fix lang | `1e1eca9` | v4.0.6 |
| #12 | [#1257](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1257) | @lexis11mob | i18n update messages.po | `188a854` | v4.0.6 |
| #13 | [#1298](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1298) | @rancur | Docker healthcheck `curl -fsL` so `/ → /login` 302 doesn't fail | `653a516` | v4.0.7 |
| #14 | [#1283](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1283) | @chloeroform | user-profile `fetch()` → `getPath()` for reverse-proxy path prefixes | `a5dd59c` | v4.0.7 |
| #15 | [#1322](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1322) | @Sycha | `.cbr` / `.cbz` → IANA mimetypes | `fc8ba00` | v4.0.7 |
| #16 | [#1096](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1096) | @SethMilliken | Safari metadata-search CSRF token | `6721638` | v4.0.7 |
| #17 | [#1213](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1213) | @hsttlrjeff | Kobo `HandleStateRequest` `.get("Location")` + `last_modified` | `1fd6c50` | v4.0.7 |
| #39 | [#1280](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1280) | @dbraendle | Set timestamp to current time for newly imported books | `f4b3535` | v4.0.16 |
| #41 | [#1231](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1231) | @burakemirsezen | Restore missing back button in epub reader | `1bdc4cc` | v4.0.16 |
| #42 | [#1226](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1226) | @antifuchs | Use `mkdir` rather than `install` to create the ingest directory | `7b86b41` | v4.0.16 |
| #44 | [#1236](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1236) | @scottjamesrogers | Fall back to original file when metadata export fails during email send | `b57e283` | v4.0.16 |
| #45 | [#1131](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1131) | @SethMilliken | cwasync: fix get-progress sync error for some older KOReader versions | `b868d85` | v4.0.16 |
| #38 | [#1233](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1233) | @scottjamesrogers | Magic shelf: add Series outerjoin so author-sort returns books | `6bafcdc` | v4.0.16 |
| #43 | [#1318](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1318) | @shavitmichael | Support kepub files in the web epub reader | `338f916` | v4.0.16 |
| #48 | [#1166](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1166) | @black-dragon74 | Kobo: pass highlight color through as a Hardcover annotation tag | `bc9e37f` | v4.0.16 |
| #37 | [#1290](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1290) | @haraldpdl | Run ingest-service python workers under `s6-setuidgid abc` (not root) | `f2616d6` | v4.0.16 |
| #40 | [#1279](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1279) | @dbraendle | Use `lazy='subquery'` for Books relationships to prevent DetachedInstanceError | `bc9386c` | v4.0.16 |
| #80 | [#1327](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1327) | @okms | Book delete: pass saved return location to the generated POST form (was assigning `window.location`); add explicit `X-CSRFToken` headers to bulk delete/edit/archive/read JSON POSTs | `8ec4c582` | v4.0.25 |
| #81 | [#1331](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1331) | @I-Would-Like-To-Report-A-Bug-Please | Move title/author directory rename ahead of fetched-metadata staging in `editbooks.py` so description/tags persist on first save when the book directory also gets renamed | `3129d906` | v4.0.25 |
| #82 | [#1144](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1144) | @mihneabulu | Kobo OIDC: add `/oauth/.well-known/openid-configuration` discovery endpoint, dedicated `/oauth/authorize` route, use `current_user.id` instead of empty-on-GET request body — fixes "Sync failed. Please try again." on newer Kobo firmware (4.45+) | `d8300288` | v4.0.25 |
| #86 | [#1336](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1336) | @I-Would-Like-To-Report-A-Bug-Please | Metadata-search: flush TinyMCE comments back to the textarea before submit (description was rendering in the editor but not posting on first save); rename modal "Save" to "Apply"; normalize missing provider descriptions to empty string | `5d6943d8` | v4.0.26 |
| #91 | [#1221](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1221) | @EuanKerr | NewBookProcessor: route auto-send to a single user when a book is dropped under `/cwa-book-ingest/<username>/`. Behavior change at release: files in any subfolder now flow through this filter; auto-send is skipped (the book still ingests) when the first segment doesn't match a CWA username. | `905cda9b` | v4.0.27 |
| #99 | [#1225](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1225) | @SethMilliken | KOReader sync: prefer the calibre `book_id` for progress records so multiple device-side checksums for the same book share one record instead of fragmenting per-checksum. Cross-device progress sync stays converged. | `dad33813` | v4.0.29 |
| #100 | [#1095](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1095) | @cd-dr | Duplicate Manager: batch cache invalidation to one call after the bulk-delete/merge loop (new `skip_cache_invalidation` parameter on `delete_book_from_table()`) plus an 800ms reload delay so background DB cleanup completes before navigation. Stops the UI freeze and crashes on large batches. | `4c2a7356` | v4.0.29 |
| #101 | [#1324](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1324) | @frbncis | KOReader sync now pushes reading progress to Hardcover (was Kobo-only). Refactors `push_reading_state_to_hardcover` to take an explicit `user` arg so it works without Flask `current_user`; `kosync.update_progress` calls it after every successful progress write. Maps to widely-reported [#1026](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1026) (31 comments). | `cb07a566` | v4.0.30 |
| #135 | [#1346](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1346) | @navels | Avoid cover-thumbnail DB deadlock during metadata edits. `do_edit_book` previously called `helper.replace_cover_thumbnail_cache(book.id)` *inside* the Calibre metadata-edit transaction; the worker then re-opened `metadata.db` while the editing transaction was still holding a write lock, blocking the gevent loop and surfacing as 504s / 502s in the Web UI under any sustained ingest load. Fix defers the thumbnail queue until after `calibre_db.session.commit()` returns, and passes the committed `book.path` + `last_modified` directly into `TaskGenerateCoverThumbnails` via a new `BookCoverSource` dataclass so the worker no longer needs to re-query `metadata.db` for the single-book refresh case. Refs long-standing CWA [#1256](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1256) (4 reactions, 6 comments). Closes fork issue [#134](https://github.com/new-usemame/Calibre-Web-NextGen/issues/134) (reporter @Onkeyuk). 3 regression tests pin the post-commit ordering and the snapshot-passed code path. | `34f1985f` | v4.0.46 |
| #168 | [#1295](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1295) | @jgoguen | Mobile-Safari CSRF token for metadata search. iOS Safari does not reliably attach the `X-CSRFToken` header to `fetch()` requests, so `POST /metadata/search` was returning 400 / silently failing on iPhone — matches the user-visible complaint on CWA [#1266](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1266). Three layers: (1) `get_meta.js` now sends `csrf_token` in the request body for `/metadata/search` (body-borne CSRF is immune to Safari's header behavior); (2) new `cwaFetch()` helper in `layout.html` wraps `fetch()` and auto-attaches `X-CSRFToken` for any non-safe method (GET/HEAD/OPTIONS/TRACE bypass) when a token is present, without stomping on a caller-set header; (3) `main.js` ajaxSetup hardening — moves the `csrftoken` lookup from page-load cache into the per-request `beforeSend` handler with a null guard, so token rotation works and Safari's empty-string return path is handled. Existing CWA call sites in `cwa_convert_library.html` and `cwa_epub_fixer.html` routed through `cwaFetch()` for proactive coverage. Resolved one small conflict with our prior fork-original CSRF tweak by taking upstream's `.first().val()` pattern. 6 regression tests pin the cwaFetch helper, ajaxSetup hardening, body-CSRF inclusion, and call-site conversions. Closes fork issue [#154](https://github.com/new-usemame/Calibre-Web-NextGen/issues/154). Live verification on cwn-local with mobile-Safari user-agent: `POST /metadata/search` returns 200 with real provider results (Amazon, Apple Books, ComicVine). | `0026ac5a` | v4.0.54 |
| #169 | [#1271](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1271) | @jgoguen | KOReader plugin no-book guards. Tapping push or pull menu items when no book is open no longer crashes the plugin; new `showNoBookMessage()` and `ensureServerConfigured()` helpers wrap every push/pull entry point, and Pull-handler diagnostic logs are tightened to the `[Pull] end for <current_file> with <reason>` shape (richer than the older `body.<field> missing`). Landed alongside #1272 in the same fork PR. Bundled plugin version bumped `1.0.2` → `4.0.54` per the new standing rule (plugin version mirrors CWNG release tag on any plugin-touching release) — resolves the version-display ambiguity raised on fork issue [#150](https://github.com/new-usemame/Calibre-Web-NextGen/issues/150) by @SethMilliken. Server-side kosync routes verified unchanged via cwn-local probes (the patches are pure client-side). Closes fork issue [#155](https://github.com/new-usemame/Calibre-Web-NextGen/issues/155). | `4b5fd7b9` | v4.0.54 |
| #169 | [#1272](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1272) | @jgoguen | KOReader plugin bulk-pull. New `koreader/plugins/cwasync.koplugin/sync_logic.lua` module + `Pull progress for all` menu entry that iterates every downloaded book — useful when finishing a book on one device and switching to another. Stacked on #1271 in the upstream branch; both landed together in the same fork PR. 8 regression tests pin: the new helpers (from #1271), the conflict-resolved Pull-handler log lines, the SyncLogic require, the bulk-pull menu entry, the upstream Lua test file presence, and the version string. | `4b5fd7b9` | v4.0.54 |
| #233 | [#1201](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1201) | @Sheol27 | **"Currently Reading" magic-shelf preset.** New `currently_reading` entry in `SYSTEM_SHELF_TEMPLATES` (read_status equal 2 = STATUS_IN_PROGRESS) so users can build a magic shelf that surfaces books KOSync/Kobo has marked in-progress. `yet_to_read` icon swapped 📖 → 📚 to avoid collision with the new preset. `build_filter_from_rule`'s no-custom-column branch refactored to handle all three statuses (STATUS_IN_PROGRESS / STATUS_FINISHED / STATUS_UNREAD) instead of treating read_status as boolean — in-progress books were previously silently falling through to "finished" matching. 7 regression tests in `tests/unit/test_magic_shelf_currently_reading.py` pin: preset existence + value 2, yet_to_read icon swap, source-pinned 3-status fallback. Diff: +50/-23 across `cps/magic_shelf.py` (+49/-22) and `cps/templates/magic_shelf_edit.html` (+1/-1). | `13b987ec` | v4.0.77 |
| #232 | [#1353](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1353) | @navels | **Incremental duplicate-key index — replaces the O(N) full-library duplicate scan with O(1) maintained-index lookups.** Stock CWA rebuilt duplicate groups from scratch on every scan (normalize title/author/series/publisher/format for every book in the library, then bucket into groups). With 10k+ book libraries this was slow and blocked other requests while it ran. **The fix**: new `cwa_duplicate_book_keys` SQL table (`book_id PK`, normalized title/author/language/series/publisher, format signature, criteria-aware fingerprint) maintained incrementally on every import, metadata edit, and deletion. New `cps/duplicate_index.py` (+578 LOC) exposes `upsert_book_keys`, `delete_book_keys`, `rebuild_duplicate_index`, `get_duplicate_groups_from_index`, `has_valid_duplicate_index_baseline`, `merge_affected_groups_into_cache`, `duplicate_index_needs_manual_full_scan`. Each book import (`add_book_to_library` / `add_format_to_book`) now calls `run_duplicate_scan_for_books([book_id])` inline via new `POST /cwa-internal/run-duplicate-scan` — single-book key upsert in milliseconds instead of full-library re-scan. Manual scans and ingest interlock via new `cwa_ingest_batch_active` marker file + `wait_for_duplicate_full_scan_to_finish()` in ingest_processor — ingest waits for any running full scan; manual full scans block during active ingest, exposed via new `POST /cwa-internal/duplicate-scan-status`. The Duplicates page renders a one-time-baseline-scan notice on fresh installs ("Duplicate scanning needs a one-time full scan. CWA now uses a duplicate index to keep duplicate checks fast during imports and metadata changes."); replaces alert dialogs with Bootstrap-modal feedback (Resolution Preview, success/error modals). **Reconciliation against fork-original work:** preserved PR #199 metadata.db flock (`metadata_db_write_lock()` + `_run_calibredb_add_with_retry`) — flock acquisition moved AFTER the new `mark_ingest_batch_active()` + `wait_for_duplicate_full_scan_to_finish()` calls in `add_book_to_library` so the active-marker check happens first, then the flock wraps the calibredb call as before. Preserved PR #210 debounce default 30 + clamp floor 10 across all 5 mirrored sites (rejected upstream's 60/5 — our PR #212 already eliminated the deadlock vector that motivated the conservative default; existing fork installs on 30 stay at 30, no new migration). Preserved PR #212 UDF connect-event listener (no `TaskReconnectDatabase` re-import; adopted `STAT_FINISH_SUCCESS, STAT_FAIL, STAT_ENDED, STAT_CANCELLED` to power new `_duplicate_full_scan_running()` worker-state check). Preserved PR #37/#56 `s6-setuidgid abc` privilege drop. Preserved v4.0.74/v4.0.75 fast-exit pattern (`_is_missing_ingest_target` + `_load_fork_cps_imports`). Preserved PR #100 batched cache-invalidation semantics — switched mechanism in `delete_selected_books` / `merge_list_book` from per-batch `invalidate_duplicate_cache()` to per-batch `_queue_duplicate_scan_after_change(book_ids)` (still one call per batch, just scoped to affected book IDs); `delete_book_from_table`'s skip-flag guard tightened to `if not skip_cache_invalidation and not refreshed_duplicate_cache` to avoid double-invalidate when the new indexed refresh handled it. Simplified `run_post_batch_follow_up()` to `/cwa-internal/reconnect-db` only — the deferred `invalidate-cache` + `queue-duplicate-scan` calls from #1349 are redundant in the new architecture (per-book incremental scans happen inline during ingest). **173 new + updated unit tests** across 6 test files (21 in `test_duplicate_index.py`, 4 in `test_duplicate_delete_index_maintenance.py`, 17 in `test_duplicate_scan_index_rewire.py`, 11 in `test_duplicate_scan_queue_settings.py`, 3 updated in `test_ingest_batch_dirty.py` for the active marker + wait-for-full-scan path, 3 updated PR #100 regression pins in `test_duplicate_manager_race_fix.py`). **Live container verification on cwn-local**: schema migration applied idempotently (`cwa_duplicate_book_keys` table + `idx_cwa_duplicate_book_keys_key` composite index created on first start; subsequent restarts are no-ops via CREATE TABLE IF NOT EXISTS). Baseline scan populated index with 15 books — normalized title + composite duplicate_key + criteria fingerprint per row. /duplicates page identified existing groups ("Alice's Adventures in Wonderland" ×5, "The Republic" ×2) from the index in 22ms. Resolution Preview Bootstrap modal (replaces `alert()`) rendered KEEP/DELETE per-book breakdown with timestamps + formats in 0.02s for 2 groups using pre-scanned indexed groups. Sidebar "Duplicates: 2" badge updates from indexed cache. `POST /cwa-internal/run-duplicate-scan {"book_ids":[5,12,14,15,16]}` returned `{"message":"Duplicate scan completed: 1 new groups","result_count":1,"success":true}`. `POST /cwa-internal/duplicate-scan-status` returned `{"full_scan_running":false,"success":true}`. No console errors, no traceback. Inspired-by @navels in [#1353](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1353). | `3047debf` | v4.0.76 |
| #251 | [#1335](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1335) | @I-Would-Like-To-Report-A-Bug-Please | **Container reported healthy while ingest + metadata write-back silently broken; healthcheck curl had no bound; reverse-proxy keepalive sockets accumulated against wedged gevent process.** Three orthogonal failure-mode fixes shipped together because they all surface the same operator-facing symptom ("container says healthy, features don't work"). (1) **Dockerfile**: curl gains `--connect-timeout 1 --max-time 2` — a wedged gevent main loop (in-process worker holding the GIL through ingest / duplicate-scan / metadata edit) now produces a deterministic failed probe within 2s instead of relying on Docker's outer `--timeout=3s` to SIGTERM curl (signal propagation isn't always clean; recovery was unbounded). (2) **`cps/gevent_wsgi.py`**: new `read_request` override sets `self.close_connection = True` on every parsed request — stops stale reverse-proxy keepalive sockets accumulating against the gevent process, so recovery from a wedge is bounded. Verified behaviourally via `curl --next` reporting two distinct connection IDs for sequential requests. (3) **`cps/web.py`**: `/health` now probes critical s6 longruns (`cwa-ingest-service`, `metadata-change-detector`) via `s6-rc -a list` — the abc-readable primitive that `scripts/check-cwa-services.sh` already uses for the admin UI's "Check NextGen Status" action; `s6-svstat` won't work because supervise dirs are root-only in the lsio base image while the Flask app runs as `abc`. New `_CRITICAL_LONGRUNS` module-level tuple + `_check_s6_service_status` + `_probe_metadata_db` helpers; body gains a `services` map; any down longrun flips response to HTTP 503 with `status=degraded`. **Fork-original work bundled here**: the s6 service probe is fork-original (CWA #1335 only does the Dockerfile + gevent halves), addresses droM4X's explicit ask in #193 for service-liveness monitoring beyond DB consistency. 10 RED → GREEN regression tests across `test_health_service_status.py` (new, 7 tests), `test_dockerfile_healthcheck.py` (+2), `test_gevent_wsgi_format_request.py` (+1). **Live container verification on cwn-local**: baseline `{"services":{"cwa-ingest-service":"up","metadata-change-detector":"up"},"status":"ok"}` HTTP 200; `s6-rc -d change metadata-change-detector` → `{"services":{"...","metadata-change-detector":"down"},"status":"degraded"}` HTTP 503; restore → 200. 100 sequential probes all 200, 50 parallel max 0.10s, mid-stress toggle 30×503/30×200 deterministic, zero new ERROR/Traceback in logs. Reporters @droM4X (fork [#193](https://github.com/new-usemame/Calibre-Web-NextGen/issues/193)) and @FRaccie (~200k-book production trace). Backport credit @I-Would-Like-To-Report-A-Bug-Please. | `92c8cfa8` | v4.0.86 |
| #231 | [#1349](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1349) | @navels | **Stale-event filter + batch post-import follow-up + bounded internal-endpoint retries.** Five orthogonal ingest-reliability improvements. (1) `_is_missing_ingest_target()` fast-exits before runtime initialization or lock acquisition — eliminates the NFS/polling-fallback re-emit loop on already-moved files. (2) The s6 ingest-service script tracks per-file processing markers under `$PROCESSING_DIR` so the watcher can't reprocess a path while it's still being handled. (3) Module-import-time work (cps, CWA_DB, EPUBFixer, audiobook, requests) defers behind a new `initialize_runtime()` so the script can fast-exit without paying the import cost when there's nothing to do. (4) Per-book `refresh_cwa_session` + `invalidate_duplicate_cache` + `schedule_debounced_duplicate_scan` calls consolidate into a single `run_post_batch_follow_up()` that fires ONCE after the dirty-marker batch goes quiet — eliminates redundant per-book HTTP calls during sustained ingest. (5) New `_post_internal_endpoint()` helper retries the cwa-internal POSTs with timeouts and clearer failure logging instead of single-shot best-effort. **Reconciliation against fork-original work:** kept fork PR #199's `CalibreDB.refresh_for_new_data()` synchronous refresh in `/cwa-internal/reconnect-db` (going back to `TaskReconnectDatabase` via `WorkerThread.add` would re-introduce the engine-disposal race that caused fork #192); kept PR #199's `_run_calibredb_add_with_retry` + `metadata_db_write_lock()` at module top; kept PR #208's deferred lock-acquire pattern but routed it through @navels' `initialize_runtime()` to instantiate `process_lock` lazily; restored PR #37 / #56's `s6-setuidgid abc` privilege drop inside @navels' new `run_processor_with_timeout()` helper (without it the ingest worker would have regressed back to running as root). PR #210's `duplicate_scan_debounce_seconds` 30-default + 10-clamp tests for `ingest_processor.py` were updated to no-op since @navels' refactor moved the scheduling server-side via `/cwa-internal/queue-duplicate-scan` — equivalent gates still pinned at `cps/cwa_functions.py` + `cps/editbooks.py` + template-fallback sites. **Complementary to v4.0.66 PR #212** (UDF connect-listener) which eliminated the GIL+SQLite mutex deadlock vector fundamentally; this PR adds defense-in-depth by avoiding the race surface entirely (deferring duplicate-scan + DB reconnect until the batch is quiet) plus orthogonal ingest reliability (stale-event filtering, retry resilience). **6 new unit tests** in `tests/unit/test_ingest_batch_dirty.py` + `tests/unit/test_ingest_processor_startup.py` (batch dirty marker writes on success but not on failure, hot-loop HTTP calls suppressed, missing-target subprocess skip, optional cps module retry semantics) + **35 smoke tests** + **165 shell tests** for the s6 service. **Live container verification on cwn-local**: missing-target fast-exit (`python ingest_processor.py /tmp/nonexistent.epub`) returns 0 with `[ingest-processor] Skipping missing ingest target` logged + no runtime initialization + no lock acquired; post-batch follow-up (`--post-batch-follow-up`) acquires lock once, hits all three internal endpoints, returns 0; real ingest of `alice-in-wonderland.epub` triggers `[cwa-ingest-service] Post-batch follow-up triggered` and `[cwa-duplicates] Cache refreshed after after_import scan: groups=2 max_book_id=12` exactly once for the batch. | `f1ae6b18` | v4.0.74 |
| #266 | [#1074](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1074) | @SethMilliken | cwasync KOReader plugin: hide/disable Push and Pull menu actions when no document is currently open. Tapping "Push progress from this device now" or "Pull progress from other devices now" with no book loaded previously fired with `self.ui.document == nil`, producing silent failures because there was no current document to compute a checksum from. New `hasActiveDocument()` helper gates both menu items via their `enabled_func`, and the auto-sync toggle now short-circuits when no document is open. New `statusTextIfActionUnavailable()` appends a contextual `" (Password Not Set)"` or `" (No Active Document)"` suffix to the menu label so users can see *why* the action is unavailable instead of an unresponsive tap. Clean cherry-pick of the upstream commit — 17/4 LOC across `koreader/plugins/cwasync.koplugin/main.lua`. | `31bdd5da` | v4.0.117 |
| #460 | [#1202](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1202) | @Korri | French (`fr`): keep the Hardcover service name untranslated in three integration labels (RunHardcoverAutoFetch, HardcoverTokenRequired, EnableHardcoverAutoFetch) — previously rendered "livres reliés" (hardback books), confusing the service name for the book format. Skips one diverged string (HardcoverSyncBlacklist, fork already correct) and one obsolete-only entry. `msgfmt -c` clean, `.mo` resolves the three fixes, msgid parity preserved (none added/dropped), header identical, exactly 3 active msgstr changed, `test_translations_compile` 30/30 pass. | `49c0e8f29` | v4.0.163 |
| #458 | [#1369](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1369) | @pablo-alcaniz | 36 shared-string Spanish (`es`) improvements curated from upstream: punctuation normalization to match source msgids, plus one real fix — `Invalid request` was mis-translated as "Rol inválido" (Invalid role), now "Petición inválida". Deliberately skips 21 fork-specific strings (EPUB Fixer, `/cwa-book-ingest`, KOReader Sync, Magic Shelf, cover-size limits) where our `es` diverges from both upstream's base and the PR. `msgfmt --check` clean via Test Suite Summary; 1886 msgids unchanged, exactly 36 msgstr changed, header identical, 24 fuzzy cleared. | `e9efaa3c` | v4.0.163 |
| #459 | [#1155](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1155) | @futurelook | 11 active-string German (`de`) fixes curated from upstream, all correcting wrong fuzzy mappings: `Range: 1-200 MB` was shown as "Bereich: 5-120 Minuten" (minutes!), `Failed to update shelves` was "...der Tags" (tags), `KOReader Sync is disabled` was "Standard-Login ist deaktiviert", plus OIDC/logfile/email/Magic-Shelves-order label fixes. Deliberately skips 16 fork-specific strings where the PR switches our informal `du` register to formal `Sie`. `msgfmt -c` clean; 1790 msgids unchanged (none added/dropped), exactly 11 msgstr changed, obsolete entries untouched. | `555d9b53b` | v4.0.163 |
| #507 | [#1395](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1395) | @GSAlex | Simplified Chinese (`zh_Hans`) translation completion — 279 previously-untranslated UI strings filled from upstream via `msgmerge --compendium` against our pot (raw cherry-pick conflicts post-2026-06-19 pot resync). msgid set identical (1800, none added/dropped), existing translations preserved, 0 new fuzzy introduced (398 pre-existing fuzzy untouched), `msgfmt -c` clean, `test_translations_compile` 30/30 pass. | `43e1327` | v4.0.169 |
| #508 | [#1033](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1033) | @HaruIjima-kun | Spanish (`es`) translation improvements — 76 fuzzy entries replaced with @HaruIjima-kun's confirmed translations and de-fuzzied (20 same-text de-fuzzy, 56 corrected mistranslations from prior auto-fuzzy-matching, e.g. "Duplicate group dismissed" was wrongly mapped to "Libros Duplicados"). Imported as a clean-translation compendium over our divergent pot (raw cherry-pick conflicts post-2026-06-19 pot resync); only fuzzy entries with a clean upstream match were touched. msgid set identical (1800, none added/dropped), 0 previously-clean translations changed or lost, translated 1300→1376 / fuzzy 276→200, `msgfmt -c` clean, `test_translations_compile` 30/30 pass. | `d6f1ddb` | v4.0.169 |
| #785 | [#1426](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1426) | @djalexz85, @fucx, @ManuelDrescher | **i18n(de): fill 19 previously-blank German strings.** Additively merged as `msgmerge --compendium` fills (not hand-resolved hunks — avoids the v4.0.47 doubled-msgstr gotcha) from CWA [#1426](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1426) (@djalexz85, +9), CWA [#1412](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1412) (@fucx, +9 disjoint), and janeczku [#3644](https://github.com/janeczku/calibre-web/pull/3644) (@ManuelDrescher, +1). Our `de.po` is the source of truth; the compendiums fill ONLY blank entries. No existing translation altered (diff-verified), translated 1456→1475 (+19), fuzzy unchanged (447), `msgfmt -c` strict pass (format specs intact), `test_translations_compile.py` 30/30. | `9dfb87a` | v4.1.9 |
| #786 | [#1420](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1420) | @Demelja | **i18n(uk): fill 141 previously-blank Ukrainian strings.** Additively merged as a `msgmerge --compendium` fill (not hand-resolved hunks — avoids the v4.0.47 doubled-msgstr gotcha) from CWA [#1420](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1420) (@Demelja). Our `uk.po` is the source of truth; the compendium fills ONLY blank entries (error messages, the metadata review queue, cover/thumbnail cache tools). No existing translation altered (polib-verified: 0 modified, +141 filled), translated 964→1105 (+141), fuzzy unchanged (676), `msgfmt -c --check-format` strict pass, `test_translations_compile.py` 30/30. | `6391157` | v4.1.9 |
| #350 | [#1344](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1344) | @shavitmichael | **Kobo sync requests are noticeably faster — roughly 15 seconds saved per sync round.** Four orthogonal optimizations in the Kobo sync hot path: (1) `generate_auth_token` no longer pre-converts kepubs by walking Kobo Sync shelves; conversion is deferred to `helper.get_download_link` and happens lazily when a Kobo actually requests `/download/<id>/kepub`, scoped to that single book, blocking (120s timeout) with EPUB fallback. (2) `KoboSyncState` and default `ReadingState` row creation deferred so the sync read isn't blocked by serialised writes. (3) Book metadata query that builds the sync payload now `joinedload`s authors / series / comments / languages — the per-book lazy-load fan-out in the metadata serializer is gone. (4) Single `downloadUrl` per book (was EPUB + EPUB3 + KEPUB); Kobo handles the EPUB fallback if kepub conversion fails. **Fork-side test updates** (no behaviour change to the cherry-pick): two existing source-pin tests had asserted the shape of the now-deleted auto-convert loop in `generate_auth_token` (fork pins for CWA #1328 blank-page lazy-load and CWA #817 unbounded library walk). The loop is gone in this design — kepub conversion still happens but at download time, scoped to the single book the device requests — which closes both regression vectors structurally rather than by defensive shape. Tests rewritten to pin the new design: `generate_auth_token` must not query `db.Books` or call `convert_book_format`; `get_download_link` must call `convert_book_format(book.id, ...)` gated on `config.config_kepubifypath` with `blocking=True`. `test_kobo_android_app_compat.py` had its `DrmType` pin broadened to the whole `cps.kobo` module so the dict-construction moving from inline into a `build_download_url` helper stays green. Greptile 4/5 confirmed safe to merge. | `27b334cc` | v4.0.146 |

## Original fork patches (no upstream PR existed)

### Bug fixes

| Fork PR | Upstream issue | Description | SHA | Release |
|---|---|---|---|---|
| (fork issue [#1108](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1108), reported by @chloeroform) | The "update available" indicator compared the installed version against `constants.STABLE_VERSION` — a snapshot `cwa-init` curled from the GitHub releases API once at container boot into `/app/CWA_STABLE_RELEASE`, read at module import. Two layers of staleness: the file is never rewritten while the container runs, and the constant is import-time bound so even rewriting it changes nothing live. Both reproduced on a running container. Long-lived Docker deployments therefore never learned about any release published after their start. New `cps/services/latest_release.py` resolves the tag on demand with a 6h success / 15min failure TTL, runs the probe through `parallel.fan_out` so the blocking GitHub call never parks the gevent hub (no `monkey.patch_all()` — an inline `requests.get` on the admin render path would freeze every request), validates the tag shape, keeps the `CWA_RELEASE_REPO` override and never raises. Also fixed `cli.py --version` and `updater.py::_stable_version_info`, which reported the newest published tag where they meant the installed version, and an empty `/app/CWA_RELEASE` yielding an empty `INSTALLED_VERSION` that silently disabled the indicator. Adopted from @chloeroform's PR #1191, reworked: their version called GitHub inline on the render path with no caching. 25 regression tests. | `5f5b98f95` | TBD |
| [#1197](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1197) | (fork issue [#1164](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1164) @auspex) | **Auto-metadata applied whichever result a provider ranked first, silently overwriting a freshly ingested book with a different book's title, author and description.** `_select_metadata_result` (`cps/metadata_helper.py`) gated only on ISBN (fork #402). A freshly ingested EPUB normally carries no ISBN, so that branch was skipped entirely and the function fell through to `results[0]` with no check that the candidate was the same book. The reporter's trace is exactly this: a search for `The Devils Joe Abercrombie` ended in `Successfully applied metadata from Kobo for book: The Heretics` — a different, then-unpublished Abercrombie title. **Fix**: candidates are scored on their *identifying* words (normalized: accents folded, bracketed asides and generic publisher tails such as `: A Novel` removed, a leading article dropped, joining words ignored) and the BEST candidate — not the first — is applied only if it clears the bar; otherwise `None` is returned, the caller logs why and moves to the next provider, and the book is left as imported. **Word-level comparison is load-bearing and was chosen after measuring, not assumed**: a character-similarity bar cannot do this job, because series titles share a long prefix — `Harry Potter and the Chamber of Secrets` scores **0.78** against `...and the Goblet of Fire`, above any threshold loose enough to accept real title variance. That false positive was found by measuring scores across a case table, not by a failing test, and is now pinned as a regression case alongside `Dune` vs `Dune: Messiah` (0.50) and the word-order pair `Blood and Iron` vs `Iron and Blood` (0.36). Order is preserved so an anagram of the same words is not a match. A candidate matching on title whose author disagrees is also refused, since titles collide across unrelated books. Real variance still matches: case, punctuation (`&` vs `and`), diacritics (`Les Misérables`), series markers and `: A Novel` all resolve to 1.0. **Secondary fix**: the success log read `book.title` *after* `_apply_metadata_to_book` had overwritten it, so it named the book we fetched rather than the book we changed — which is why the reporter's log said "for book: The Heretics". The pre-apply title is now captured and logged. **Back-compat**: the two new parameters default to `None`, preserving the legacy first-result behaviour and the #402 ISBN priority for any caller that passes no title; a source-pin test fails if `fetch_and_apply_metadata` ever stops passing them, since that would silently make the gate inert. **Red/green**: `tests/unit/test_1164_metadata_confidence_gate.py` — 21 failed / 1 passed against pristine `origin/main`, 22 pass after; the pre-existing #402/#403/#405 suite stays 16/16. Full `smoke or unit` selection in a clean worktree: 4375 passed / 3 failed, and all 3 (`test_required_directories_exist`, `test_802_metadata_change_dispatch`, `test_ingest_batch_dirty`) reproduce identically on pristine `origin/main` in a separate clean worktree — pre-existing, unrelated, not touched by this change. Stdlib only (`difflib`/`re`/`unicodedata`); no dependency, license, route or external URL change. Fork-original. | `54ec99033` | v4.1.23 |
| [#1194](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1194) | (fork issue [#1169](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1169) @magdalar) | **A metadata edit ran the DELETE cache path, so an edited book was evicted from the restored catalog accumulator with nothing able to bring it back in place.** `useUpdateMetadata.onSuccess` (`frontend/src/lib/queries.ts`) called `removeBookFromCache(id)` — the helper written for #578, where dropping a deleted book from every cached catalog snapshot is exactly right because the book is gone. An edit is not a delete: the book still exists and still belongs in an unfiltered listing. On remount `Catalog` seeds `allBooks` from the snapshot (`Catalog.tsx:261`) and `useBooks` re-requests only the page the user left on, and `dedupAppend` (`Catalog.tsx:125-138`) only ever UPSERTS or APPENDS — it has no path that restores a row to its sorted position. So the edited book came back as the LAST card of everything loaded, or not at all when it lived on a page that is never re-requested. **Measured, not inferred**: instrumented run against the pre-fix bundle put the book at grid index 0 before the edit and **index 23 of 24** after it — from the top of a Newest-sorted library that is indistinguishable from the reporter's "the book you just edited is missing", and it persists until a reload drops the module-level `_cache`. **Fix**: edit semantics get their own path. `applyBookEditToCache(id, patch)` patches the book in place where membership is fixed (plain library) so the card keeps its position AND shows the new title/author/series immediately — including on pages the catalog will not re-request — and DROPS snapshots whose membership only the server can decide (search / entity / discovery / #498 saved filter), so those rebuild from page 1 rather than carrying a stale card. That distinction is recorded on the snapshot itself (`membershipFiltered`, set in `Catalog.tsx` where the knowledge lives) instead of being re-derived by string-parsing `resetKey`, whose trailing segment is filter JSON that can contain the delimiter. `removeBookFromCache` is untouched and still serves both delete paths (`queries.ts:604,707`). `qc.removeQueries({queryKey:['books']})` is deliberately KEPT rather than softened to `invalidateQueries`: a retained page is replayed on remount and the merge would re-add a stale card before the refetch could return without it — the property `search-after-edit.spec.ts` (#744) pins. **Field mapping mirrors the server rather than guessing**: the metadata payload carries authors '&'-joined and tags comma-separated, and the save path splits the submitted string the same way (`cps/editbooks.py:1247-1249`), so the client split reproduces what the next list fetch would return — including an author name containing '&', which the server splits identically. Only fields the payload actually carries are patched, so a partial response cannot blank a card's authors. **Red/green**: `frontend/e2e/catalog-edit-retains-book.spec.ts` drives the reporter's flow (load 2 pages → quick-edit → save → client-side back) against a mocked two-page library so the "edited book is on a page you are no longer sitting on" condition is deterministic rather than a function of seed size; verified RED on the pre-fix bundle (index 0 → 23) and GREEN after, desktop AND mobile. **Honest scope**: `cwn-local`'s 12-book seed is a single page, and a mock-free run of the same flow passes on BOTH bundles — it is a no-regression check, not a reproduction; the bug needs more than one loaded page, which is why it is reported from real libraries and not from the test seed. **Regression sweep**: full SPA e2e desktop+mobile, 294 passed / 12 failed, and the same 12 fail identically on a pre-fix bundle built from the same tree (10 `a11y.spec.ts` light-theme colour-contrast + 2 `cover-upload-refresh.spec.ts` timeouts) — pre-existing, filed separately, not touched by this change which contains no CSS. No dependency, license, route or external URL change. Fork-original. | `ffa334152` | v4.1.23 |
| [#1189](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1189) | (fork issue [#991](https://github.com/new-usemame/Calibre-Web-NextGen/issues/991) @Metamatam) | **A KOReader device kept reporting the document digest of a book file it no longer held, so sync broke permanently once the server's copy changed and no amount of re-downloading recovered it.** The server resolves a sync by matching the digest the device sends against `book_format_checksums`, which only ever holds digests of bytes the server actually served (`cps/progress_syncing/checksums/koreader.py`). `CWASync:getDocumentDigest` in `koreader/plugins/cwasync.koplugin/main.lua` answered from KOReader's per-document sidecar cache (`partial_md5_checksum`) and hashed the file only when that cache was empty. The sidecar is keyed by file PATH, so it outlives the file at that path being replaced — after any event that rewrites a library file (a Calibre `restore_database` rebuild, metadata write-back via `cover_enforcer`, EPUB Fixer, a re-import) the device's cached digest describes bytes that no longer exist anywhere. Re-downloading is actively counter-productive: `do_download_file` registers the digest of the *newly served* bytes (`cps/helper.py:1871-1889`), which the device never computes, so each attempt adds another unreachable row while the device repeats its stale value. That is the reporter's exact trace — device digest `6b10209c…` absent from the table while book 728 carried three registered digests, and `No book found for checksum` on every retry. **Fix**: precedence is inverted so the bytes on disk are the authority. The policy is pure and lives in `sync_logic.lua` as `SyncLogic.resolveDocumentDigest(computeFromFile, readCachedDigest)`; the cache is consulted only when hashing is impossible (file missing, unreadable, detached SD card), where a stale digest still beats none because progress already stored under it can round-trip. **Recompute cost is accepted, not optimised, and not measured on e-reader storage**: one open plus 12 seek/read pairs of 1 KiB per call, once per book in bulk library pull (`pullLibraryProgress`). The cross-family review (Terra) raised this and agreed cache-first must not be restored; there is no sound freshness check to optimise with, because KOReader rewrites the sidecar on ordinary progress saves without recomputing the digest, so neither its mtime nor its presence indicates whether the file underneath changed. The replaced path was not free either — it opened and parsed each book's sidecar — and in bulk pull every book is followed by an HTTP round trip that dominates either cost. **Blast radius is exactly the bug**: the computed and cached digests differ only when the file at that path was replaced, so an unchanged book resolves identically to before. Server-side no change was needed — once the true digest resolves, the existing `book_id` re-key (#633) and the checksum-union lookup already reunite prior progress with the book. **Plugin version bumped 4.1.16 → 4.1.23, which is load-bearing**: the Updates Manager compares that string to decide whether a device needs the new plugin, so a device-side fix shipped without the bump would never reach anyone. **Behavioural coverage of the shipped function**, added after the review noted the Python tests only read `main.lua` as text: `koreader/plugins/cwasync.koplugin/tests/document_digest_test.lua` slices the real `getDocumentDigest` source out of `main.lua` and loads it into a sandbox with stubbed `util` / `io` / `DocSettings` / `self.ui`, asserting the stale sidecar loses, the sidecar is not even read when hashing succeeds, an explicit path (the bulk-pull mode) is the one hashed and the one whose sidecar is opened, the inline sampler is still used when `util.partialMD5` is absent, and an unhashable file falls back. Verified red against pre-fix `main.lua`. Plus 10 policy assertions in `sync_logic_test.lua` (stale/accurate cache, unreadable file, sources that throw, empty-string sidecar values, non-string reads) and 7 structural tripwires in `tests/unit/test_991_document_digest_precedence.py`, all verified failing against pre-fix `HEAD` in an isolated worktree — these are documented as tripwires, not behavioural coverage, since CI runs no Lua runner. Full kosync/checksum/plugin unit selection 406 passed / 15 skipped, all three Lua suites green. **A second cross-family review (Terra) against the post-fix branch — which the first review never saw — found three MEDIUMs, all fixed in `3166a5914`.** Two were the same class as the bug itself: the inline sampler (builds without `util.partialMD5`) returned `nil` for a zero-byte file and so answered from the stale sidecar, while the server's `calculate_koreader_partial_md5` breaks out of the identical loop and returns `md5("")` rather than `None` — a device/server digest divergence for the one file the server could still resolve; and `f:close()` sat inside the `pcall`'d closure, so a `seek`/`read` that throws (card detached mid-pull) stranded the descriptor, once per book across `pullLibraryProgress`. The third was test-quality: the sampler stub's `bit.lshift` returned the operand unshifted and its reader answered only at offset 0, so the "12 x 1 KiB" claim was never observed. A fourth came from running the suite rather than reading it — `document_digest_test.lua` located `main.lua` through a literal `"../"`, passing only from `tests/` and failing elsewhere with `cannot read`, which reads like a missing file rather than a wrong cwd; it now resolves through `package.path` like both sibling suites, which matters because CI runs no Lua runner to catch the drift. The zero-byte and handle-close cases are red against the prior commit and green after; the sampler coverage is a drift tripwire (it passes either side, since the offsets did not change) earning its place by mutation instead — drifting an offset, dropping a sample, changing the read size, removing the close, and restoring the nil-on-empty each trip it. 369 passed / 15 skipped / 0 failed, all three Lua suites green from both directories. No dependency, license, route or external URL change. Fork-original. | `e2cfba394` | v4.1.23 |
| [#1185](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1185) | (fork issue [#1166](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1166) @iroQuai, @HLRobius, rogovmtlz on Discord; completes [#1112](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1112)) | **The edit pencil rendered on top of a book card's series/author line, and "Read now" wrapped to two lines on a phone.** The bottom of the card was not a layout: the pencil was `position: absolute` against `.wrap` and room for it was *reserved* by padding the label (`.readNowInset { padding-right: calc(44px + var(--sp-2) * 2) }`, the #1112 fix). Reserving space only works while the card is wider than the reservation. Measured at a 375px viewport the card is 164px (Comfortable) / 106px (Compact) / 82px (Dense) against a fixed 60px reservation, so at Compact and Dense the label is left ~20-46px, wraps to two lines, grows the row, and the 44px pencil (`bottom: 8px`, top at `wrap.bottom - 52`) rises through the 44px label band into the metadata above it. Two consequences share the one cause: a book with no readable format (`getPrimaryReadTarget` returns null for MOBI/AZW3) renders no `.readNow` at all, so the absolute pencil had nothing to sit over and landed directly on the series at ANY width; and a 44px touch target never scaled, so by Dense it was most of the card. Desktop measured clean at 1280px (pencil 24px, `.readNow` 32px, flush) which is why this read as mobile-only - the 8px intrusion needs the coarse-pointer block to take both to 44px. **Fix**: the read link and the pencil share one flex row in normal flow below the metadata, so the browser allocates the width instead of the stylesheet guessing at it and a control cannot land on card text at any width, density or locale - the same by-construction move `.badgeRow` already makes above. `.readNowInset` is deleted; `.removeBtn` stays absolute because it belongs to the cover. For narrow cards a container query on the CARD's inline size (not the viewport - the same card renders at 82px in a dense grid and 260px in search on one screen) scales the controls to 32px and drops the label wording, keeping the icon and its `aria-label`; 32px stays above the 24x24 floor in WCAG 2.2 SC 2.5.8 and the row keeps a 44px min-height so the read link's touch area is unchanged. Label-hiding is scoped to rows that actually carry a pencil, so the Discover / More-by-author rails (fixed 116px on mobile, under the threshold but with the row to themselves) keep "Read now". **Red/green**: `frontend/e2e/card-action-row.spec.ts` asserts geometry rather than pixels (four assertions per density across dense/compact/comfortable, plus the no-read-target case) so it will not fire on unrelated restyles; driven against the real SPA on `cwn-local`, red pre-fix and green after, desktop and mobile. No dependency, license, route or external URL change. Fork-original. | `d2971f0a2` | v4.1.23 |
| [#1178](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1178) | (fork-original) | **The book detail page scrolled horizontally on a phone when a title, author or series carried one long unbreakable token.** `frontend/src/pages/BookDetail.module.css` `.title`/`.authors`/`.series` had no break rule, and the header text column is ~217px at a 390px viewport, so any wider token overflowed its own box and propagated to `documentElement`. Measured on cwn-local: 290px of document overflow for a 35-character compound title, `h1` scroll 519 vs client 217; 0px after. This is the ~35px CI baseline overflow tracked in `notes/e2e-mobile-overflow-ci-triage.md` that resisted several rounds of diagnosis — no element BORDER BOX crosses the viewport edge, so every `getBoundingClientRect` sweep came back empty and the tag-pill/chip/actions-row hypotheses were all looking for the wrong shape. The `scrollWidth > clientWidth` probe added in #1177 named `<h1 class="_title_sjq14_163">` (48px more content than its box) on its first CI run. Fix is `overflow-wrap: anywhere`, the same treatment the tag pill got in #1170; normal wrapping still breaks between words first. Regression test measures the delta the long metadata adds (seed-independent): red 109px against a redeployed pre-fix build, green 0 after, plus a direct pin on the title box. CI E2E `hidden-books.spec.ts:252` went RED->GREEN on both projects, 267->273 passed. | `b983694b3` | v4.1.23 |
| [#1171](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1171) | (fork issue [#1170](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1170)) | **The book detail page scrolled horizontally on a phone when a book carried a long subject tag.** The read-only tag pill (`frontend/src/components/Pill.module.css`) was `white-space: nowrap` with no `max-width`, so a Library-of-Congress heading such as `France -- History -- Revolution, 1789-1799 -- Fiction` rendered wider than the wrapping flex row containing it and pushed the document past the viewport. Measured on a stock library as a guest: 12px of overflow at 390px, 82px at 320px, 323px with a single unbroken token. Only the no-edit-role render is affected — the editor branch uses removable chips that already wrap, which is why every admin-authenticated mobile spec missed it. Fix drops `nowrap` and adds `max-width: 100%` + `overflow-wrap: anywhere`; pills that fit still occupy one line, only over-long ones wrap. Verified `max-width` alone with `nowrap` retained does **not** fix it (82px unchanged), confirming `nowrap` as the root cause. Regression test measures the overflow the long tags ADD (baseline vs pathological render), not absolute page overflow, so it holds on any fixture — CI’s detail page carries ~35px of unrelated baseline overflow (see `notes/e2e-mobile-overflow-ci-triage.md`). Red at 323px against a redeployed pre-fix build, green after. | 41905a99e | v4.1.22 |
| [#1165](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1165) | (fork issue [#1109](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1109)) | **Books carrying an ISO 639-2/B language code (`ger`, `fre`, `dut`) displayed "Unknown" and were rejected on upload.** `cps/iso_language_names.py`'s `LANGUAGE_NAMES` is keyed on ISO 639-2/**T** only (`deu`, `fra`, `nld`); the 20 bibliographic variants are absent from all 28 locale tables. `get_language_name()` did a bare `names.get(lang_code, "Unknown")`, so a /B code missed the table on every read path — book detail (`cps/web.py:1886`, `:3515`), the language browse list (`cps/db.py:1975`/`:1995`), the edit form (`cps/editbooks.py:572`, `:1684`), magic shelves and OPDS — and wrote `ERROR {cps.isoLanguages:79} Missing translation for language name: ger` once per lookup per book, polluting logs users attach to unrelated reports. **Second, worse surface**: `get_valid_language_codes_from_code()` matches input against table keys, so a /B code fell through to *remainder*, and `edit_book_languages(upload_mode=True)` turns a remainder entry into `ValueError("'ger' is not a valid language")` — an EPUB whose OPF declares `ger` (passed through untouched by `get_lang3`, which returns any 3-letter code as-is) was refused. **Fix**: `ISO6392B_TO_T`, a fixed 20-entry /B→/T table in `cps/isoLanguages.py`, consulted on miss by a new `_resolve_lang_code()` helper (returns `(name, resolved)` so callers distinguish a hit from the "Unknown" sentinel) plus a `canonical_lang_code()` normalizer; `get_valid_language_codes_from_code()` now accepts /B input and stores the /T form, de-duplicating when a book declares both. The table is written out rather than derived from pycountry at import (a packaging change could silently empty it) — 639-2/B is a closed list — and `test_iso6392b_table_matches_pycountry` pins it against pycountry as a drift alarm. The unmappable-code log drops ERROR→WARNING: an unrecognised code is a metadata property, not an application fault. **21 new tests** in `tests/unit/test_1109_iso6392b_language_codes.py` (all 20 codes × display + validation, cross-locale non-divergence, log level, dedupe, drift pin) plus the **3 pre-written smoke tests un-quarantined** from `tests/quarantine.py`. Mutation-verified: removing the display alias fails 46, removing the validation alias fails 23, reverting the log level fails 1. **Live-verified on cwn-local** — book 2 tagged `ger`: pre-fix `<span class="meta-pill">Unknown</span>` + 2 ERROR lines per page load; post-fix `German` on the detail page, `German` in the `/language` browse list, 0 ERROR lines. Suite 2F/4327P = baseline.  **Two further defects, fixed in follow-up commits on the same PR.** (1) `get_valid_language_codes_from_code()` validated against the *per-locale* name table. Those tables are translation data and are legitimately incomplete — 27 of the 28 shipped locales are missing between 1 and 48 of the 424 codes, and `ell` alone is absent from 11 — so a valid code missing from the user's UI locale fell into *remainder* and became the same upload `ValueError`: **a Greek book tagged `ell` was refused for a pt_BR user and imported fine for an English one**, and a book declaring both `ger` and `deu` was rejected outright because `deu` resolved while `ger` did not. Validity is a property of the code, not of the locale's translation coverage, so validation now resolves against `_reference_language_codes()` — the one complete table — with `test_reference_table_is_the_superset_of_every_locale` pinning the property that makes it a valid oracle. (2) From the cross-family review: canonicalization ran *after* the locale-table lookup, so a table that ever gained a /B key would capture `ger` and store it verbatim while defeating the `ger`+`deu` de-duplication; the two passes are now one that normalizes /B→/T before any lookup. That collapse surfaced that `names.items()` on an unrecognised locale had always raised `AttributeError` even though `get_language_names()` is documented to return `None` for one and `get_language_name()` already guards it — the validator guards it too now and falls through to the reference table. **The real caller is under test**: `edit_book_languages(upload_mode=True)` is the only production consumer and the place *remainder* becomes the reporter's `ValueError`, and nothing exercised it, so four tests now drive it directly. **Terra review**: no critical/high/medium; six low/nit, five fixed (ordering, the `canonical_lang_code` monkeypatch test that only proved routing, the backend-coupled `AttributeError` assertion, the reference-table docstring overclaim, the missing caller test) and one declined with reason. **107 tests** total, each red-proofed against the commit it pins. **Live A/B through the production module in cwn-local** (Python 3.13, pycountry 24.6.1): `ell`@pt_BR, `ell`@gl, `ger`@en and `ger,deu`@en all go rejected→accepted, `zzz` stays rejected, display unchanged (`en/ger`→German, `de/ger`→Deutsch). Suite 2F/4340P = baseline. | `30dd3fae5` | v4.1.22 |
| [#1157](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1157) | (fork issue [#1094](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1094) @auspex) | **A failed conversion dropped the book from the library entirely.** `main()` in `scripts/ingest_processor.py` imported the incoming book only inside `if convert_successful:` and had no `else`, so any conversion failure — the reporter's 63MB PDF killed by the service safety timeout, but equally a corrupt source, unsupported internals, or a missing input plugin — returned without ever calling `add_book_to_library()`, while the `finally` block's `delete_current_file()` removed the source from the ingest folder. The service then logged `Successfully processed`. The two sibling branches that decline to convert (`auto_convert` off; format in `auto_convert_ignored_formats`) already import the original, so the fallback makes behaviour consistent rather than adding a rule. **Fix**: an `elif conversion_attempted:` branch imports the original. The guard flag matters — the ignore-list branch reports `convert_successful=False` *having already imported*, so a bare `else` would have filed every deliberately-unconverted book twice; that near-miss is pinned by `test_ignored_format_is_imported_exactly_once`. Also addressed from the same report: `backup()` now logs the absolute destination and the safety-timeout branch names `/config/processed_books/failed/<file>` (the reporter's "I've got no idea where 'failed backup' is"), and the service no longer claims the processor "should have timed out internally at N seconds" — `ingest_timeout_minutes` bounds only `is_file_in_use()`, so no internal conversion timeout exists; it now points at the setting that does govern it. **Second defect, found by the cross-family review and confirmed by reading `run:245`:** the fallback could not help the reporter's own case at all. The service runs the processor under `timeout "$safety_timeout"`, and nothing inside the processor bounded conversion — so an overrun was a SIGTERM to the processor, not a Python-visible failure, and `main()` never regained control to reach the new branch. (Their log shows `CON_ERROR ... EXIT/ERROR CODE: -15` a second before the shell's `SAFETY TIMEOUT`, which is `timeout` signalling the process group: whether Python survives long enough to print anything is a race, and whether it survives long enough to *import* is a race it usually loses.) **Fix**: `convert_book()`/`convert_to_kepub()` now run the converter with `subprocess.run(timeout=...)` from `CWA_CONVERSION_DEADLINE_SECONDS`, which the s6 script derives from the same `safety_timeout` it enforces (one source of truth; ~10% reserve, capped at a fifth of the budget; unset when `timeout 0` means no hard limit). An overrun is now an ordinary `(False, "")` and the book is imported. **Fourth defect, found by the second review leg:** that deadline was read fresh at each `subprocess.run()`, so a kepub ingest of a non-EPUB — which converts twice back to back inside one hard `timeout` — granted each stage the full deadline, letting the two outlast the watchdog and re-opening this very bug at the DEFAULT setting (at `S=2700/D=2430`, stage one can burn 2429s and stage two starts with a fresh 2430s against 271s of real time). The budget is now ONE allowance anchored to process start (the span `timeout` actually measures); each stage draws on what is left, and an exhausted budget yields a small positive value so it expires as a handled `TimeoutExpired` rather than a `ValueError` escaping the handlers. Two smaller repairs from the same leg: the shell reserve had a cliff (a flat 30s floor inverted the limits — a 31s timeout left 1s to convert while 30s left 23s), now capped so the deadline is monotonic in the timeout, with the shipped 900s/2700s endpoints unchanged and pinned; and `float("inf")` parses but fails at `int()` with `OverflowError`, which is neither `TypeError` nor `ValueError` and escaped while evaluating the `timeout=` argument, now caught. The whole-run anchor means a slow copy-in eats conversion time — the correct direction, since the watchdog starts at process launch and anchoring later lets the inner deadline outrun it; the timeout message now says so. **Third defect, same review**: `convert_to_kepub()`'s generic `except Exception` printed and fell through, returning `None` — `main()` unpacks that call, so it raised `TypeError`, skipped the fallback and dropped the book. Every non-success path now returns a pair. Also, that path backed up only the intermediate epub, so for a non-epub input the failed folder never held the file the user actually dropped in; it now keeps both. **Declined, with reason**: the review's `automerge=overwrite` finding (a rescued file could replace a good existing format) is real but pre-existing and not introduced here — the same unconditional `add_book_to_library(filepath)` runs on the auto-convert-off and ignore-list branches today, and the default is `new_record` (`cwa_schema.sql:46`). Tracked separately rather than folded into this fix. **Verified live in cwn-local mirroring the reporter's config** (`auto_convert=1`, target epub): pre-fix an unconvertible PDF logged `CON_ERROR` then `Successfully processed` with the book count unchanged at 42; post-fix the same file imports (42→43), `/book/205` and `/api/v1/books/205` both 200 with the PDF format attached. Deadline path verified live too: a 12MB txt with a 1s deadline overran, logged the actionable message, backed up to the named folder and imported as **TXT** — the original, not a converted file. Happy path re-checked (txt→epub converts, one EPUB row, fallback silent), ignore-list path re-checked (exactly one row), and an `.acsm` ticket re-checked (not filed, guidance printed, its promise about processed_books/failed now true). 27 regression tests, mutation-verified falsifiable; full unit+smoke suite 3F/4206P against a 3F baseline on `main` — the same three pre-existing failures. 49 regression tests in the module, every fix mutation-verified falsifiable; full unit+smoke 2F/4246P against the same 2F baseline on `main` (`0e423fb04`). Live re-verified in cwn-local (container Python 3.13.14): a 309MB txt with a 1s budget fires the deadline, backs up and imports as **TXT** (42→43), while a generous budget and an unset budget both still convert to **EPUB** — the anti-vacuity control that the fallback fires only on failure. `/security-review` clean (no finding above the confidence bar). Split out rather than stretched: **#1161** (converter descendants can outlive the deadline; `start_new_session` would take them out of the outer `timeout`'s group, so it needs its own signal design) and **#1163** (a failure *after* a successful conversion still unwinds without importing either file). No dependency, license, route or external URL change. Fork-original. | `a7f13064e` | v4.1.22 |
| [#1156](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1156) | (fork issue [#1117](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1117)) | **The SPA's "read" marker was a 22px unlabelled checkmark circle in the top-right of a cover, far less visible than the classic view's labelled green badge.** The reporter reads in the light theme because of their eyesight and could not pick the marker out at a glance; the classic theme has always drawn a green pill reading "Read" at the bottom-left of the cover, so the two views disagreed on the same state. **Fix**: `BookCard` now renders a labelled green pill at the bottom-left, matching the classic placement and wording, and scales back up on phone and tablet breakpoints instead of shrinking. The label goes through `t()` so a German library reads "Gelesen" exactly as the classic view does, with the msgid anchored in `cps/spa_strings.py` (babel does not scan `.tsx`). No dependency, license, route or external URL change. Fork-original. | `0e423fb04` | v4.1.22 |
| [#1149](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1149) | (fork issue [#1121](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1121), downstream of [#1048](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1048) @auspex) | **`calibre_db.session` was a single shared attribute standing in for per-thread state, so a background task could tear down the session a request was still streaming from.** `CalibreDB.session_factory` is a `scoped_session`, which exists to hand each thread its own Session; `init_session()` then did `self.session = self.session_factory()`, publishing *the calling thread's* Session onto one attribute of a module-level singleton that **~270 call sites** read. Whichever thread called it last won, and the thread-locality the wrapper exists to provide was discarded by the assignment. It bites harder here than in a typical Flask app because CWNG runs gevent **without** `monkey.patch_all()` (pinned by the AST guard in `test_no_stdlib_futures_on_request_path`), so `WorkerThread` is a real OS thread rather than a greenlet — two genuinely concurrent users of one Session. **Every background task inherits it** (`duplicate_scan`, `thumbnail`, `metadata_backup`, `hardcover_sync`, `auto_send`, `convert`), which is why the symptom kept resurfacing with unrelated-looking tracebacks. **The #1048 link was recorded on #1121 as a hypothesis and is now OBSERVED.** #1121 said exactly what would settle it — "queue a manual scan while a slow paginated browse is streaming, and assert the browse completes" — and noted that two earlier attempts had failed because closing and re-using a Session *sequentially* is harmless (a Session simply re-acquires a connection, even under `NullPool`). The reporter's traceback is a cursor being **drained**, so the interleave had to be genuine. `test_1121_worker_close_during_request_fetch.py` builds it: the reader iterates `yield_per` so rows arrive from a live cursor in batches, pauses mid-stream, and only then lets the worker run `duplicate_scan`'s teardown shape (`ensure_session()` … `session.close()`, `cps/tasks/duplicate_scan.py:90,324-325`). Against `main` the reader dies — `InvalidRequestError: Object <_Row> cannot be converted to 'persistent' state, as this identity map is no longer valid. Has the owning Session been closed?` **Scoped honestly**: that is the *mechanism*, not the reporter's exact frame (they saw `sqlite3.ProgrammingError` from `_raw_all_rows`); which error surfaces depends on how far into the fetch the close lands, and the PR says so rather than over-claiming. **Fix**: `session` becomes a property resolving through the `scoped_session` registry on every read. The setter is kept because callers assign to it — `cps/editbooks.py:1072` drops a poisoned session with `calibre_db.session = None` then calls `ensure_session()`, and tests substitute stubs — but those assignments are now scoped to the assigning thread instead of leaking to every other one. **Three contract gaps, each found by the suite rather than by inspection, and fixed rather than worked around**: `__init__` no longer assigns `self.session = None`, because that would run the setter and tear down the *calling* thread's session and `CalibreDB()` is constructed inside request handlers (`cps/web.py:1373`, `magic_shelf.py:1009,1076`); the thread-local slot is built lazily rather than only in `__init__`, since several tests construct instances via `CalibreDB.__new__(CalibreDB)` to avoid needing a Flask app and a property that raises `AttributeError` on a partially built instance is a worse contract than the plain attribute it replaced; and the property needs a **deleter**, because `mock.patch.object(calibre_db, "session", ...)` records that the attribute was absent from `__dict__` and restores by deleting it. `dispose()` reads a new non-creating `_peek_session()` so it does not materialise a Session for the disposing thread purely to close it. Incidental gain: threads read the current `session_factory` on every access, so a factory rebuilt by `setup_db` now reaches every thread rather than only the one that ran the loop. **Explicitly NOT claimed: this does not make `calibre_db` thread-safe.** `notes/fix-udf-gil-deadlock-DESIGN.md` records that `StaticPool` is architecturally required (the calibre engine is `sqlite://` with per-connection `ATTACH` of calibre.db + app.db; per-thread connections via QueuePool/NullPool were considered and rejected as a much larger change), so **every thread still shares one DBAPI connection** and transaction boundaries on it remain process-global — a `commit()` on one thread still commits another's in-flight writes. That is not a regression: with a single shared Session the same held, *plus* the identity map was shared and could be closed underneath a reader. Session sharing is what the reported crashes come from; the residual connection-level sharing is filed as follow-up, not treated as fixed. **Blast radius, and it earned its keep**: the first version of this change caused **38** regressions and the second **25** — both contract gaps above, neither a design fault. Final state verified by running the full `tests/unit` + `tests/smoke` suite on `main` and on the branch in the same venv and diffing the failure sets: **4174 passed / 8 failed on the branch vs 4172 / 8 on `main`, the 8 byte-identical, zero regressions** (the +2 are this change's own tests). Both new test files proven RED against `main` by swapping in `git show main:cps/db.py` and GREEN after. Un-quarantines `test_1121_session_is_thread_local.py` from #1122, which was RED by design. **One caveat recorded rather than hidden**: during an intermediate state (deleter not yet added, so `patch.object` teardown was raising) one suite run stalled ~10 minutes at 0.6% CPU without `pytest-timeout` firing — the signature of the known GIL↔sqlite-mutex class in that design note. It did not recur; the committed code ran the full suite to completion twice (185s, 176s). No route, dependency, license or external URL change. Fork-original. **Extended to also fix [#1150](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1150), which the review of this PR surfaced and which is the other half of the same question — "what is the lifetime of a Session here".** Restoring *thread*-locality is necessary but not sufficient: requests are gevent greenlets and `monkey.patch_all()` is deliberately absent, so every concurrent request runs on **one** OS thread and `threading.local()` cannot separate them. They therefore still shared a Session, while `shutdown_session` (`cps/__init__.py:469-482`) calls `session_factory.remove()` on **every** request teardown — so the first request to finish closed the Session the others were still reading from, and the victim failed wherever it happened to be (`InvalidRequestError: Instance ... is not persistent within this Session`). That is on `main` today, pre-dates this PR, and is reachable by any two concurrent requests; the property alone would have made it *sharper*, since a foreign `remove()` then changes what `calibre_db.session` returns mid-request. **Fix**: `setup_db` builds the factory through a new `_make_session_factory()` that passes `scopefunc=greenlet.getcurrent`. `greenlet.getcurrent()` is a distinct object per greenlet **and** a distinct root greenlet per OS thread — measured, 4 distinct across main + 3 threads — so greenlet scope *subsumes* thread scope rather than replacing it: worker threads stay isolated exactly as the #1121 fix made them, and request greenlets are now isolated from each other too. Per-request `remove()` becomes correct instead of destructive, because it can only reach the caller's own Session. **A leak the scopefunc introduces, caught by measuring rather than by assuming**: passing any `scopefunc` swaps SQLAlchemy's `ThreadLocalRegistry` (a `threading.local`, freed with its thread) for `ScopedRegistry`'s plain dict, which retains a Session for every greenlet that never reaches a teardown — measured at **50 retained for 50 dead greenlets**. Greenlets are weak-referenceable, so the registry map is a `WeakKeyDictionary`, which measures **0**; the earlier "registry does not leak" finding was about `ThreadLocalRegistry` and does **not** carry over. `greenlet` is already a pinned hard requirement (`requirements.txt:40`) and a dependency of gevent, so **no new dependency** — the `ImportError` fallback to default thread scope exists only for the tornado path, where there are no greenlets and thread scope is already right. Verified live in `cwn-local` against the real `@app.teardown_appcontext` hook, and by parallel-request hammering per `notes/verify/FAILURE-MODES.md` class 7. Regression coverage in `tests/unit/test_1150_greenlet_scoped_sessions.py`, including a **falsifiability control** (class 9b) that runs the identical interleave against the factory `main` builds and asserts it still crashes — the guard cannot be imported on `main`, so this is what keeps it honest. Transaction boundaries remain process-global for the `StaticPool` reason above; unchanged by this, and tracked separately. | `d50be7518` | v4.1.22 |
| [#1146](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1146) | (fork issue [#1144](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1144)) | **Every load of the library fetched page 1 twice, at two different sizes, and threw the first response away.** `Catalog.tsx` requests `rowsPerLoad × columnCount` books; `columnCount` started as a **guess** derived from `books_per_page` and was then corrected by a `ResizeObserver` reading the grid's computed `gridTemplateColumns`. The guess and the measurement rarely agree, so `perPage` changed immediately after first render and the query refired — observed on the wire as `page=1&per_page=4` followed by `page=1&per_page=12`. Not a visual defect: sampling the rendered card count every 200ms across the first 2.4s of a load gives `0 16 16 16…`, so the discarded response is never painted. The cost is a wasted round trip per load per user, and on a large library the first-page book query is the expensive query on that page. **Fix**: gate the query on the measurement so only the measured size is ever requested. **The obvious form of that gate deadlocks, and that is the substance of this change** — during first load the skeleton *replaced* the grid, so `gridNode` was null, the observer never attached, `measure()` never ran, the query never enabled, and the library came up **empty** (observed on a deployed branch: `page=1` requests 0, rendered cards 4 — the Discover strip alone). The element that reports the column count only existed once there was something to show. The loading state therefore now renders **inside** the grid container rather than in place of it; a CSS grid reports its track count with no children, so it is measurable on the first paint, and as a side effect the load state lays out on the same track geometry as the cards that replace it instead of reflowing into them. Two guards keep the gate from ever being the reason a library is blank: a series rendered as a list builds no grid and does not wait, and any path that fails to measure within 150ms falls back to the guess and queries anyway — **worst case is the old redundant fetch, never an empty page**. `isFirstLoad` also had to account for the pre-measurement render explicitly, since react-query does not consider a *disabled* query "loading"; without that the page fell through to the empty state and flashed "No books here" before issuing its first request. **Verified against the real backend, not a mock**: the new spec counts what the app puts on the wire with no route interception, and was proven **RED against the pre-fix build** by reverting only the source change, rebuilding and redeploying while keeping the test — `Received array: ["4", "12"]`, the reported signature exactly — then **GREEN on desktop and mobile (375×667)**. **Also repairs `infinite-scroll.spec.ts`, one of the 19 specs failing on every run in [#1130](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1130)**, which could not be repaired honestly until this was settled: it pinned `PER_PAGE = 24` and asserted it on every page-1/page-2 request, but `per_page` is `rowsPerLoad × measured columns` and varies with viewport and density, so **no constant is correct**. The constant was the bug; the duplicate fetch underneath it is what made the failure read as a stale test. The mock now reads the size off the request and serves two pages of whatever was asked for, and gained an assertion that both pages are requested at one size. **Removing the redundant fetch then exposed a latent bug in `useIntersectionObserver` that the redundant fetch had been hiding, and the full-suite blast-radius run is what caught it** — `author-separator.spec.ts` went red, and an A/B against the same container and the same database (baseline build passes in 1.5s, fixed build fails after 19s) proved it was caused here rather than flaky. Root cause: the hook read `sentinelRef.current` at the moment its effect ran, with deps `[enabled, onIntersect, threshold, rootMargin]` — none of which change when the sentinel element *mounts*. Every paged list renders its sentinel only after results arrive, so the order that matters is: results land, `enabled` flips true while the accumulator is still empty and the empty state is rendering, the effect runs and finds no sentinel, and the grid and its sentinel then mount with nothing re-running. The observer stayed bound to nothing, scrolling silently stopped loading, and only "Load more" still worked. The second fetch used to churn `isFetching`/`hasMore` enough to re-run the effect *after* the sentinel had mounted, so the observer was getting attached by accident. Tracking the sentinel as **state** rather than a ref makes the ref callback re-run the effect when the element appears (and again with null when it goes), so the observer follows what is rendered; this covers `Shelf`, `Table`, `MagicShelfView` and `AdvancedSearch`, which all mount their sentinel the same way. **Checked, not assumed, whether that bug is live on `main`**: probing scroll auto-load on the library and table views against an unmodified `main` build reaches page 2 in both (library as `[1,1,2]` — the duplicate fetch, then page 2), so it is **latent** upstream of this change and gets no user-facing changelog claim. Covered by a new spec that scrolls the **real** observer instead of clicking the fallback button — deliberately, since the existing #704 spec stubs `IntersectionObserver` out entirely, which is precisely why nothing caught this — verified RED against the old hook (`requested pages [1]`, never 2) and GREEN after, desktop and mobile. **Blast radius**: full SPA e2e suite run three times (baseline 7 failed / 278 passed, final 10 failed / 278 passed); the four `infinite-scroll` failures and both `author-separator` failures are **fixed**, and the residual delta is an `a11y.spec.ts` cluster **proven non-deterministic** by running it twice against one unchanged build (4 vs 9 failures, different pages each time, including `login (unauthenticated)` and `account`) whose violations are light-theme *sidebar* contrast selectors this diff cannot reach — filed separately. Frontend only — no route, dependency, license or external URL, and no auth/session/CSRF/crypto/subprocess/file-path surface, so no `/security-review` class applies. Fork-original. | `a287cdff1` | v4.1.22 |
| [#1136](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1136) | (fork issue [#1135](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1135)) | **The series line on a book card was composited below the WCAG AA contrast floor.** `BookCard.module.css` set `.series { color: var(--text-muted) }` and then applied `opacity: 0.85` on top of it. `--text-muted` (`#54606b`) is already the muted token and passes at 5.71:1; the extra blend composites it to roughly `#6e7881` and 3.99:1, under the 4.5:1 AA floor for 13px text. The composited colour never appears in the source, which is why static contrast math over the token alone reads as passing and only a runtime scan sees it — axe measured 3.98 against that exact foreground. `BookDetail`'s `.series` computes to `#59646f` with no opacity and passes at 5.35:1, so the card was the only failing surface, reached most often via the More-by-this-author strip. **Fix**: drop the second fade and let the muted token stand on its own. Honest limit: the red could not be reproduced on demand locally — the violation needs a series-bearing card on the scanned page and the dev library has almost none, so the a11y spec passes either way here; the arithmetic and the mechanism are what establish it. No dependency, license, route or external URL change. Fork-original. | `794591847` | v4.1.22 |
| [#1101](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1101) | (fork issue [#920](https://github.com/new-usemame/Calibre-Web-NextGen/issues/920) @iroQuai) | **KOReader highlight sync was undiagnosable: every failure mode on `/kosync/syncs/annotations` was silent, and two of them returned HTTP 200 while saving nothing.** The device surfaces one bit — "Highlights synced" or "Server push failed" — and the plugin has only the HTTP status to go on, so the server log is the sole diagnostic surface for a highlight-sync report. It had none: `push_annotations`/`pull_annotations` carried no INFO logging at all, only two `log.exception` fan-out guards and one `log.debug`. Reproduced on the wire against a live `cwn-local`: four distinct outcomes — 400 `invalid_deleted`, 400 `invalid_delete_source`, an unknown document, and a malformed annotation — produced **zero** log lines between them. Two are worse than silent because they are **200**, so the device tells the user the sync succeeded: an unmatched document returns `matched: false` (the usual shape of a book with no registered checksum, i.e. "my highlights sync but never appear"), and a payload `apply_portable` declines returns `skipped: N` with the highlight dropped on the floor. This is the root cause of #920's ten-day, three-wrong-diagnosis loop: @iroQuai read `docker logs`, correctly saw no annotation processing, and reasonably concluded the request never arrived — but silence was the *expected* output of a fully successful push too, so the observation could not discriminate anything. The progress route next door logs every save at INFO (promoted in #312 for exactly this reason); the annotation routes never got the same treatment. **Fix**: every rejection returns through one `_reject()` helper that logs user + document + error code + message, so no branch can regress to silence individually; the success path logs `created/updated/deleted/skipped` plus `pushed`/`named_deletes` counts; `matched: false` and `skipped > 0` log at **WARNING** since both are 200s that lose data; a delete naming ids that matched no live row logs the ids. **Hardening found in self-review of this change**: `is_valid_key_field` bars only colons and over-length, so a newline passes — logging the raw rejected `document` would have let a device forge log lines in the very surface being added. All nine device-supplied interpolations go through `_loggable()` (repr-escape + 80-char cap). **14 regression tests** in `tests/unit/test_920_koreader_annotation_sync_observability.py`, verified **12 RED on main / 14 GREEN after** (2 added with the hardening), driven through real Flask routing so a rejection that short-circuits before the handler body still has to account for itself. Two test premises were corrected against observed behaviour rather than assumed: an unrecognised key is `skipped`, not a 400, and `is_valid_key_field` accepts spaces. 62 adjacent KOReader/annotation tests re-run green. **Live-container verified** on patched `cwn-local`: the exact delete push the device makes now logs `created=0 updated=0 deleted=1 skipped=0 (pushed=0 named_deletes=1)`, the 400 logs `error=invalid_deleted (...)`, the unmatched book logs `WARN ... matched NO book — 1 annotation(s) and 0 delete(s) were NOT saved`. **Cross-family review (Terra, 2 rounds, session resumed for round 2) caught a regression this PR introduced**: the unmatched-book reply is returned *before* the `annotations`/`deleted` shape checks, so taking `len()` of them for the log line turned a request that has always answered 200 into a **500** (`{"annotations": 1}` -> `TypeError: object of type 'int' has no len()`; `{"deleted": true}` likewise) — confirmed on the wire, fixed via `_describe_count()` (counts a real list, names the shape otherwise), 6 shape cases added of which 4 were RED against the pre-fix commit. Round 2 added three LOW fixes: `_describe_count({})` was calling the single most common shape KOReader sends "not an array" (Lua emits `{}` for an empty table and the handler accepts it as an empty set); the rejection-body test asserted only a substring of `message`, so all six rejection contracts now pin the exact body incl. `invalid_payload` and the invalid-document response that were uncovered; and `test_successful_delete_logs_the_named_ids` was misnamed (it pins counts — aggregate logging kept deliberately, since ids are volume with no diagnostic gain on success and are already logged when they match nothing). Final: **27 tests** in the new file, **108** across the KOReader/annotation suites, live `cwn-local` rebuilt with 0 errors since restart. Logging only — no behaviour, route, dependency, license or external URL change; adds ~2 lines per sync, matching the existing kosync progress convention. Fork-original. | `4b5eacb95` | v4.1.21 |
| [#1099](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1099) | (fork issue [#1089](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1089) @chloeroform) | **Firefox 153 users got permanent, always-visible scrollbars — in BOTH UIs.** Not a regression we shipped: reproduces on v4.1.10 too. The SPA styled scrollbars **only** through the legacy `::-webkit-scrollbar` pseudo-elements (`frontend/src/styles/global.css` globally, plus `.strip` in `DiscoverSection.module.css` and `MoreByAuthor.module.css`), and the classic caliBlur theme did the same for its page chrome (`cps/static/css/caliBlur.css`). Those were a WebKit/Blink extension Firefox ignored, so Firefox fell through to the platform's native *overlay* scrollbars — fade-when-idle, zero layout space. **Firefox 153 began honouring `::-webkit-scrollbar`**, and any engine that honours it demotes that scroller to a **classic** scrollbar: visible the whole time content overflows, permanently stealing its width from the content box. **Fix**: `scrollbar-width`/`scrollbar-color` (standard; Firefox 64+, Chrome/Edge 121+, Safari 18.2+) become the single source of truth for the slim dark look — they carry the same intent *without* taking a scroller off native overlay behaviour — and the legacy webkit block is retained only as a fallback scoped by `@supports not (scrollbar-width: thin)`. Keyed off the **feature, not the engine**, deliberately: the reporter proposed a Firefox carve-out, but that would need rewriting for whichever engine adopts the pseudo-element next and leaves the trap in place; this way old Chrome/Safari keep today's styling and no future engine re-triggers it. Accepted fidelity loss: `::-webkit-scrollbar-thumb:hover` brightened the thumb and the standard properties have no hover variant, so modern engines lose that brighten (the fallback keeps it). **Verified in real Firefox 153** (Playwright 1.62 ships it) with `ui.useOverlayScrollbars=1` = the reporter's config; note the dev Mac is set to "always show scroll bars", which masks the bug entirely. Isolated mechanism test: the shipped CSS is the *only* variant producing a classic scrollbar (15px gutter); native, `scrollbar-width: thin`, `scrollbar-color`, and the fix all yield 0. Live `cwn-local`, SPA across home / book detail / mobile 420x620: sidebar 15px→0, Discover strip 11px→0, 4 layout-stealing scrollbars→0. **Classic caliBlur across home / book detail / shelves: both page columns 15px→0, 6→0** — found by auditing the sibling surface rather than stopping at the reported one, and shipping only the SPA half would have made the changelog entry an over-claim for anyone on the classic theme. In caliBlur `body` already set `scrollbar-color` (inherited), but `scrollbar-width` is not inherited, so the columns needed their own `thin` declaration once the webkit rules stopped applying. Scoped out with reason: `css/libs/` (vendored third-party) and `kthoom.css` (its webkit rule sets `display: none` paired with `scrollbar-width: none` — honouring it hides the scrollbar, which is the intent, so there is no demotion to undo). Built bundle audited post-`npm run build`: 9 `::-webkit-scrollbar` occurrences, 0 ungated. **7 regression tests** in `tests/unit/test_1089_firefox_scrollbar_overlay.py`, RED on main (2 failures) / GREEN after — a source scan asserting no `::-webkit-scrollbar` rule ships outside an `@supports not (scrollbar-width: ...)` guard, three self-checks proving the scanner catches the bug's shape / accepts a guarded block / ignores commented-out rules (without them the main assertion could pass by matching nothing), plus a pin that the standard properties survive on all three surfaces so the bug can't be "fixed" by deleting the theming. **Deliberately a source pin, not an e2e spec**: the regression only reproduces in a *headed* browser in overlay mode — headless Firefox reports 0px for the broken CSS too, so an assertion in the SPA harness (headless, Chromium) would have passed against the bug. CSS + one test file; no route, dependency, license or external URL. Fork-original. | `cdb1451b5` | v4.1.21 |
| [#1103](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1103) | (fork issue [#1095](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1095) @chloeroform, adopts their PR [#1102](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1102)) | **Adding the library to an iOS home screen produced a screenshot of the page instead of the app icon — and two dead icon assets shipped in every image.** One root cause: nothing checked that the files under `cps/static/` and the references to them agree, so the tree carried assets nothing points at *and* pointed at assets that don't fit. **(1) `apple-touch-icon` was declared but never wired up.** All nine declarations — eight Jinja templates plus `cps/spa.py`, which injects its `<head>` tags in Python rather than a template — emitted `<link rel="apple-touch-icon" sizes="140x140" href=".../favicon.ico">`. Two independent faults in one tag: iOS cannot use an `.ico` for a home-screen icon at all, and the ICO contains no 140x140 member (its members are 16/32/48/64/128/256), so the declared size was a lie about a file iOS was going to reject anyway. Result: "Add to Home Screen" on iOS/iPadOS had nothing usable and fell back to a thumbnail of whatever page the user happened to be on. A purpose-built **180x180 PNG in matching NextGen branding was already on disk, unreferenced**, at `static/img/apple-touch-icon.png` — the classic half-a-feature shape: the asset was made, the wiring never landed. All nine now point at it with the true size, and both paths stay prefix-aware, so it holds behind a reverse-proxy subpath mount (the #571/#574 class, pinned by a test). **(2) `cps/static/icon.png` + `icon.svg` deleted** — the pre-`favicon.ico` app icons (`icon.svg` is the old teal `#45b29d` calibre-web "C" glyph, not NextGen branding). Audited across every reference channel: templates, Python, `cps/static/js`, CSS `url(...)`, the SPA source *and the built production bundle*, dynamic `url_for('static', filename=<expr>)` construction, PWA manifests, every `rel="icon"`/`apple-touch-icon`/`mask-icon`/`msapplication-*`/OG tag, Dockerfile, `root/` s6 scripts, workflows, packaging, docs and tests — zero references. `git log -S` shows a reference to these paths **never existed** in this repo's history: they were carried into the refactored tree in `73eecc175` and wired to nothing from day one, shipping ~29 KB of dead payload per image. The only code touching them is `cache_buster.py`'s untargeted `os.walk`. **Tests** (`tests/unit/test_static_asset_integrity.py`): **5 RED on a clean `main` worktree / 12 GREEN after** (incl. the 3 pre-existing `test_574_spa_favicon.py`). Two of them **render the tag out of Flask and read it off the response** rather than grepping template source, so a broken injection is caught even when the source string still looks right; one parses the PNG IHDR chunk directly (no image library) to assert the declared `sizes` matches the file's real pixels, since iOS picks by declared size and a lying `sizes` is worse than none. Plus a partial invariant — every *literal* `url_for('static', filename='...')` in a template resolves to a shipping file — with a pinned one-entry exemption for `koplugin.zip` (zipped by the Dockerfile at image-build time), so a new missing asset can't hide behind the exemption. **Live `cwn-local`**: `/static/icon.{png,svg}` 200 -> 404, `favicon.ico` + `img/apple-touch-icon.png` still 200, classic and SPA `<head>` both emit the 180x180 PNG, subpath mount emits `/cwa/static/img/apple-touch-icon.png`, no new ERROR/WARN/Traceback in `docker logs --since 4m`. **Cross-family review (Terra) returned a block and was right**: the branch originally also shipped `cps/static/robots.txt` with `Disallow: /` to fix `GET /robots.txt` 404ing (the route exists in `cps/web.py` but the file has never existed here *or* upstream in janeczku/calibre-web). Terra flagged that as silently de-indexing every already-public instance on upgrade, citing `GOOGLE_SITE_VERIFICATION` support as evidence that some operators deliberately want indexing — **verified**: `cps/admin.py:166` reads that env var and nine templates emit `<meta name="google-site-verification">`. Choosing a crawl policy is a behaviour decision, not an asset cleanup, so it was cut from this PR and tracked as [#1104](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1104) with the design options. Terra's MEDIUM (tests were source-pins, not route behaviour) drove the two rendered-response tests above; its second HIGH ("robots.txt violates rule 6 as a bundled asset") was declined — rule 6 covers dependencies, licenses and external service URLs, and the anti-slop bar's "bundled asset" means a third-party asset with licensing implications, not a first-party text file. No route, dependency, license or external URL change; no `/security-review` class touched. Fork-original + community adoption. | `93652eb30` | v4.1.21 |
| [#1096](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1096) | (fork issue [#1074](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1074) @bentsea) | **With anonymous browsing on, a guest clicking "Read" got a 500 in the classic view and a silent bounce to the home page in the SPA — the book never opened in either UI.** Three independent defects stacked on one click, each of which alone blocks the flow. (1) **`cps/web.py:read_book`** carried a redundant `import json` inside its `if current_user.is_authenticated:` branch. Python binds local-vs-global per *name* for the whole function at compile time, so that nested import made `json` a local for every line; a signed-in user executed it and never noticed, a guest skipped the branch and hit the unconditional `json.dumps(reader_settings)` at the end of the epub path -> `UnboundLocalError: cannot access local variable 'json'`, the reporter's exact traceback. Deleted (the module already imports json at `cps/web.py:8`); the two further copies in `login_post` were benign — each sat immediately above its only use — but are the same trap one edit away from firing and went with it. (2) **`frontend/src/lib/api.ts`** conflated "this visitor is anonymous" with "this visitor's session ended". The reader's `GET /books/<id>/bookmark` and `GET /reader/settings` answer 401 for an anonymous caller *by design* (`_require_real_user`, pinned by `tests/unit/test_api_v1_reader.py`); `classifiedFetch` then confirmed via `/auth/me`, saw `role.anonymous` (#1023), and spent the destructive remedy — `navigateToLogout()`, which `cps/logout.py::cleanup_local_logout` uses to delete the session server-side. A session can only be *lost* if it once existed, so the probe now reports "gone" only when an authenticated `/auth/me` was observed first (`noteSessionIdentity`, fed from `useMe`'s bootstrap read). This narrows the remedy without removing it: #824/#1067 expiry detection is unchanged and `auth-expiry.spec.ts` stays green. (3) **`frontend/src/pages/Reader.tsx`** gated epub.js startup on a reader-settings *payload*, which a guest can never receive, so even without the bounce the reader sat empty forever. Hydration now waits for the query to *settle* rather than succeed and boots on the client defaults — settled-with-nothing is a real answer. `useBookmark`/`useReaderSettings` also stop retrying a 401, which is definitive, not flaky. **Root-cause sweep, not a spot fix**: an AST pass over all of `cps/` established that `read_book` was the *only* live instance of the conditional-shadowed-import shape (8 other candidates proved to be unconditional top-of-function imports); `test_no_conditionally_shadowed_module_import_in_cps` now sweeps for it repo-wide so the class cannot return silently. **Red/green, both halves, on a real container** (`cwn-local`, `config_anonbrowse=1`, Guest granted viewer): classic `GET /read/2/epub` 500 -> 200 with the reader rendered and `readerSettings: {}` as designed, zero `UnboundLocalError` in logs, authenticated read still 200 with real saved settings, and failed-login activity logging (which one removed import served) still writes valid JSON. SPA pre-fix: `/app/read/2` -> bookmark 401 + settings 401 -> probe -> **`GET /logout` 302** -> lands on `/app/`, no reader — the reporter's literal symptom; post-fix: same three 401s absorbed, no `/logout`, URL holds, epub iframe live. 4 unit tests + new `frontend/e2e/guest-reader.spec.ts` (2 tests), all verified RED against the pre-fix build and GREEN after; `auth-expiry.spec.ts` (9 tests) re-run green so the expiry path is provably preserved. No new route, dependency, license or external URL. Fork-original. A cross-family review of the PR then found that the narrowing in (2) keyed off a signal an *in-app sign-in* never supplies: `useLogin`/`useMagicLinkPoll` seed the me-cache with `setQueryData`, so the app flips to the authenticated tree and issues protected calls a round trip before the invalidation refetch answers, and a session dying in that window read as a guest who was never signed in. Both seeding sites now record the identity they seed with, closing the gap; `auth-expiry.spec.ts` gains a route-mocked case that fails against the parent commit and passes after. | `c390535a0` | v4.1.21 |
| [#1071](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1071) | (none — fork-original) | **The classic UI had no link to support the project.** The new UI carried one in its user menu; the classic themes carried none, so a user on the default or caliBlur theme who wanted to contribute had to leave the app to find where. Adds a "Support Calibre-Web NextGen" entry to the classic user menu in `cps/templates/layout.html`, which both classic themes render, so one template change covers default and caliBlur. Additive markup only — no route, dependency, license or external service URL (the target is the existing support page). Fork-original. | `fb1178f93` | v4.1.20 |
| [#1080](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1080) (adopts community [#1075](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1075) @chloeroform) | (fork issue [#1022](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1022) @chloeroform; measured in [#868](https://github.com/new-usemame/Calibre-Web-NextGen/issues/868) @hayvan96) | **The `cwa-auto-library` startup step walked every file under `/config` and `/calibre-library` on every boot, so a large library added minutes to container startup** (#1022 measured ~5 minutes stuck before "Existing library found"). Fix: `scripts/auto_library.py` now (a) tries `/config/app.db` directly before falling back to a full `os.walk()` of `/config` for `app.db`, and (b) locates `metadata.db` with a top-down walk that stops descending into a directory once it yields a `metadata.db` — a Calibre library keeps its DB at the root and never nests another library inside its book folders, so this skips the deep per-book recursion (the ~5-minute cost) while still comparing every candidate library root for "largest wins". Adopted from @chloeroform's #1075 **with the fresh-install crash it introduced removed**: #1075 re-initialised `self.app_db = None` and only re-set it on the found-at-default branch, so a brand-new container (no `app.db` anywhere) left `app_db=None` and the later `sqlite3.connect(self.app_db)` in `update_calibre_web_db` failed into `sys.exit(1)` — a boot crash-loop on every fresh deployment, a path the reporter's existing-library case never exercised. A cross-family review (GPT-5.6-Terra) then caught two more issues in the first cut that were fixed here: the metadata default-shortcut silently dropped "largest wins" (a stale root DB could shadow the real sub-folder library — replaced by the prune-on-find walk above), and `os.path.exists()` accepted a *directory* named `app.db`/`metadata.db` (now files-only via `isfile`/walk-filenames). The rework keeps `app_db` aligned to the canonical `/config/app.db` in every branch. Red/green: `tests/unit/test_auto_library_default_path.py` (9 tests) — the crash tests fail against #1075-as-submitted; the perf (prune/skip-walk) and root-authoritative contract tests fail against main; all pass on the rework. Contract note: a `metadata.db` at the library root is now authoritative over a library nested below it (the mount point Calibre-Web uses). No new dependency, license or external URL; pure-stdlib boot script. Community-adopted, credit @chloeroform. | `84a5a99` | v4.1.20 |
| [#1068](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1068) | (fork issue [#1067](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1067) @mrfearless) | **The new UI signed users out for real whenever a request failed to reach the server: shelf navigation intermittently spun and landed on the login page, and "remember me" could not bring the session back.** Root cause was the auth-loss classifier in `frontend/src/lib/api.ts`, not the session layer. `classifiedFetch` treated a *rejected* `fetch` - i.e. any transport-level failure: connection reset, read timeout, DNS, offline, CORS-blocked redirect - as proof the session had expired, and its remedy was `navigateToLogout()`, a hard navigation to `/logout`. That route is not cosmetic: `cps/logout.py::cleanup_local_logout` deletes the `User_Sessions` row and clears the remember-me cookie, so a single dropped request destroyed a still-valid session and no retry could recover it. The same over-broad classification applied to a bare 401 and to any redirect-to-HTML, both of which a route's own permission check or a reverse proxy can produce without the session having ended. On a low-power NAS (the reporter is on a QNAP TS-453A) that drops the occasional request under load, this made shelf-to-shelf navigation a coin flip and explains why re-ticking "remember me" never helped. Fix introduces one authoritative confirmation - `sessionIsGone()` re-probes `/api/v1/auth/me` and only reports a lost session on a positive answer (401, an `opaqueredirect`, or, with anonymous browsing on, a `role.anonymous` payload per #1023); anything else, including a probe that itself cannot reach the server, keeps the session. `redirect: 'manual'` on the probe is load-bearing: an expired external-auth session (#824, Authelia) answers with a cross-origin redirect whose *following* would trip CORS and reject the probe with the same `TypeError` a dead network produces, so not following it keeps #824 detectable while a transport fault stays a transport fault. The probe is single-flight, because a server that just dropped one request is usually dropping the several others in flight and one probe per failure would aim a burst at the server already struggling. **Root-caused against two rejected hypotheses, both disproved live rather than argued away**: (a) Flask-Login strong session protection (`config_session` defaults to 1 / strong, and `_create_identifier` keys on IP + User-Agent) - disproved by flipping the User-Agent mid-session on `cwn-local` and observing the session survive, because `login_user(remember=True)` sets `session["_permanent"]`, which routes `_session_protection_failed` down the non-destructive branch; (b) the v4.1.19 anonymous-browsing change (`6c79754c5`) - inert unless `config_anonbrowse` is on, which does not match a reporter using a password login. **Red/green**: new `frontend/e2e/session-survives-transport-fault.spec.ts` drives the reporter's literal action (open `/app/shelves` with the shelves request aborted) and asserts both that no logout navigation occurs and - the load-bearing half, with `/logout` deliberately left un-intercepted - that `/api/v1/auth/me` still returns a non-anonymous user afterwards, so a regression fails by actually destroying the session. RED against the pre-fix bundle in `cwn-local` at exactly 1 logout navigation, GREEN after, on **desktop 1280x800 and mobile 375x667**. Also pins a route-level 401 with a live session as non-destructive, the single-flight probe count under concurrent failures, and both #824 directions. `frontend/e2e/auth-expiry.spec.ts` was updated rather than deleted: its four expiry tests simulated expiry by failing one endpoint while the session was in fact still valid, which under the corrected semantics is "one endpoint misbehaved", so they now end the session at the probe and keep asserting #824's real requirement. No new route, dependency, license or external URL; no backend change. Fork-original. | `1c25897c3` | v4.1.20 |
| [#1065](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1065) | (fork issue [#1064](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1064) @KucharczykL) | **The new-UI edit page re-flowed by 8px whenever a book's rating changed: the Languages input grew while the book was unrated and shrank the moment a star was clicked.** Root cause was two divergent star renderers, not the `Clear rating` link the reporter suspected (that button is always rendered, merely `disabled` at 0). `RatingSelector` in `EditBook.tsx` drew a rated book through `<StarRating>`, whose track is an `inline-flex` with a `gap: 2px` between five slots (5x26 + 4x2 = 138px), and an unrated book through five bare lucide `<Star>` glyphs laid out directly in `.ratingStars` with no gap at all (5x26 = 130px). Four missing gaps is exactly the 8px measured, and because Rating is the `flex: 0 0 auto` sibling of a `flex: 1 1 0` Languages field on the same `.row`, the whole row re-flowed on every rating change. Fix removes the second renderer rather than reserving space for it: `StarRating` gains a `decorative` prop that draws the empty track at rating 0 (instead of returning `null`) and marks the track `aria-hidden` because the caller - the edit page's `role="slider"` - already carries the name and value; a duplicate `role="img"` inside a slider announced the rating twice. `EditBook` now renders `<StarRating rating={rating * 2} size={26} decorative />` in both states, so the widget can no longer disagree with itself about its own width. The native tooltip the rated state used to get from `StarRating`'s `title` moves onto the slider, where it now also covers the unrated case. Non-decorative callers (`BookDetail`) are unchanged. **Red/green**: a new Playwright spec in `frontend/e2e/edit-rating.spec.ts` measures the star track and the Languages input across unrated -> 4 stars -> 2.5 stars; RED against the pre-fix bundle in `cwn-local` at exactly 8px, GREEN after, on **desktop 1280x800 and mobile 375x667**. Live measurement post-fix: Languages 404.45px in both states, delta 0. Full SPA e2e matrix (desktop + mobile, including the a11y gate and `book-detail.spec.ts`'s `role="img"` rating assertion) green. No new route, dependency, license or external URL. Fork-original. | `a6d77136` | v4.1.20 |
| [#1062](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1062) | (fork issue [#1061](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1061) @KucharczykL) | **Star ratings could not be set on the new UI edit page: clicking a star did nothing on an unrated book and wiped the rating on a rated one.** Root cause was DOM semantics, not the click maths. `EditBook.tsx`'s `Field` wrapper renders a `<label>`, and a `<label>` forwards any click inside it to its first labellable descendant. The rating selector owns a `Clear rating` `<button>`, so every star click produced two events: the real one on the star (React set the value) and a browser-synthesised activation on the button (`onChange('')` cleared it) - net zero on a rated book, and on an unrated one the button is disabled so the pre-existing 0 simply stayed. Observed live in Chrome: a single `page.mouse.click` on a star dispatched `path` then `BUTTON._clearRating`, and `aria-valuenow` went 3.5 -> 0. Fix: `Field` gained a `composite` mode that renders `<div role="group" aria-labelledby>` instead of `<label>`, used by the Rating field - same visual, no implicit activation, and the label text still names the group for assistive tech. Only the Rating field is composite; the identifiers block already used `role="group"`. **Red/green**: 3 new Playwright specs in `frontend/e2e/edit-rating.spec.ts` - all 3 RED against the pre-fix bundle in `cwn-local` (star click leaves the value at 0 / snaps to 0; the widget sits in a `<label>` owning a control), all GREEN after, on **desktop 1280x800 and mobile 390x844**. Coverage is behavioural (unrated -> 5, then 2.5, then 3.5 at the documented half-star granularity; the chosen value survives Save + reload) plus a structural pin so a future re-wrap in a `<label>` fails by name rather than as flake. **Live container verification on cwn-local**: pre-fix reproduction of both reporter cases with the click-event trace above; post-fix an unrated book goes 0 -> 4 on a real mouse click at 75% width, and the value round-trips through the metadata API. No new route, dependency, license or external URL; no CSS change. Fork-original. | `6a3a52970` | v4.1.20 |
| [#1053](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1053) | (fork issues [#1023](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1023) @bentsea, [#1045](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1045) anonymous in-app report) | **Anonymous browsing was walled off in the new UI.** With `config_anonbrowse` on, the SPA showed a login screen and no library; the classic view let the same visitor in, which is why the in-app reporter switched back. Two layers. (1) `/api/v1/auth/me` sits in `_PUBLIC_ENDPOINTS` (it must, or login is impossible) and so bypasses the blueprint gate that *does* honour `config_anonbrowse` — it then re-decided with a bare `current_user.is_authenticated`, which is False for `ub.Anonymous` by definition, and 401'd. `useMe()` maps 401 → `me = null` and `App.tsx` renders its login tree for `!me`, so one endpoint's answer gated the whole app while `/books` was already serving guests. Now also passes when anon-browse is on, via `getattr(config, 'config_anonbrowse', 0)` so a minimal bootstrap config fails closed to 401 rather than faulting to 500. (2) `theme`/`ui_font_body`/`ui_font_display` are `User` columns, not `UserBase`, and `Anonymous.loadSettings` hand-copies the Guest row — it never picked them up, so `serialize_user(Anonymous())` raised AttributeError; fixing (1) alone would have turned the 401 into a 500. **SPA follow-through:** `me` is no longer equivalent to "signed in", so `AuthenticatedAuthLanding` would have bounced a guest off `/login` and left them unable to sign in at all — the login/magic-link routes now render the real forms for a guest, and the account menu offers Sign in instead of a dead "My account" (401s) and "Sign out" (no session). **Red/green** 7 tests (`test_1023_anon_browse_spa_login_wall.py`): anon-on serves Guest, anon-off still 401s, missing setting fails closed, payload flagged anonymous, plus an AST-derived drift guard requiring `ub.Anonymous` to carry every attribute `serialize_user` reads. **Live e2e** cwn-local: guest sees 40 books / 0 password inputs, menu shows Sign in, `/app/login` renders the form and a real login lands as the user; with anon-browse off `/auth/me`, `/books` and classic `/` all refuse exactly as before. No new dependency/URL. `/security-review` clean (no HIGH/MEDIUM). | `6c79754c5` | v4.1.19 |
| [#1049](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1049) | (fork issue [#521](https://github.com/new-usemame/Calibre-Web-NextGen/issues/521)) | **Russian, Ukrainian, Greek and Japanese libraries sorted with distinct letters merged.** The #521 diacritic fold stripped combining marks from *every* script, so Й/И, Ї/І, ά/α and が/か each collapsed into one letter in the book list, the A–Z filter and OPDS feeds — all three read the same SQLite collation UDF. Folding now applies only where the base character is Latin, and the result is recomposed so kept marks rejoin their letter. **Red/green** 13 assertions in `tests/unit/test_521_unicode_collation.py`, one of them driving the real UDF rather than the helper, plus the reporter's original #521 Latin cases pinned unchanged. No new route, dependency, license or external URL. Fork-original. | `809d3cfde` | v4.1.19 |
| #1024 | (upstream issue [crocodilestick/Calibre-Web-Automated#1288](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1288), reporter @blurrycontour; community PR [#1018](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1018) by @budget-coder) | **The duplicates badge no longer polls `/duplicates/status` on a quiet instance.** Any open tab asked the server for duplicate status every 2.5s indefinitely — measured live at **25.0 req/min (~1,500/hr/tab)** — which the reporter saw as reverse-proxy log bloat. **Root cause was the restart condition, not the interval**: `handleStatusResponse` ended in an unconditional `if (data.enabled) startStatusPolling()`, so every response re-armed the timer, and the 60th attempt's response resolved *after* `stopStatusPolling()` nulled `pollTimer`, starting a fresh cycle forever — `POLL_MAX_ATTEMPTS = 60 // ~2.5 minutes` bounded nothing. Second half: the server reports `needs_scan` (admin must trigger a full scan) *alongside* `stale`, and the old code polled on `needs_scan \|\| stale`; that state waits on a human and is where a **default install sits**, so keying the fix on `stale` alone would have left the reported case untouched. **Fix**: poll only while a scan is genuinely in flight (`stale && !needs_scan`), back off 2.5s→30s, schedule at most one follow-up per response via `setTimeout` (no free-running `setInterval` to leak or re-arm), stop on the first settled response; freshness on a quiet instance comes from page load + the existing `visibilitychange` refresh (verified: +1 request exactly, no loop restarted). **9 new behavioural tests** in `tests/unit/test_1288_duplicates_idle_polling.py` execute the shipped JS in Node against a stubbed DOM/fetch with a virtual clock and count real requests — **RED on main: 7 failed incl. `1441 requests in an hour of idling`**, independently reproducing the live 25 req/min; 2 correctly pass on main (a running scan *is* polled; no-permission stays off the wire) proving the suite isn't blanket-failing; 9/9 green on branch. A source-pin on the constant would have passed on the #1018 shape, hence the executing harness. **Live A/B on cwn-local** (duplicates enabled = schema default, instance in the reporter's `{stale:true, needs_scan:true}` state): 25.0 req/min → 1 per page load; 81s idle = 1 request (pre-fix ~33). **Feature intact end-to-end**: real `POST /duplicates/trigger-scan` → 200 queued → scan completed → `count:9` → on reload the "Duplicate Books Detected" modal appeared on its own listing 9 groups (screenshot). **Anti-slop disposition of #1018**: @budget-coder found the right file but 60s only divides the traffic 24× while the self-restarting loop remains (still indefinite), never fires in the `needs_scan` default state, leaves the `~2.5 minutes` comment describing ~60 minutes, and lags a finished scan by up to a minute — reworked to remove the loop rather than slow it, author credited, their PR closed with a pointer. | `ad927971e` | v4.1.19 |
| [1055](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1055) | (fork issue [#1048](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1048) @auspex) | **The new UI could not run a duplicate scan at all, and the Admin panel's "Duplicate Books" row was a byte-identical copy of the sidebar link.** The only manual trigger, `POST /duplicates/trigger-scan`, is on the legacy blueprint whose page the SPA route shadows, so the SPA had no reachable path to it; worse, `Duplicates.tsx`'s `needs_scan` empty state told admins to "run it from CWA settings", a page with zero scan controls (`grep -c 'trigger_scan' cps/templates/cwa_settings.html` -> 0) — a dead end. Added `POST /api/v1/duplicates/scan` (`cps/api/duplicates.py`), admin-or-edit gated and **not** csrf-exempt (unlike the legacy route: the SPA sends `X-CSRFToken` on every POST, so the endpoint keeps standard `/api/v1` protection). It **delegates** to the legacy view rather than re-implementing it, so cache invalidation, task queueing, the synchronous fallback and auto-resolution stay a single implementation — pinned by a test that fails if `TaskDuplicateScan` is ever re-implemented in the API module. SPA: `useTriggerDuplicateScan` + a "Scan for duplicates" header button with an `aria-live` status line, and the wrong empty-state copy replaced. Admin panel row now deep-links to `/cwa-settings#duplicate-detection` (new anchor on the existing section) — the reporter's expectation that the admin entry configures detection rather than re-opening the review list. **Red/green**: 13 tests in `test_1048_duplicate_scan_api.py`, 12 RED on main (endpoint absent, wrong copy, duplicated admin href, missing anchor, unanchored msgids), all 13 GREEN on branch; `tsc --noEmit` + `npm run build` clean. **Live container verification on cwn-local**: `POST /api/v1/duplicates/scan` as admin -> `{"queued":true,"task_id":...}` 200 and the task ran to `Duplicate scan completed: 9 groups`; anonymous and no-CSRF both rejected 400 at the CSRF layer, non-admin/edit 403 at the guard; a **real UI click** on the button queued and completed a second scan and rendered the status line; mobile 390x844 button 151x38px with no viewport overflow and the header wrapping cleanly; the admin row's deep link lands with the duplicate-detection section scrolled to the top of the inner scroll container; zero new Tracebacks in container logs. New msgids anchored in `cps/spa_strings.py` (babel does not scan .tsx). **Cross-family review (Terra) findings, both fixed before merge**: (1) no single-flight — the SPA button re-enables as soon as the POST returns, so clicks/tabs could stack an unbounded FIFO of full-library scans; a scan already queued or running now short-circuits to an idempotent `200 {"already_running":true}` via the existing `cwa_functions._duplicate_full_scan_running()`. (2) the legacy trigger's fallback rebuilds the index INLINE on the request path when the worker can't take the task, and CWNG runs gevent without `monkey.patch_all()`, so that freezes every other request for the length of a full scan; `trigger_scan()` gained `allow_sync_fallback` and the API passes `False` (-> retryable 503), while the classic page keeps the historical behaviour via the default `True`. Re-verified live: back-to-back POSTs -> second short-circuits; 8 concurrent POSTs -> exactly 1 queued, 7 short-circuited (gevent's cooperative scheduling makes check-then-enqueue effectively atomic). `/security-review` clean (no HIGH/MEDIUM). Also generalized `test_603_admin_legacy_link_prefix`'s #909 pin: it asserted the literal "Duplicate books" admin row that this change correctly replaces, so it now cross-checks every Classic fall-through card against `routes.ts` and fails if one sends users to Classic for a path the SPA owns — verified still red when `/duplicates` is turned back into a Classic row. No dependency, no license change, no external URL. Fork-original. | `416ca9bf8` | v4.1.19 |
| [1047](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1047) | (fork issues [#870](https://github.com/new-usemame/Calibre-Web-NextGen/issues/870), [#867](https://github.com/new-usemame/Calibre-Web-NextGen/issues/867) @auspex, [#866](https://github.com/new-usemame/Calibre-Web-NextGen/issues/866) @auspex) | **Smart shelves could not be marked for Kobo sync from the new UI, and the shelf-parity warning that explains an inert mark never rendered on ANY shelf.** The Kobo half was fully wired server-side (`MagicShelf.kobo_sync`, its migration, and `cps/kobo.py`'s collection materialisation + DeletedTag tombstones); the only writer was the classic whole-shelf edit form, so the SPA had no narrow write and shipped no button. Added `POST /api/v1/magicshelf/<id>/kobo-sync` (owner-only — `cps/kobo.py` selects magic shelves by `user_id == current_user`, so a write against another user's public shelf would be a no-op with side effects; bumps `last_modified` because the Kobo tag/tombstone payloads carry it as the change stamp; mirrors the classic route's #359 warning when the global `config_kobo_sync_magic_shelves` is off), surfaced `kobo_sync` on both magic-shelf read payloads, and added `kobo_sync_magic_shelves` to the `/me` feature flags so `MagicShelfView.tsx` only renders the button when it can actually do something. Also brought the #866 inert-mark notice (`me?.kobo_only_shelves_sync === false`) to the smart-shelf page, which lacked it; ordinary shelves already had it working. *(An earlier draft of this row claimed `/api/v1/auth/me` never emitted `kobo_only_shelves_sync` and that the notice was dead on all shelf types. That was wrong — `serialize_user` has supplied it since #1008, and `_me_payload` returns `serialize_user`'s dict. Verified live: with main's `auth.py` + `serializers.py`, `/api/v1/auth/me` already returns the field. The redundant re-assignment and the test that stubbed `serialize_user` out to manufacture a red were removed in the review pass.)* **Review pass** (cross-family Sol + `/security-review`, disposition in the PR comments): non-boolean request bodies performed the opposite write via `bool("false")`, commit failures outside `OperationalError`/`InvalidRequestError` left the long-lived global session unrolled-back, the driver exception was echoed to the client, and the cache invalidations were fire-and-forget; all fixed. Test-boundary gaps tracked in [#1052](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1052). **Red/green**: 20 tests in `test_870_magic_shelf_kobo_toggle.py` — the original 12 RED on main (endpoint absent), the 8 added by the review pass RED with the source fixes stashed, all 20 GREEN on branch; 66 adjacent tests (`test_api_v1_auth`, `test_api_v1_shelves`, `test_667_spa_magic_shelf_visibility`, `test_kobo_magic_shelf_intent_migration_359`, `test_866_kobo_shelf_only_archive_sweep`) still pass; `tsc --noEmit` + `npm run build` clean. **Live container verification on cwn-local**: default state (global setting OFF) renders no button — the dead-control case; GET `/api/v1/magicshelf/1` returns `kobo_sync`; POST persists + bumps `last_modified` + returns the inert-intent warning; non-owner 403 (no write), missing field 400 (no write), unknown shelf 404, toggle-off 200; setting ON → button renders, real UI click flips DB `kobo_sync 0→1` and the label to 'Kobo sync on'; #866 notice verified rendering on BOTH `/app/magic/1` and an ordinary `/app/shelf/4`; mobile 390×844 button 128×36px, no viewport overflow, hit-testable; zero new Tracebacks in container logs. No new route on the classic surface, no dependency, no external URL. Fork-original. | `f74c280` | v4.1.19 |
| [#1020](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1020) | (fork issues [#920](https://github.com/new-usemame/Calibre-Web-NextGen/issues/920) @iroQuai, [#699](https://github.com/new-usemame/Calibre-Web-NextGen/issues/699)) | **KOReader annotation sync froze the WHOLE app for ~10s per highlight, so the plugin timed out and reported "Server push failed" on syncs the server had saved.** Root cause: the sync-target fan-out (`push_annotations` -> `apply_push` -> `dispatch_existing_annotation_sync` -> `HardcoverHandler.push`) ran `requests.post(timeout=10)` synchronously on the request greenlet; CWNG runs gevent WITHOUT `monkey.patch_all()`, so an unpatched blocking socket stops the hub and every other request with it. The KOReader provider sets `push_all_local=true`, so each sync re-pushes the whole local set -> the freeze scales with highlights-per-book (that is the reported create-works/delete-fails asymmetry), blows the plugin's 15s `ANNOTATION_TIMEOUTS`, and starves the 3s/3-retry Docker healthcheck into a restart (the reporter's "crash"). Fix: only the REMOTE half moves to the WorkerThread (`cps/tasks/annotation_sync.py`), the same pattern `tasks/hardcover_sync.py` already uses for shelf adds; the annotation is persisted and committed on the request path exactly as before and its target row sits at `pending` until the worker reports. Jobs coalesce per request (one task per sync, not per highlight); handlers gained `for_session()` so the worker's thread-local app.db session is used instead of the non-thread-safe global `ub.session`; an enqueue failure falls back to inline so a sync is never lost. **Red/green**: 11 new tests in `test_annotation_sync_background_920.py` (import-level RED on main - the seam does not exist), plus a live A/B on cwn-local with the same box, same data, Hardcover blackholed: PUT of 3 annotations 30.22s -> 0.131s and a concurrent anonymous `GET /login` 29.02s -> 0.0145s (idle baseline 0.010s); with a stubbed Hardcover, create -> `synced` (remote id captured) and delete -> local `hidden=1` + remote `tombstone`, both under 0.15s on the wire. App stayed at 12-25ms per request through the worker's full 30s of blocked sockets, so the healthcheck can no longer miss probes. No new route/dependency/URL. Fork-original. | `3003c917c` | v4.1.19 |
| [#1015](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1015) | (CWA issue [#1307](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1307), reporters @TheDarkSpock + @bigbold1023) | **Kobo two-way sync intermittently archived magic-shelf books off the device, and the re-download lost the reader's annotations.** The silent half of [#468](https://github.com/new-usemame/Calibre-Web-NextGen/pull/468). `get_magic_shelf_book_ids_for_kobo` built the *allowed* set — the destructive input to `compute_kobo_books_to_archive(synced - allowed)` — by calling `magic_shelf.get_books_for_magic_shelf`, which takes the authoritative id list and then hydrates it through a fresh `CalibreDB` session ending in `[book_map[bid] for bid in page_ids if bid in book_map]`. That trailing guard silently DROPS every id whose `Books` row that session did not return. No exception is raised, so #468's `reliable` flag stays `True` and the fail-safe never engages: the dropped books leave `allowed`, get emitted as `ChangedEntitlement(archived=True)`, and have their `KoboSyncedBooks` rows deleted — so the device drops the local copy and later re-downloads them as a *fresh* entitlement, taking the annotations with the old one. On a CWA build the calibre `metadata.db` is rewritten underneath the running app by auto-ingest, which is exactly the intermittent, subset-of-books trigger both reporters describe. **Fix:** the destructive path asks for the id LIST (`get_book_ids_for_magic_shelf(shelf.id, raise_on_error=True)`) instead. The id list *is* the membership; hydration is presentation and must never narrow a deletion decision. Every downstream consumer of `magic_shelf_book_ids` already used only ids, so this also drops a full `Books` hydration per sync. #468's raise-on-error contract is preserved and re-pinned. **Red/green** `tests/unit/test_kobo_magic_shelf_membership_uses_id_list_1307.py`, 7 tests: a lossy-hydration stand-in (full ids `{1,2,3}`, hydration resolves `{1,2}`, neither call raises) proves membership came back `{1,2}` with `reliable=True` and that book 3 reached the archive set — 4 tests fail on `main`, all 7 pass on the branch; plus the #468 failure path, an AST pin that the collector never calls the hydrating helper, and a pin on the silent-drop expression itself so the mechanism stays visible. The three #468 membership tests were re-pointed at the id-list API (contract unchanged). 493 kobo/magic unit tests pass. **Live on `cwn-local`** with the global magic-shelf-sync flag on and a real `kobo_sync` magic shelf: three consecutive real `GET /kobo/<token>/v1/library/sync` calls over the wire (fresh token then continuation tokens) returned 200 with the magic-shelf book delivered as `NewEntitlement`, **zero** `IsRemoved` entitlements across all three, the `kobo_synced_books` tracking row created and retained, and no new ERROR/Traceback in container logs. No new route, dependency, license or external URL. | `bd821db03` | v4.1.19 |
| [#1011](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1011) | (fork issue [#1009](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1009); community PR [#1010](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1010) by @Sanjays2402, adopted-and-improved) | **The admin user-table column set "sync only selected shelves to Kobo" without running the shelf reconciliation, so a user switched over from that screen kept every collection on their device.** Follow-up to [#1008](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1008): four call sites write `User.kobo_only_shelves_sync` — the classic `/me` form (`cps/web.py`), the SPA profile endpoint (`cps/api/account.py`), the admin single-user form (`_handle_edit_user`) and the admin user *table* (`edit_list_user`, `POST /ajax/editlistusers/<param>`). The first three ran `update_on_sync_shelfs` on the 0 -> 1 transition; the table only flipped the flag. Reproduced live over the real endpoint on `cwn-local`: `POST pk=3&value=true` returned 200 with `kobo_only_shelves_sync=1` and **zero** `shelf_archive` rows; patched, the same request writes tombstones for exactly the two `kobo_sync=0` shelves and leaves the Kobo-sync shelf alone. **Two further defects found reviewing the community patch, both fixed here.** (1) The reconciliation ran unconditionally after `ub.session_commit()`, which swallows `OperationalError`/`InvalidRequestError`, rolls back and returns `""` either way — so a save that failed still wrote shelf tombstones, telling the device to drop collections while the account had the restriction switched off. The endpoint now uses a commit whose failure it can see and reports it to the admin (previously a lost save was reported as success). (2) A mid-loop failure returned 400 without rolling back; `ub.session` is not request-scoped and nothing rolls it back on teardown, so a half-applied edit survived to be flushed by the next commit anywhere in the process. **Root-cause structure, not a fourth copy:** the transition rule and its failure policy move into `kobo_sync_status.needs_shelf_reconciliation()` + `reconcile_shelves_safely()`, and all four call sites go through them — four hand-rolled copies of this test are exactly how the SPA endpoint drifted out of sync in the first place. `update_on_sync_shelfs` also now commits once per user instead of once per shelf (a bulk enable of 100 users x 20 shelves was 2000 serial fsyncs inside one request). **Red/green** `tests/unit/test_1009_admin_table_kobo_shelf_transition.py`, 17 tests: 6 endpoint tests red against main's `admin.py` (single-row `pk` — the path table.js actually posts and the community patch did not cover — bulk `pk[]`, off-transition, commit-failure gating, mid-loop rollback, per-user failure isolation), plus helper and real-in-memory-SQLAlchemy tombstone tests. The five #866 SPA tests were re-pointed at the deeper `kobo_sync_status` seam so they now exercise the shared predicate and failure policy rather than a module re-export. **Live e2e on `cwn-local`** across single-row, repeat, off, bulk, an unrelated param (locale, unaffected by the commit change) and a nonexistent pk (400); SPA profile endpoint re-verified post-refactor (200, flag set, 2 tombstones); no new errors in container logs. Reviewed by Claude `/code-review high` + cross-family GPT-5.6-Terra; both independently identified the commit-gating defect. i18n: reuses the existing translated msgid "Settings DB is not Writeable" rather than shipping a new untranslated string. No new route/dependency/URL. | `b58f7d582` | v4.1.18 |
| [#1008](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1008) | (fork issue [#866](https://github.com/new-usemame/Calibre-Web-NextGen/issues/866) @auspex) | **Marking a shelf "Kobo sync on" in the new UI did nothing, and the device kept syncing the whole library.** Two independent defects behind one symptom. (1) The shelf mark is inert until the per-account setting "Sync only selected shelves to Kobo" is also on, and nothing in the new UI said so — the reporter marked a six-book shelf and watched their Kobo page through 11,000 books (~500 requests) until the device slept. The shelf page now says it, where the mark is made, with a one-click fix and a plain-English note that off-shelf books leave the device but stay in the library. `/me` carries `kobo_only_shelves_sync` so the shelf page can tell without fetching the whole account payload (app passwords, locale + language lists) on every shelf view; the flag is absent-tolerant, so an older server stays quiet rather than warning wrongly. (2) The root-cause half: the classic `/me` form runs `kobo_sync_status.update_on_sync_shelfs` on the 0 -> 1 transition; the SPA profile endpoint only flipped the flag, so the user's non-synced shelves were never tombstoned for the device. Wiring the SPA into that function exposed that the function itself was wrong, in two ways found by cross-family review and confirmed live. Its membership query joined `Shelf` on `user_id` alone, never on `Shelf.id == BookShelf.shelf`, so one ordinary shelf anywhere in the account paired with every synced book and matched `kobo_sync == 0` — a book **on** the Kobo-sync shelf was archived purely because an unrelated plain shelf existed. And it deleted each book's `KoboSyncedBooks` row, which is the only input `HandleSyncRequest` uses to emit the `ChangedEntitlement(archived=True)` that makes the device drop a book; a swept book is by definition outside the eligible set that handler queries, so the device was never told and the books stayed on the reader forever — @auspex's actual symptom. It also wrote `ArchivedBook` rows, hiding those books from the user's own web library. Since `cps/kobo.py:296-340` already computes the same difference correctly, with the #468 fail-safe, and does it on the wire, the transition now records shelf tombstones only and leaves books to the sync. Verified end-to-end on `cwn-local` through a real `GET /kobo/<token>/v1/library/sync`: the off-shelf book came back as `ChangedEntitlement` with `IsRemoved: true`, the on-shelf book was untouched, and the handler cleaned up its own tracking row afterwards. | ade2b7874 | v4.1.18 |
| [#1004](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1004) | (fork issue [#855](https://github.com/new-usemame/Calibre-Web-NextGen/issues/855) @monimkxl-web) | **Any render error blanked the entire SPA — "the screen went black and nothing else. Had to close the browser."** The React app shipped with **no error boundary of any kind** (`grep -riE "errorboundary\|componentDidCatch\|getDerivedStateFromError" frontend/src` → empty), so React's documented contract applied: an error thrown during render with no boundary above it unmounts the whole tree back to the root. `#root` was emptied (measured `innerHTML.length === 0`) leaving only the page background, with no fallback UI and no in-app way to recover — hence the browser restart. The magic shelf of the original report was one trigger; the failure was the **missing global safety net**, so the fix is at the root rather than per-page. Adds `frontend/src/components/ErrorBoundary.tsx` (dependency-free class component; hard rule 6 respected — no `react-error-boundary`) with a theme-token-styled fallback carrying **Reload**, **Back to library**, and a `<details>` disclosure of the error message so a reporter can paste it without being talked through devtools. Wired at **two** levels: a router-level `RouteBoundary` in `App.tsx` keyed on `useLocation()` so it self-clears on navigation (one bad route can't strand the session), and an outer boundary in `main.tsx` above the providers as the last-resort net. The fallback reads no app data, calls no hooks in the class, and falls back to an identity translator when rendered above the i18n provider, so it cannot itself throw. Red/green via a new Playwright spec that aborts the lazily-imported `Reader-*.js` chunk — a real production scenario (stale `index.html` after a container upgrade, flaky network), not a synthetic throw. Hardened after cross-family review (Terra): `hasError` is tracked separately from the thrown value, because `throw null` is legal JS and keying the fallback off the value's truthiness re-rendered the crashing subtree straight back into the blank screen (confirmed live against the pre-review commit); the thrown value is stringified through a defensive `errorText()`; focus moves to the fallback heading (the crash unmounts whatever was focused) and `role="alert"` is scoped to the one concise sentence rather than the whole container. | a86365c2c | v4.1.18 |
| [#999](https://github.com/new-usemame/Calibre-Web-NextGen/pull/999) | (fork issue [#987](https://github.com/new-usemame/Calibre-Web-NextGen/issues/987) @chloeroform) | **Book covers whose artwork background matched the theme blended into the page.** Every cover surface leaned on `box-shadow: var(--cover-shadow)` alone to separate art from page, and a shadow disappears entirely when the cover's own edge pixels are the page colour — nothing in the markup drew a boundary. Added `1px solid var(--border)` to the shared `BookCover` frame (browse grid, catalog, Discover strip, More-by-this-author), the detail cover, and the table + duplicates row thumbnails. The hairline sits on the frame rather than the `<img>` so #660's letterboxed matte is enclosed, and the fallback card's own border was dropped to avoid a doubled edge. Cross-family review (Terra, K3 out of quota) caught that the frame's border made its `overflow` clip round at `r - 1px` while children still rounded at `r`, leaving a 1px corner halo — children's radii removed so the frame owns the corner. **Red/green** `cover-fit.spec.ts` (4 new cases, 9 passed/1 seed-skipped); RED independently reproduced on CI's clean `:dev` image (`Expected "1px" / Received "0px"`). Measured on the live container across all five themes × desktop+mobile; `box-sizing: border-box` means no cover resized (grid frame 132.00px, detail 280.00px, unchanged). Adopted from @chloeroform's PR #996 with credit. No new route/dependency/URL. Fork-original. | `d1151627` | v4.1.18 |
| [#977](https://github.com/new-usemame/Calibre-Web-NextGen/pull/977) | (fork issue [#640](https://github.com/new-usemame/Calibre-Web-NextGen/issues/640) @standhaftsohnsergius) | **The new UI forgot your Library sort order and read filter on every reload.** Sort and read-status were component state only, so a refresh (or any navigation back to Library) dropped the user's choice back to the default. Both are now persisted per browser and restored on mount; scoped views (series, author, tag) keep their own natural ordering rather than inheriting the saved Library sort. Source pins re-anchored to the extended initializer. Cross-family review (Kimi K3) verdict MERGE, findings dispositioned on the PR. No new route/dependency/URL. Fork-original. | `ad015791` | v4.1.17 |
| [#974](https://github.com/new-usemame/Calibre-Web-NextGen/pull/974) | (fork issue [#941](https://github.com/new-usemame/Calibre-Web-NextGen/issues/941) @auspex, @chloeroform) | **Container startup re-chowned the entire ~1,800-file application tree on every boot.** `set_ownership` walked all of `/app` recursively, costing seconds to tens of seconds and, on overlay/copy-up storage back-ends, duplicating every file into the container's writable layer. The application code is read-only and world-readable, so the walk was almost entirely wasted work; ownership is now narrowed to the directories the app actually writes (metadata change logs, export temp). **Red/green** `test_set_ownership.py::test_full_app_tree_is_not_recursively_walked` (23 passed), plus a live PUID=501 container repro in the PR body. Cross-family review (Kimi K3) verdict MERGE; the writer-attribution comment was corrected per its finding, and the EACCES follow-up it surfaced was filed as [#992](https://github.com/new-usemame/Calibre-Web-NextGen/issues/992) and fixed in the same release. `/security-review` clean. Fork-original. | `e3ce37f9` | v4.1.17 |
| [#983](https://github.com/new-usemame/Calibre-Web-NextGen/pull/983) | (fork-original) | **Convert on the book edit page was a free-text format box, so an unsupported or mistyped target only failed after the conversion job started.** The control now reads "Convert from [EPUB] to [MOBI]" with two dropdowns: the source list is limited to formats the configured converter can actually read, the target list excludes the selected source, and the target is validated server-side rather than trusted from the form. No new route/dependency/URL. Fork-original. | `783dd7a2` | v4.1.17 |
| [#992](https://github.com/new-usemame/Calibre-Web-NextGen/pull/992) | (fork issue [#992](https://github.com/new-usemame/Calibre-Web-NextGen/issues/992); surfaced by the K3 cross-family review of [#974](https://github.com/new-usemame/Calibre-Web-NextGen/pull/974)) | **Dismissing the duplicate-scan setup notice returned a 500 and the notice never went away on stock containers.** `dismiss_duplicate_scan_setup_notice()` wrote the per-user marker `cwa_duplicate_index_setup_notice_<uid>` to `/app`, which ships root-owned and is never chowned by the image or cwa-init, so the runtime user's `open(...,'w')` raised `EACCES` (surfaced as a 500) — the notice could not be dismissed. `/app` is also wiped on image upgrade, the wrong layer for per-user state. **Root cause, not symptom:** the marker path is relocated to the writable, persistent `/config` volume — the sibling of the existing `/config/cwa_ingest_batch_*` markers in `duplicate_index.py` — and the previously-triplicated path literal (one write in `duplicates.py`, two reads in `render_template.py`) is collapsed into one source of truth in a new dependency-free `cps/duplicate_notice.py` so the write and read sides cannot drift. **Red/green** `tests/unit/test_duplicate_setup_notice_path.py`, 4 tests (module absent → red on main): the resolved path is under `/config` and never `/app`, it is stable + per-user, neither callsite hard-codes the old `/app` literal, and both import + call the shared helper (SSOT pin). Four existing `cps.duplicates` stub harnesses updated to register `cps.duplicate_notice`; full duplicate-suite parity vs main (10 failed / 20 errors pre-existing env, +4 new passing, 0 new failures). **Not verified:** the live EACCES cannot be reproduced in `cwn-local` — Docker Desktop's `fakeowner` ignores `/config` permission bits — so verification is the red/green regression + import-safety + static trace, not a container repro of the 500. **Cross-family review (GPT-5.6-Terra, MERGE-WITH-FIXES) tightened it before merge:** the path resolves from `constants.CONFIG_DIR` rather than a hard-coded `/config`, so a `CALIBRE_DBPATH` or bare-metal install doesn't get this one file written outside its own state directory (which would have re-created the same 500 elsewhere); a read-only fallback keeps honouring a dismissal recorded under the old `/app` path, so nobody who already dismissed the notice sees it again; both `render_template` read sites go through one `duplicate_setup_notice_dismissed()` predicate; and the tests gained behavioural coverage (round-trip through the exact write the endpoint performs, per-user isolation, legacy-marker fallback, `CALIBRE_DBPATH` resolution) on top of the source pins, 8 passing. No new route/dependency/URL. Fork-original. | `ba4643ac` | v4.1.17 |
| [#933](https://github.com/new-usemame/Calibre-Web-NextGen/pull/933) | (fork issue [#863](https://github.com/new-usemame/Calibre-Web-NextGen/issues/863) @Andrew-H2O; follow-up to the narrower card-flex change in [#888](https://github.com/new-usemame/Calibre-Web-NextGen/pull/888)) | **On iPad and other touch devices, persistent “Read now” actions formed a wavy line under mixed one- and two-line book titles.** The earlier fix stretched card wrappers and pushed the action down, but left short titles at one line despite a two-line clamp and left adjacent remove/edit controls hover-only. Book titles now reserve the full two-line block; coarse/no-hover media applies across hybrid pointers; Read now stays persistent on touch; and shelf removal / quick edit use visible 44px touch targets. Desktop retains opacity-zero idle state with mouse-hover and keyboard-focus reveal. **RED→GREEN:** the real catalog Playwright spec failed on main in desktop/mobile/iPad projects with a 19.5px short-vs-wrapped title delta, then passed 4/4 while checking light/dark, ≤1px action-bottom delta, direct computed opacity, mouse, touch, and keyboard. The SPA a11y gate passed 22 tests with 9 intentional skips; production build and assigned container were green. #881 read/favorite quick-actions are explicitly separate and not claimed here. No new dependency/route/string. Fork-original. | `9687cd71` | v4.1.17 |
| [#924](https://github.com/new-usemame/Calibre-Web-NextGen/pull/924) | (fork issue [#920](https://github.com/new-usemame/Calibre-Web-NextGen/issues/920), found by the pre-release verify gate on the v4.1.14 candidate; supersedes the mechanism added by [#906](https://github.com/new-usemame/Calibre-Web-NextGen/pull/906) for [#905](https://github.com/new-usemame/Calibre-Web-NextGen/issues/905) @iroQuai) | **A second KOReader device destroyed the first device's highlights, permanently, just by opening the book.** #906 let a push declare itself `complete`, and the server soft-deleted every live row of that source the push omitted. The inference is unsound: "the user deleted their last highlight" (#905, must delete) and "this device never had them" (#920, must not) are **byte-identical pushes**, and the server has no third fact to separate them. The KOReader-native provider is push-only by construction (`koreader_annotations_provider.applyToDevice` returns 0 off-Kobo) and `main.lua` gated `apply_to_device` on a `volume_id` that is nil for koreader-origin rows (`content_id: null`), so device A could **never** converge — yet still declared its empty set complete. `_reap_absent` then hid every row and `apply_portable` never un-hides a tombstone, so the re-push returned `skipped` forever: silent, permanent, HTTP 200. **Fix — the decision moves to the only party holding the missing fact.** The plugin persists a watermark of the ids it last pushed in the book's own sidecar, diffs it against its live set, and NAMES deletions (`deleted: [...]`, scoped by `delete_source`); the server obeys the list and infers nothing. `complete` is accepted and ignored. Fails safe by construction: a lost watermark (fresh install, wiped sidecar) yields no deletions, and the "did we actually read the local set?" guard (`local_set_known`) stops a nil-`volume_id` Kobo read — an empty list that means "couldn't read", not "empty" — from deleting the library. **Second, independent defect found while fixing it, and the reason #920 could not actually fire in production:** lua-Spore rebuilds the request body from *exactly* the keys in `api.json`'s `payload` list and silently drops the rest (`Spore.lua:137-141`, vendored 0.4.2 per `koreader-base/thirdparty/lua-Spore`). #906 added `complete`/`complete_source` to the client and never touched `api.json` — **so both were discarded before the request was sent, and #906's delete-sync has never worked on the wire.** #905's symptom is still live for every user; the reap was never reachable except from a hand-rolled client (the verify gate's repro was curl-driven, which bypasses Spore). The new `deleted` field would have died the same silent death, so `api.json` declares it and `tests/unit/test_cwasync_plugin_wire_contract.py` pins client-sent keys ⊆ declared payload for **every** method — that test, run against the pre-fix tree, independently reports `push_annotations passes ['complete', 'complete_source']`, i.e. it rediscovers #906 unaided. **Red/green:** `test_920_koreader_push_only_authority.py` 21 tests — 19 red pre-fix, the headline one reproducing the destruction over the real PUT as `deleted: 3 != 0` for both `{}` and `[]`; scoping (source/user/book), soft-delete + tombstone, fan-out, no re-delete of a hidden row, and malformed `deleted`/`delete_source` → 400 (also closing the `TypeError: unhashable type` → **500** on a non-hashable `complete_source`, since the unhashable `in` lookup is gone). `test_koreader_annotation_delete_reap.py` rewritten: #905's user-facing guarantee is preserved end to end through the new mechanism. `sync_logic_test.lua` covers `computeDeletions` (incl. the empty-watermark and last-highlight cases). Annotation/kosync/plugin suites 486 pass; full CI selection 2786 pass (2 pre-existing env failures, unrelated, reproduce on the published tag). **Not verified + stated in the PR:** physical KOReader hardware (#906's own standing exemption) — the plugin change is Lua driven through unit tests and a Spore-semantics reading of the vendored source, not a device. Plugin 4.1.14 (already in lockstep) ships with this release; the server accepts old plugins and deletes nothing for them. No new route/dependency/URL. Fork-original. | `424584b53` | v4.1.14 |
| [#918](https://github.com/new-usemame/Calibre-Web-NextGen/pull/918) | (fork issue [#736](https://github.com/new-usemame/Calibre-Web-NextGen/issues/736) @auspex; same defect surfaced in [#351](https://github.com/new-usemame/Calibre-Web-NextGen/issues/351) / [#579](https://github.com/new-usemame/Calibre-Web-NextGen/issues/579) @uschi1) | **Admin → Theme reported "Settings saved." and changed nothing.** The picker hardcoded its own `Light=0`/`Dark=1` numbering, matching neither `ui_themes.THEME_CODES` (1=dark, 2=light) nor the SPA slugs, so "Light" wrote `config_theme=0` — which `ui_themes` documents as resolving to dark on read. A genuine save plus a genuine dark render produced the "saved but nothing changed" symptom. `test_theme_registry.py` existed to stop exactly this drift but pinned only backend↔`themes.ts`, so `Admin.tsx` invented a third numbering behind it. **Fix:** `config_theme` travels as a slug validated against `ALLOWED_THEME_SLUGS` (the raw int no longer crosses the wire — that was the drift vector); the picker renders the `THEMES` SSOT (six themes, not two); all three account-create paths seed from it; a legacy `0` is honoured as light so @auspex's saved click survives, while `User.theme` keeps its 0→dark rule and existing accounts are untouched. **Found during the blast-radius pass:** `register_post` (self-registration) still seeded `content.theme = getattr(config, 'config_theme', 1)` raw, so with an admin-saved Light a self-registered account booted dark while an admin-created one booted light — the same drift, on the public path. All three paths now pinned. **Red/green:** the pre-fix `<option value="0">` pin is red on main; the create-path seeding pin fails 3 of 4 params against the old hardcode; removing the slug validation fails 10 of the update-config tests. **Live wire on cwn-local, real endpoints session-cookied:** `GET /api/v1/admin/config` → `'dark'` (slug, not int); `POST {"config_theme":"light"}` → 200 + DB `2` + reads back `'light'`; `POST` bogus slug → 400 nothing stored; `POST {"config_theme":0}` → 400 (the exact payload the pre-#736 form wrote is now rejected); a user created under a Light default lands `theme=2` and serves `theme='light'` on `/auth/me` — the #736 promise end-to-end. App boots with no circular import from the new `ui_themes` import in `web.py`; container logs clean. `config_sql.py:82` is `Column(Integer, default=1)`, so a `0` is never the schema default — only a deliberate old-form click, which is why reading it as light is right. Greptile 4/5; both findings actioned (untested validation path; a picker pin whose regex started at the form-state object and matched the right `<select>` only by luck). Not covered: the classic Jinja create path is unit- and container-Python-verified but not driven. i18n: 2 msgids anchored in `cps/spa_strings.py`. No new route/dependency/URL. Fork-original. | `d834a5e` | v4.1.14 |
| [#910](https://github.com/new-usemame/Calibre-Web-NextGen/pull/910) | (fork issue [#627](https://github.com/new-usemame/Calibre-Web-NextGen/issues/627) @uschi1; same web-visibility cause as [#509](https://github.com/new-usemame/Calibre-Web-NextGen/issues/509), distinct from [#633](https://github.com/new-usemame/Calibre-Web-NextGen/issues/633)) | **KOReader devices exchanged the correct position, but the book page showed no “KOReader Progress” entry for an already-known book.** The earlier #627 fixes proved filename-checksum resolution and ingest registration; they did not trace the accepted progress through the separate state carrier consumed by the classic and SPA book-detail pages (failure mode Class 1). `KOSyncProgress` stored device-to-device position correctly and `ReadBook` changed to in-progress, but `update_book_read_status()` only created `KoboReadingState.current_bookmark` when it created a brand-new `ReadBook`. A pre-existing read/unread row with no Kobo state therefore stayed detached from both UI consumers indefinitely; opening via OPDS/web created the missing state through another path, explaining the reporter's screenshots. **Fix:** one `_ensure_visible_reading_state()` helper creates or completes the `KoboReadingState` + bookmark + statistics graph for both new and existing `ReadBook` branches, then mirrors the accepted KOReader percentage into the bookmark. **RED→GREEN:** `tests/unit/test_627_kosync_visible_progress.py` replays the plugin's auth handshake and progress PUT against the real Flask blueprint/SQLAlchemy models; pre-fix returns `{"authorized":"OK"}` and stores `KOSyncProgress(document="1031", percentage=68.86)` but fails `NoResultFound` for the visible state, while the fix creates it at 68.86. **Live wire on isolated `cwn-p627`/8107:** v4.1.12 baseline auth + PUT reproduced device storage with zero Kobo state; patched auth + PUT created exactly one state/bookmark, classic `/book/2` rendered `KOReader Progress: 68.9%`, and SPA `/api/v1/books/2` returned `kosync_progress: 68.86`. Eight parallel device PUTs converged to one progress/state/bookmark row at 68.0 with no lock/integrity errors. Server-side only: existing plugins already make this handshake + PUT sequence, so no plugin update is required. Physical KOReader hardware remains **ASSUMED**, not verified. PR #906 is present in the base but unreleased and unrelated: its device-plugin update enables highlight-deletion completeness reconciliation, not progress visibility. | `8bef3622d` | v4.1.14 |
| [#903](https://github.com/new-usemame/Calibre-Web-NextGen/pull/903) | (fork issue [#896](https://github.com/new-usemame/Calibre-Web-NextGen/issues/896), from [#873](https://github.com/new-usemame/Calibre-Web-NextGen/issues/873) @KucharczykL) | **The Fetch Metadata modal's 🔑 Keys panel reported "Not configured" for Hardcover even when a working `HARDCOVER_TOKEN` / `HARDCOVER_TOKEN_FILE` was set.** Searches worked; only the badge lied, so a correct setup looked broken. Root cause: `metadata_keys()` (`cps/search_metadata.py`) derived `configured` from the raw config column (`getattr(config, spec["config"], None)`), but an env/file token is **deliberately never persisted** to that column (`resolved_hardcover_token()` resolves per call so container rotation keeps working, #743/#819) — so the column is `None` while the provider itself resolves a token fine. #743 fixed the *zero-result classifier* in this same file to use the resolver but never touched `metadata_keys()`; commit `1dd39c759` (PR #790) shows the one-hunk miss. `metadata_keys_save()` had the same defect from the other side — it derived `configured` from the **posted value alone**, so clearing the admin field while an env token was live returned `configured: false` and `CoverPicker.tsx` (`setConfigured(r.configured)`) flipped the badge to a lie until a refetch corrected it. **Why the #743 guards didn't catch it**: `test_classifier_does_not_read_raw_column` greps for the *literal* `getattr(config, "config_hardcover_token"`, which registry indirection (`spec["config"]`) never spells; and `test_consumer_uses_resolver` is a file-level `"resolved_hardcover_token()" in src` grep already satisfied by the classifier — so the broken function sat in a file both guards certified clean. That is coverage theater, and it is why the new tests are behavioural. **Fix**: an optional `resolver` hook on `PROVIDER_KEY_REGISTRY` plus one `_provider_configured()` helper used by **both** endpoints (SSOT), so a provider whose key can come from env/file can never have one path fixed and the other regress. ~20 LOC, one production file; providers without a resolver (Google Books) keep the column path unchanged. **Red/green** `tests/unit/test_896_metadata_keys_resolver.py`, 10 tests, 5 red on main — they call the real view via `__wrapped__` and assert on the JSON both front-ends consume: env token → `configured: true`, `HARDCOVER_TOKEN_FILE` → `true`, DB token → `true` (no #790 regression), **no token anywhere → `false`** (the fix discriminates, it is not constant-true), Google unaffected, payload shape/`bool` type pinned (`frontend/src/lib/coverPicker.ts` types it `boolean`), token value never serialized, and the save-path env case. Also strengthened the defeated #743 pin so registry indirection can't hide a raw-column read again. **Production-runtime verification**: published `:latest` with `HARDCOVER_TOKEN` set, session-cookied `GET /metadata/keys` → `configured: false` (the reporter's symptom, reproduced over real HTTP in the shipped image); patched into that same image → `configured: true`, Google/Goodreads still `false`, token value absent from the payload; in-container negative (token unset → `false`, restored → `true`). Adjacent suites 62 pass; metadata/provider/cover selection 701 pass. `tests/unit/test_duplicate_index.py` errors are pre-existing on pristine main (20 errors there unpatched) — not from this change. Privacy invariant unchanged: presence only, never the value. No new route/dependency/URL; the endpoint's auth stays `@user_login_required` + admin-only save. Fork-original. | `33e035fa` | v4.1.18 |
| [#901](https://github.com/new-usemame/Calibre-Web-NextGen/pull/901) | (fork issue [#876](https://github.com/new-usemame/Calibre-Web-NextGen/issues/876) @KucharczykL) | **Hardcover auto-fetch finished every run but saved no statistics, and the admin Hardcover panel stayed blank.** Each run logged `Error saving stats to database: 'CWA_DB' object has no attribute 'execute_write'` yet still reported success (`364 processed, 12 auto-matched`), so `hardcover_auto_fetch_stats` stayed empty forever. Root cause: `CWA_DB` (`scripts/cwa_db.py`) writes throughout via `self.cur.execute` + `self.con.commit` and never defined the `execute_write()`/`execute_read()` convenience methods, but the Hardcover auto-fetch feature calls both — `cps/tasks/auto_hardcover_id.py:417` (`_save_stats`) → `execute_write`, AttributeError swallowed into a WARN at `:433` (**the reported symptom**), and `cps/cwa_functions.py:1314,1317` (Stats & Activity tab) → `execute_read`, same AttributeError caught by the outer handler → `hardcover_stats = None` → **admin Hardcover panel silently blank** (the half the reporter never saw). Table `hardcover_auto_fetch_stats` already exists (`cwa_schema.sql:156`) — no schema/migration work; the whole defect is the two missing methods. **Fix**: add both helpers to `CWA_DB` (20 LOC, one file) rather than rewriting the three callsites — they are the shared path the feature assumed. `execute_read` returns `fetchall()` because `cwa_functions.py:1335` indexes `total_processed[0][0]`; a `fetchone()` contract would break the panel. **Red/green** `tests/unit/test_cwa_db_execute_helpers.py`, 10 tests, all fail on main; the behavioural one drives the real `_save_stats` against a real sqlite DB and reproduces the reporter's WARN byte-for-byte pre-fix. Pins commit durability across a fresh connection (an uncommitted write would still pass a same-connection read but be lost to the admin panel's own CWA_DB), the `[0][0]` contract, the empty-table `SUM`→`[(None,)]` guard shape, param binding, and a source-pin against silent removal. **Production-runtime verification**: published `:latest` confirmed missing both helpers; with the patched file mounted into that same image, the task's exact INSERT lands and the admin tab's exact SELECT+guard returns `364`/`12` (was blank), and a second `CWA_DB` reads `[(364,)]` proving the commit. Purely additive; adjacent suites 35 pass. Not exercised: a live auto-fetch against the real Hardcover API (needs token + library). No new route/dependency/URL. Fork-original (CWA DB helper). | `0f03c2784` | v4.1.13 |
| [#774](https://github.com/new-usemame/Calibre-Web-NextGen/pull/774) | (fork issue [#615](https://github.com/new-usemame/Calibre-Web-NextGen/issues/615) @hayvan96) | **The new UI kept showing outdated interface translations after an upgrade.** After updating, the French read button reverted to English "Read now" and the read toggle showed the pre-v4.1.4 inverted "mark as read" wording — even though the strings were fixed in v4.1.4 (#619) and the reporter had confirmed it. Root cause is **cache correctness, not translation data**: the per-locale catalog the SPA loads from `/api/v1/i18n/<locale>.json` changes every release but was served at a stable URL with `Cache-Control: public, max-age=3600` and **no validator**, so a browser or self-hoster's reverse proxy kept serving a pre-fix catalog an upgrade couldn't invalidate. Verified the server data was correct at every version (live cwn-local + `git show v4.1.6:fr.po`, no fuzzy/dupes). **Fix** (`cps/api/i18n.py`, one production file): attach a content ETag (sorted `{msgid:translation}` hash, locale-folded) and switch to `Cache-Control: no-cache` + `make_conditional` — an unchanged catalog costs a 304, a changed one is fetched fresh. **Red/green** 4 tests in `test_api_v1_i18n.py` (ETag+no-cache present, ETag tracks content, conditional→304, #615 French strings pinned); all fail on the old headers. **Live e2e** cwn-local: fresh GET → 200 + ETag + no-cache + correct French; conditional GET → 304 empty body. No new route/dependency/URL (stdlib hashlib). Fork-original (cache correctness). | `39d88a38f` | v4.1.9 |
| [#959](https://github.com/new-usemame/Calibre-Web-NextGen/pull/959) | (fork issue [#948](https://github.com/new-usemame/Calibre-Web-NextGen/issues/948) @chloeroform) | **The New UI separated authors with a comma, so a multi-author book was unreadable** — @chloeroform's screenshot shows "Dumas, Alexandre, Maquet, Auguste" for two people. **Root cause: joining a comma-containing value with a comma.** Calibre encodes a comma inside an author name as `|` (`cps/api/serializers.py` decodes it back), so `name` legitimately contains a comma — "Dumas, Alexandre" is an ordinary author. The SPA then joined those names with `', '`, making the separator indistinguishable from the data. **The reporter's suggested design was NOT adopted** — they proposed a middle dot/semicolon, but `' & '` is not a new convention to invent: Calibre joins authors with `&`, `cps/templates/index.html` + `detail.html` already render `&amp;` between author links, `cps/api/edit.py:84` hands the edit form `' & '.join(...)`, and the SPA's own field is labelled **"Authors (separate with &)"**. So the app *told* users authors are `&`-separated and then displayed them comma-separated — a self-contradiction on one screen. This is a **New UI parity regression**, not a preference. **Wider than reported, and the audit found it:** the issue named the library + book views; a sweep found **eight** independent `', '` literals across six files — `BookCard.tsx` (grid), `Catalog.tsx` (list), `BookDetail.tsx` (book view), `Table.tsx` (table view Authors column), `CoverPicker.tsx`, and three in `EditBook.tsx` (metadata-search previews, which **misrepresented what would be written** since the apply path already used `' & '`). That duplication *is* the defect — the fix collapses all of them to one `frontend/src/lib/authors.ts` (`AUTHOR_SEPARATOR`/`formatAuthors`). **Scoped, not blanket:** tags/languages/publishers/formats keep `', '` — their values cannot contain commas — and a test pins that they are not swept up. **Red/green, mutation-tested:** `tests/unit/test_948_author_separator_parity.py`, 10 tests, **9 red on pristine main** (the exhaustiveness guard printed all seven offending file:line); reverting one site → 2 red; changing the separator to the reporter's middle dot → 2 red via the **backend↔SPA correspondence pin**, which fails if either half moves alone. `pytestmark = pytest.mark.unit` set deliberately — CI selects `-m "smoke or unit"` and an unmarked guard is silently deselected (the #958 class). **A false red was thrown away first:** the initial attempt used `git stash`, which aborted on the untracked new file, so "pristine" was actually the *fixed* tree and passed 10/10 proving nothing; a second attempt under zsh failed to word-split the file list and reverted nothing. The honest red reconstructs pristine files via `git show origin/main:<path>`. **Live wire on cwn-local, reporter-mirrored, pre-fix first:** the seed had no comma-containing author, so the two authors of book 189 were renamed to sort form to match @chloeroform's data — `/api/v1/books` then returned `["Dumas, Alexandre", "Maquet, Auguste"]` and all three surfaces reproduced the exact symptom (`Dumas, Alexandre, Maquet, Auguste`). Post-fix: `Dumas, Alexandre & Maquet, Auguste`, **each author still its own link** (`/app/authors/3`, `/app/authors/4` — a swallowed delimiter would pass a substring check but break navigation). **Discriminating controls:** single-author cards unchanged (`Fyodor Dostoyevsky`, no stray separator) and the table's tags column still comma-joined, so the fix cannot be faking it. **The decisive check:** `GET /api/v1/books/189/metadata` — the backend — independently returns `"Dumas, Alexandre & Maquet, Auguste"`, byte-identical to what the SPA now displays and different from what it displayed before. Real save through the edit form round-tripped both authors intact (2 authors, names preserved). Mobile 375px clean, no horizontal overflow. **E2E** `frontend/e2e/author-separator.spec.ts` (seed-resilient): 4/4 green on the fixed build, **3 failed on the pristine build** with `Expected "Alexandre Dumas & Auguste Maquet" / Received "Alexandre Dumas, Auguste Maquet"`. Note this spec does **not** gate CI yet ([#953](https://github.com/new-usemame/Calibre-Web-NextGen/issues/953)); the pytest guard is what gates. **Honest residual:** the live save recomputed book 189's `author_sort` in the local dev library; files, paths and names were restored and the original value was never captured, so it was left at the calibre-canonical value rather than a guess. `/security-review` **not required**, checked mechanically: no auth/session/CSRF, crypto, subprocess, user-controlled path, or new route — a display-join constant. No new dependency/license/URL (the SPA has no vitest and rule 6 forbids adding one, which is why the unit guard is a source-pin). Fork-original. | `376f82c5c` | v4.1.15 |
| [#957](https://github.com/new-usemame/Calibre-Web-NextGen/pull/957) | (fork issue [#921](https://github.com/new-usemame/Calibre-Web-NextGen/issues/921)) | **New accounts ignored the admin's configured default theme, differently depending on how they signed up.** #736 made the admin theme picker real; #918 routed three create paths through `config_theme_code()` and pinned them with a hand-maintained 3-tuple whose docstring said adding a path without listing it "is fine". It omitted `cps/api/auth.py`, so the New UI's own signup kept copying `config_theme` raw (#921) while the pin stayed green. Auditing all seven `ub.User()` sites found three more, two of them wider than #921 itself: `oauth_bb.py` and `usermanagement.py` hardcoded `user.theme = 1` — stale upstream code from `1a5a8bb95` ("Enforce dark theme"), never revisited when `a9143e873` (#845) brought six themes back — and the LDAP import seeded nothing, letting `Column(default=1)` win. The raw copy is wrong only at the legacy `config_theme == 0`; the hardcodes are wrong at six of seven values. Reproduced on the wire: with the admin set to Light, `POST /api/v1/auth/register` stored `theme=0` (reads back dark) pre-fix and `theme=2` (light) post-fix, against an admin-created control that was light throughout. The pin is replaced by an AST guard that enumerates every `ub.User()` site and fails on an unclassified one, inverting the contract that let #921 through; both it and the #918 pin were also missing `pytestmark = pytest.mark.unit`, so CI's `-m "smoke or unit"` had been deselecting the whole theme guard — it never gated a PR until now. | 74a876440 | v4.1.15 |
| [#955](https://github.com/new-usemame/Calibre-Web-NextGen/pull/955) | (fork issue [#954](https://github.com/new-usemame/Calibre-Web-NextGen/issues/954) @darkmatterpelican) | **"Change cover" made the whole server unreachable until it finished** — and so did "Search metadata", which nobody had reported. **Root cause is structural, not a slow provider.** `cps/server.py` serves on `gevent.pywsgi` and deliberately never calls `monkey.patch_all()` (s6 forks subprocesses), so every request is a greenlet on **one OS thread** — verified live in the shipped container: `monkey.is_module_patched('socket')` is **False**. `gather_cover_candidates` fanned its 16 providers out through a stdlib `concurrent.futures.ThreadPoolExecutor` and waited on `as_completed`, which parks that thread on a `threading.Condition` the hub cannot preempt. The fan-out therefore froze **every other in-flight request** for its whole duration. The identical pattern existed in **three** request-path fan-outs — `services/cover_picker.py`, `search_metadata.py` (the "Search metadata" button) and `services/cover_booster.py` (which the picker itself calls inline) — so fixing only the reported one would have been whack-a-mole. `services/cover_preview.py` had already discovered this for its Wand pool and documented it in a comment, but kept the knowledge private; the fix extracts it into one shared `cps/services/parallel.py` (`fan_out`) that all three now use, and an AST guard test fails the build if any of them reimports `concurrent.futures`. Jobs still run on **real OS threads** (providers do blocking socket reads; greenlets cannot help unpatched) — only the *waiting* changes, via `gevent.threadpool.ThreadPool` + `gevent.iwait`, so throughput is unchanged. **Red/green, mutation-tested rather than trusted:** 20 tests across `test_cover_picker_gevent_responsiveness.py` + `test_request_fanout_gevent_responsiveness.py`; the headline test drives the real `gather_cover_candidates` and measures the stall a co-scheduled greenlet suffers — **1187ms red on pristine main**, ~30ms green. Reverting `cover_picker.py` → 3 red; reverting `cover_booster.py` + `search_metadata.py` → 4 red. **A false green was caught and thrown away first:** the initial heartbeat checked its exit flag *before* recording the gap, so it never sampled the one gap that mattered and **passed on unfixed code**; fixed to record before testing the flag, which is what turned it red. **A regression I introduced was caught by my own verification, not by the tests:** `gevent.iwait` hands back completed jobs in waves, so reading the clock in the consumer loop stamped a whole wave with one identical time — live, **10 of 16 providers all reported exactly 5316ms** where pristine main reported **16 distinct** durations (742→10484ms), which would have quietly wrecked the picker's per-provider timings. `elapsed_ms` is now stamped **inside the worker** the instant a job returns; a new test pins it (6 jobs / 2 workers must show ≥3 distinct completion waves) since the original 2-provider timing test could never batch and so could never catch it. **Live wire on cwn-local, pre-fix first, on the real endpoints:** reproduced the reporter's symptom — a `/book/188` page that answers in **30ms took 11.4s** while `POST /book/188/cover/candidates` ran; 6 parallel requests → **6/6 stalled ~10.2s**. Post-fix, during a 13.6s cover search: **6/6 → HTTP 200 in 125–232ms**, 99 candidates from 16 providers, **16/16 distinct** durations restored. **A 302 in the pre-fix capture was chased down and proved to be my own harness, not a product bug** — those jars logged in without `remember_me`, and `strong` session protection *clears* a non-permanent session rather than just marking it stale; with a browser-shaped login (the form checks `remember_me` by default) it is 200 throughout, so no phantom follow-up was filed. **Scope stated:** `CWA_COVER_PICKER_TIMEOUT` is defined in `services/cover_picker.py` and **used nowhere** — a dead knob, so the fan-out has no overall deadline; left alone here (it changes which candidates appear) and filed separately. Container restored to its exact pre-tick state (all three files byte-identical to main, staged `parallel.py` removed, healthy, `GET /login → 200`). `/security-review` **not required**, checked mechanically: no auth/session/CSRF, crypto, subprocess, user-controlled path, or new route — a concurrency-primitive swap behind unchanged signatures. No new dependency/license/URL (gevent + stdlib, both already in use). Follow-up filed: [#956](https://github.com/new-usemame/Calibre-Web-NextGen/issues/956) (`CWA_COVER_PICKER_TIMEOUT` is a dead knob). Fork-original. | `df4baf2b4` | v4.1.15 |
| [#952](https://github.com/new-usemame/Calibre-Web-NextGen/pull/952) | (fork issue [#928](https://github.com/new-usemame/Calibre-Web-NextGen/issues/928) @chloeroform) | **Saving a default library view turned the library home into the search page** — @chloeroform reported the "Advanced search" form pinned above the books, the page retitled "Advanced search", and the library title, actions and Discover strip gone. **Root cause: the saved default view ([#498](https://github.com/new-usemame/Calibre-Web-NextGen/issues/498)) is a *filter over the library*, but [#891](https://github.com/new-usemame/Calibre-Web-NextGen/pull/891) implemented it by swapping the whole page component** — `return defaultFilter ? <AdvancedSearch defaultFilter={defaultFilter} /> : <Catalog />`. The chrome the user lost was not styled away; a different page was rendering. Both reported symptoms are that one line. **Fix:** render `<Catalog>` with the filter and drive its listing from the advanced-search endpoint, so the library keeps its heading, actions, count and Discover strip and only the listing is scoped. The filter applies to the plain library only — an entity view, a discovery view or an active search is an explicit navigation out of it — and a notice strip names the subset and offers **Show all books**, because a standing filter that hides books with no way out is indistinguishable from missing books. **Red/green (e2e, real SPA in `cwn-local`); the first red run was thrown away as invalid** — it failed on its *control* assertion because `data-testid=catalog-page` is added by this branch, so it proved nothing about the bug. The honest red keeps the instrumentation and mutates only the defect back in: red at L140 (exactly after a default filter is set, `catalog-page` disappears — the reporter's symptom), green 3/3 desktop + 3/3 at 375px. **Live measurement** through the real `/ajax/view` endpoint (`read_status: unread`): search form absent, title `Your Library`, actions + Discover visible, and the filter genuinely discriminating (library 41, unread 35, page shows 35 — a proper subset, so the count cannot be faked by ignoring the filter). **Known follow-up, disclosed not buried:** the v4.1.15 pre-tag gate found this left `AdvancedSearch`'s `defaultFilter` prop dead, so **Edit default view** opens a blank form — filed as [#962](https://github.com/new-usemame/Calibre-Web-NextGen/issues/962) rather than patched naively, since passing the filter to `/search` unconditionally would apply a saved default to every ordinary Advanced search. Fork-original. | `c17bac617` | v4.1.15 |
| [#950](https://github.com/new-usemame/Calibre-Web-NextGen/pull/950) | (fork issue [#944](https://github.com/new-usemame/Calibre-Web-NextGen/issues/944) @jdbway; adopts their PR [#945](https://github.com/new-usemame/Calibre-Web-NextGen/pull/945); mirrored upstream as CWA [#1434](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1434)/[#1435](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1435)) | **Setting a duplicate auto-resolution cooldown stopped auto-resolution running at all**, and the log counted *up* (`241.0 minutes remaining` for a 30-minute cooldown). Two independent bugs, both diagnosed by @jdbway. **(1) Bool coercion.** `get_cwa_settings()` collapses every int setting to a bool unless allowlisted; the allowlist was a **duplicated literal** in `scripts/cwa_db.py` and `cps/cwa_functions.py` that had to match byte-for-byte, and had drifted by **exactly one key** — the cooldown. So the admin page round-tripped 30 while the scan task read `bool(30)` → `True` → `int(True)` → **1 minute**. **(2) Mixed time bases.** `cwa_duplicate_resolutions.timestamp` is written by the schema `DEFAULT CURRENT_TIMESTAMP`, which SQLite evaluates in **UTC**, but the cooldown compared it against a naive **local** `datetime.now()`. West of UTC every `elapsed` is negative (−240 min in EDT) so `elapsed < cooldown` is permanently true; east of UTC it is inflated and the cooldown never fires. Compounded, auto-resolution was dead west of UTC. **Adopt-and-improve, not merge — and the deviation is measured, not preferred.** #945 aligns the two allowlists (fixes the symptom, leaves the divergence class), adds **no tests**, and fixes bug 2 on the **wrong side**: it switches the *write* to local. But the write side is **already correct UTC** — proven by a test that passes on unmodified main — so bug 2 is entirely a read-side defect. Stamping local instead **orphans every existing row**: `SELECT MAX(timestamp)` over a mix of local and legacy-UTC strings returns the **stale UTC row**, measured here as `elapsed` still −235 min after the patch, i.e. **#945 does not fix any install that already has history**, and it ships no migration. Unifying on **UTC** instead needs **no migration** and is DST-safe. Allowlists collapse to one definition (`INTEGER_SETTINGS`/`FLOAT_SETTINGS`/`JSON_SETTINGS` in `cwa_db`, imported by `cwa_functions`), so the two readers **cannot** drift again; the cooldown decision moves into `_cooldown_remaining_minutes`, extracted because it shipped broken precisely by being untestable inside a 100-line method. @jdbway's `update_cwa_settings` cache-refresh is kept — a real invariant, since the scan task reads `cwa_db.cwa_settings` cached in `__init__`. **Red/green with proven teeth:** `tests/unit/test_944_duplicate_cooldown.py`, 20 tests, **17 red pre-fix**, and the suite is **mutation-tested** rather than trusted: reverting the read to naive local → **5 red**; dropping the cooldown key from the allowlist → **9 red**; applying **#945's own local-stamp approach** → **2 red**, incl. the end-to-end user scenario — the empirical basis for the deviation. Tests force `TZ=America/New_York`, because **CI runs in UTC where `local == UTC` and neither bug can reproduce** (they would have been falsely green). Adds a guard that every non-flag int default is allowlisted, so the next divergence fails a test instead of a user. **Live wire on cwn-local (EDT, 10 real pre-existing rows), pre-fix first:** reproduced the reporter's exact symptom — `30 min → True → 1 min`, `elapsed = -240.0`, `"skipped due to cooldown: 241.0 minutes remaining"`; the 10 real rows confirmed **UTC on disk**, validating the no-migration choice. Post-fix, through the **real admin endpoint** (`POST /cwa-settings`, HTTP 200): persisted `1|30`, scan task reads **30**, and the boundary is exact — no history → runs, just-resolved → blocked 30.0, 29 min → blocked 1.0, 31 min → runs. Container restored to its pre-tick state (settings + rows). **Honest notes:** an `AttributeError: module 'cps.schedule' has no attribute 'refresh_hardcover_auto_fetch'` appeared in the live logs and is **not this change** — it is `docker cp` staleness (the call site is `d55567575`, absent from my diff; the function exists in repo `schedule.py:366` but not in the older image), and attempting to cp the matching `schedule.py` cascaded further skew and broke the container, which independently confirms image-vs-main drift; the container was recreated and verified healthy. Two suite failures (`test_required_directories_exist`, `test_ingest_batch_dirty`) were **re-verified on pristine main by reverting all three files**, not cited from prior ticks. Full CI selection: 2952 passed / 2 pre-existing. `/security-review` **not required** (no auth/session/CSRF, crypto, subprocess, user-controlled path, or new route — settings coercion + a timestamp comparison). No new dependency/license/URL (stdlib `datetime`). Fork-original. | `e96a4abd3` | v4.1.15 |
| [#942](https://github.com/new-usemame/Calibre-Web-NextGen/pull/942) | (fork issue [#874](https://github.com/new-usemame/Calibre-Web-NextGen/issues/874) @chloeroform; adopts + reworks their PR [#940](https://github.com/new-usemame/Calibre-Web-NextGen/pull/940)) | **Every container start set permissions on the whole Calibre library twice.** cwa-init hardcoded `/config`, `/app/calibre-web-automated` and `/calibre-library` into its chown list, then appended every value from `dirs.json` on top — which declares `calibre_library_dir=/calibre-library` and `tmp_conversion_dir=/config/.cwa_conversion_tmp`. So the library was walked twice and a subtree of `/config` re-walked after `/config`'s own recursive pass. @chloeroform found it in the startup log. **Fix:** list-building, de-duplication and the pass move to `scripts/set_ownership.sh`; the hardcoded `/calibre-library` is dropped so it comes from `dirs.json` alone; de-duplication is by **containment**, not equality, which is what removes the `.cwa_conversion_tmp` walk. **Adopt-and-improve, not merge:** #940 emptied the list to `dirs.json` alone, which is a regression on two of its three entries — verified, not assumed. `/config` is load-bearing because cwa-init writes `app.db` (L163) and `user_profiles.json` (L215) **as root after** the early chown, and `dirs.json` declares `/config/.cwa_conversion_tmp`, not `/config` — `cps/cwa_functions.py:2401` opens that file `r+` as `abc`, so root-owned 0644 breaks every profile-picture upload on a fresh install. The app tree is load-bearing because `COPY --chown=abc:abc` resolves at **build** time and `abc` is uid 911 in the image (confirmed: `stat` reports `911:1001`), which the base image then usermods to `$PUID` — orphaning the tree for every documented `PUID != 911` install, while `cps/editbooks.py` writes `metadata_change_logs/` under it as `abc`. Both stay as a floor `dirs.json` cannot remove; `scripts/auto_library.py:133` rewrites `dirs.json` in place at runtime, so a crash mid-write must not be able to reduce the pass to nothing. **The promised marker gate was built, measured and rejected:** the app tree is only expensive on a *fresh* container (2.54s here, 26.4s in the reporter's log; 1820 entries, each a copy-up on overlayfs), and a marker kept in the image's writable layer is necessarily absent exactly then — it would have skipped only the already-cheap restart case (0.034s). Shipping it would have been a cope. **Red/green** `tests/unit/test_set_ownership.py`, 21 tests driving the real bash with a recording `chown` stub (behaviour, not source-pins): mutating the script back to main's no-dedup shape goes 4 red; mutating it to #940's empty floor goes 12 red. The first draft of these tests was weaker than it looked — two stayed green under the no-dedup mutation because the harness uses tmp paths and never reproduced the literal `/calibre-library` collision, and the trailing-slash assertion compared unnormalised strings; both fixed. **Live wire on cwn-local (`PUID=501/PGID=20`), pre-fix first:** reproduced the reporter's exact log (library twice, `.cwa_conversion_tmp` separate); after `docker cp` + restart the pass is 4 entries each walked once, and `.cwa_conversion_tmp` is still `501:20` — covered by `/config`'s `-R`, so the dedupe removes work without losing coverage. Healthy, `GET / -> 200`, no `Traceback`/`PermissionError`. **Not reproduced, stated not implied:** the `/config` half of #940's regression could not be demonstrated live on this box — Docker Desktop for macOS mounts `/config` `type fakeowner`, which does not enforce permission bits; the identical test on the container's own filesystem raises `PermissionError`, so the mechanism is proven by control + code path, not by reproducing the symptom. `/security-review` clean, and independently found the change **narrows** a pre-existing surface (`read_dirs_json` now requires a leading `/`; the old code chowned any string value as root). **Scope stated:** this is the duplicate-walk half only; the reporter's other question — is the app-tree chown needed at all (answer: almost none of it; all 1820 entries are already world-readable) — is filed with the measurements + write-path audit as [#941](https://github.com/new-usemame/Calibre-Web-NextGen/issues/941) and #874 stays open for it, because its failure mode is silent. No new route/dependency/URL (stdlib bash + `json`). Fork-original. | `4d3420fe9` | v4.1.15 |
| [#939](https://github.com/new-usemame/Calibre-Web-NextGen/pull/939) | (fork issue [#936](https://github.com/new-usemame/Calibre-Web-NextGen/issues/936), found by the v4.1.14 pre-tag adversarial gate) | **Russian screen readers announced the reader progress bar as "Прочитано: 45% r"** — a stray `r` left over from the English "read", live since v4.1.13 (`d86265ecc`/#694). Consumed as `aria-valuetext` at `frontend/src/pages/Reader.tsx:653`, so it is spoken but never painted: invisible to sighted users, screenshots and every visual gate. **Root cause is the extractor, not the translator.** babel's python-format heuristic reads the *literal* percent in `{pct}% read` as a format spec (`% r` = space-flag + `r` conversion), so `pybabel extract` flags the entry both `python-brace-format` **and** `python-format`; `msgmerge` then fans that bogus flag out to every locale. A msgid is interpolated by exactly one mechanism, so the pair is always a mis-detection — and it **inverts `msgfmt --check`**: gettext validates each translation against the phantom `% r` spec, so the corrupt Russian msgstr (which preserved it) PASSED while the *correct* pt_BR one (`{pct}% lido`, which dropped it) was the sole FATAL across 28 locales. **The locale failing the check was the correct one**, which is why nothing caught this: a novel variant of the v4.0.47 class — not "malformed file silently dropped" but "malformed *string* silently validated", invisible precisely because it was faithful to a bogus spec. **Fix is durable, not a hand-edit:** the issue proposed stripping the flag by hand, but `update-translations.yml` re-runs `pybabel extract`, so that would have silently regressed on the next translation sync. New `scripts/fix_pot_format_flags.py` (stdlib-only, idempotent) strips `python-format` from any entry also flagged `python-brace-format`, wired into `scripts/update_translations.sh` **between extract and msgmerge** — verified empirically that `msgmerge` drops the flag from a `.po` once the POT is clean, so the repair propagates to every locale by itself. Rewording the msgid is not a fix but a coin flip (read→`% r`, done→`% d`, complete→`% c`, finished→`% f` all trigger it). **Blast radius measured before the bulk edit:** exactly 1 POT entry and 1 entry per locale — the 150 legitimate `python-format` entries are untouched; the whole diff over 29 translation files is 29 flag lines + 1 msgstr. **Wider than the issue described:** it reported ru+pt_BR, but all 28 locales carried the bogus flag (msgmerge had propagated it even where the msgstr is empty). **Red/green** `tests/unit/test_i18n_format_flags.py`, 58 tests, 31 red pre-fix: the headline one drives the **compiled `.mo`** through `gettext` and reproduces `Прочитано: 45% r`; plus the both-flags invariant across POT + every locale, and `msgfmt --check` per locale. **Gating `--check` only became possible here** — before this fix pt_BR, the correct translation, was the sole failure, so the check could not be turned on without failing on truth; it is now 0 failures / 28 and enforced in CI as a sibling to the plain-`msgfmt` ship gate (that one answers "does the locale ship at all" — the v4.0.47 hu case; this one "is the string it ships correct"). Also corrected `test_translations_compile.py`'s module docstring, which claimed it ran `msgfmt --check` while the code ran plain `msgfmt` — the false claim of coverage was part of why the gap persisted. **Live wire on cwn-local, pre-fix first:** the SPA reads this string from `/api/v1/i18n/ru.json` (`_load_catalog` parses the `.po` directly via babel `read_po`, not the `.mo`), and the running container served `'Прочитано: {pct}% r'` → rendered `Прочитано: 45% r`; after `docker cp` + restart the same endpoint serves `'Прочитано: {pct}%'` → `Прочитано: 45%`, all 2532 Russian strings still served, container healthy, logs clean of parse errors. **Not done, stated not implied:** no browser was driven with a Russian-locale screen reader to hear the string — the catalog route is the wire the SPA consumes and is verified there. No new route/dependency/URL (stdlib `re`/`sys`). Fork-original (i18n toolchain). | `23b9bc08c` | v4.1.15 |
| [#924](https://github.com/new-usemame/Calibre-Web-NextGen/pull/924) | (fork issue [#920](https://github.com/new-usemame/Calibre-Web-NextGen/issues/920), found by the pre-release verify gate on the v4.1.14 candidate; supersedes the mechanism added by [#906](https://github.com/new-usemame/Calibre-Web-NextGen/pull/906) for [#905](https://github.com/new-usemame/Calibre-Web-NextGen/issues/905) @iroQuai) | **A second KOReader device destroyed the first device's highlights, permanently, just by opening the book.** #906 let a push declare itself `complete`, and the server soft-deleted every live row of that source the push omitted. The inference is unsound: "the user deleted their last highlight" (#905, must delete) and "this device never had them" (#920, must not) are **byte-identical pushes**, and the server has no third fact to separate them. The KOReader-native provider is push-only by construction (`koreader_annotations_provider.applyToDevice` returns 0 off-Kobo) and `main.lua` gated `apply_to_device` on a `volume_id` that is nil for koreader-origin rows (`content_id: null`), so device A could **never** converge — yet still declared its empty set complete. `_reap_absent` then hid every row and `apply_portable` never un-hides a tombstone, so the re-push returned `skipped` forever: silent, permanent, HTTP 200. **Fix — the decision moves to the only party holding the missing fact.** The plugin persists a watermark of the ids it last pushed in the book's own sidecar, diffs it against its live set, and NAMES deletions (`deleted: [...]`, scoped by `delete_source`); the server obeys the list and infers nothing. `complete` is accepted and ignored. Fails safe by construction: a lost watermark (fresh install, wiped sidecar) yields no deletions, and the "did we actually read the local set?" guard (`local_set_known`) stops a nil-`volume_id` Kobo read — an empty list that means "couldn't read", not "empty" — from deleting the library. **Second, independent defect found while fixing it, and the reason #920 could not actually fire in production:** lua-Spore rebuilds the request body from *exactly* the keys in `api.json`'s `payload` list and silently drops the rest (`Spore.lua:137-141`, vendored 0.4.2 per `koreader-base/thirdparty/lua-Spore`). #906 added `complete`/`complete_source` to the client and never touched `api.json` — **so both were discarded before the request was sent, and #906's delete-sync has never worked on the wire.** #905's symptom is still live for every user; the reap was never reachable except from a hand-rolled client (the verify gate's repro was curl-driven, which bypasses Spore). The new `deleted` field would have died the same silent death, so `api.json` declares it and `tests/unit/test_cwasync_plugin_wire_contract.py` pins client-sent keys ⊆ declared payload for **every** method — that test, run against the pre-fix tree, independently reports `push_annotations passes ['complete', 'complete_source']`, i.e. it rediscovers #906 unaided. **Red/green:** `test_920_koreader_push_only_authority.py` 21 tests — 19 red pre-fix, the headline one reproducing the destruction over the real PUT as `deleted: 3 != 0` for both `{}` and `[]`; scoping (source/user/book), soft-delete + tombstone, fan-out, no re-delete of a hidden row, and malformed `deleted`/`delete_source` → 400 (also closing the `TypeError: unhashable type` → **500** on a non-hashable `complete_source`, since the unhashable `in` lookup is gone). `test_koreader_annotation_delete_reap.py` rewritten: #905's user-facing guarantee is preserved end to end through the new mechanism. `sync_logic_test.lua` covers `computeDeletions` (incl. the empty-watermark and last-highlight cases). Annotation/kosync/plugin suites 486 pass; full CI selection 2786 pass (2 pre-existing env failures, unrelated, reproduce on the published tag). **Not verified + stated in the PR:** physical KOReader hardware (#906's own standing exemption) — the plugin change is Lua driven through unit tests and a Spore-semantics reading of the vendored source, not a device. Plugin 4.1.14 (already in lockstep) ships with this release; the server accepts old plugins and deletes nothing for them. No new route/dependency/URL. Fork-original. | `424584b53` | v4.1.14 |
| [#918](https://github.com/new-usemame/Calibre-Web-NextGen/pull/918) | (fork issue [#736](https://github.com/new-usemame/Calibre-Web-NextGen/issues/736) @auspex; same defect surfaced in [#351](https://github.com/new-usemame/Calibre-Web-NextGen/issues/351) / [#579](https://github.com/new-usemame/Calibre-Web-NextGen/issues/579) @uschi1) | **Admin → Theme reported "Settings saved." and changed nothing.** The picker hardcoded its own `Light=0`/`Dark=1` numbering, matching neither `ui_themes.THEME_CODES` (1=dark, 2=light) nor the SPA slugs, so "Light" wrote `config_theme=0` — which `ui_themes` documents as resolving to dark on read. A genuine save plus a genuine dark render produced the "saved but nothing changed" symptom. `test_theme_registry.py` existed to stop exactly this drift but pinned only backend↔`themes.ts`, so `Admin.tsx` invented a third numbering behind it. **Fix:** `config_theme` travels as a slug validated against `ALLOWED_THEME_SLUGS` (the raw int no longer crosses the wire — that was the drift vector); the picker renders the `THEMES` SSOT (six themes, not two); all three account-create paths seed from it; a legacy `0` is honoured as light so @auspex's saved click survives, while `User.theme` keeps its 0→dark rule and existing accounts are untouched. **Found during the blast-radius pass:** `register_post` (self-registration) still seeded `content.theme = getattr(config, 'config_theme', 1)` raw, so with an admin-saved Light a self-registered account booted dark while an admin-created one booted light — the same drift, on the public path. All three paths now pinned. **Red/green:** the pre-fix `<option value="0">` pin is red on main; the create-path seeding pin fails 3 of 4 params against the old hardcode; removing the slug validation fails 10 of the update-config tests. **Live wire on cwn-local, real endpoints session-cookied:** `GET /api/v1/admin/config` → `'dark'` (slug, not int); `POST {"config_theme":"light"}` → 200 + DB `2` + reads back `'light'`; `POST` bogus slug → 400 nothing stored; `POST {"config_theme":0}` → 400 (the exact payload the pre-#736 form wrote is now rejected); a user created under a Light default lands `theme=2` and serves `theme='light'` on `/auth/me` — the #736 promise end-to-end. App boots with no circular import from the new `ui_themes` import in `web.py`; container logs clean. `config_sql.py:82` is `Column(Integer, default=1)`, so a `0` is never the schema default — only a deliberate old-form click, which is why reading it as light is right. Greptile 4/5; both findings actioned (untested validation path; a picker pin whose regex started at the form-state object and matched the right `<select>` only by luck). Not covered: the classic Jinja create path is unit- and container-Python-verified but not driven. i18n: 2 msgids anchored in `cps/spa_strings.py`. No new route/dependency/URL. Fork-original. | `d834a5e` | v4.1.14 |
| [#910](https://github.com/new-usemame/Calibre-Web-NextGen/pull/910) | (fork issue [#627](https://github.com/new-usemame/Calibre-Web-NextGen/issues/627) @uschi1; same web-visibility cause as [#509](https://github.com/new-usemame/Calibre-Web-NextGen/issues/509), distinct from [#633](https://github.com/new-usemame/Calibre-Web-NextGen/issues/633)) | **KOReader devices exchanged the correct position, but the book page showed no “KOReader Progress” entry for an already-known book.** The earlier #627 fixes proved filename-checksum resolution and ingest registration; they did not trace the accepted progress through the separate state carrier consumed by the classic and SPA book-detail pages (failure mode Class 1). `KOSyncProgress` stored device-to-device position correctly and `ReadBook` changed to in-progress, but `update_book_read_status()` only created `KoboReadingState.current_bookmark` when it created a brand-new `ReadBook`. A pre-existing read/unread row with no Kobo state therefore stayed detached from both UI consumers indefinitely; opening via OPDS/web created the missing state through another path, explaining the reporter's screenshots. **Fix:** one `_ensure_visible_reading_state()` helper creates or completes the `KoboReadingState` + bookmark + statistics graph for both new and existing `ReadBook` branches, then mirrors the accepted KOReader percentage into the bookmark. **RED→GREEN:** `tests/unit/test_627_kosync_visible_progress.py` replays the plugin's auth handshake and progress PUT against the real Flask blueprint/SQLAlchemy models; pre-fix returns `{"authorized":"OK"}` and stores `KOSyncProgress(document="1031", percentage=68.86)` but fails `NoResultFound` for the visible state, while the fix creates it at 68.86. **Live wire on isolated `cwn-p627`/8107:** v4.1.12 baseline auth + PUT reproduced device storage with zero Kobo state; patched auth + PUT created exactly one state/bookmark, classic `/book/2` rendered `KOReader Progress: 68.9%`, and SPA `/api/v1/books/2` returned `kosync_progress: 68.86`. Eight parallel device PUTs converged to one progress/state/bookmark row at 68.0 with no lock/integrity errors. Server-side only: existing plugins already make this handshake + PUT sequence, so no plugin update is required. Physical KOReader hardware remains **ASSUMED**, not verified. PR #906 is present in the base but unreleased and unrelated: its device-plugin update enables highlight-deletion completeness reconciliation, not progress visibility. | `8bef3622d` | v4.1.14 |
| [#903](https://github.com/new-usemame/Calibre-Web-NextGen/pull/903) | (fork issue [#896](https://github.com/new-usemame/Calibre-Web-NextGen/issues/896), from [#873](https://github.com/new-usemame/Calibre-Web-NextGen/issues/873) @KucharczykL) | **The Fetch Metadata modal's 🔑 Keys panel reported "Not configured" for Hardcover even when a working `HARDCOVER_TOKEN` / `HARDCOVER_TOKEN_FILE` was set.** Searches worked; only the badge lied, so a correct setup looked broken. Root cause: `metadata_keys()` (`cps/search_metadata.py`) derived `configured` from the raw config column (`getattr(config, spec["config"], None)`), but an env/file token is **deliberately never persisted** to that column (`resolved_hardcover_token()` resolves per call so container rotation keeps working, #743/#819) — so the column is `None` while the provider itself resolves a token fine. #743 fixed the *zero-result classifier* in this same file to use the resolver but never touched `metadata_keys()`; commit `1dd39c759` (PR #790) shows the one-hunk miss. `metadata_keys_save()` had the same defect from the other side — it derived `configured` from the **posted value alone**, so clearing the admin field while an env token was live returned `configured: false` and `CoverPicker.tsx` (`setConfigured(r.configured)`) flipped the badge to a lie until a refetch corrected it. **Why the #743 guards didn't catch it**: `test_classifier_does_not_read_raw_column` greps for the *literal* `getattr(config, "config_hardcover_token"`, which registry indirection (`spec["config"]`) never spells; and `test_consumer_uses_resolver` is a file-level `"resolved_hardcover_token()" in src` grep already satisfied by the classifier — so the broken function sat in a file both guards certified clean. That is coverage theater, and it is why the new tests are behavioural. **Fix**: an optional `resolver` hook on `PROVIDER_KEY_REGISTRY` plus one `_provider_configured()` helper used by **both** endpoints (SSOT), so a provider whose key can come from env/file can never have one path fixed and the other regress. ~20 LOC, one production file; providers without a resolver (Google Books) keep the column path unchanged. **Red/green** `tests/unit/test_896_metadata_keys_resolver.py`, 10 tests, 5 red on main — they call the real view via `__wrapped__` and assert on the JSON both front-ends consume: env token → `configured: true`, `HARDCOVER_TOKEN_FILE` → `true`, DB token → `true` (no #790 regression), **no token anywhere → `false`** (the fix discriminates, it is not constant-true), Google unaffected, payload shape/`bool` type pinned (`frontend/src/lib/coverPicker.ts` types it `boolean`), token value never serialized, and the save-path env case. Also strengthened the defeated #743 pin so registry indirection can't hide a raw-column read again. **Production-runtime verification**: published `:latest` with `HARDCOVER_TOKEN` set, session-cookied `GET /metadata/keys` → `configured: false` (the reporter's symptom, reproduced over real HTTP in the shipped image); patched into that same image → `configured: true`, Google/Goodreads still `false`, token value absent from the payload; in-container negative (token unset → `false`, restored → `true`). Adjacent suites 62 pass; metadata/provider/cover selection 701 pass. `tests/unit/test_duplicate_index.py` errors are pre-existing on pristine main (20 errors there unpatched) — not from this change. Privacy invariant unchanged: presence only, never the value. No new route/dependency/URL; the endpoint's auth stays `@user_login_required` + admin-only save. Fork-original. | `4404ea67a` | v4.1.13 |
| [#901](https://github.com/new-usemame/Calibre-Web-NextGen/pull/901) | (fork issue [#876](https://github.com/new-usemame/Calibre-Web-NextGen/issues/876) @KucharczykL) | **Hardcover auto-fetch finished every run but saved no statistics, and the admin Hardcover panel stayed blank.** Each run logged `Error saving stats to database: 'CWA_DB' object has no attribute 'execute_write'` yet still reported success (`364 processed, 12 auto-matched`), so `hardcover_auto_fetch_stats` stayed empty forever. Root cause: `CWA_DB` (`scripts/cwa_db.py`) writes throughout via `self.cur.execute` + `self.con.commit` and never defined the `execute_write()`/`execute_read()` convenience methods, but the Hardcover auto-fetch feature calls both — `cps/tasks/auto_hardcover_id.py:417` (`_save_stats`) → `execute_write`, AttributeError swallowed into a WARN at `:433` (**the reported symptom**), and `cps/cwa_functions.py:1314,1317` (Stats & Activity tab) → `execute_read`, same AttributeError caught by the outer handler → `hardcover_stats = None` → **admin Hardcover panel silently blank** (the half the reporter never saw). Table `hardcover_auto_fetch_stats` already exists (`cwa_schema.sql:156`) — no schema/migration work; the whole defect is the two missing methods. **Fix**: add both helpers to `CWA_DB` (20 LOC, one file) rather than rewriting the three callsites — they are the shared path the feature assumed. `execute_read` returns `fetchall()` because `cwa_functions.py:1335` indexes `total_processed[0][0]`; a `fetchone()` contract would break the panel. **Red/green** `tests/unit/test_cwa_db_execute_helpers.py`, 10 tests, all fail on main; the behavioural one drives the real `_save_stats` against a real sqlite DB and reproduces the reporter's WARN byte-for-byte pre-fix. Pins commit durability across a fresh connection (an uncommitted write would still pass a same-connection read but be lost to the admin panel's own CWA_DB), the `[0][0]` contract, the empty-table `SUM`→`[(None,)]` guard shape, param binding, and a source-pin against silent removal. **Production-runtime verification**: published `:latest` confirmed missing both helpers; with the patched file mounted into that same image, the task's exact INSERT lands and the admin tab's exact SELECT+guard returns `364`/`12` (was blank), and a second `CWA_DB` reads `[(364,)]` proving the commit. Purely additive; adjacent suites 35 pass. Not exercised: a live auto-fetch against the real Hardcover API (needs token + library). No new route/dependency/URL. Fork-original (CWA DB helper). | `0f03c2784` | v4.1.13 |
| [#774](https://github.com/new-usemame/Calibre-Web-NextGen/pull/774) | (fork issue [#615](https://github.com/new-usemame/Calibre-Web-NextGen/issues/615) @hayvan96) | **The new UI kept showing outdated interface translations after an upgrade.** After updating, the French read button reverted to English "Read now" and the read toggle showed the pre-v4.1.4 inverted "mark as read" wording — even though the strings were fixed in v4.1.4 (#619) and the reporter had confirmed it. Root cause is **cache correctness, not translation data**: the per-locale catalog the SPA loads from `/api/v1/i18n/<locale>.json` changes every release but was served at a stable URL with `Cache-Control: public, max-age=3600` and **no validator**, so a browser or self-hoster's reverse proxy kept serving a pre-fix catalog an upgrade couldn't invalidate. Verified the server data was correct at every version (live cwn-local + `git show v4.1.6:fr.po`, no fuzzy/dupes). **Fix** (`cps/api/i18n.py`, one production file): attach a content ETag (sorted `{msgid:translation}` hash, locale-folded) and switch to `Cache-Control: no-cache` + `make_conditional` — an unchanged catalog costs a 304, a changed one is fetched fresh. **Red/green** 4 tests in `test_api_v1_i18n.py` (ETag+no-cache present, ETag tracks content, conditional→304, #615 French strings pinned); all fail on the old headers. **Live e2e** cwn-local: fresh GET → 200 + ETag + no-cache + correct French; conditional GET → 304 empty body. No new route/dependency/URL (stdlib hashlib). Fork-original (cache correctness). | `39d88a38f` | v4.1.9 |
| [#768](https://github.com/new-usemame/Calibre-Web-NextGen/pull/768) | (fork community PR [#763](https://github.com/new-usemame/Calibre-Web-NextGen/pull/763) @Kyraminol) | **Book pages show when reading progress first synced and when it last synced (Kobo + KOReader).** Adopts @Kyraminol's #763 (nullable `kobo_bookmark.created_at`, lazy `before_flush` first-positive-progress stamping, earliest-date preservation across all three Kobo-state merge branches, PRAGMA-guarded idempotent migration) plus three additions: the three new SPA labels anchored in `spa_strings.py` (original diff would have left non-English locales on English fallback), `test_spa_strings_anchored.py` marked `unit` so CI actually selects the guard, and the migration ALTER routed through `_run_ddl_with_retry`. Adversarially verified pre-adoption (before_flush blast radius: isinstance-guard exit for non-Kobo flushes; migration cold-boot: idempotent on populated DB). 12 red/green feature tests + 5 anchor tests; live cwn-local: migration added the column on a populated table (7 rows, NULLs preserved), real listener stamped first positive progress (UTC), API returns ISO-8601 `+00:00` fields, SPA + classic both render. Greptile 4/5, 1 P2 dispositioned (docstring). | `9740c6dc2` | v4.1.8 |
| [#755](https://github.com/new-usemame/Calibre-Web-NextGen/pull/755) | (fork issue [#737](https://github.com/new-usemame/Calibre-Web-NextGen/issues/737) @auspex) | **The Admin "Duplicates found" notice nagged about books the user had archived.** After a duplicate scan, archiving one book of a pair left the sidebar badge + `duplicate-notifier.js` popup counting the group, while the `/duplicates` page correctly showed nothing — so clicking through hit an empty page and the notice re-fired on every 2.5s poll ("limbo"). Root cause: the `/duplicates/status` endpoint serves its `count` from `cwa_duplicate_cache` (serialized at scan time) filtered only by `filter_dismissed_groups`; it never applied the per-user archived/hidden exclusion the page applies via `get_common_filters` (`get_duplicate_groups_from_index` drops groups with `<2` visible books). **Fix**: `get_duplicate_status` now routes cached groups through new `filter_visible_duplicate_groups`, which re-validates each group's `book_ids` against `_visible_duplicate_book_ids` (same `get_common_filters` SSOT), drops groups below two visible books, and reflects the reduced count — generalises to hidden/deleted too. **Red/green**: `tests/unit/test_737_duplicate_status_excludes_archived.py`, 9 tests, all fail without the fix (verified by stashing) — the reporter's archived-pair case drops, fully-visible pair kept, triple 3→2, mixed groups, `user_id=None` passthrough, legacy no-`book_ids` kept, plus source pins on the route wiring + `get_common_filters` usage. Container smoke on cwn-local (healthy boot + funcs callable in real app Python). One production file, no new route/dependency/URL. Fork-original (duplicate-status parity). | `0f58f1502` | v4.1.8 |
| [#756](https://github.com/new-usemame/Calibre-Web-NextGen/pull/756) | (fork issue [#725](https://github.com/new-usemame/Calibre-Web-NextGen/issues/725) reporter + @mrdynamo) | **The new-UI table view had no Tags column.** The redesigned table view could show/hide five columns (title, authors, series, formats, read) but not a book's tags — the reporter used the table view for at-a-glance metadata review/editing, where genres/subjects are what you want visible. Root cause: `serialize_book_list_item` (the `/api/v1/books` list serializer) never emitted `tags` (only the detail serializer did), and `Table.tsx` had a hardcoded 5-column set. **Fix (3 files, ~14 LOC prod):** (1) `cps/api/serializers.py` — list serializer now emits `tags` as a flat name list (matching its other flat arrays; detail keeps `{id,name}`); `fill_indexpage` already `joinedload`s `Books.tags` (db.py:1240) so **no extra query / no N+1**; (2) `frontend/src/lib/api.ts` — `Book` gains optional `tags?: string[]` (absent on older servers → no tags); (3) `frontend/src/pages/Table.tsx` — a "Tags" column between Series and Formats, unsortable (no server sort token, matching Series/Formats/Read), toggleable via the existing Columns menu, "—" when empty. Filtering + arbitrary custom columns deferred. **Red/green** (`test_api_v1_serializers.py`): tag-names emitted + empty-when-absent + exact-dict full-item updated — all 3 fail on main, 11 pass on branch. **Typecheck + build** clean. **Live cwn-local e2e** (SPA `/app/table`, session-cookied): `GET /api/v1/books` returns `tags` populated; Tags column renders comma-joined names; Columns menu picks up a toggleable Tags checkbox; screenshot captured. "Tags" msgid already anchored (`spa_strings.py:592`). No new route/dependency/URL. Fork-original (SPA surface). | `7f8d7b31f` | v4.1.8 |
| [#758](https://github.com/new-usemame/Calibre-Web-NextGen/pull/758) | (fork issue [#750](https://github.com/new-usemame/Calibre-Web-NextGen/issues/750) @chloeroform) | **Every OPDS feed rendered `<title>` as the bare instance name.** In an OPDS reader, "Read Books", "Unread Books", every shelf, the author/series/category lists and search results all showed the same Atom `<title>` — the instance name — so readers that list feeds by title showed a wall of identical entries. Root cause: all ~26 feed routes funnel through `render_xml_template('feed.xml', ...)`, which injected only `instance=config.config_calibre_web_title`, and `feed.xml` rendered `<title>{{ instance }}</title>`. **Fix** (single chokepoint, SSOT): `render_xml_template` resolves a per-feed title from the existing `OPDS_ROOT_ENTRY_DEFS` map keyed by `request.endpoint` (`_opds_feed_title_for_endpoint`) — so the top-level, index, and per-entity detail feeds inherit their already-`N_()`-extracted title with zero new msgids and zero caller edits; the named dynamic feeds (shelf, magic shelf, search) pass an explicit `feed_title` (the shelf's name / the query). `feed.xml` renders `Instance - Feed Name`, falling back to the bare instance for the catalog root. `<author><name>` stays the instance (it is the catalog provider). **Red/green** 29 tests (`test_750_opds_feed_titles.py`: behavioural resolver in an app context + SSOT no-dangling-key pin + template/caller source-pins; all 29 fail on main). **Live e2e** (cwn-local, basic-auth curl): all 16 top-level/index feeds, a shelf detail ("… - CREATE_TEST_…"), and `/opds/search/a&b` (escaped to `a&amp;b`) render distinct valid XML titles; root `/opds` stays bare; no new container errors. No new route/dependency/URL/msgid; two files. Fork-original (OPDS). | `cb0445350` | v4.1.8 |
| [#746](https://github.com/new-usemame/Calibre-Web-NextGen/pull/746) | (fork issue [#719](https://github.com/new-usemame/Calibre-Web-NextGen/issues/719) @standhaftsohnsergius) | **New-UI menu items and whole screens stayed in English even with a complete translation.** In the redesigned interface, strings like "Table view" and "Smart shelves" — and the admin settings, cover picker, advanced search and book-editor screens — rendered English while the classic UI translated them. Root cause: the SPA translates via `t('English source')` resolved against a catalog built from the `.po` files (`cps/api/i18n.py`), but `pybabel extract` only scans Python + Jinja (`babel.cfg`), never `.tsx`. SPA-only strings not re-declared in `cps/spa_strings.py` are dropped from `messages.pot`, msgmerge marks them obsolete, and the SPA falls back to English. The hand-maintained anchor list had drifted — **379 frontend `t()` literals were unanchored.** **Fix**: automate the sync instead of patching the reported strings. New `scripts/extract_spa_strings.py` parses every `t('literal')` from the frontend and every `_('literal')` anchor from `spa_strings.py` — `--check` is a CI gate that fails on drift, `--write` regenerates a delimited `AUTOGEN` block; `cps/spa_strings.py` gains that block anchoring all 379 (curated section untouched, 388 insertions 0 deletions, module still does nothing at runtime); `tests/unit/test_spa_strings_anchored.py` pins the invariant. **Red/green**: `--check` reported 379 unanchored (exit 1) → 0 after `--write` (exit 0); 5/5 gate tests green. **Real pipeline**: `pybabel extract` now emits 2348 msgids (was 2042); every previously-missing string is in `messages.pot`, and translations flow via the existing `update-translations` workflow. No new route/dependency/URL; runtime blast radius zero. Fork-original (SPA i18n tooling). | `6bb9d35b6` | v4.1.8 |
| [#732](https://github.com/new-usemame/Calibre-Web-NextGen/pull/732) | (fork issue [#730](https://github.com/new-usemame/Calibre-Web-NextGen/issues/730) reported on Discord by neontapir) | **New-UI author names containing a comma showed a raw pipe** — an author like "William H. Keith, Jr." rendered under book titles as "William H. Keith| Jr." on the Library and author pages, and on the book detail page. Root cause: Calibre escapes a comma inside a single author name as `|` in the DB, and every classic display path un-escapes it (`web.py`, `api/browse.py`, `api/edit.py`, `api/duplicates.py`), but the two SPA book serializers (`serialize_book_list_item`, `serialize_book_detail` in `cps/api/serializers.py`) emitted `a.name` raw. **Fix**: `.replace("|", ",")` in both serializers — one SSOT for the API, so every consumer (BookCard, BookDetail, Table, EditBook preview) gets the corrected name. **Red/green** unit pins (`test_api_v1_serializers.py`: both serializers un-escape the pipe). Route→serializer→frontend chain traced (`GET /api/v1/books` → list item; `GET /api/v1/books/<id>` → detail; `BookCard.tsx`/`BookDetail.tsx` render verbatim). Nil blast radius — `|` in a stored author name always means an escaped comma. No new route/dependency/URL; one production file. Fork-original (SPA serializers). | `21b0c7ec4` | v4.1.8 |
| [#717](https://github.com/new-usemame/Calibre-Web-NextGen/pull/717) | (fork issue [#716](https://github.com/new-usemame/Calibre-Web-NextGen/issues/716) @Arjan61) | **Downloading a book on an iPhone stranded the user** — in the new UI, tapping a format to download navigated Safari away from the SPA to a page it couldn't render, leaving iPhone users stuck until they force-restarted the app. Root cause: book files are served `Content-Disposition: inline` (deliberate, for byte-range / in-browser reading) and iOS Safari ignores the anchor's `download` hint, so a same-tab tap unloaded the app tab. **Fix**: add `target="_blank" rel="noopener"` to the SPA download anchors (`BookDetail.tsx` format downloads, `EditBook.tsx` format downloads, `Annotations.tsx` .md/.csv/.json exports) so a download gets its own browsing context and never unloads the app; the wide-blast-radius `inline`→`attachment` server flip was deliberately out of scope. **Red/green e2e** (`frontend/e2e/download.spec.ts`, desktop + mobile): anchors carry `target="_blank"` + `rel~=noopener` + `download`; desktop guard confirms the download still fires with no stray tab. **Verified on real iOS Mobile Safari** (iPhone 17 Pro simulator, iOS 26.2, WebKit): the reporter's exact flow (login → book → tap EPUB download) hands off to Safari's native download manager in-place with the SPA tab fully preserved — the stranding is gone. No new route/dependency/URL; three SPA files. Fork-original (SPA surface). | `713664cd9` | v4.1.7 |
| [#711](https://github.com/new-usemame/Calibre-Web-NextGen/pull/711) | (fork issue [#669](https://github.com/new-usemame/Calibre-Web-NextGen/issues/669) @chloeroform; community PR [#703](https://github.com/new-usemame/Calibre-Web-NextGen/pull/703)) | **New-UI admin page rendered a stray, unlabeled user-count ("1") next to "User administration" that read as a glitch.** Removed the count `<span>` (as proposed in @chloeroform's #703) plus the now-dead `.count` CSS rule; `.addBtn` keeps `margin-left:auto` so the header layout is unchanged. Cosmetic element removal (no behavioural logic to regress); Vite build green; no remaining `styles.count` reference. Credit: @chloeroform (#703). | `26701dffe` | v4.1.6 |
| [#710](https://github.com/new-usemame/Calibre-Web-NextGen/pull/710) | (fork issue [#633](https://github.com/new-usemame/Calibre-Web-NextGen/issues/633) @Glalith121) | **KOReader cross-device reading position stopped syncing for a book after it was re-uploaded/edited — one device ahead (80%), the other stuck (67%) with a manual pull reporting "already synced".** Root cause: KOReader keys progress by a partial-MD5 of the *file*, so two devices holding byte-differing copies of the same book (a re-download after a metadata edit, a sideload, or a format the server didn't embed metadata into) hash to different checksums. The fork unifies them by mapping checksum→`book_id`, but that map (`book_format_checksums`) was populated **only** on metadata-embedded downloads (`helper.do_download_file`, gated on `metadata_was_embedded`), so any device that got a raw file never resolved to a `book_id`; its progress orphaned under the raw checksum, invisible to the other device's `book_id`-keyed record (`kosync.get_progress_record` matched only `(book_id, this_checksum)`). **Fix (3 parts):** (1) `helper.do_download_file` now registers the fingerprint of **every** served file, not just embedded exports (still gated on `is_koreader_sync_enabled()` for the CWA #1183 table guard); (2) `get_progress_record` unifies across **all** of a book's registered checksums when the `book_id` resolves (new `get_book_checksums`); (3) on PUT a found record is re-keyed onto `str(book_id)` so the table self-heals. **12 tests** (`tests/unit/test_633_kosync_cross_device_orphan.py`: behavioural orphan-unification + a real-DB integration test against real in-memory SQLite + no-cross-book-leak + source-pins); adjacent kosync/koreader/checksum suites 61 pass; zero branch-introduced regressions (full touched-domain set-diff empty). **Live cwn-local HTTP**: device-2 GET under its own different checksum returns device-1's 80% (the fixed symptom); device-2 PUT re-keys the orphan onto the book_id (self-heal). Greptile 5/5 (one P2 declined: pre-existing transient-settings gate, self-healing by design). No new route/dependency/URL; two production files. Fork-original (KOReader sync protocol). | `a4364cbe9` | v4.1.6 |
| [#693](https://github.com/new-usemame/Calibre-Web-NextGen/pull/693) | (fork issue [#668](https://github.com/new-usemame/Calibre-Web-NextGen/issues/668) @chloeroform) | **A custom profile picture set in the classic account panel was invisible in the new UI** — the top-bar account control and `/account` both showed a placeholder glyph. Root cause: profile pictures live in `/config/user_profiles.json` (`{username: "data:image/…;base64,…"}`, written by the `profile_pictures` blueprint) and the classic UI reads the whole map via `/profile_pictures/user_profiles.json`, but `/api/v1/auth/me` never exposed the avatar and no SPA component rendered one. **Fix**: `/api/v1/auth/me` returns the *current* user's `avatar` only (not every user's base64 like the classic endpoint — lighter payload, no cross-user exposure); a single `_me_payload()` builds the me-shape across `/me`, login, and magic-link so the field can't drift (the magic-link response was also missing `instance_name` — now unified); `serialize_user` stays pure (avatar injected at the route, mirroring the existing `features`/`instance_name` enrichment); a `data:image/` guard rejects a corrupted entry so it can't become an arbitrary URL. A reusable `<Avatar>` (SSOT for avatar rendering) is consumed in the top bar + account page, falling back to the neutral glyph when unset. **11 RED→GREEN tests** (`tests/unit/test_668_newui_profile_pictures.py`: `_user_avatar` guards, `/me` carries the avatar / null-when-unset, AST source-pin that all me-shaped routes go through `_me_payload`); existing magic-link test updated for the unified shape (50 auth/serializer/account tests green). **Live cwn-local**: real HTTP `login`+`/me` return the data-URI with an avatar set and null when empty; Playwright (desktop) — top-bar 20×20 rounded avatar `<img>` + account 48×48 for `cwng84test`, glyph fallback for `admin` (no broken image). No new route/dependency/URL. Fork-original (SPA + `/me`). | `04eb36c17` | v4.1.6 |
| [#695](https://github.com/new-usemame/Calibre-Web-NextGen/pull/695) | (fork issue [#683](https://github.com/new-usemame/Calibre-Web-NextGen/issues/683) @uschi1) | **Marking a book "unread" left a ghost reading-progress percentage the user couldn't clear.** Opening a book "just to test it" stuck it at e.g. "0.6% read"; the read/unread toggle flipped the status bit but never touched the progress, so there was no way to reset it. Root cause: `helper.edit_book_read_status` only wrote `ub.ReadBook.read_status` (or the custom read column) — it never cleared `KoboReadingState.current_bookmark.progress_percent` (the "% read" surfaced on the classic detail page + new-UI book page, fork #587) nor the web-reader `ub.Bookmark` resume point (which would re-derive the percentage on the next reader scroll). **Fix**: new `helper.reset_reading_position(session, user_id, book_id)` clears both stores; `edit_book_read_status` calls it in **both** read-status backends whenever the resulting status is unread. Marking a book *read* is unchanged. No new UI — the existing unread toggle (classic ajax, SPA `POST /api/v1/books/<id>/read`, bulk edit) gains the reset. Device→server Kobo sync doesn't route through this function, so no protocol interference; propagation of the reset to devices is automatic via the existing `KoboBookmark.last_modified` onupdate + the `ub.py` before_flush listener. **9 RED→GREEN tests** (`tests/unit/test_683_unread_resets_progress.py`: behavioural clear/scoping/no-op on a real in-memory ub session + source-pins that both branches reset and both percent fields + the bookmark are cleared); adjacent read-status/progress suites (587/615/634/579/kosync-custom-column/319) 33 pass. **Live cwn-local HTTP e2e** (session-cookied real SPA route): seeded book at 0.6% + an EPUB bookmark → `POST /api/v1/books/2/read {read:false}` → progress NULL, content-source NULL, bookmark deleted, read_status=0; a control book marked read kept its 0.45 progress. No new route/auth/dependency change, single production file (`cps/helper.py`). Fork-original. | `47050accf` | v4.1.6 |
| [#682](https://github.com/new-usemame/Calibre-Web-NextGen/pull/682) | (fork issue [#667](https://github.com/new-usemame/Calibre-Web-NextGen/issues/667) @chloeroform) | **The new-UI sidebar listed every smart shelf, ignoring the per-user "Magic Shelves Visibility" settings from `/me` — the filter only worked in the classic view.** Root cause: two implementations of "which magic shelves can this user see". The SPA endpoint `GET /api/v1/magicshelves` rolled its own `or_(user_id==uid, is_public==1)` query instead of the canonical `magic_shelf.get_visible_magic_shelves_for_user`, so it never applied the `hidden_magic_shelf_templates` filter that the classic sidebar and OPDS already honour — unchecking a shelf in `/me` had no effect in `/app`. **Fix**: the endpoint now delegates to `get_visible_magic_shelves_for_user` for authenticated callers (one source of truth; identical `MagicShelf` return shape, new visible set is a strict subset of the old); anonymous callers keep the public-only path. **3 tests, 2 RED→GREEN** (`tests/unit/test_667_spa_magic_shelf_visibility.py`: behavioural filter contract on a real in-memory ub session + source-pins that the endpoint delegates and no longer rebuilds an `or_()` visibility query — both fail against main's endpoint file); adjacent `test_468_magic_shelf_membership_timestamp` still green. **Live cwn-local HTTP e2e** (session-cookied `GET /api/v1/magicshelves`): baseline lists "Highly Rated", inserting a `hidden_magic_shelf_templates` row for that template removes it from the response, clearing the row restores it — no 500s, no import error from dropping the `or_` import. Single production file, no new route, no auth/dependency change. Fork-original (SPA surface). | `9fc139343` | v4.1.6 |
| [#685](https://github.com/new-usemame/Calibre-Web-NextGen/pull/685) | (fork issue [#684](https://github.com/new-usemame/Calibre-Web-NextGen/issues/684) @PulsarFTW) | **Container crash-looped on every startup on servers that had ever synced annotations to Hardcover.** `init_db → migrate_Database → migrate_annotation_decouple_source_target` failed at `_migrate_step4_sanity_check` and re-raised each boot (unrecoverable; rolling back tags didn't help unless the tag predated the migration). Root cause: step 4 asserted `COUNT(kobo_annotation_sync WHERE synced_to_hardcover=1) == COUNT(annotation_sync_target WHERE target='hardcover')`, wrongly assuming the target table holds only migration-backfilled rows — but the live annotation-sync pipeline (`services/annotation_sync/hardcover.py`, `user_book_data.py`) writes `'hardcover'` rows there in normal use, so the total legitimately exceeds the legacy synced count and the check raised. **Fix**: replace the total-equality assertion with a backfill-*completeness* check (no `synced_to_hardcover=1` row left without a matching `annotation_sync_target 'hardcover'` row — the inverse of step 2's `NOT EXISTS` backfill); anchored on the legacy synced rows so organic pipeline rows can't trip it, while a genuinely incomplete backfill still raises. Idempotent migration recovers stranded servers on next boot. **3 RED→GREEN tests** (`test_annotation_decouple_migration.py`: organic-target-rows at step + full-migration level, plus incomplete-backfill still-raises); file 20 pass, broader annotation-migration suite 43 pass. **Container-runtime verification** (cwn-local, SQLite 3.53.1): real migration on a #684-mirror temp DB completes clean, annotations + all 3 target rows preserved; real restart boots healthy, `/login` 200, idempotent skip no traceback. Fork-original (startup DB migration). | `8e19d54` | v4.1.6 |
| [#675](https://github.com/new-usemame/Calibre-Web-NextGen/pull/675) | (operator-reported, new-UI) | **New-UI sidebar didn't list a user's shelves under the SHELVES heading** — the heading showed with Tasks/About directly beneath it while the user's actual shelves rendered as the very last block of the nav, off the bottom of the drawer and disconnected from their header. Root cause: the shelf list started directly under the SHELVES header (`914b1975f`) but drifted to the end as later feature commits (About/Tasks, Table view, Smart shelves, Duplicates) were inserted between the header and the list. **Fix**: reorder the sidebar JSX so shelves render directly under the SHELVES header, smart-shelf/power tools next, and the low-frequency info pages (Tasks/About) last — a pure DOM reorder (changed-region code multiset verified identical), no visibility/role logic touched, `shelves.length > 0` guard preserved. **RED→GREEN e2e regression guard** (`frontend/e2e/sidebar.spec.ts`) pins a shelf link below the SHELVES header and above Tasks on desktop and inside the opened mobile drawer; full e2e suite green; sub-path prefix (`/cwa/app/shelf/…`) + a11y verified. Fork-original (SPA surface). | `0b72553e5` | v4.1.6 |
| [#649](https://github.com/new-usemame/Calibre-Web-NextGen/pull/649) | (fork issue [#634](https://github.com/new-usemame/Calibre-Web-NextGen/issues/634) @iroQuai, follow-up to #637) | **The "Currently reading" badge never rendered on the new-UI (SPA) book page.** The classic detail page shows the sync-driven marker off `read_status_raw == STATUS_IN_PROGRESS` (fork #509), but `GET /api/v1/books/<id>` returned only a flattened `read` boolean, so the SPA book page couldn't render the badge even with KOReader progress synced. **Fix**: new `helper.book_is_in_progress` (single source of truth mirroring `web.show_book`, incl. the custom-read-column overlay — the tri-state lives only in `ub.ReadBook`), surfaced as `in_progress` on the detail payload and rendered as a passive accent-pill marker in `BookDetail.tsx` (with the synced percentage when known). **11 tests** (`tests/unit/test_634_spa_currently_reading_badge.py`, incl. custom-column overlay against a real in-memory `ub` session + endpoint-wiring AST pin); live cwn-local e2e: real API returns `in_progress:true`/`42%` for the in-progress book and `false` for finished/unread, SPA renders "Currently reading · 42%" only on the in-progress book (no #637 regression). Fork-original. | `2c336c071` | v4.1.6 |
| [#642](https://github.com/new-usemame/Calibre-Web-NextGen/pull/642) | (fork issue [#638](https://github.com/new-usemame/Calibre-Web-NextGen/issues/638) @boegill) | **Fetch-metadata results showed the same cover for every volume of a series, and applying metadata stored that wrong cover.** Root cause: the cover booster's iTunes title-search fallback matched on the first 6 title tokens with tokens ≤2 chars dropped, so "Vol.1"/"Vol.2"/"Vol.3" of a long-titled series were indistinguishable — Apple returns the same first hit for each volume's query and every volume collapsed onto one cover; contributing, the Kobo provider parsed an ISBN from `__NEXT_DATA__`/LD-JSON then discarded it, forcing every Kobo record onto the fuzzy no-ISBN path. **Fix**: `_itunes_result_matches` rejects hits whose volume designator (Vol./Volume/Tome/Book/Part/No./#N/trailing integer) disagrees with the query's (a missed boost keeps the original, correct cover); the Kobo provider retains its validated ISBN into `identifiers["isbn"]` like the other six providers. **8 RED→GREEN tests** (`tests/unit/test_cover_booster.py` volume classes, `tests/unit/test_638_kobo_isbn_retention.py`); live cwn-local pre/post on session-cookied `POST /metadata/search` with the reporter's exact query: pre-fix all three Kobo volumes shared one mzstatic URL, post-fix Vol.1/Vol.2 keep distinct per-volume Kobo covers. Fork-original. | `0a7279c5c` | v4.1.6 |
| [#637](https://github.com/new-usemame/Calibre-Web-NextGen/pull/637) | (fork issue [#634](https://github.com/new-usemame/Calibre-Web-NextGen/issues/634) @alva-seal + @iroQuai) | **With a Calibre custom column configured as the read column, the "Currently Reading" smart shelf listed every column-marked-read book and never the in-progress ones, and the detail-page "reading now" badge could never render.** Root cause: the custom-column branch of `build_filter_from_rule` coerced the rule value with `bool()`, collapsing STATUS_IN_PROGRESS (2) into `True` — the identical filter to "Read"; the in-progress tri-state only exists in `ub.ReadBook` (KOReader/Kobo sync writes it there regardless of the configured column). **Fix**: value 2 queries ReadBook IN_PROGRESS for the user AND excludes column-marked-read books (marking read via the column never clears the ReadBook row); value 0 matches absence of a truthy row (the old `value == False` shape hid every never-touched book from "Yet to Read"); `show_book` overlays IN_PROGRESS so `read_status_raw` can reach 2 and the badge (fork #509) renders. **9 tests, 7 RED→GREEN** (`tests/unit/test_634_custom_read_column_currently_reading.py`, behavioral against real in-memory SQLite with a real bool cc class); live cwn-local pre/post: `/magicshelf/preview` value=2 returned the READ book pre-fix and only the in-progress book post-fix, value=0 went 0 → 16 books, badge renders only on the in-progress book. Fork-original. | `95bfcbdd2` | v4.1.5 |
| [#636](https://github.com/new-usemame/Calibre-Web-NextGen/pull/636) | (fork issues [#525](https://github.com/new-usemame/Calibre-Web-NextGen/issues/525) @natabat + @Metamatam, [#627](https://github.com/new-usemame/Calibre-Web-NextGen/issues/627) @uschi1, [#633](https://github.com/new-usemame/Calibre-Web-NextGen/issues/633)-class) | **KOReader/Crossink clients in 'filename' document-matching mode never resolved books** (`No book found for checksum` on every sync), and devices holding files whose bytes no longer match any stored binary checksum (pre-checksum-era downloads, metadata re-embeds, side-loads) had no path back into sync short of re-downloading. **Fix**: new `koreader_filename` checksum channel — md5 of the served basename, oracle-verified against @natabat's real Crossink hash (`md5("More Everything Forever - Adam Becker.epub")`, UTF-8, lowercase extension, no case folding, unique hit over 7 candidate normalizations) — registered at the download chokepoint and by a new boot-backfill filename pass covering every (book, format) pair with no file I/O. Lookup already version-agnostic. **Greptile finding fixed in-flight**: the binary backfill pass's any-version LEFT JOIN would have permanently skipped books whose files were unreadable when the filename pass first ran — join now filters `version='koreader'`; `get_latest_checksum` scoped to a version channel (default binary) so filename rows can't shadow the partial-MD5. **14 RED→GREEN tests** (`tests/unit/test_627_525_filename_checksum.py`); live cwn-local e2e: boot backfill committed 20 digests, filename-mode `PUT /kosync/syncs/progress` → 200 matched via `koreader_filename` with `ReadBook` updated (the exact #627 no-op chain), cross-device `GET` 200, chokepoint re-creates deleted rows without duping. Fork-original. | `80e7987de` | v4.1.5 |
| [#632](https://github.com/new-usemame/Calibre-Web-NextGen/pull/632) | (main-branch CI failure, runs 28635515788 / 28590300379 / 28422116634) | **The Update Translations workflow failed non-fast-forward whenever anything landed on main between its checkout and its push** (3 of the last 30 runs; typical loser: racing the autopilot's post-merge docs backfill). **Fix**: top-level `concurrency` group (`cancel-in-progress: false`) serializes runs of the workflow so two quick merges can't race each other's translation commit; the push now runs in a 3-attempt loop behind `git pull --rebase origin main` (commit holds only generated files, so rebase is safe); a genuine rebase conflict aborts cleanly and fails loudly; exhausted retries exit 1 so the job can't go green without the commit landing. **3 RED→GREEN tests** (`tests/unit/test_update_translations_push_race.py`) pin the concurrency block, the rebase-retry loop, and the fail-loud property. CI-infra only, not user-facing. Fork-original. | `64ff52287` | n/a (workflow, not shipped in image) |
| [#631](https://github.com/new-usemame/Calibre-Web-NextGen/pull/631) | (fork issue [#561](https://github.com/new-usemame/Calibre-Web-NextGen/issues/561), @darkmatterpelican) | **A single problem PDF could hang the whole server on download.** Embed-on-download (`config_embed_metadata`, default ON) ran `calibredb export` synchronously in-request with no timeout and no process group; a PDF tripping calibre's podofo hang pinned a core, ate GBs of RAM, and 504'd the instance (RPi 4 report). **Fix**: `process_open` starts children in their own session (POSIX) so the `calibredb → calibre-parallel` tree is group-killable; `do_calibre_export` bounds `communicate()` (90s default, `CWA_EMBED_TIMEOUT` override), kills the group on expiry, returns `(None, None)`; the 3 callers that crashed with TypeError on `(None, None)` (download serve incl. GDrive, mail GDrive branch, kepubify convert) now degrade to the original un-embedded file, mirroring the existing non-GDrive mail fallback; checksum block skipped on fallback so KOReader never checksums an un-embedded file. **14 RED→GREEN tests** (`tests/unit/test_561_embed_timeout_fallback.py`); live cwn-local e2e with a `sleep 600` calibredb shim: `GET /download/18/pdf` → 200 in 90.4s with the byte-identical original (was: indefinite hang), timeout ERROR logged, no orphan process; happy path 200 in 1.5s unchanged. Fork-original. | `3a1c01b4f` | v4.1.5 |
| [#626](https://github.com/new-usemame/Calibre-Web-NextGen/pull/626) | (verification program, `notes/verify/` Layer 2) | **SPA e2e matrix harness + real CI e2e job.** The SPA's only automated signal was that TypeScript compiles — every Class-1 UI bug that shipped (v4.1.1 subpath/stale-card/mobile-scroll, #288, #576, #1411) was invisible to CI. New `frontend/e2e/` Playwright harness drives the real SPA in a running container across the matrix axes those bugs live in: desktop 1280 + mobile 375 full flow (login → grid → detail → reader), clean-console assertion, default-state no-modal check, drawer reachability + scroll-lock, no-horizontal-overflow, axe a11y gate (fails on NEW criticals; known backlog named in `KNOWN_CRITICAL`), opt-in reverse-proxy subpath project. CI: the placeholder `e2e-tests` job replaced with a real one — pulls the shipped GHCR image (tag's image on tag runs, `:dev` otherwise), seeds a book through real ingest, runs the harness. **Wiring fixed in self-review**: `on.push` filtered out tag events (release-gate condition was dead code) → `on.push.tags: ['v*']`; tag runs race the GHCR release build (image publishes 15–35 min post-tag, v4.0.169 lesson) → 40-min pull poll. Advisory until several green tag runs, then promote to a hard gate. Proven green in CI runs 28612679943 + 28618574277. Run locally: `cd frontend && npm run test:e2e`. Fork-original (test infra; dev-only deps `@playwright/test` + `@axe-core/playwright`). | `b9a35ad5f` | v4.1.5 |
| [#625](https://github.com/new-usemame/Calibre-Web-NextGen/pull/625) | (fork issue [#572](https://github.com/new-usemame/Calibre-Web-NextGen/issues/572), @magdalar) | **New UI lost quick-edit conveniences the classic UI had** — no shortcut to drop into edit from library/search results, and the book page showed tags read-only. **Fix**: opt-in `quickEdit` pencil on `BookCard` (hover/focus-revealed, edit-role-gated, suppressed in multi-select) wired into Catalog + AdvancedSearch, plus an inline `TagEditor` on BookDetail (chip × remove + "Add tag", case-insensitive dedupe) POSTing via the existing partial-update `/api/v1/books/:id/metadata`. **5 source pins RED→GREEN** (`tests/unit/test_572_quick_edit.py`); live cwn-local e2e: card pencil → `/book/:id/edit`, inline tag add persisted (confirmed via API). Fork-original (SPA surface). | `72da0b6d8` | v4.1.4 |
| [#624](https://github.com/new-usemame/Calibre-Web-NextGen/pull/624) | (fork issue [#585](https://github.com/new-usemame/Calibre-Web-NextGen/issues/585), @Glennza1962) | **New UI sidebar ignored the classic sidebar-visibility config** — sections an admin/user had hidden (Hot, Top Rated, Discover, …) still showed. **Fix**: expose a `{key: bool}` `sidebar` map on `/api/v1/auth/me` computed from the same `check_visibility(SIDEBAR_* bit)` the classic UI + OPDS use; `Sidebar.tsx` filters nav by it (missing keys stay visible ⇒ backwards-compatible). Reorder deferred with design (`notes/585-sidebar-reorder-design.md`, no schema change). Tests RED→GREEN (`tests/unit/test_585_sidebar_visibility.py`), 30 passed; live cwn-local: hiding Hot in config drops it from the SPA nav. Fork-original (SPA surface). | `acda6ba16` | v4.1.4 |
| [#629](https://github.com/new-usemame/Calibre-Web-NextGen/pull/629) | (fork issue [#628](https://github.com/new-usemame/Calibre-Web-NextGen/issues/628), @iroQuai) | **New UI: on phones, the library's view-settings (gear) menu could open offscreen.** The toolbar wraps on narrow screens; when the gear landed on the LEFT edge of a wrapped row, the right-aligned 220px menu anchored to the 38px button extended past the left viewport edge (measured `left:-158` at 360px; hidden at 375px where the gear happens to wrap right — why the e2e mobile project missed it). **Fix**: at ≤520px anchor the menu to the toolbar (`.settingsWrap` static + `.toolbar` relative) so it drops below the toolbar aligned to its right edge — onscreen at any wrap position; desktop untouched (media-scoped, verified pixel-equivalent at 1280). **Red/green e2e spec at 360×740** (`frontend/e2e/mobile.spec.ts`; red = menu `x=-155.8` on the pre-fix bundle), full 15/15 SPA e2e matrix green. Evidence `notes/evidence-628/`. Fork-original (SPA surface). | `077519e66` | v4.1.5 |
| [#623](https://github.com/new-usemame/Calibre-Web-NextGen/pull/623) | (fork issue [#573](https://github.com/new-usemame/Calibre-Web-NextGen/issues/573), @magdalar) | **New UI series view couldn't sort by series number, and the position wasn't shown.** The API already returns `series_index`; two SPA gaps. **Fix**: add `seriesasc`/`seriesdesc` to the read-only API `SORT_MAP` (mirroring `web.py`), expose "Series order"/"Series order (reverse)" options gated to series views (ascending default), and render the position as a cover badge. **7 source pins RED→GREEN** (`tests/unit/test_573_series_number_sort.py`); live cwn-local e2e: a 3-book series renders #1/#3.5/#10 ascending, options absent on the main library. Fork-original (SPA + API sort). | `85d6c117a` | v4.1.4 |
| [#622](https://github.com/new-usemame/Calibre-Web-NextGen/pull/622) | (fork issue [#584](https://github.com/new-usemame/Calibre-Web-NextGen/issues/584), @Glennza1962) | **New UI: clicking an Admin "More server configuration" card replaced the whole SPA with the classic page** (felt like a full revert to the old UI). Those deep config screens are intentionally still server-rendered during the hybrid cutover. **Fix**: open the cards with `target="_blank" rel="noopener noreferrer"` so the legacy page loads in a new tab and the new UI stays put; href keeps `resourceUrl()` (#603 prefix preserved). Native config-page rebuild deferred (roadmap). **3 source pins RED→GREEN** (`tests/unit/test_584_admin_legacy_link_new_tab.py`); live cwn-local: all 6 cards carry `target=_blank`. Fork-original (SPA surface). | `99a5bf0bc` | v4.1.4 |
| [#611](https://github.com/new-usemame/Calibre-Web-NextGen/pull/611) | (fork issue [#586](https://github.com/new-usemame/Calibre-Web-NextGen/issues/586), @alva-seal) | **App passwords didn't work with the KOReader plugin** — OAuth/LDAP-only users (no local password) got 401. kosync `authenticate_user()` only tried LDAP + the local hash, never the app-password table the OPDS/web path already uses. **Fix**: add a `usermanagement._verify_app_password()` fallback (purely additive, after LDAP/local), covering KOReader progress AND annotation sync. **6 RED→GREEN tests** (`tests/unit/test_586_kosync_app_password_auth.py`, behavioural + source pin); live cwn-local: `GET /kosync/users/auth` with an app password → 200 (was 401), wrong password → 401. `/security-review` clean. Fork-original. | `dea267ca8` | v4.1.4 |
| [#610](https://github.com/new-usemame/Calibre-Web-NextGen/pull/610) | (fork issue [#578](https://github.com/new-usemame/Calibre-Web-NextGen/issues/578), @KucharczykL) | **New UI didn't keep the first-page scroll position on Back.** The #593 scroll-restore read `window.scrollY` in the Catalog unmount cleanup — which runs after the DOM swapped to the shorter book page, so the browser had already clamped scrollY to ~0 and Back landed at the top. **Fix**: track the live scroll offset in a ref via a passive scroll listener and save that on unmount (the navigating click flushes the cleanup before the clamp's async scroll event). **Red/green pin** (`tests/unit/test_578_scroll_restore.py`); live e2e (teenyverse 72-book lib: depart 1349 → Back 1349, was 37). Fork-original (SPA surface). | `eceb8d7ca` | v4.1.4 |
| [#621](https://github.com/new-usemame/Calibre-Web-NextGen/pull/621) | (fork issue [#603](https://github.com/new-usemame/Calibre-Web-NextGen/issues/603), @chloeroform) | **New UI: behind a reverse proxy on a sub-path, the Admin "More server configuration" cards 404'd** — they rendered raw root-absolute `<a href="/admin/config">` … so on a `https://host/cwa/` mount they pointed at the domain root, outside the app. Tail of #571 (which prefixed the API client + wouter router but not a handful of native `<a href>` links out to legacy routes). **Fix**: route the nine `SERVER_SETTINGS` hrefs through `resourceUrl()` — the single-source-of-truth prefix helper in `api.ts` (idempotent, leaves external/`data:` URLs alone, no-op at empty prefix ⇒ root-mount unaffected). **Audit-around-the-ask**: the other native legacy links are already prefix-safe — Login's OAuth buttons use Flask `url_for` (`cps/api/auth.py:_oauth_providers`), NotFound builds `BASE_PREFIX + path`, TopBar's are external — pinned so the class can't reopen. **5 source pins RED→GREEN** (`tests/unit/test_603_admin_legacy_link_prefix.py`; red = the raw `href={href}`), 44 passed across the SPA/proxy suites. **Live cwn-local (admin), sub-path emulated with an in-browser path-stripping proxy** (`X-Script-Name: /cwa` + strip): root mount `__CWNG_PREFIX__=""` cards render `/admin/config`, click → 200; sub-path `__CWNG_PREFIX__="/cwa"` all nine cards render `/cwa/admin/config` …, full page load logged **zero** 4xx/5xx. Evidence `notes/evidence-603/`. Fork-original (SPA surface). | `4304f6475` | v4.1.4 |
| [#620](https://github.com/new-usemame/Calibre-Web-NextGen/pull/620) | (fork issue [#609](https://github.com/new-usemame/Calibre-Web-NextGen/issues/609), @Glennza1962) | **New UI ignored the admin-configured site title — the top bar, login card, and browser tab always said "Calibre-Web NextGen".** Root cause: the SPA hardcoded the brand in TopBar/Login/MagicLink/index.html, and no `/api/v1` endpoint exposed `config_calibre_web_title` outside the admin-gated settings API. **Fix**: `instance_name` rides `/api/v1/auth/config` (public — the login screen renders pre-auth; same disclosure as the classic login page, which already shows the title unauthenticated), `/auth/me`, and the login payload, blank falling back to the stock name; a shared `BrandName` component renders a custom title as plain text and keeps the two-tone treatment for the stock name; `document.title` follows the configured title on login + app shell. **4 RED→GREEN endpoint tests + 3 frontend source pins** (`tests/unit/test_609_instance_title_api.py`; red = `KeyError: 'instance_name'` pre-fix). **Live cwn-local (bind-mounted branch code)**: default title pixel-identical two-tone; with `config_calibre_web_title = "Glenn's Book Emporium"` (the reporter's example) the login card, authenticated top bar, and tab title all follow it — login POST verified on the wire, Playwright at desktop 1280 + mobile 375 (clean two-line wrap, no overflow). `/security-review` clean (disclosure parity; React text-node + `document.title` sinks). Evidence `notes/evidence-609/`. Fork-original (SPA surface). | `5c575ea79` | v4.1.4 |
| [#619](https://github.com/new-usemame/Calibre-Web-NextGen/pull/619) | (fork issue [#615](https://github.com/new-usemame/Calibre-Web-NextGen/issues/615), @hayvan96) | **"Mark as read" carried the *unread* sense in 17 locales — the new UI's read toggle on an unread book said fr "Marquer comme non lu"; the classic detail read CTA said "Lu" (past participle) where it means "open the reader".** Root cause 1: the lowercase msgid pair `Mark as read`/`Mark as unread` (book_table, _book_organizer, SPA BookDetail) had byte-identical msgstrs in ar, cs, el, es, fi, fr, gl, id, pt, sk, sl, sv, tr, uk, vi, zh_Hans_CN, zh_Hant_TW; the title-case pair (classic detail) was correct everywhere — hence classic-right/new-UI-wrong. Fix: each locale repaired from its own human-translated title-case `Mark As Read` donor (assert-guarded single replacement per file; msgfmt green on all 28 locales). Root cause 2: verb/status msgid collision (same class as #577) — detail.html CTA switched `_('Read')` → `_('Read now')`, aligning classic with the SPA button; status contexts (cover badge, listenmp3 label) keep `Read`. fr `Read now` de-fuzzed to "Lire" — **fuzzy entries are dropped by msgfmt from the compiled .mo**, so the classic UI would have silently fallen back to English (caught in live verify, now pinned). **6 RED→GREEN pins** (`tests/unit/test_615_mark_as_read_inversion.py`): cross-locale identical-pair scan, fr reporter expectation, SPA catalog parity, CTA verb msgid, status contexts, fr non-fuzzy. **Live cwn-local, French session**: classic `/book/7` renders `<span>Lire</span>` + correct toggle pair; SPA `/app/book/7` (unread) shows "Marquer comme lu", `/app/book/4` (read) shows "Lu ✓"; `/api/v1/i18n/fr.json` serves the corrected pair. Evidence `notes/evidence-615/`. Fork-original (17-locale sweep is fork surface; reporter notes CWA fixed fr only). | `b0622ae57` | v4.1.4 |
| [#618](https://github.com/new-usemame/Calibre-Web-NextGen/pull/618) | (fork issue [#602](https://github.com/new-usemame/Calibre-Web-NextGen/issues/602), @iroQuai) | **Classic UI (caliBlur) on mobile: tapping the username in the drawer never opened the account submenu — logout was unreachable.** Root cause: caliBlur's `max-width: 767px` block positions the drawer **by slot** — `#main-nav > li:nth-child(1)` reserves the 130px full-width profile band, `a.profileDrop` is forced to 120px with the username at `top: 70px`. The fork's "Switch to New UI" pill renders as the first `#main-nav` item, so it stole the slot: the profile li collapsed to 60px, its 120px anchor overflowed into the Upload row, and the invisible `opacity: 0` `#btn-upload` file input there swallowed every username tap (`elementFromPoint` returned the file input; Playwright click timed out on pointer-interception). Controlled experiment (pill removed live) restored stock geometry — trigger isolated. **Fix (additive CSS, same media block)**: pill gets `height: auto` in slot 1; the slot props (`height: 130px; z-index: 99; width: 100%`) assigned to `#main-nav > li.dropdown` by class — identical when the pill is absent. **2 RED→GREEN pins + 1 ordering-assumption pin** (`tests/unit/test_602_caliblur_mobile_profile_slot.py`). **Live Playwright on cwn-local (deployed file), 360×740**: username hit-test returns `.profileDrop`, dropdown opens, Logout lands on `/login`; desktop caliBlur 1280px unchanged; default theme untouched (caliBlur.css loads only for theme 1). Evidence in `notes/evidence-602/`. Fork-original (pill is fork surface). | `583d1a9a9` | v4.1.4 |
| [#616](https://github.com/new-usemame/Calibre-Web-NextGen/pull/616) | (fork issue [#612](https://github.com/new-usemame/Calibre-Web-NextGen/issues/612), @mstewart14) | **New UI: navigating shelf A → shelf B kept A's books on screen and appended B's after them; removing a leftover book removed it from the shelf now being viewed.** Root cause: `/shelf/:id` route reuse + `useShelf`'s unscoped `placeholderData: (prev) => prev` briefly served shelf A's rows under shelf B's query key, and the accumulator keyed by the route id at effect-run time — A's rows got stamped as "B, seen" and B's real rows landed on the append path behind them. Identical latent bug in `MagicShelfView` (`/magic/:id`). Catalog/Table/AdvancedSearch already carried the `isPlaceholderData` guard — these were the two stragglers. **Fix (three layers)**: `placeholderData` in `useShelf`/`useMagicShelfBooks` scoped to the same shelf id; both accumulators skip placeholder data (Catalog parity); `page` + rename/reorder UI modes reset on id change (also fixes arriving on shelf B paged to A's page N skipping B's first pages, and rename mode leaking across shelves). **6 RED→GREEN pins** (`tests/unit/test_612_shelf_nav_accumulation.py`) across all three layers. **Live Playwright on cwn-local (SPA rebuild, bind-mounted)**: pre-fix A→B rendered 6 cards under a "3 books" header (reporter's symptom verbatim); post-fix A→B→A each shows only its own 3, 120 ms rapid-switch race settles clean, rename opened on A doesn't survive navigation to B. Fork-original (SPA-only surface). | `6085f3850` | v4.1.4 |
| [#614](https://github.com/new-usemame/Calibre-Web-NextGen/pull/614) | (fork issue [#608](https://github.com/new-usemame/Calibre-Web-NextGen/issues/608), @blahblah57) | **New UI Table view cover thumbnails rendered as 32×46 slivers — real covers (2:3 aspect) were side-cropped by `object-fit: cover` and looked "very squished" despite ample horizontal space.** Fix (CSS-module only, `frontend/src/pages/Table.module.css`): thumbnail + placeholder bumped to 48×72 (true 2:3, in-family with sibling thumbs: Duplicates 36×52, EditBook 40×60, CoverPicker 48×64), `.coverCol` 40→56px. Adjacent fix found during live verify: a title made of one long unbreakable token (filename-style auto-ingest titles) forced ~114px horizontal scroll on desktop — `overflow-wrap: anywhere` on `.table td`, gated behind `@media (min-width: 768px)` because ungated it collapses min-content width on phones and shreds every cell to ~1ch columns (caught live at 375px). **6 RED→GREEN pins** (`tests/unit/test_608_table_cover_size.py`): width ≥44px, ~2:3 aspect, placeholder parity, column ≥ thumb, `object-fit: cover` retained, media-gated `overflow-wrap` (base mobile rule must NOT have it). **Live Playwright on cwn-local (SPA rebuild + docker cp)**: desktop 1280 measured 48×72 with 0px table overflow (was 32×46 + 114px); mobile 375 measured 48×72, no cell shredding, page never overflows viewport, in-wrapper scroll +16px vs pre-fix baseline 799px (torture-title artifact, by-design scrollable). Fork-original (no upstream issue — Table view is SPA-only). | `2ac58fa2d` | v4.1.4 |
| [#605](https://github.com/new-usemame/Calibre-Web-NextGen/pull/605) | (fork issue [#576](https://github.com/new-usemame/Calibre-Web-NextGen/issues/576) follow-up, @iroQuai) | **The #60 "Back to the classic view" feedback popup was undismissable and rendered on every classic page in v4.1.1/v4.1.2.** The overlay is hidden via the HTML `hidden` attribute, but the partial's own `.cwng-fb-overlay { display:flex }` outranks the UA/normalize `[hidden] { display:none }` rule, so `hidden` was visually ignored. The partial ships on every classic page (layout.html, logged-in gate) with JS deciding whether to activate: without the `?cwng_feedback` marker the script exits before wiring any dismiss listener (dead buttons), and with the marker `close()` only toggles the impotent attribute — either way the modal (z-index 100000, full-viewport) could not be closed. The card also had no viewport-bounded height (707px on a 720px viewport), overflowing phones with no way to scroll. **Fix (CSS-only, in the partial):** `.cwng-fb-overlay[hidden] { display:none !important }` makes `hidden` authoritative; `.cwng-fb-card` gets `max-height:calc(100vh - 40px)` (+`100dvh` upgrade), `overflow-y:auto`, `-webkit-overflow-scrolling:touch`. **3 RED→GREEN pins** (`tests/unit/test_60_feedback_popup_dismiss.py`). **Live Playwright matrix on cwn-local**: no-marker load hidden, marker opens + strips URL + locks body scroll, "No thanks"/Escape close + per-version suppression, mobile 375×667 card fits (627px) and scrolls its own overflow. Theme-independent (no `cwng-fb`/`[hidden]` rules in caliBlur/style.css). Fork-original (no upstream issue). | `bc342c565` | v4.1.3 |
| [#532](https://github.com/new-usemame/Calibre-Web-NextGen/pull/532) | (fork issue [#522](https://github.com/new-usemame/Calibre-Web-NextGen/issues/522), @chloeroform) | **Changing the "Regular Expression for Title Sorting" in /admin/viewconfig didn't reorder the existing collection — the new rule only applied to books edited afterwards.** Calibre's `metadata.db` stores a per-book `sort` column and recomputes it only when a title changes (`books_update_trg`, `WHERE OLD.title <> NEW.title`); UI/OPDS listings `ORDER BY books.sort`. The `title_sort` UDF reads `config_title_regex` at call time, but nothing re-ran it over existing rows on a regex change, so `sort` stayed stale until each book was individually edited. **Fix:** `CalibreDB.reapply_title_sort()` runs `UPDATE books SET sort = title_sort(title)` through the registered UDF; `update_view_configuration` calls it inside the existing `config_title_regex` change branch (`set_from_dictionary` returns True only on an actual change, so it fires only when the regex really changed). `title` left untouched so the trigger doesn't re-fire (`recursive_triggers` off regardless). Mirrors Calibre desktop's regex-change recalc. **9 RED→GREEN tests** (`tests/unit/test_title_sort_regex_recompute_522.py`, real in-memory sqlite with the trigger + UDF): regex-only change leaves sort stale (the bug); the recompute reapplies the new regex to all rows and never mutates titles; the recompute re-raises (rolled back) instead of silently reporting success on a failed UPDATE, and the viewconfig handler flashes an admin error when it fails; source-pins on the method, the UDF UPDATE, and the viewconfig wiring (fail without the fix). **Live e2e through the real `/admin/viewconfig` route** on cwn-local: regex change recomputed 19 books immediately (log `recomputed sort for 19 books`), restore recomputed back (idempotent). Fork-original (no upstream issue). | `9907c7b5` | v4.1.0 |
| [#531](https://github.com/new-usemame/Calibre-Web-NextGen/pull/531) | (fork issue [#523](https://github.com/new-usemame/Calibre-Web-NextGen/issues/523), @chloeroform) | **The "Discover (Random Books)" row never appeared: enabling "Show Random Books in Detail View" (with "No. of Random Books to Display" set) had no visible effect for any user.** The strip is rendered + populated server-side (`index.html`: `<div class="discover random-books">`, gated on `current_user.show_detail_random()`; `random` filled in `web.py` to `config_random_books`), but caliBlur hid it via a leftover `.pace-progress, .random-books { display: none }` rule. caliBlur is the only shipped theme (`admin.py` forces `g.current_theme = 1`), so there was no theme to fall back to — the feature was dead for everyone. **Fix (theme CSS only):** (1) `caliBlur.css` splits the hide rule so `.pace-progress` (loading bar) stays hidden but `.random-books` is released; (2) un-hiding exposed a second issue — caliBlur's `.discover > h2` rule pins the heading to the fixed header bar (`position:fixed; top:60; left:240`), where it collided with the page title and rendered as garbled overlapping text, so `caliBlur_override.css` re-renders the random heading (matching caliBlur's selector depth) as a static section heading above the cover row. The strip's cards reuse the `#books_rand`/`.book` styling caliBlur already defines. **4 RED→GREEN tests** (`tests/unit/test_random_books_caliblur_523.py`): caliBlur no longer hides `.random-books`; `.pace-progress` stays hidden; the override unpins the heading (`position:static`); strip markup stays gated on `show_detail_random()`. 29 caliBlur CSS source-pin tests still green. **Live Playwright on caliBlur (the only theme), desktop 1280 + mobile 390**: the strip shows the configured count of covers with a clean "Discover (Random Books)" section heading above them and the page title uncollided in the header bar. Fork-original (no upstream issue). | `23ddd66b1` | v4.1.0 |
| [#537](https://github.com/new-usemame/Calibre-Web-NextGen/pull/537) | (fork issue [#536](https://github.com/new-usemame/Calibre-Web-NextGen/issues/536), @chloeroform) | **Bulk actions and drag-to-merge 404'd behind a reverse proxy mounted on a sub-path.** `book_organizer.js`'s `postJson()` helper — the single chokepoint for `/ajax/view` (cover badge toggle), `/shelf/add_selected_to_shelf` (bulk add-to-shelf), `/ajax/readselectedbooks` (bulk mark read/unread) and `/ajax/deleteselectedbooks` (bulk delete) — called `fetch(url, …)` on a bare root-absolute path, so the requests bypassed the app sub-path and hit the proxy root (404). Auditing the same pattern (the reporter's "grepping reveals other problematic calls") surfaced one sibling: `drag-drop-merge.js` POSTed to a bare `/ajax/mergebooks`. **Fix:** route both through the existing `getPath()` helper (the same one `filter_grid.js`/`get_meta.js`/`edit_books.js` already use) — `fetch(getPath() + url, …)` at the `postJson` chokepoint and `fetch(getPath() + "/ajax/mergebooks", …)` in the merge POST. `getPath()` returns `""` at root, so it is a no-op for non-proxy deployments (zero behaviour change for the common case). Same bug class and fix as fork #514 (`duplicates.js`) and PR #14. **5 RED→GREEN source-pin tests** (`tests/unit/test_536_getpath_bulk_ajax.py`): the bulk-chokepoint `fetch` and the merge `fetch` must route through `getPath()`, and no bare absolute `fetch('/…')` may remain (fail without the fix). `node --check` clean on both files; 17 adjacent JS-pin tests (#514 proxy-path, #352 drag-merge, cover-settings, mobile-organizer) still green. Live reverse-proxy E2E deliberately not run — the fix is a provable no-op at root, so a root-deployment test exercises the unchanged path, and the identical #514 pattern is already E2E-verified in production. Fork-original (no upstream issue). | `15a7b9599` | v4.1.0 |
| [#539](https://github.com/new-usemame/Calibre-Web-NextGen/pull/539) | (fork issue [#533](https://github.com/new-usemame/Calibre-Web-NextGen/issues/533), @chloeroform) | **The "Calibre-Web NextGen" version in the admin Version Information table was plain text — no path from "what version am I running" to "what changed in it".** The version is read verbatim from `/app/CWA_RELEASE` (`cwa_get_package_versions`, no `strip()`), so the string can be whitespace-padded, `"Unknown"` when the probe failed, or a `DEV_BUILD-…` marker on a dev/canary image — a naive `/releases/tag/<version>` link would 404 on dev builds and dangle on `Unknown`. **Fix:** new `cps.updater.release_url_for_version()` resolves the string to a GitHub URL — the exact `…/releases/tag/<tag>` page when it parses as a release tag (`vX.Y.Z`/`X.Y.Z`, validated via the existing `_normalize_tag`; release images write `VERSION=<tag>` verbatim so the tag page resolves), the always-resolving `…/releases` listing for any non-tag marker (`DEV_BUILD-…`, `nightly`; no 404), and `None` for empty/`"Unknown"` so the template renders plain text. The slug is shared with `_REPOSITORY_API_URL`, so a `CWA_RELEASE_REPO` override retargets the link and the updater together. `admin.html` wraps the version in `<a … target="_blank" rel="noopener noreferrer">` only when the URL is present. No new dependency/env var/external service (the updater already hits `api.github.com/repos/<slug>`); no new translatable string (link text is the version itself). **10 unit tests** (`tests/unit/test_533_version_release_link.py`): the URL builder across release/dev/non-semver/unknown/empty/None inputs + trailing-newline strip + `CWA_RELEASE_REPO`-override slug, plus source-pins on the template anchor and the view kwarg; the loader is hermetic (snapshots/restores `sys.modules`). RED on `main` (function absent → collection error), GREEN with the fix; selected under `-m "smoke or unit"` and green under CI's `-n auto --dist=loadfile`. Real-cell Jinja render verified both branches. Full `/admin/view` click-through deferred as disproportionate for a template-conditional + pure-helper change (cwn-local down; `:dev` canary exercises it). Fork-original (no upstream issue). | `ee4a19c84` | v4.1.0 |
| #526 | [CWA #1411](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1411) (@Mr-Me-torn) | **Sorting the "Books List" table view by Title, Title Sort, or Series ID returned an empty table and flooded the log with `sqlite3.OperationalError: no such column: title`; only Author sorting worked.** `list_books()` (cps/web.py) built the ORDER BY for the `sort`/`title`/`series_index` columns as a raw `text(sort_param + " " + order)` fragment. The default render path eager-loads the one-to-many `Books.data` relationship under a LIMIT, so SQLAlchemy wraps the book query in a subquery alias (`anon_1`) where the column is exposed as `books_<name>`; the bare outer `ORDER BY title` cannot resolve against that alias. Author branches never broke because they use ORM column objects, which SQLAlchemy rewrites to `anon_1.books_<name>`. **Fix:** map the branch to ORM column objects via `getattr(db.Books, col_name)`, mirroring the working author_sort branch; `authors_sort` (stale alias) maps to the real `author_sort` column. Blast radius checked: `admin.py:711` uses the same raw-text pattern but on a flat (non-subquery-wrapped) query with `sort` validated against `User.__table__.columns` — not affected. **8 RED→GREEN tests** (`tests/unit/test_1411_booklist_title_sort.py`, real in-memory SQLite reproducing the joined-eager-load-under-LIMIT subquery shape): the raw text() ORDER BY raises `no such column` for title/sort/series_index; ORM columns resolve and sort; source-pin fails if the raw text() fragment is reintroduced. Live container HTTP verify on cwn-local: authenticated `/ajax/listbooks` for title/sort/series_index in both directions returned 200 with correct alphabetical ordering and no `OperationalError` in the logs. Marker fix (Greptile #526 P2): the regression module was missing `pytestmark = pytest.mark.unit`, so CI's `-m "smoke or unit"` filter deselected all 8 tests — added the marker so the suite actually guards. Fork-original (upstream issue open, no upstream PR). | `a63176826` | v4.0.172 |
| #519 | [CWA PR #1406](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1406) (@kurtlieber) | **"Send to eReader" died with `TypeError: sequence item 0: expected str instance, dict found` instead of showing why the send failed.** The background mail task's `smtplib` error handler ran `'\n'.join(e.args)` over every exception; `smtplib.SMTPRecipientsRefused` carries its payload as a single *dict* arg (`{address: (code, reason)}`), so the join raised inside the handler and the user saw that secondary crash instead of the recipient rejection. **Fix:** extract a pure, stdlib-only helper `compose_smtp_error_text` (`cps/services/mail_error.py`) that decodes or stringifies every smtplib payload shape (bytes `smtp_error`, `message`, the recipients dict, arbitrary `args`); always returns a `str`, never raises. Original `smtp_error → message → args` precedence preserved; the recipients-dict branch is new. `cps/tasks/mail.py` delegates to it (single caller). **12 unit tests** (`tests/unit/test_smtp_error_text.py`, stdlib-only): the reported case now renders `kid@home.net: 550 User unknown`; multi-recipient, malformed-status, bytes/`message`/args shapes, never-raises property; source-pins fail if the raw `e.args` join is reintroduced. Independent fork reimplementation of @kurtlieber's upstream PR #1406. | `05579e16b` | v4.0.171 |
| #506 | [fork #468](https://github.com/new-usemame/Calibre-Web-NextGen/issues/468) (reporters @Glennza1962, @bigbold1023) | **Kobo re-downloads the whole magic-shelf set on (almost) every sync** — books drop back to "Download"/"Unread" and lose their reading position. **Root cause — magic-shelf membership timestamp advances on a cache rebuild even when nothing changed:** `get_book_ids_for_magic_shelf` (cps/magic_shelf.py) caches a magic shelf's membership with a 30-minute TTL; on expiry it deletes + re-adds the cache row with a fresh `created_at = now()` regardless of whether the book set changed. `created_at` doubles as the Kobo sync "membership added" timestamp (`get_magic_shelf_membership_added_at` → `max(created_at)` = T_magic). The Kobo magic-shelf arm (cps/kobo.py:386-413) fires when `T_magic > device cursor` and resets its sub-cursor to -1, re-emitting the ENTIRE membership; after a clean draining sync the cursor equals the previous T_magic, so any TTL rebuild re-fires the whole shelf — **regardless of shelf size**, which is why it reproduced under 100 books and why the SYNC_ITEM_LIMIT(100) batch-walk bug was the wrong lead. It also explains the reporter's key clue ("happens every time *unless I do a back-to-back sync*"): a back-to-back sync lands inside the 30-min TTL, the cache is not rebuilt, T_magic is stable, the arm stays silent. **Fix:** when the rebuilt membership SET equals the cached set, preserve the existing `created_at` instead of stamping a new one, so T_magic advances only on a genuine membership change (comparing as sets so a browse re-sort doesn't re-fire the device). **Verified:** 9 unit tests (`tests/unit/test_468_magic_shelf_membership_timestamp.py`) — a faithful two-sync model of the arm reproduces the sub-100 re-fire and the back-to-back-quiet clue, plus AST pins on the cache fix that are RED on main / GREEN on branch; 51 adjacent kobo/magic-shelf tests green. **Live-container two-sync Kobo verification is the remaining pre-merge gate** (data-loss-class sync path). | `e9fc62b` | v4.0.169 |
| #505 | [fork #502](https://github.com/new-usemame/Calibre-Web-NextGen/issues/502) (@sambong) | **Right-clicking an image in the epub web reader gave no context menu, so readers couldn't "Save image as".** The reader-native-menu work (task #30, `notes/2026-06-17-reader-native-menu-DESIGN.md`) attaches a `contextmenu → preventDefault` listener to every rendered section's iframe document so the in-app highlight popup is the affordance for text selection — but it suppressed the menu on **every** target, images included, where there is no highlight affordance. **Root cause** is the blanket `ev.preventDefault()` in `suppressMenuOnContents` (`cps/static/js/reading/epub.js`), not the reporter's measured parent-document `div.overlay` (that backdrop is `visibility:hidden`, so it is not a pointer-event target per spec — a red herring; the real listener lives inside the epub.js `#viewer` iframe). **Fix:** the contextmenu handler now early-returns on image/media targets (new `isReaderMediaTarget()` walks the ancestor chain for `img`, SVG `image`, `picture`, `svg`, `canvas`, `video`, `audio`), so the browser's native menu shows on images while text still gets the highlight popup. Modality-complete: the same function's global `-webkit-touch-callout:none` (added for text long-press) also killed the iOS long-press "Save Image" callout, so a per-section content stylesheet re-enables the callout on media (Android long-press fires `contextmenu`, covered by the handler). **3 RED-on-main/GREEN tests** added to `tests/unit/test_reader_native_menu.py`: a structural pin that `preventDefault` is guarded by a media-target early-return, a pin on the callout re-enable, and a node-executed behavioral test of `isReaderMediaTarget` (SVG `<image>` → true, `<p>` → false, descendant-of-`<svg>` → true). The 5 pre-existing suppression tests still pass (text suppression, callout present, re-apply on render, no `user-select` kill). **Live e2e on cwn-local** (headless Chromium, Alice in Wonderland whose illustrations are SVG `<image>`): pre-fix `contextmenu` on the image → `defaultPrevented=true` (menu blocked) while text → `true`; post-fix image → `defaultPrevented=false` (native menu allowed) and text stays `true`. Sibling to #503, same reporter. Reported by @sambong. | `3075ff6` | v4.0.169 |
| #504 | [fork #503](https://github.com/new-usemame/Calibre-Web-NextGen/issues/503) (@sambong) | **The epub reader's Settings panel was clipped on short browser windows and could not be scrolled — the Theme row at the top and the Font / Spread / Reflow options at the bottom were unreachable.** The settings redesign (#484) made the panel taller (added the text-margin slider + reflow checkbox) but only gave `#settings-modal .md-content` its `max-height: 86vh; overflow-y: auto` scroll cap inside a `@media (max-width: 600px)` block — the cap was gated on viewport **width**. On a short-but-wide window (e.g. a Synology NAS admin tab) the `position: fixed`, vertically-centred panel grew taller than the viewport and clipped at both top and bottom with `overflow-y: visible`. **Root cause** is the width-gated scroll cap, not the reporter's "body has height:0" theory — a `position: fixed` element is sized against the viewport, not the body. **Fix:** move `max-height: 86vh; overflow-y: auto` to the base `#settings-modal .md-content` rule so it applies at every viewport size; the media query keeps only its mobile `max-width: 92vw` override. **2 RED-on-main/GREEN source-pin tests** (`tests/unit/test_503_settings_modal_desktop_scrollable.py`) assert the scroll cap lives in the base rule and is not gated behind `@media`. **Live e2e on cwn-local** with a headless Chromium harness: at 1280×460 the pre-fix panel measured `contentTop=-26`, `contentBottom=486 > 460`, `canScroll=false`, Reflow unreachable; post-fix the panel fits (`top=32`, `bottom=428`), `overflow-y: auto`, `canScroll=true`, and the Reflow checkbox becomes reachable by scrolling. Verified across 1280×720 / 1280×560 / 1280×460 / 390×740 plus before/after screenshots. Regression introduced by our own #484. Reported by @sambong. | `2d5b395` | v4.0.169 |
| #500 | [fork #499](https://github.com/new-usemame/Calibre-Web-NextGen/issues/499) (@jasonxbergman) | **The sidebar shelf count included books you'd archived, even though opening the shelf hid them** — archive a book and the badge stayed at the old total. **Root cause — view/count asymmetry:** the shelf view runs through `calibre_db.common_filters` (cps/db.py), which excludes the current user's archived books (`Books.id.notin_(archived_book_ids)`); the sidebar badge (layout.html) and the `book_count` sort modes used a raw `shelf.books.count()` that ignored archive state, so the count and the view diverged by exactly the archived count. **Fix:** `_shelf_book_count(shelf, user)` (cps/shelf.py) now counts only `BookShelf` rows whose `book_id` is not in that user's archived set (single app.db query); `sort_shelves_for_user` passes the user, and `render_template.py` attaches the archive-aware count as `shelf.book_count` for the sidebar (template keeps a guarded fallback). Per-user (one user's archive never changes another's count), anonymous/no-user falls back to the raw count, and `is_archived=False` tombstone rows don't shrink the count. Note: language/hidden filter parity for the badge is out of scope (those need a cross-DB query and rarely apply to personal shelves). **Verified:** 9 RED-on-main/GREEN tests (`tests/unit/test_499_shelf_count_excludes_archived.py`; real in-memory app.db engine + source pins on the sort path and template), 231 shelf/render unit tests green. Live archive-flow e2e verified on cwn-local: baseline shelf badge → archive a book via the real `/ajax/togglearchived` route → badge decrements and the shelf view agrees → unarchive → count restored, with no shelf/render errors in the container logs. | f48df94d | v4.0.168 |
| [#491](https://github.com/new-usemame/Calibre-Web-NextGen/pull/491) | [fork #479](https://github.com/new-usemame/Calibre-Web-NextGen/issues/479) (@WasabiBurns) | **Hardcover metadata search returned HTTP 401 and gave up when a stale per-user token shadowed a valid global one.** `search()` picked the first of `(user OR global OR env)` and aborted on a 401. Now it tries each distinct configured token (per-user → global → env) until one is accepted — trimming a stray `Bearer ` prefix + whitespace — and only aborts when *every* token is rejected. Per-user-write progress-sync path intentionally unchanged. **Verified:** 3 RED-on-main/GREEN tests run in-container (fallback-on-401, abort-only-when-all-rejected, Bearer/whitespace trim). Follow-up tracked: surface token status in admin UI (expiry is currently log-only). | `921c8f3b` | v4.0.167 |
| [#490](https://github.com/new-usemame/Calibre-Web-NextGen/pull/490) | [fork #468](https://github.com/new-usemame/Calibre-Web-NextGen/issues/468) (@Glennza1962, @bigbold1023) | **Kobo shelf-sync wrongly archived the book you're reading on a transient DB error, forcing a re-download** ("seems random"). The two-way-sync deletion path archives `synced_book_ids - allowed_book_ids`; `allowed` includes magic-shelf membership, and `get_book_ids/get_books_for_magic_shelf` swallowed a `SQLAlchemyError` as `([], 0)` — so a failed membership query looked like "shelf empty", dropping the in-progress book into the to-archive set → `ChangedEntitlement archived=True`. **Fix:** `raise_on_error` threaded through the magic-shelf helpers (browse callers unchanged); `get_magic_shelf_book_ids_for_kobo` returns `(ids, reliable)`; new pure `compute_kobo_books_to_archive()` returns EMPTY when membership is unreliable so a read-side failure never drives device-side archival (next clean sync reconciles). A *reliable* empty allowed-set still archives. Contributor A (no-language books dropped from progress shelves) was already fixed by #482 in v4.0.166. **Verified:** 8 RED-on-main/GREEN tests (real helper + reliability via fault injection + wiring pins), 376 kobo+magic_shelf tests green, live Kobo simulator on cwn-local (normal sync rm=0, no tracebacks, fail-safe warning correctly absent). | `45961b67` | v4.0.167 |
| [#469](https://github.com/new-usemame/Calibre-Web-NextGen/pull/469) | [fork #465](https://github.com/new-usemame/Calibre-Web-NextGen/issues/465) / [CWA #1396](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1396) (@stuhby) | **Hardlinked files dropped into ingest subfolders were silently never imported.** A download client (Readarr / Bookshelf via qBittorrent) that hardlinks a completed file into a per-author subfolder of the ingest tree triggers only `IN_CREATE` — `link()` opens no file descriptor, so no `IN_CLOSE_WRITE` ever fires — and the inotify watcher in `cwa-ingest-service/run` listened only for `close_write` + `moved_to`, so those books were skipped until moved to the ingest root by hand. **Fix:** the watcher now also watches `-e create`; `handle_event` is event-type aware and ingests a CREATE only for a *completed hardlink* (`st_nlink > 1`, non-zero stable size), leaving a single-link in-progress download (`nlink == 1`) for its eventual `close_write` so partial files are never ingested; directory CREATEs are ignored via the regular-file check. **Verified:** 7 RED-on-main/GREEN tests driven through the real `bash` `handle_event` with a real `os.link` hardlink + static watch-line pin; live cwn-local end-to-end (hardlink in a subfolder ingested; new `$events` arg unbroken in the fallback watcher); `/security-review` clean (quoting airtight, filepath reaches Python only as a distinct `execve` argv). Inotify event-delivery path verified on the Linux canary (teenyverse). | `9aa62646c` | v4.0.166 |
| [#482](https://github.com/new-usemame/Calibre-Web-NextGen/pull/482) | [fork #461](https://github.com/new-usemame/Calibre-Web-NextGen/issues/461) (@chloeroform) | **In-progress books with no language metadata vanished from the "Currently Reading" magic shelf while the book's own page still showed reading progress.** @chloeroform saw it split PDF-vs-EPUB on v4.0.161. **Root cause — read-path asymmetry, not the kosync writer:** the magic-shelf query (`cps/magic_shelf.py` `build_book_query_for_magic_shelf`) applies `CalibreDB.common_filters`, whose language clause `Books.languages.any(lang_code == user.filter_language())` fires whenever `default_language != "all"`. A book with no language metadata (UI-uploaded PDFs routinely have none) fails the clause and is dropped from the shelf; the book-detail page reads KOReader progress WITHOUT `common_filters`, so it keeps showing the book as in-progress. EPUBs carrying `eng` survived; no-language PDFs did not. `update_book_read_status` audited clean (writes ReadBook + Kobo bookmark atomically for the same book_id). **Fix:** progress shelves are activity-driven, not browse-driven — new `rules_reference_read_status()` gate detects a `read_status` rule in the (nested) rule set, and the single shelf-query choke point then passes `return_all_languages=True` to `common_filters`, skipping ONLY the language clause; archived / per-user-hidden / denied-tags / restricted-column stay enforced and browse-only custom shelves keep language filtering. **Verified:** 8 RED-on-main/GREEN tests (`tests/unit/test_magic_shelf_language_bypass_461.py`; gate behaviour across templates/nested groups/`id`-`field` aliases/browse-only/empty + AST-pin the query threads a non-constant `return_all_languages` into `common_filters`), 170 magic_shelf+kosync tests green, existing `currently_reading` pins still green + OBSERVED live repro on cwn-local (user `cwng84test` in-progress `{2 eng, 18 pdf <NONE>, 19 comic <NONE>, 20 eng}` → eng language clause drops 18/19 exactly as reported) + container-Python deploy check of the helper. Dual-language Playwright e2e is the remaining pre-merge gate (`notes/461-magic-shelf-koreader-design.md`). Live RED/GREEN on the real `/magicshelf/11` route confirmed no-language in-progress books reappear and a denied-tag book stays excluded (access control intact). | `30974d149` | v4.0.166 |
| #471 | [fork #470](https://github.com/new-usemame/Calibre-Web-NextGen/issues/470) (@sltvtr) | **The desktop Fetch-Metadata modal footer (the only "Close" button) stayed inside the viewport on long result lists.** @sltvtr reported on v4.0.164 (docker/x86) that the *Fetch Metadata* popup on the Edit Book page grew taller than the browser window, pushing the footer "Close" button off the bottom of the screen; zooming the page to 80% was the workaround. **Root cause:** the caliBlur theme already bounds the scrolling results region (`#meta-info` → `max-height: calc(100vh - 357px)`) and modal body, but it also pushes the whole dialog down — `padding-top: 60px` on `#metaModal` plus a `translateY(10%)` transform on the dialog (~90px on a 907px-tall dialog). That ~150px top offset exceeds the 90px the theme reserves in modal-content's `max-height: calc(100% - 90px)`, so the bounded modal still extended ~85px past the viewport bottom and the footer scrolled off-screen. **Fix:** new `cps/static/css/meta-modal-fit.css` (linked after `caliBlur.css`), desktop-only (`@media min-width:768px`), zeroes `#metaModal` `padding-top` and the dialog `transform` so the theme's existing viewport-relative caps keep header + scrollable results + footer all on screen; the results list keeps its own internal scroll and caliBlur scrollbar styling. Mobile (≤767px) is untouched — the #288 modal-body cap already keeps its footer reachable. Scoped to `#metaModal` so no other modal is affected. **Verified:** 3 source-pin regression tests (`tests/unit/test_470_meta_modal_footer_fit.py`, RED-on-main/GREEN; pins the rules, the `#metaModal`-only scoping, and the critical load-after-caliBlur invariant) + live Playwright on cwn-local (forced caliBlur = real user config): before, footer bottom 992px / Close button bottom 977px below a 907px viewport; after, modal fully inside (bottom 841), footer + Close button fully visible at 1917×907 and 1366×768, mobile 390×844 unaffected, `#meta-info` still scrolls. | `328d64c7` | v4.0.165 |
| #464 | [fork #463](https://github.com/new-usemame/Calibre-Web-NextGen/issues/463) (@Andrew-H2O) | **Custom CSS and the server announcement banner moved from Basic Configuration to the UI Configuration page.** @Andrew-H2O reported the Custom CSS option took a long time to find: it was on *Edit Basic Configuration* inside a section labelled *Logfile Configuration*, next to log level and log paths — and the documentation already said it lived under *UI Configuration*. **Root cause:** when Fork #323 (custom CSS) and Fork #225 (announcement banner) were added, both form fields were appended to the nearest section on the basic-config template (`config_edit.html`), and their `_config_string` persistence calls were added to that page's handler (`_configuration_update_helper`). Neither belongs with logfile settings. **Fix:** moved both fields into a new "Site Customization" section on the UI Configuration page (`config_view_edit.html`, using its `conf.` context variable), and moved the two `_config_string` calls to `update_view_configuration`. No schema, column, migration, or sanitiser change — the `config_custom_css` / `config_server_announcement` columns and their render-time escaping are untouched; only the admin form location moved. **Verified:** 5 placement-pin regression tests (`tests/unit/test_463_css_banner_ui_config_placement.py`, RED-on-main/GREEN; AST-isolates both handler bodies, pins template presence/absence and the `conf.` binding) + 2 pre-existing tests (`test_custom_css_injection`, `test_225_server_announcement`) updated to the new location, 26 green + live cwn-local HTTP e2e (real CSRF/session): both fields render on `/admin/viewconfig` under "Site Customization", are absent from `/admin/config`, a UI-config save persists both to the DB, a real Basic-config save (`/admin/ajaxconfig`) does NOT clobber them, and the saved values render back inside the textareas (confirming the `conf.` binding, not Jinja `Undefined`). | `78117e71` | v4.0.165 |
| #462 | [fork #319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) initial-load state (@droM4X 2026-06-13) | **The caliBlur cover-hover read-toggle button shows the correct icon and tooltip on initial page load in listing views.** @droM4X reported that on `/read/stored` and other book lists, an already-read book showed "Mark As Read" / `glyphicon-check` until clicked once, then corrected. **Root cause (`cps/static/js/caliBlur.js`):** the button's initial state and the toggle handler both probed for a `.badge.read` element, which only the random-books strip (`index.html`) renders. Every real listing renders read state through the `cover_badges` macro (`image.html`) as `.cover-badge-read` — a different class — so the probe always returned "unread" and the button initialized wrong, and clicking a read book desynced the badge. **Fix:** detect read state from both structures (`.badge.read, .cover-badge-read`) at the button-init and toggle-handler sites. **Verified:** 2 source-pin tests (`tests/unit/test_read_toggle_icon_action_state_319.py::TestReadStateDetectionCoversCoverBadge`, RED-on-main/GREEN; full #319 suite 18 green) + live Playwright on cwn-local (caliBlur theme, book #18 finished, `/read/stored`): read book initializes to "Mark As Unread" / `glyphicon-unchecked`, `.cover-badge-read` present / `.badge.read` absent. caliBlur-theme only, no server/route change. | `6de21be` | v4.0.164 |
| [#457](https://github.com/new-usemame/Calibre-Web-NextGen/pull/457) | [CW #3403](https://github.com/janeczku/calibre-web/issues/3403) | **Metadata edits no longer fail for authors whose names transliterate alike.** Editing any field of a book by an author like "George Pólya" (when "George Polya" also exists), or the Chinese homophones `姚尧`/`妖妖`, crashed with `(sqlite3.IntegrityError) UNIQUE constraint failed: authors.name` — even a cover-only edit. **Root cause:** the registered `lower` SQLite UDF is `unidecode.unidecode(s.lower())` (`cps/db.py`), which strips accents and transliterates; `prepare_authors` resolved the author via `func.lower(db.Authors.name).ilike(in_aut).first()`, so two distinct authors folded together and the unordered `.first()` picked the wrong twin, which `create_objects_for_addition` then renamed onto the submitted name → UNIQUE collision (autoflush). **Fix (`cps/editbooks.py`):** new pure `select_existing_author` helper resolves exact-name → true Unicode case-fold variant → first-candidate fallback, so a transliteration twin is never chosen for the rename path; legitimate casing/accent-correction renames preserved. Applied at `prepare_authors` (crash site) and `prepare_authors_on_upload` (same idiom; read-only `.sort`, but could mis-attach an upload); `add_objects` already disambiguates via `.all()`+exact, audited unaffected. **Verified:** 13 regression tests (`tests/unit/test_author_transliteration_collision_3403.py`, RED-on-main/GREEN — real production UDF + Calibre-shaped schema reproduces the IntegrityError, helper defeats it; AST source-pins) + 44 adjacent author/editbook tests green + live cwn-local HTTP e2e (real CSRF/session/library): seeded twin "Léwis Carroll", pre-fix POST `/admin/book/5`→twin logged the exact `UNIQUE constraint failed` and rejected the edit; post-fix edit succeeds and persists, both authors stay distinct, zero IntegrityError. Original report @annProg; accented-name repros @apetresc, @wnmurphy. | `37ae0f6b` | v4.0.164 |
| #454 | [fork #319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) icon follow-up (@droM4X 2026-06-12) | **The grid/listing hover read-toggle uses the check/unchecked matched pair, not the eye glyph.** v4.0.150 standardized the detail-page toggle, but the caliBlur cover-hover "Toggle Read Status" button still showed `glyphicon-eye-open` — the visibility glyph, the exact read-vs-hide icon confusion #319 was opened about. **Fix (`cps/static/js/caliBlur.js`):** button is built from the book's badge state with action semantics (unread → `glyphicon-check` "Mark As Read"; read → `glyphicon-unchecked` "Mark As Unread"), title attribute matches, and the success/error handlers rest on the icon for the NEXT action instead of reverting to the eye. **Verified:** 4 source-pin tests (`tests/unit/test_grid_read_toggle_icon_319.py`, RED-on-main/GREEN) + live Playwright on cwn-local: initial state check/"Mark As Read", click → badge appears + unchecked/"Mark As Unread", second click restores, 0 console errors (JPEGs `notes/319-hover-*.jpeg`). Remaining eye glyphs are the nav "Read Books" link + book-organizer dropdown — navigation iconography, not toggles; left as-is. | `ecd45e2e2` | v4.0.162 |
| #453 (carries [#443](https://github.com/new-usemame/Calibre-Web-NextGen/pull/443) by @KucharczykL) | [fork #445](https://github.com/new-usemame/Calibre-Web-NextGen/issues/445) / [CWA #1060](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1060) | **The magic-shelf before_request hook no longer floods docker logs at DEBUG level.** It logged up to 2 + N lines on every authenticated request ("Found N total...", one "Hiding system shelf template..." / "Hiding public shelf..." per hidden shelf, "Filtered to N visible...") — a polling UI tab meant six identical lines every ~3s for the #445 reporter. Now one merged DEBUG message per user, deduped per (total, visible, hidden-set) snapshot change via module-level `_MAGIC_SHELF_COUNTS_LOGGED` — same pattern as the #108 author-sort dedup; the hidden templates/shelves are named inline in that single message so no debug signal is lost. The per-request orphaned-system-shelf WARNING ("doesn't match any current template") is likewise deduped per (user, shelf) via `_ORPHANED_SYSTEM_SHELF_WARNED`. Base dedup + 4 tests by @KucharczykL (#443); extended to the "Hiding..." lines + orphan WARNING with 3 more tests. **Verified:** 7 unit tests (string pins + behavioral dedup on the real helper) red-on-main/green + live cwn-local at DEBUG: pre-fix signature (repeating Found/Hiding/Filtered block every poll) gone; single merged line fires once on login and once per hide/unhide change. | `4c45bb3d0` | v4.0.162 |
| #452 | [fork #433 follow-up](https://github.com/new-usemame/Calibre-Web-NextGen/issues/433) | **Hardcover progress sync recovers from stale book ids.** After v4.0.161 fixed the nullable-field crash, the #433 reporter's next log showed `insert_user_book error: We weren't able to find that book. Was it deleted?` + "could not be added, skipping progress sync" — Hardcover merges duplicate books and retires the old book id, so the `hardcover-id` stored in Calibre metadata goes stale. **Fix (`cps/services/hardcover.py`):** `add_book` now re-resolves the current book id on that error — edition first (`editions(where:{id}){book_id}`; editions are re-parented to the canonical book on merge), slug fallback (`books(where:{slug}){id}`) — and retries the insert once; same-id resolution and other errors do not retry; unresolvable ids log an actionable "refresh the book's metadata" hint. Mutation extracted to `_insert_user_book`, resolution in `_resolve_book_id` (exception-safe). **Verified:** 8 regression tests (`tests/unit/test_hardcover_stale_book_id_recovery.py`, 4 RED-on-main/GREEN + 4 invariant guards) + 38 adjacent hardcover tests green + both resolve queries validated OBSERVED against the real Hardcover GraphQL API (edition→book_id and slug→id round-trip on a live book). | `d0ac907c2` | v4.0.162 |
| [#450](https://github.com/new-usemame/Calibre-Web-NextGen/pull/450) | [fork #448](https://github.com/new-usemame/Calibre-Web-NextGen/issues/448) | **Dropping a `.acsm` file into ingest produced only Calibre's raw "No plugin to handle input format: acsm" traceback plus a CON_ERROR line ending in a literal `None`.** An `.acsm` is an Adobe fulfillment ticket, not a book; stock Calibre cannot convert it, and the generic conversion-failure path gave the user nothing actionable. **Fix (`scripts/ingest_processor.py`):** new module-level `_CONVERSION_FAILURE_GUIDANCE` map + `conversion_failure_guidance(input_format, filename)` helper; `convert_book()`'s `CalledProcessError` branch prints an `ACSM_NOTICE` naming both recovery paths (ACSM Input plugin via `CWA_CALIBRE_USER_PLUGINS` + plugins dir, or fulfill in Adobe Digital Editions / Calibre desktop and ingest the download) and where the original landed (`processed_books/failed`). Adjacent bug fixed in both CON_ERROR prints (`convert_book`, `convert_to_kepub`): they interpolated `e.stderr` from an uncaptured subprocess — always `None`, rendering a literal "None" in the log — now "(see ebook-convert/kepubify output above)". Extensible map, not an inline special case. **Verified:** 10 regression tests (`tests/unit/test_acsm_ingest_guidance_448.py`, red-on-main/green) + live cwn-local e2e: dummy `.acsm` drop reproduced the exact pre-fix symptom, patched container prints the guidance, file confirmed in `processed_books/failed`, and a normal EPUB ingest still imports cleanly. Failure mode found by @jbelascoain in #388. | `934196a3f` | v4.0.162 |
| [#437](https://github.com/new-usemame/Calibre-Web-NextGen/pull/437) | [fork #431 LubimyCzytac.pl provider](https://github.com/new-usemame/Calibre-Web-NextGen/issues/431) | **LubimyCzytac.pl metadata search returned "no results" for every query.** The Polish catalog redesigned its search results and book pages in 2026; the provider's XPaths still targeted the old DOM (`authorAllBooks__single` result tiles, a single `application/ld+json` block, dt/dd publisher rows), so `parse_search_results` matched zero nodes (the reporter saw HTTP 200 + "No results found") and the detail parser silently dropped publisher/description/dates. **Fix (`cps/metadata_provider/lubimyczytac.py`):** retargeted search to the new `book-card book-card--l` tiles in `listSearch` (title/url via `a.book-card__title`, authors via `div.book-card__author a[href*=autor]`); description now reads the full `#book-description` block; publisher reads the `Wydawnictwo:` span link; `_parse_from_summary` selects the `@type==Book` ld+json block instead of `[0]` (now an Organization block) so `datePublished` resolves; edition-date dt rows retargeted to the new `Data wydania:` / `Data 1. wyd. pol.:` labels. No new deps, no new external URLs (same lubimyczytac.pl). **Verified:** 7 regression tests against captured fixtures of the redesigned pages (`tests/unit/test_lubimyczytac_site_redesign_431.py`, 5 RED-on-main/GREEN) + live full-`search()` e2e returning 10 fully-enriched results (title, authors, publisher, date, rating, language, series, isbn, cover, description). CWA #1330 `tags=[]` contract preserved. | `390653f50` | v4.0.162 |
| #435 | [fork #433 Hardcover sync error](https://github.com/new-usemame/Calibre-Web-NextGen/issues/433) | **Hardcover progress sync no longer crashes on nullable GraphQL fields.** Reading progress pushed from KOReader/Kobo failed with `'NoneType' object has no attribute 'get'` whenever the Hardcover user_book has `edition: null` (no edition selected) or a mutation returns `user_book: null` with an error string — `dict.get(key, default)` only falls back on *absent* keys, and Hardcover serializes these as present-but-null. **Fix:** null-normalized every response access in `cps/services/hardcover.py` (`or {}` / `or []`), guarded `update_reading_progress` against `add_book` returning None, kept the fetched user_book when `change_book_status` is rejected, surfaced Hardcover `error` fields in logs, defaulted null `started_at`, and added `exc_info=True` to the catch in `cps/kobo.py` so the next report carries a traceback. Logs an actionable hint when a book has no edition page count. **Verified:** 6 RED-on-main/GREEN regression tests (`tests/unit/test_hardcover_nullable_response_fields.py`) + live-wire repro against a real local mock Hardcover GraphQL server via `HARDCOVER_API_ENDPOINT` (unfixed code raises the reporter's exact error; fixed code completes). | `154440b51` | v4.0.162 |
| #399 | [fork #319 dup-series](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) data-safety (D3/D11) | **Resolving duplicates can no longer leave a phantom half-deleted book.** The auto-resolve loop deleted a duplicate's files (`helper.delete_book`) BEFORE committing the DB deletes (`delete_whole_book` + `calibre_db.session.commit`); a failure in between left the files gone but the `Books` row surviving — a ghost book that shows in the library and 404s on open, unrecoverable without the backup. **Fix:** DB-commit-first, files-last — a DB failure now leaves the book fully intact (files + rows) so the per-book except records an error and the duplicate stays resolvable next run; a post-commit file-cleanup failure is logged, not raised (row already gone; backup + orphaned files reclaimable). The files-last cleanup is handed a plain-value stand-in (`_DeletedBookFileRef`, id+path) instead of the ORM `book`, which `delete_whole_book`'s intermediate custom-column commits expire/detach (Greptile #399 catch — would raise `DetachedInstanceError` + orphan files on any custom-column library). **Verified:** 14/14 source pins (3 RED-on-main/GREEN); live cwn-local — clean run 6 losers removed, 0 errors, no DetachedInstanceError; injected DB-commit failure → loser row + files both intact (no phantom). /security-review clean. Scoped to the resolution path; the same files-first order in `delete_book_from_table` + the app.db/metadata.db commit split are documented follow-ups (`notes/d3-d11-delete-ordering-design.md`, D4 tie-in). | `c2dfc96ea` | v4.0.159 |
| #397 | [fork #319 dup-series](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) data-safety (D2) | **Resolving duplicates is safe even if a duplicate scan runs at the same moment.** `execute_resolution` (HTTP, gevent greenlet) and `TaskDuplicateScan` auto-resolve (a real OS worker thread — `gevent.monkey.patch_all` is not called) could both enter the destructive delete loop on the same group: double-delete, half-removed duplicate, or a transient error leaving the library inconsistent. **Fix:** a module-level `threading.Lock` serializes the `dry_run=False` resolution; acquired **non-blocking** (`acquire(blocking=False)`) so a contending caller declines cleanly with `in_progress` rather than parking the gevent hub greenlet and stalling every HTTP request (Greptile P2). Released once in `finally`. **Verified:** 5 RED-on-main/GREEN source pins; live two-real-thread A/B on cwn-local — fixed run: 6 clean deletes, contended thread declines "already in progress"; unfixed run: 4 errors + a book orphaned/duplicate persists. /security-review clean. | `93bc01a09` | v4.0.159 |
| #395 (+#396 guard `d6ce17c31`) | [fork #319 dup-series](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) data-safety (D7) | **Books that are *missing* a title or author are no longer grouped as duplicates of each other.** Two unrelated books that both had no title (or no author) collapsed into one "duplicate" group keyed on empty-string metadata — and could then be offered for deletion. **Fix:** incomplete-metadata books are excluded from grouping; #396 moved the guard onto the actual grouping/write path and corrected the author-presence check. **Verified:** RED-on-main/GREEN source pins + a behavioural distinct-keys test. /security-review clean. | `985aaacdf` | v4.0.159 |
| #394 | [fork #319 dup-series](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) data-safety (D1) | **The duplicate resolver no longer closes a shared DB session mid-operation.** `auto_resolve_duplicates` called `session.close()` on the process-shared `calibre_db.session`, which could surface errors or a half-finished cleanup when the library was in use concurrently. **Fix:** stop closing the shared session inside the resolver. RED-on-main/GREEN source pin; /security-review clean. | `ee4183091` | v4.0.159 |
| #393 | [fork #320](https://github.com/new-usemame/Calibre-Web-NextGen/issues/320) follow-up (@SpookyUSAF on v4.0.158) | **Shelf reorder covers stay thumbnail-sized for more setups, including behind certain reverse proxies.** The v4.0.158 fix (#385) inlined the cover-sizing into the page, which some proxied/default-theme setups still rendered oversized. **Fix:** moved the rule out into a regular stylesheet that sizes covers self-containedly regardless of theme or proxy. Pairs with #389 (theme-force airtight) as the root-cause fix for the #320 display-bug class. Verified live (both themes + 375px). | `f503987a2` | v4.0.159 |
| #391 | (operator report from a phone; mobile UX) | **On phones the hamburger menu button is on the left, the same side the navigation drawer slides out from**, so the button and the menu it opens line up; tapping it opens the menu, tapping outside still closes it. Verified live with hit-tested taps at 375px / 320px / 1280px (desktop unaffected). | `202171bdf` | v4.0.159 |
| #390 | (operator report from a phone; mobile UX) | **On phones the select/settings (organizer) buttons above a book list sit on the right, matching desktop**, so tapping the gear opens its menu on screen instead of off the left edge where it was getting clipped. Verified live across viewports. | `8c2045b3a` | v4.0.159 |
| #389 | (operator audit — root cause behind the #320 default-theme display-bug class) | **caliBlur theme enforcement is now airtight; the deprecated default (light) theme can no longer leak through.** `admin.before_request` forced `g.current_theme = 1` only AFTER a block of unguarded `config.*` reads + a DB autoconfig/recovery block, and two `@app.before_request` handlers in `cps/__init__.py` run before it — any exception there left `g.current_theme` undefined, so templates (`{% if g.current_theme == 1 %}`) fell back to the deprecated default theme on the rendered (usually error) page. That fallback is the root cause behind default-theme-only display bugs like #320's oversized reorder covers (a caliBlur-only repro can't see them). **Fix:** force the theme as the FIRST statement of `before_request` (exception-proof) + delete the dead per-user compute it discarded (single source of truth) + default an unset theme to caliBlur in the standalone `http_error.html` and `shelfdown.html` templates. **Verified live** (3-stage container proof: neutered the force → a layout-extending page lost caliBlur i.e. escape reproduced, while the error page kept it i.e. belt held; restored → caliBlur returns; no before_request/500 errors). 7 red/green source pins; Greptile 4/5, both findings (shelfdown completeness + test-anchor robustness) fixed in-branch. | `80c120910` | v4.0.159 |
| #385 | [fork #320](https://github.com/new-usemame/Calibre-Web-NextGen/issues/320) follow-up (@droM4X + @SpookyUSAF on v4.0.157) | **Shelf reorder covers stay thumbnail-sized on the default theme; Back button aligned + spaced.** The v4.0.157 reorder grid is fork-new and on the default theme depends on the global `.container-fluid .book .cover { height: 225px }` box to size covers; that box renders **uncapped** for this page on the reporters' instances, so covers fell back to near-natural 'large icon' size. caliBlur was unaffected (it sizes the image directly via `.cover img { width: 150px }`) — which masked the bug in a caliBlur-only repro (cwn-local's default runtime force-enables caliBlur). **Fix:** id-scoped theme-independent `#reorder-grid .reorder-item .cover img { max-height: 225px !important }` (beats the responsive max-width cascade, no-op where already correct, doesn't touch caliBlur's width:150) + Back button wrapped in a grid column for consistent left gutter across themes + top margin. **Verified live** (Playwright, both themes + 375px): default 131x225 and the rule still caps the simulated-uncapped state to 131x225; caliBlur unchanged 150x225 with Back alignment improved -15->0; reorder+autosave+DB persistence intact. 2 pins; Greptile !important-pin suggestion applied; /security-review clean. | `2b0bb5d5b` | v4.0.158 |
| #383 | (operator report — follow-up to #382; inherited caliBlur design, same glyph upstream) | **The mobile menu button is a recognizable three-bar hamburger.** caliBlur hides Bootstrap's icon-bars and paints a profile-head SVG over the toggle, so the only navigation control on a phone reads as "account" — the product owner couldn't find the menu. Theme-scoped `:before` repaint (override CSS loads only for caliBlur, after it): standard hamburger SVG, 50% clip dropped; geometry, position, and the 40×40 tap target unchanged; stock Bootstrap theme untouched. Verified live with hit-tested taps at 375px (open / outside-close / toggle-reachable cycle), 320px, and 1280px (media-scoped — desktop unaffected). 1 media-block-anchored pin (Greptile P2 fixed in-branch). | `ac1755317` | v4.0.157 |
| #382 | (operator report from a phone; inherited from upstream caliBlur, 2024 — the same drawer bricks stock CWA on mobile) | **On phones, tapping outside the open sidebar closes it, and the menu button stays tappable while the drawer is open.** The caliBlur off-canvas drawer inserts a full-screen `.sidebar-backdrop` (z-9, inside `.navbar`) with NO close handler that also covered the navbar toggle — one open and every tap on the page was dead until a reload. Fix: delegated backdrop tap-to-close (delegated because `mobileSupport()` recreates the backdrop on resize) + `.navbar-header` z-10 so the toggle outranks the backdrop. Reproduced and verified with HIT-TESTED taps (`elementFromPoint`) — synthetic `.click()` bypasses hit-testing and had masked the bug in automation. 3 pins; mobile design pass of 5 pages at 375px logged polish tier to notes/. | `ad4b698e9` | v4.0.157 |
| #377 | (no tracker issue — root-caused from 3 silent 10-min CI freezes in 24h; same symptom class as upstream [CWA #1256](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1256)'s hub stall, which the #212 listener fix narrowed but did not close) | **The whole app can no longer freeze when a background task and a page load hit the database simultaneously.** AB-BA deadlock between the Python GIL and sqlite's per-connection mutex: serialized-mode sqlite (Linux builds, `threadsafety==3`) takes the connection mutex inside EVERY `sqlite3_*` call, including the short ones pysqlite makes GIL-held (`reset`/`bind`/`finalize`/`create_function`); a thread mid-`step` in a Python-UDF trampoline (mutex held, wants GIL) + any GIL-held short call (wants mutex) starves every thread in the process — pytest-timeout, xdist IPC, the gevent hub — until SIGKILL. Reachable in production: `WorkerThread`/APScheduler are real OS threads and 7 task modules (thumbnail, metadata_backup, duplicate_scan…) query the same StaticPool shared connection the web greenlets use. **Why it never reproduced on the dev machine:** macOS sqlite is multi-thread mode (`threadsafety==1`) — no connection mutex exists; the hammer ran 3000 iterations clean on macOS and froze at iteration 1 in a 2-CPU `python:3.13` container. Evidence: py-spy dump of the frozen process (GIL holder spinning in an adaptive pthread mutex; six threads parked on a FREE lock they can't take without the GIL), GIL-liveness heartbeat stopping at beat 1 — `notes/fix-udf-gil-deadlock-DESIGN.md` + `notes/udf-gil-deadlock-artifacts/`. **Fix:** `_SerializedSqliteConnection`/`_SerializedSqliteCursor` — per-connection RLock taken (GIL released while waiting) before every pysqlite C entry point, wired via `connect_args['factory']` into both calibre engines; `interrupt()` deliberately unwrapped (documented mutex-free-safe, must stay usable to break a stuck statement). **Verification (serialized build):** deterministic child-process red/green test — the unguarded control PROVABLY freezes (harness can't pass vacuously), the guarded path survives the exact interleave; 2000-iteration stampede hammer clean @160/s vs frozen-at-iteration-1 unpatched; the two previously-freezing concurrency tests now mirror production connect_args. CI hardening: `-o faulthandler_timeout=90` (faulthandler dumps all stacks WITHOUT the GIL — future freezes produce named stacks, not silent walls). Greptile's 3 P2s applied (hook registrars wrapped; arg-order-agnostic source pin; docstring). | `b10735026` | v4.0.157 |
| #376 | [fork #331](https://github.com/new-usemame/Calibre-Web-NextGen/issues/331) (@Gusdezup: "is there a plan to reduce the response header size on /v1/library/sync so it fits within nginx's default 4K buffer?") | **Kobo sync works behind reverse proxies with stock 4K buffers (Synology DSM, default nginx).** With Kobo store proxying on, the store's ~2-2.5KB JWT is embedded in our sync-token JSON and base64'd AGAIN (+33% on the largest component) → 4352B total response headers in the reporter-mirror repro → HTTP 502 through an nginx:alpine sidecar with default `proxy_buffer_size` while direct HTTP works ("Sync failed" on device). Our own cursor fields are only ~495B — the store token dominates. **Fix:** transport-level compression — `build_sync_token` emits `z1:` + b64(zlib(json)); `from_headers` sniffs the prefix and keeps parsing legacy plain-b64 tokens. Devices echo the token opaquely → zero device migration; `:` can't appear in b64 and `.` (kobo-store raw-token sentinel) can't appear in the prefix → no format collisions; schema VERSION untouched (transport, not schema). Decompression bounded (CWE-409): 16KB compressed pre-filter + 64KB `decompressobj(max_length)` cap + unconsumed-tail rejection; hostile/truncated/oversized tokens degrade to a fresh sync exactly like malformed legacy tokens (no 500 path — verified across 8 hostile-input classes). **Post-fix:** 3502B worst-case headers (high-entropy JWT budget pin — a run-of-'x' fixture compresses 10x and would pass trivially; real store JWTs are b64 of cryptographic material and barely compress) / 1158B typical; 200 through the same 4K proxy, cursor preserved, full magic-shelf sync clean. 12 unit tests; 324 kobo/synctoken green; simulator's decoder updated for `z1:`. Reviews: Greptile 3 findings dispositioned (P1 bounded-decompression fixed in-PR, P2 budget test added, P2 level-9 declined-then-moot), /security-review clean. | `6e2492991` | v4.0.157 |
| #372 | [fork #359](https://github.com/new-usemame/Calibre-Web-NextGen/issues/359) — the REAL root cause, found after v4.0.147–155 shipped five correct-but-latent cursor fixes that never moved the reporter's symptom | **Magic Shelves marked for Kobo sync actually reach the device.** `config_kobo_sync_magic_shelves` ships default-False and gates every Kobo-side magic-shelf path — the membership delivery arm, the NewTag/ChangedTag collection emission, the cache refresh — while ALSO emitting a `DeletedTag` tombstone for every magic shelf the user owns on every sync. Meanwhile the magic-shelf edit UI let users tick the per-shelf "Enable Kobo sync" checkbox with the global flag off, silently swallowing the intent. Diagnosis keyed off the reporter's own evidence being internally inconsistent: the `Found N books to remove` log line he pasted only executes in the `kobo_only_shelves_sync=True` branch (his recap claimed sync-all), and his "cache refreshes on sync" timestamps predated the release he credited (his own UI visits — observer effect). **Fix (three prongs):** (1) one-time boot migration `migrate_kobo_magic_shelf_intent` enables the global flag where any magic shelf carries `kobo_sync=1` — marker-atomic (marker writes BEFORE the commit so flip + never-again guard land together; a deliberate admin disable is never re-overridden, live-verified across container restart), runs before `config_sql.load_configuration` so it's live the same boot, defers without marker on pre-flag schemas; (2) `magic_shelf_edit.html` disables the per-shelf checkbox with an amber hint naming the exact CWA Settings toggle when the global flag is off; (3) create/edit POST handlers return an explicit `warning` for persisted-but-inert kobo intent (persist-not-reject: rejecting would break rule edits via the disabled-but-checked checkbox; coercing would erase intent). **Live verification (cwn-local rebuilt from main):** RED mirror of the reporter (flag=0, magic shelf over 19 books, shelf-only, fresh device) → 1 book delivered, 0 magic collections, 9 DeletedTags; GREEN after migration → 17 entitlements + NewTag with 19 items, clean termination on token-carried re-sync; **150-book magic shelf with a single shared `last_modified` paged 2 rounds (100+67), composite keyset parked mid-collision (`id=103`), sub-cursor advanced (`msli=103`) then folded/reset — 150/150 delivered exactly once** (first live-scale validation of the v4.0.147–155 cursor stack); sync-all mode collections emit; Playwright admin round-trip (settings toggle off/on → checkbox gates/un-gates, JPEGs in `notes/fix-359-checkbox-*.jpeg`); authenticated-fetch API probe returns success+warning. **14 unit tests** in `tests/unit/test_kobo_magic_shelf_intent_migration_359.py`; 462 touched-area tests green. Adds `scripts/manual/kobo_sync_simulator.py` (reusable Kobo device simulator: token round-trip, continuation, per-round cursor decode). Greptile's two findings on the first cut (marker atomicity; API bypass) fixed in-PR and live-verified. | `d05156ab0` | v4.0.156 |
| #369 | (Greptile follow-up review on [PR #368](https://github.com/new-usemame/Calibre-Web-NextGen/pull/368) — P sub-cursor stale-token re-injection after rebuild reset) | **The cache-rebuild reset of the magic-shelf sub-cursor actually persists to the next sync.** v4.0.154 introduced cache-rebuild detection that resets the LOCAL `magic_shelf_last_id` to `-1` when `magic_shelf_membership_added_at > sync_token.magic_shelf_membership_at`. Greptile noticed that the `new_magic_shelf_last_id` computation at the end of the loop still sourced from `sync_token.magic_shelf_last_id` (the stale persisted value) instead of the local variable. When a post-rebuild batch contains no magic books (e.g. it's full of regular books that sort before the magic ones in `(lm, id)` order), the else-branch silently overwrites the reset with the stale token value, `magic_shelf_membership_at` is advanced to match, and subsequent syncs no longer re-trigger the rebuild detection — leaving low-id magic books undelivered until another shelf modification. **Fix:** one-line correctness fix — both branches of `new_magic_shelf_last_id` (the `max()` advance branch and the no-magic-books else branch) source from the local `magic_shelf_last_id` variable so the rebuild-reset value propagates to the token write. **3 RED→GREEN AST-based tests** in `tests/unit/test_kobo_magic_shelf_use_local_sub_cursor_after_reset_368.py`: defense-in-depth pins against the specific buggy patterns (`max(sync_token.magic_shelf_last_id, ...)` and `new_magic_shelf_last_id = sync_token.magic_shelf_last_id`); positive AST assertion that the `max()` sources from the local variable. **456 Kobo+sync unit tests pass.** Live `cwn-local`: route returns HTTP 200. | `410427fa0` | v4.0.155 |
| #368 | (Greptile review on [PR #367](https://github.com/new-usemame/Calibre-Web-NextGen/pull/367) — P sub-cursor staleness across cache rebuilds) | **Kobo sync correctly resets the magic-shelf sub-cursor when the cache is rebuilt.** v4.0.153 persisted `magic_shelf_last_id` in the SyncToken across sync sessions to walk through large shelves. But if a sync ended with `cont_sync=False` on an exactly-full batch (book_count == SYNC_ITEM_LIMIT), the persisted `magic_shelf_last_id` sits at a high value (e.g. 149). If the user then adds a low-id book to the magic shelf and the cache rebuilds before the next sync, the arm filter `id IN magic_set AND id > magic_shelf_last_id` silently excludes the new book. The empty batch triggers `batch_drained`, fold fires, cursor advances past the new T_magic, and the book stays undelivered until ANOTHER cache rebuild lifts cache.created_at past the new cursor.lm. **Fix:** track `magic_shelf_membership_at` on the SyncToken (default `datetime.min`; VERSION 1-3-0 → 1-4-0; MIN_VERSION unchanged — pre-1-4-0 tokens parse cleanly and default the new field to min). At the start of each `HandleSyncRequest`, compare `magic_shelf_membership_added_at > sync_token.magic_shelf_membership_at`: if newer, reset `magic_shelf_last_id = -1` so the sub-cursor walk restarts from id=0. At end of request, persist `sync_token.magic_shelf_membership_at = max(stored, current)` so the next sync compares against the most-recent cache.created_at the device has seen. **6 RED→GREEN tests** in `tests/unit/test_kobo_magic_shelf_reset_on_cache_rebuild_367.py`: SyncToken field default + round-trip + pre-1-4-0 backward-compat; AST pin for the membership-at comparison in HandleSyncRequest; AST pin for the write-back; behavioral SQLite test reproducing Greptile's exact scenario (session 1 leaves magic_shelf_last_id=149; user adds id=10; cache rebuilds; session 2 delivers id=10). **453 Kobo+sync unit tests pass.** Live cwn-local: VERSION 1-4-0, magic_shelf_membership_at default = datetime.min, HTTP `/v1/library/sync` returns 200, both runtime markers present in deployed module. | `cc28e74f2` | v4.0.154 |
| #367 | (Greptile follow-up review on [PR #366](https://github.com/new-usemame/Calibre-Web-NextGen/pull/366) — P1 termination failure introduced by v4.0.152) | **Kobo sync no longer infinite-loops on magic shelves with >= SYNC_ITEM_LIMIT books.** v4.0.152's `batch_drained`-gated fold introduced a new correctness bug for large magic shelves: when `len(magic_shelf_book_ids) >= SYNC_ITEM_LIMIT` (100), the deferred fold never fires. The bare `id IN magic_shelf_book_ids` arm is cursor-unaware — it re-introduces ALL magic books on every round regardless of cursor position. Concrete failure (Greptile's scenario): 200 magic books at one T_bulk. Round 1: ORDER BY (lm, id) LIMIT 100 → ids 1-100; cursor → (T_bulk, 100). Round 2: query `(lm > T_bulk) OR (lm == T_bulk AND id > 100) OR (id IN {1..200})` → matches 200 books → ORDER BY (lm, id) LIMIT 100 yields ids 1-100 AGAIN. `batch_drained = False` every round; cursor never changes; `cont_sync = True` forever. **Fix:** add a magic-shelf sub-cursor. New SyncToken field `magic_shelf_last_id` (default -1; VERSION 1-2-0 → 1-3-0; MIN_VERSION unchanged for backward-compat — pre-1-3-0 tokens parse cleanly and default the new field to -1). The arm becomes `Books.id IN magic_shelf_book_ids AND Books.id > magic_shelf_last_id`. After each batch, advance `magic_shelf_last_id` to the max id of magic-shelf books emitted. When the fold fires (cursor advances past T_magic — cache.created_at window closed), reset `magic_shelf_last_id = -1` so the next cache rebuild starts walking the arm from id=0. With the fix: round 1 emits ids 1-100, sub-cursor=100; round 2 query becomes `... OR (id IN {1..200} AND id > 100)` → ids 101-200; round 3 finds nothing, fold fires, termination. **8 RED→GREEN tests** in `tests/unit/test_kobo_magic_shelf_sub_cursor_large_shelves.py` pin: arm shape (membership + sub-cursor + `magic_shelf_arm` name); sub-cursor sourced from sync_token; advance + reset + write back to token; behavioral SQLite test reproducing PRE-FIX infinite loop (200-book scenario; round 1 == round 2 output proves the bug) AND POST-FIX correctness (all 200 magic books deliver in ≤4 rounds; mixed 200 magic + 100 regular library delivers all 300). **5 new SyncToken tests** pin the field default, round-trip, pre-1-3-0 backward-compat, garbage-input defaults, presence in built data payload. VERSION pin relaxed to `>= 1-3-0`. **447 Kobo+sync unit tests pass.** Live cwn-local: VERSION 1-3-0, magic_shelf_last_id default -1, all 4 runtime markers present; HTTP `/v1/library/sync` returns 200 with v1-3-0 token shape; decoded token data carries `magic_shelf_last_id: -1` alongside the other fields on the wire. | `513bf1cad` | v4.0.153 |
| #366 | (Greptile review on [PR #361](https://github.com/new-usemame/Calibre-Web-NextGen/pull/361) — P1 correctness finding still present through v4.0.151) | **Magic-shelf fold no longer permanently drops pending regular books when batch is full.** Greptile's review on the v4.0.147 Kobo sync cursor PR surfaced a real correctness bug carried through v4.0.151: when `magic_shelf_arm_active=True` AND the sync batch is FULL (`len(books_list) == SYNC_ITEM_LIMIT`), the cursor fold unconditionally advances cursor to `magic_shelf_membership_added_at`. Any pending regular books whose `Books.last_modified` falls between the batch's max and T_magic are then permanently dropped on every subsequent sync (their lm < T_magic so composite_keyset fails; the magic-shelf arm gate `cache > cursor` goes False because cache no longer > cursor). **Concrete failure scenario** (Greptile's): 50 magic-shelf-only books + 100 regular shelf books, all at one T_bulk; cache fresh (T_magic > T_bulk); round 1 delivers first 100; v4.0.151 fold fires; cursor jumps to T_magic; round 2: 100 remaining regular books have `lm = T_bulk < T_magic`; none match any inner-cursor arm; **100 books permanently dropped**. **Fix:** defer the fold when batch is full. New gate `batch_drained = len(books_list) < SYNC_ITEM_LIMIT`. When batch is full the next round walks the remaining ties via the composite keyset; the magic-shelf arm stays active so already-emitted magic books may re-emit (idempotent on the device — bandwidth waste only, no data loss). Once pending books drain (batch < limit), fold fires, cursor jumps past T_magic, arm goes False, termination achieved. Also addresses the Greptile P2 finding: `books_last_id` reset to -1 alongside the early sync-token reset block (when KoboSyncedBooks is empty for the user) — harmless before because `lm > datetime.min` is True for every valid book, but keeps the two cursor components consistent for future readers. **5 RED→GREEN tests** in `tests/unit/test_kobo_magic_shelf_fold_defer_when_batch_full.py`: AST pin for the named `batch_drained` flag in the fold's outer if; AST-walk pin against bare `if magic_shelf_arm_active:` regression; behavioral SQLite test reproducing PRE-FIX (Greptile's exact 50+100 scenario; 50 regulars dropped) and POST-FIX (all 150 deliver in ≤5 rounds); termination test for magic-only collections. **277 Kobo unit tests pass.** Live `cwn-local`: route returns 200; both runtime markers (`batch_drained` flag, `sync_token.books_last_id = -1` reset) verified in deployed module. V2-track: magic books re-emitting during multi-round drainage is bandwidth waste; proper fix needs a `magic_shelf_last_id` sub-cursor for keyset pagination within the arm. | `06c147812` | v4.0.152 |
| #365 | (fork issue [#359](https://github.com/new-usemame/Calibre-Web-NextGen/issues/359) v4.0.147 follow-up, reporter @recruiterguy) | **Magic-shelf books now deliver in sync-all-library mode too — closing the v4.0.147 gap.** @recruiterguy verified v4.0.147 deployed cleanly but reported magic-shelf books still don't deliver; their diagnostic showed `magic_shelf_cache.created_at` still at 2026-06-04 (unchanged across 5 post-deploy sync requests). **Root cause:** their user has `current_user.kobo_only_shelves_sync=False` (sync-all-library mode). The v4.0.147 fix gated BOTH the magic-shelf helper calls AND the membership arm on `kobo_only_shelves_sync=True`, so for sync-all users (a) `get_magic_shelf_book_ids_for_kobo` was never called → magic_shelf_cache TTL never re-evaluated → cache.created_at stayed at the time of the user's last UI shelf view (older than every subsequent device cursor); (b) the else-branch filter was bare `composite_keyset_books_only` with no third arm to let magic-shelf books with old `Books.last_modified` through. **Fix:** (1) Move `magic_shelf_book_ids` / `magic_shelf_membership_added_at` computation OUTSIDE the `if kobo_only_shelves_sync` block so the cache TTL re-evaluates on every sync regardless of mode. (2) Split `inner_cursor_filter` into `inner_cursor_filter_with_bookshelf` (only_kobo_shelves branch: composite keyset + `BookShelf.date_added` arm from #220 + magic-shelf arm) and `inner_cursor_filter_sync_all` (else branch: composite keyset + magic-shelf arm; drops `date_added` since that branch doesn't join BookShelf and referencing it would SQL-error). (3) The deletion-detection block (synced_book_ids minus allowed_book_ids) stays gated on `kobo_only_shelves_sync` — that logic is specifically for the outer-membership-filter case; in sync-all mode the user wants every book on the device. **5 new AST-based tests** in `tests/unit/test_kobo_magic_shelf_sync_all_mode_359.py` pin: helper calls are NOT inside `kobo_only_shelves_sync` block; unconditional calls exist at function scope; sync-all branch uses the new filter; sync-all filter doesn't reference `BookShelf.date_added`; only_kobo_shelves filter keeps it; deletion-detection stays gated. Pre-existing `inner_cursor_filter` pin in `test_kobo_sync_composite_keyset_cursor.py` relaxed to accept either the original single name or the split-by-branch pair. **272 Kobo unit tests pass.** Live cwn-local route returns HTTP 200 with valid sync response; all 4 runtime markers present in deployed module. | `c849b0c75` | v4.0.151 |
| #364 | (fork issue [#319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) icon-standardization sub-asks, reporter @droM4X 2026-05-28 follow-up) | **Read-toggle icon now shows action + uses matched checkbox pair.** @droM4X's 2026-05-28 thread comment surfaced two distinct asks: (a) the toggle was pairing `glyphicon-ok` (heavy checkmark) with `glyphicon-unchecked` (checkbox) — two visual languages instead of a matched pair; (b) the icon showed current state rather than the action the click would perform, so it didn't line up with the button label text. **Fix:** swap to the `glyphicon-check` / `glyphicon-unchecked` matched pair (both checkbox-shaped) across all read-status surfaces — `detail.html` button + JS handler, `index.html` grid card read badge, `image.html` cover overlay badge, `caliBlur.js` dynamic badge insertion. On the interactive detail-page button, the icon now shows ACTION: unread book shows ☑ glyphicon-check (label "Mark As Read" — click to make it checked), read book shows ☐ glyphicon-unchecked (label "Mark As Unread" — click to make it unchecked). On passive state badges (grid card, cover overlay) the icon still shows STATE — a check means "this book is read" — because there's no click context to invert. `glyphicon-ok` stays where it's semantically correct (custom-column bool=true rendering, send-button success state, book-merge "will be kept" label). **5 RED→GREEN tests** in `tests/unit/test_read_toggle_icon_action_state_319.py` pin: matched pair, action-inversion on the button, JS handler mirroring, passive-badge consistency across index.html / image.html / caliBlur.js. Pre-existing `tests/unit/test_user_hide_ux_safety_319.py` relaxed to accept either `glyphicon-check` (preferred) or `glyphicon-ok` (compat). 54 #319-related unit tests pass. **Live cwn-local + Playwright** navigated to `/book/2` (detail page) + `/?data=root` (grid), captured screenshots in `notes/fix-319-icon-action-{detail-book,grid}.jpeg`; 0 console errors; visible icon switch from glyphicon-ok to glyphicon-check on both surfaces. V2-track: caliBlur theme's read-toggle BUTTON (separate from `detail.html`'s `#toggle-read-btn`) at `caliBlur.js:1097-1100` still uses `glyphicon-eye-open` / `-ok` in its success-state flow — different code path that needs its own pass. | `b3113200c` | v4.0.150 |
| #363 | (fork issue [#218](https://github.com/new-usemame/Calibre-Web-NextGen/issues/218), reporters @yodatak + @magdalar) | **Reload-metadata button on the book detail page replaces the manual curl/Chrome workflow that two reporters were stuck on.** The `POST /admin/book/<id>/reload_metadata` endpoint shipped in v4.0.110 to pull on-disk EPUB metadata back into the catalog (for users who edit via grimmory / Calibre desktop and want CWNG to show the updated description without a full re-ingest), but the UX gap was real: @yodatak's manual curl returned HTTP 400 (no CSRF token), and @magdalar opened the URL in Chrome and got 405 (route is POST-only, Chrome sent GET). **Fix:** add a Refresh-icon button on the detail page next to Edit Metadata, gated on `current_user.role_edit()` (matches the route's `@edit_required`). Click handler POSTs with `X-CSRFToken` (CSRF gate doesn't 400 anymore), disables the button + swaps icon to hourglass during the AJAX (prevents double-submit re-parsing the EPUB twice), reloads the page on update so the refreshed title/comments/publisher/pubdate/languages render immediately, and surfaces `responseJSON.error` on failure so the user sees the route's specific error (404 missing file, 500 parse fail) rather than a generic "could not reload". **8 RED→GREEN tests** in `tests/unit/test_reload_metadata_ui_button_218.py` pin the button shape (`<button type="button">` not `<a href>` so GET-in-browser-tab can't reproduce @magdalar's 405), the `role_edit()` gate, the endpoint URL match, the `X-CSRFToken` header, the in-flight `disable`, the `location.reload()` on update, and the `responseJSON.error` surfacing. **Live cwn-local Playwright** clicked the button as `cwng84test` (edit role); network panel confirmed `POST /admin/book/2/reload_metadata` returned 200 OK; 0 console errors; screenshot in `notes/fix-218-detail-with-reload-button.jpeg`. V2-track: file-size refresh after reload (@magdalar's "Send to eReader size indicator" follow-up) — needs a separate endpoint that `stat()`s the on-disk file to update `data.uncompressed_size`. | `ca5ddc876` | v4.0.149 |
| #362 | (fork issue [#319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319) sort-dropdown sub-bug, reporter @droM4X after testing v4.0.140) | **Hidden Books page sort dropdown drops users into the unfiltered library.** Clicking any sort option (Title A→Z, Date added, Author, etc.) on `/hidden/<sort>` navigated to `/hidden_books/<sort>`, which fell into the `books_list` catch-all and rendered the whole library with no hidden filter applied — the wrong page silently. **Root cause:** `render_hidden_books` sets the body CSS class to `"hidden_books"` (not `"hidden"` — Bootstrap's `.hidden{display:none!important}` would blank the entire page; that's the v4.0.136 fix). The `_book_organizer.html` sort dropdown builds URLs via `url_for('web.books_list', data=page, sort_param=...)` so sort links become `/hidden_books/<sort>`. The dispatcher only matched `data == "hidden"`, so the `/hidden_books/...` URL fell through to the `else` branch that renders an unfiltered listing. **Fix:** `books_list` now matches `data in ("hidden", "hidden_books")` and routes both URL shapes to `render_hidden_books`. The body CSS class stays `"hidden_books"` so the Bootstrap collision fix is preserved. The `/hidden` redirect target stays canonical at `data="hidden"` — the alias only handles sort-dropdown URLs the template generates with body class as data segment. **4 RED→GREEN tests** in `tests/unit/test_hidden_books_sort_dropdown_319.py` — AST-based source pins so the `render_hidden_books` substring noise can't false-pass the `"hidden_books"` literal check; structural pin that an if/elif branch testing `data == "hidden_books"` actually calls `render_hidden_books`. Pre-existing `tests/unit/test_user_hidden_books.py::TestHideRouteShape` relaxed to accept the new `data in (...)` shape alongside the old `data == "hidden"`. **Playwright on cwn-local**: navigated to `/hidden/stored`, clicked sort dropdown "Title A → Z", URL became `/hidden_books/abc/`, page title remained "Hidden Books (0)" (correct handler); 0 console errors. **Adjacency-swept**: no other `render_*` function has the page_name/data mismatch — read/unread/archived all use the same string for both; hidden is unique because of the Bootstrap class collision. V2-track: refactor `_book_organizer.html` to accept a separate `data_segment` parameter so the body CSS class and URL data value can differ cleanly — touches every render function. | `bacaea005` | v4.0.148 |
| #361 | (fork issues [#347](https://github.com/new-usemame/Calibre-Web-NextGen/issues/347), reporters @andree392 + @shavitmichael + @Glennza1962, and [#359](https://github.com/new-usemame/Calibre-Web-NextGen/issues/359), reporter @recruiterguy) | **Two faces of one defect: the Kobo sync cursor was missing arms.** #347 — full sync never finishes when >SYNC_ITEM_LIMIT books share one `Books.last_modified` (classic bulk-import signature; @andree392's library had 4458 books all stamped 2021-03-29 13:31:44). The device's "My Books" counter climbed past 5000+ while the server re-sent the same first 50 books forever; debug log: `changed entries: 4458 / selected to sync: 50 / remaining: 4458` repeating. **#359** — Magic Shelves marked `kobo_sync=1` never delivered, even with `magic_shelf_cache` populated. The inner cursor (`last_modified > token` OR `date_added > token`) ran before the outer `(kobo_sync shelf OR magic-shelf)` filter; magic-shelf-only books had NULL `BookShelf.date_added` (not in `book_shelf_link`) and old `last_modified`, so both arms returned false and the rows were excluded before the outer membership filter saw them. **Root cause for both:** `HandleSyncRequest`'s ORDER BY is `(last_modified, id)` but the cursor was `books_last_modified` alone — no id tiebreaker. The PR #213 / CWA #1351 (@raphi011) constraint had to be preserved: adding `magic_shelf_book_ids` unconditionally to the inner OR causes an infinite `cont_sync` loop. **Fix:** `SyncToken` gains `books_last_id` (default `-1`); `VERSION` bumped `1-1-0` → `1-2-0`; `MIN_VERSION` stays `1-0-0` so devices on older tokens parse cleanly without forced re-sync (`id > -1` is True for every valid book id, so no books at exactly `cursor_lm` are dropped on the first post-upgrade sync). Both cursor sites use a composite keyset `(last_modified > cursor_lm) OR (last_modified == cursor_lm AND id > cursor_id)` — standard keyset pagination on ORDER BY `(lm, id)`. The magic-shelf branch gains a third inner-OR arm `Books.id IN magic_shelf_book_ids` gated on `MagicShelfCache.created_at > cursor_lm`; the cursor folds `magic_shelf_membership_added_at` into `new_books_last_modified` so the arm goes False on the next sync (preserves the #213 termination guarantee structurally). `new_books_last_id` resets to `-1` when the cursor is pushed past the batch's max book ts via the #220 `date_added` fold or the #359 `magic_shelf_cache` fold. `changed_entries` batch materialized once into `books_list` (was double-queried). **26 RED→GREEN tests** (`tests/unit/test_kobo_synctoken_books_last_id.py` — 11 pinning the field, default, round-trip, backward-compat with pre-`1-2-0` tokens, garbage-input defaults, VERSION/MIN_VERSION; `tests/unit/test_kobo_sync_composite_keyset_cursor.py` — 15 pinning the inline-query composition, magic-shelf arm gate, `books_last_id` cursor write, batch materialization, and a real-SQLite behavioral test that reproduces the pre-fix 150-book drop on a 250-book collision and proves the post-fix exhaustively walks @andree392's 4458-book collision in <=90 batches). 423 kobo/sync unit tests pass. **Live `cwn-local` verification:** HTTP `/v1/library/sync` returns 200 with v1-2-0 token carrying `books_last_id`; follow-up sync with the returned token does not re-emit the previously-delivered book (cursor advance correct); no errors in container logs; runtime markers confirmed `composite_keyset_books_only`, `inner_cursor_filter`, `magic_shelf_arm_active`, `sync_token.books_last_id = new_books_last_id`, `get_magic_shelf_membership_added_at` all present in the loaded module. Adjacency v2-track: reading-state cursor + archive cursor share the trap class but have much lower collision likelihood; magic-shelf collections > SYNC_ITEM_LIMIT books need a `magic_shelf_last_id` sub-cursor — see `notes/fix-kobo-sync-composite-keyset-347-359-adjacency.md`. | `180bce4db` | v4.0.147 |
| #339 | (fork-internal test pins — no user-visible behavior change) | **Six source-inspection regression pins for the no-account (fully local) Kobo sync path on firmware 4.45+.** Pins two existing-in-codebase invariants that were previously unpinned: `HandleInitRequest` rewriting `oauth_host` to a local `url_for(kobo.HandleOauthRequest)` under the `not config.config_kobo_proxy` guard (without it a no-account Kobo on firmware 4.45+ is bounced to `oauth.kobo.com` and sync fails), and the `/oauth/.well-known/openid-configuration` discovery route advertising the `issuer` / `authorization_endpoint` / `token_endpoint` fields the firmware reads before OAuth (stock CW 0.6.24 404s here — CWA #1144 by @mihneabulu shipped this at v4.0.25). These two invariants make the fully-local Kobo handshake work and were not previously source-pinned — only the sibling `library_sync` + `reading_services_host` rewrites had coverage. Highest-reach Kobo symptom cluster across both upstreams: janeczku/calibre-web#3364, janeczku/calibre-web#3218, crocodilestick/Calibre-Web-Automated#1264. No production code changes. | `5e012f0b` | v4.0.146 |
| #349 | (community / household-driven feature, no upstream issue) | **Two-way annotation work, Phases 1 and 2.** **Phase 1 (web-reader highlight CRUD):** select text in the epub reader → popup (4 color swatches + note) → Save creates a `source='webreader'` highlight that paints as an overlay and persists. Clicking a highlight reopens the popup for recolor / edit / delete. Routes hang off the existing `annotations` blueprint with IDOR guard (`_find_owned_annotation` scopes by user+book+annotation_id), CSRF tokens forwarded via `X-CSRFToken`, and Hardcover fan-out when that sync target is enabled. Soft-delete (no row drop) — the existing tombstone flow handles propagation to sync targets. **Phase 2 (KOReader↔server annotation bridge):** `GET/PUT /kosync/syncs/annotations` on the existing kosync blueprint (reuses Basic auth + checksum→book resolution, no new credentials). A `device_origin_id` column + idempotent PRAGMA-guarded migration suppresses feedback loops. The cwasync.koplugin gains a `KoboReader.sqlite` write provider so the stock Kobo reader renders bridged highlights. **Verification:** 20+ unit tests for the Phase 1 endpoints (create/edit/delete/IDOR/Hardcover-fanout/CSRF/i18n); over-the-wire HTTP cycle on `cwn-local`; Playwright on the live kepub reader (selection → popup → save → overlay paint, screenshots in `notes/feat-annotation-phase1-*.png`); Phase 2 verified against a real `KoboReader.sqlite` (1,771 device highlights — found + fixed a missing NOT NULL `EndContainerChildIndex` the protocol docs hadn't documented, with the INSERT now passing every NOT-NULL gate and `INSERT OR IGNORE` proven idempotent); Phase 2 Nickel render verified on real hardware 2026-05-26 (a server highlight inserted via the provider's exact 17-column INSERT appears in the book's Annotations list and on the page in Nickel after a safe-eject). Greptile 3/5 with two P1 findings (push-callback ignored success flag; device-backup unguarded) — both addressed in the merge with the push callback branching on `ok2` to show "Server push failed" and a module-level `_backed_up` guard so a single KOReader run snapshots `KoboReader.sqlite` once instead of per-call, pinned by `tests/unit/test_annotation_push_toast_and_backup_guard.py`. Phase 3 (server→stock-Nickel wifi push) deliberately excluded — disproven on hardware. | `c3454592` | v4.0.146 |
| #356 | (community PR by @sarsey-walker) | **Three bundled hardening fixes: Hardcover auto-fetch session stability, "Use via HTTPS" config persistence, and TLS-aware container healthcheck.** (1) **Hardcover auto-fetch**: long scans could fail with detached-ORM-instance errors and contaminate the global `ub.session` from a background worker thread (poisoning later web requests). The task now queries book IDs (not full `Books` objects), re-fetches each book in its own short-lived scope inside `_process_book`, and never reaches into `ub.session` from the worker. Avoids the N+1 reload regression from an earlier fix attempt. (2) **"Use via HTTPS" persistence**: the admin toggle existed in the model and the UI but the Basic Configuration save handler never persisted it. Saving now writes the checkbox state, and `apply_https_runtime_config` re-applies the cookie-security flags to the running process so the change takes effect without a restart. The shared helper is the single source of truth — used at boot and after every save. (3) **TLS-aware container healthcheck**: when CWA serves HTTPS directly via cert/key, the previous HTTP probe spammed `Gevent SSL Error: [SSL: HTTP_REQUEST]`. The new `/usr/local/bin/cwa-healthcheck` helper reads the configured cert/key from `app.db`; if both files exist on disk it probes `https://` with `-k` (the cert is the server's own and curl has no CA trust for it); otherwise stays on `http://`. The HEALTHCHECK CMD invariants the existing tests pinned (`/health` target, `CWA_PORT_OVERRIDE` default 8083, bounded `--connect-timeout` 1s + `--max-time` 2s) are preserved — they just moved from inline curl to the helper script. `tests/unit/test_dockerfile_healthcheck.py` now inspects the union of both files so the same invariants stay enforced wherever they live. | `6bb68457` | v4.0.146 |
| #355 | (fork issue [#354](https://github.com/new-usemame/Calibre-Web-NextGen/issues/354), reporter @Neuromancien59) | **The admin Configuration page stops returning 500 after a long-running upgrade.** Reporter on Debian/x86 v4.0.144 hit `GET /admin/config` → HTTP 500 (`UndefinedError: No first item, sequence was empty`) with startup log `Failed to initialize OAuth blueprints: no such column: oauthProvider.oauth_authorize_url`. **Root cause** (`cps/ub.py:1386` — `migrate_oauth_provider_table`): the migration probed one column (`oauth_base_url`) as a stand-in for its five-column group and added the group only if that probe threw, so a DB with `oauth_base_url` present but the rest of the group missing — the exact partial state a multi-release upgrade produces — passed the probe and the missing columns were never added. The second column group (`metadata_url` proxying for `scope`/`username_mapper`/`email_mapper`/`login_button`) had the same shape, and both ALTERs ran as one all-or-nothing batch. OAuth init then failed on the missing column at boot, `oauth_bb.oauthblueprints` was left empty, and `config_edit.html` did `provider \| selectattr(...) \| first` on the empty sequence → 500. **Fix:** rewrite the migration to introspect the live schema with `sa_inspect` and ADD only the columns actually missing, one ALTER per statement, tolerating `duplicate column name` per column so a concurrent gunicorn-worker boot can't abort the loop mid-way. `config_edit.html` falls back to `{}` when no generic provider is configured (`(... \| first) or {}`) so the page renders even if OAuth init ever leaves the list empty (defense-in-depth — once the migration is fixed the list is populated, but the page should never 500 on an empty list regardless of the cause). **6 regression tests** in `tests/unit/test_354_oauth_provider_migration.py` (real in-memory SQLite engines) pin partial-group-1 repair, partial-group-2 repair, double-run idempotency, all-old-schema repair, all-new-schema no-op, and the duplicate-column mid-loop race — the first three RED on `main` and GREEN on this branch (matching the reporter's exact error string). Adjacency: `migrate_user_session_table` shares the probe-by-proxy shape but the partial-state trigger is structurally closed there (both columns landed in the same upstream commit); documented as v2-track in `notes/fix-354-adjacency.md`. | `1ad232e8` | v4.0.145 |
| #353 | (fork issue [#352](https://github.com/new-usemame/Calibre-Web-NextGen/issues/352), reporter @SethMilliken) | **Book title/author text is selectable again on Safari, and the merge script no longer runs on the book detail page.** PR #161's drag-and-drop merge set `draggable="true"` on the whole `.book` card; Safari/WebKit suppresses native text selection of every descendant of a draggable ancestor, so users couldn't select the title or author in the grid view. The same script also matched the lone `.book` element rendered on the detail template and added a pointless drag source there. **Fix** (`cps/static/js/drag-drop-merge.js`): tighten `BOOK_SELECTOR` from `.book` to `.book.session` so init only runs on the grid (index / search / author / shelf render `book session`; the detail page renders `book blur ...` without `session`); move the drag SOURCE from the whole card onto the inner `.cover` element so the `.meta` column's title/author text stays selectable; keep the drop TARGET on the whole card so the source cover can be released onto any part of the target. Drag handlers walk up via `getBookEl(e.target).closest(BOOK_SELECTOR)`, so merge logic is unchanged. Defensive `console.warn` on the else path if a future template ever renders `.book.session` without a `.cover` (caught by Greptile). **Verified cross-engine on cwn-local** (`scripts/manual/probe_352_drag_merge.py`, WebKit desktop + WebKit mobile + Chromium desktop): cards no longer carry `draggable`, covers do, `range.selectNodeContents(title)` returns the title text (the reporter's literal complaint), and the detail page has zero `drag-merge-enabled` elements. **4 source-pin tests** in `tests/unit/test_352_drag_merge_text_selection.py` pin the selector tightening, the card NOT being draggable, dragstart binding moved off the card, and the drop target staying on the card — RED on the PR #161 baseline, GREEN with this fix. Full unit suite 1223 passing. Out-of-scope follow-up (V2-track): an opt-in toggle with a visible "merge mode" indicator (the reporter's third ask) — A+B materially address the UX-hijacking objection by restoring text selection and removing the broken detail-page init, but a default-off feature flag remains a separate product decision. | `1bb367e0` | v4.0.144 |
| #348 | (fork issue [#336](https://github.com/new-usemame/Calibre-Web-NextGen/issues/336), reporter @batiti93) | **Documented why OPDS downloads land on KOReader as `Author - Title.epub` while the CWA library names files `Title - Author.epub`, and the one-checkbox fix.** Not a server bug: the OPDS download already sends the library name in `Content-Disposition` (verified on a live container: `filename=...Title - Author.epub`). KOReader's `OPDSBrowser:getFileName` (opds.koplugin/opdsbrowser.lua) builds `item.author .. " - " .. item.title` from the catalog entry and ignores the header by default; it only uses the server name when the catalog's **Use server filenames** toggle (`raw_names`) is on, which defaults off. Swapping `<author>`/`<title>` in the feed would change KOReader's output but corrupt the catalog display + every other OPDS client, so the resolution is the KOReader setting, not a feed change. Added a note to the README KOReader-sync section so users hitting the same `Author - Title` rename find the **Use server filenames** fix in one search. **3 source-pin tests** in `tests/unit/test_336_opds_filename_docs.py` (RED before the note, GREEN after): README mentions the setting, contrasts both filename orderings, and the guidance lives in the KOReader section. Reporter also got the full explanation + exact KOReader steps on the issue thread. | `5e6bd75b` | v4.0.143 |
| #344 | (fork issue [#343](https://github.com/new-usemame/Calibre-Web-NextGen/issues/343), reporter @Oakwhisper) | **Book covers overlapped the title text on Safari (main books grid).** On Safari/WebKit — desktop AND mobile — the covers on the books grid overlapped the title (no spacing between cover-bottom and title-top) and the hover-shading overlay looked misaligned; Chromium rendered correctly. **Root cause**: the cover anchor `#books > .cover > a` (+ `#books_rand` / `.isotope-item` variants) is `display: inline-block` with no `vertical-align`, so it defaults to `baseline`. It wraps a block-level `<img>`, and WebKit's baseline+descender computation for an inline-block-wrapping-a-block places the following `.meta` block ~7px too high (Chromium reserves +10px for the identical markup). **Fix**: one property — `vertical-align: top` on that rule in `caliBlur.css` — removes the baseline dependency the two engines disagreed on. **Verified** with Playwright across WebKit + Chromium, desktop + mobile, on cwn-local: cover-bottom→title-top gap went from −7px (webkit desktop) / −6px (webkit mobile) → **+10px everywhere**, matching Chromium; hover overlay aligns with the cover; 0 console errors; index/search/author grids all confirmed. The fix selector covers the shared `index.html` grid (index / search / author / series / category / discover / hidden-stored); the book detail page uses `.single .cover` and is untouched. **2 source-pin tests** in `tests/unit/test_343_safari_cover_alignment.py` (RED before, GREEN after) tie the fix to the exact rule + guard the `display:inline-block`/`height:100%` the layout needs. Cross-engine repro harness at `scripts/manual/measure_cover_grid.py`. Greptile review applied (CSS trailing semicolon; env-var the harness test creds). | `8b8a57de` | v4.0.142 |
| #1 | (audit; user-reported #1206 / #1078 / #1097 / #1270 / #1329) | Revert hardcoded Safari skip in `uploadprogress.js` so `$.fn.uploadprogress` stays defined and `main.js` doesn't crash on Safari | `3b7d70c` | v4.0.7 |
| #77 | [#71](https://github.com/new-usemame/Calibre-Web-NextGen/issues/71) | Interface-language switcher: fix `compile_translations.sh` aborting at the first broken `.po` (was shipping 6/28 locales), drop redundant `Locale('en')` prepend in `cw_babel.py` (was duplicating English), repair fatal msgfmt errors in `es` and `ru` `.po` files | `5ff8cfbd` | v4.0.24 |
| #132 | (audit 2026-05-11, item B2) | Kobo reading-state race: two concurrent PUTs to `/v1/library/<uuid>/state` from the same device (legitimately happens on wake-from-sleep) could race past the read-then-write check in `get_or_create_reading_state` and both INSERT, leaving duplicate `(user_id, book_id)` rows. The next `.one_or_none()` then raised `MultipleResultsFound` → 500 to the device, which retries forever. Two-part fix: (1) `UniqueConstraint(user_id, book_id)` added to `ReadBook`, `KoboReadingState`, `KoboSyncedBooks`, `ArchivedBook` (model + `CREATE UNIQUE INDEX IF NOT EXISTS` via a new `migrate_kobo_unique_constraints` migration that dedupes existing duplicates with sensible winner rules — newest LM wins for KoboReadingState, status-level + LM for ReadBook, archived-wins for ArchivedBook — reparents KoboReadingState's bookmark/statistics children into the winner, sums `times_started_reading` across ReadBook losers so the user's read counter is preserved); (2) `get_or_create_reading_state` rewritten to `INSERT ... ON CONFLICT(user_id, book_id) DO NOTHING` via the sqlite dialect, so the race can't produce a duplicate even before the DB constraint trips. Migration is gated by a marker file in `CONFIG_DIR/.cwa_migrations/` so it runs at most once per install. 29 regression tests pin: model constraints declared, UNIQUE index enforced, dedupe rules per table, child reparenting, bookmark merge, idempotent marker behavior, concurrent-insert threaded test (16 threads, exactly one row), source-pin AST that `get_or_create_reading_state` uses `sqlite_insert + on_conflict_do_nothing` and not the legacy `.one_or_none()`. | `de2bd2e3` | v4.0.45 |
| #133 | (audit 2026-05-11, item B4) | Kobo per-callsite `common_filters` policy on `get_book_by_uuid`. Upstream's `calibre_db.get_book_by_uuid` bypasses `common_filters`, so the Kobo blueprint was handing back books the user's web UI would have hidden (denied-tagged, language-filtered, custom-column-restricted, or per-user-hidden via PR #105). Five callsites in `cps/kobo.py` audited; new helper `get_book_by_uuid_for_kobo(uuid, *, enforce_policy)` on `CalibreDB` makes the policy explicit. ENFORCE on the two policy-boundary surfaces (`HandleMetadataRequest` GET, `add_items_to_shelf`) — denied/hidden books shouldn't expose metadata via Kobo or leak onto a shared kobo-synced shelf. DO NOT enforce on device-trailing or destructive user-initiated surfaces (`HandleStateRequest` GET/PUT, `HandleTagRemoveItem`, `HandleBookDeletionRequest`) — reading-state must keep syncing for already-on-device books even if they later become denied, and cleanup operations must never block. `allow_show_archived=True` on the enforced path because `ArchivedBook` doubles as the Kobo's device-deletion track. Decision matrix documented in `notes/KOBO-B4-COMMON-FILTERS-POLICY.md`. 12 regression tests pin: helper contract (kwarg-only `enforce_policy`, branches on flag, passes `allow_show_archived=True`), per-callsite policy via `inspect.getsource` so a refactor can't silently flip the policy, audit invariant that no Kobo callsite still uses unfiltered `calibre_db.get_book_by_uuid`, behavioral mocks that prove `common_filters` is invoked iff `enforce_policy=True`. | `566d5227` | v4.0.45 |
| #136 | (fork issue [#125](https://github.com/new-usemame/Calibre-Web-NextGen/issues/125), reporter @SpookyUSAF) | Admin "Check for Update" button now actually queries this fork's releases. PR #28 (v4.0.8) had only fixed the s6-init bootstrap probe; `cps/updater.py` was still hardcoded to `janeczku/calibre-web`, so users on the fork saw "no update available" forever even when newer fork releases existed. Two changes: (1) `_REPOSITORY_API_URL` now resolves through the same `CWA_RELEASE_REPO` env contract PR #28 introduced (default `new-usemame/Calibre-Web-NextGen`); (2) new `_normalize_tag()` helper accepts both `vX.Y.Z` (fork) and `X.Y.Z` (upstream) and returns None on unparseable tags. The pre-fix `int(tag.split('.')[0])` raised `ValueError` on every fork tag — once the URL pointed at the fork, the check would have crashed. 12 regression tests pin the default fork target, the env override, three tag parsing shapes, garbage rejection, and source-level invariants on the version-comparison branch. | `13f75aa0` | v4.0.46 |
| #137 | (fork issue [#121](https://github.com/new-usemame/Calibre-Web-NextGen/issues/121), reporter @droM4X, tested on Readest) | Three independent OPDS bugs. **(1) Log spam:** every OPDS catalogue fetch logged `OPDS Login failed for user ""` at WARN because clients commonly issue an unauthenticated probe before sending creds. `verify_password` now short-circuits on empty username — no DB query, no WARN. Real auth failures still log normally. **(2) Anon+auth multiplexing:** when anonymous browsing was enabled and the client sent stale credentials, the server returned 401 instead of falling back to the Guest catalog. `requires_basic_auth_if_no_ano` now retries with the synthetic Guest credentials when the first attempt fails AND `config.config_anonbrowse` is on. Auth-only deployments keep their 401 behavior unchanged. **(3) Three OPDS root labels rendered in English on translated UIs:** "Alphabetical Books", "Recently added Books", "Magic Shelves" — `OPDS_ROOT_ENTRY_DEFS` held bare Python literals so pybabel-extract never picked them up. Every title + description now wrapped with `N_()` (`flask_babel.lazy_gettext`); next extract cycle lands them in `messages.pot`. Existing translations for the older entries unchanged. 7 regression tests pin all three fixes including a behavioral mock asserting `log.warning` is never called for the empty-username probe. | `0edf8414` | v4.0.46 |
| #138 | (fork issues [#125](https://github.com/new-usemame/Calibre-Web-NextGen/issues/125) follow-up + [#121](https://github.com/new-usemame/Calibre-Web-NextGen/issues/121) finish) | Two follow-ups discovered during deep verification of v4.0.46. **(a) Inline update indicator** on the admin Version Information table — PR #136 fixed the in-fork "Check for Update" button, but Docker builds disable `feature_support['updater']` so the button never renders for Docker users (≈everyone on the GHCR image). New `cwa_get_update_indicator()` helper in `cps/admin.py` wraps the existing `cwa_update_available()` and feeds `cwa_is_outdated` + `cwa_latest_tag` into the admin template; the Version Information row now reads "Update available: vX.Y.Z — `docker pull ...`" when outdated, falling back to the original "Current Version" label otherwise. Exception-safe so a version-probe glitch never breaks the admin page render. **(b) Hungarian translations** for the four OPDS root labels that previously rendered in English even after #137 wrapped them with `N_()` — "Alphabetical Books" → "Könyvek ábécé sorrendben", "Recently added Books" → "Nemrég hozzáadott könyvek", "Random Books" → "Véletlenszerű könyvek", "Magic Shelves" → "Varázspolcok". Verified end-to-end against the local container with admin user `locale='hu'` via Basic auth → 16/16 root entries render translated, survives cold container restart. 6 regression tests pin the indicator helper, route, and template surface. | `23b1b2da` | v4.0.47 |
| #140 | (deep-verification follow-up of v4.0.47) | v4.0.47 shipped without `hu/messages.mo` because an obsolete `#~ msgid "Random Books"` block in `hu.po` collided with the new active entry — `msgfmt` rejected the file and `scripts/compile_translations.sh` tolerated the per-locale failure, so production Hungarian fell back to English. Two fixes: (1) deleted the obsolete `#~ msgid` block (commented-out anyway); (2) new `tests/unit/test_translations_compile.py` parametrized over all 30 shipped locales runs plain `msgfmt <po> -o <mo>` (matching production strictness, not `--check`) so a malformed `.po` triggers a unit-test failure long before CI builds the Docker image. Post-fix verification: pulled `ghcr.io/new-usemame/calibre-web-nextgen:v4.0.48`, confirmed `hu/messages.mo` (155842 bytes) ships and all 4 Hungarian OPDS labels resolve via `gettext.translation('messages', languages=['hu'])` inside the production container. | `eadb18ee` | v4.0.48 |
| #142 | (community contribution from @droM4X) | Hungarian translation completion: 90 strings translated across `cps/cwa_settings.html` (Auto-Convert / Auto-Ingest / Magic Shelves on Kobo / Hardcover Auto-Fetch panels) and Kobo settings descriptions, plus the existing batch from #138 reverified. Verified `msgfmt` compiles cleanly (174812 byte `.mo`); the regression test from #140 runs against every shipped locale to catch obsolete-msgid collisions. | `2ef428d2` | v4.0.49 |
| #143 | (fork issue [#141](https://github.com/new-usemame/Calibre-Web-NextGen/issues/141), reporter @droM4X) | User-facing template URLs pointed at upstream CWA. Three sites updated: `layout.html` "See Changelog" in the in-app update banner (the one @droM4X reported), `http_error.html` "Create Issue" on internal error pages (bug reports should land in *our* tracker), `admin.html` "CWA GitHub" button on the admin panel. Two upstream references intentionally stay (`stats.html` attribution; `config_db.html` wiki link — we don't have an equivalent docs page yet) and are listed in `ALLOWED_UPSTREAM_REFS` so the new `tests/unit/test_template_fork_urls.py` scan-test catches future cherry-pick regressions without fighting legitimate credit. 4 regression tests pin the broad scan plus each of the three offending sites individually. | `14f1e81c` | v4.0.49 |
| #144 | (fork issue [#139](https://github.com/new-usemame/Calibre-Web-NextGen/issues/139), reporter @droM4X) | "Update Translations and Wiki Status" workflow was failing every run on `git push` with `GH013: Repository rule violations found for refs/heads/main` because `github-actions[bot]` is not in the main-protection ruleset's admin-bypass list. README translation-status table had been stale since the v4.0.46 merge wave. Workflow now uses `secrets.GH_PAT \|\| secrets.GITHUB_TOKEN` for checkout and commits under new-usemame's identity (which IS admin-bypass eligible). Two locale fixes surfaced in passing: `es.po:1519` had `%(size)s` localized to `%(tamaño)s` (Spanish users hitting the oversized-cover error path would have crashed on `KeyError`), `sk.po:554` translated `"{} User Successfully Imported"` without the `{}` placeholder (Slovak admins importing LDAP users saw the count silently disappear). All 30 locales now pass `msgfmt --check`, not just plain `msgfmt`. | `a2351350` | v4.0.49 |
| #145 | (fork issue [#139](https://github.com/new-usemame/Calibre-Web-NextGen/issues/139) follow-up) | #144 assumed `GH_PAT` was already configured as a repo secret; it wasn't, so the workflow still hit GH013. Personal-repo rulesets refuse to add the GitHub Actions integration as a bypass actor (`Actor GitHub Actions integration must be part of the ruleset source or owner organization`). Workflow now gates the commit + wiki-sync steps on a `Detect commit PAT` step (mirrors the existing wiki-PAT detection pattern). When `GH_PAT` is unset: extract/compile/validate translations, trigger dev build, notice "GH_PAT not set; README won't auto-update", exit 0. When set: full direct-push behavior. Either way, the Actions dashboard stops being red — which is the symptom @droM4X actually flagged. README auto-update remains a follow-up that requires the operator to add `GH_PAT` as a repo secret. | `1c73a2e8` | v4.0.49 |
| #174 | (fork issue [#162](https://github.com/new-usemame/Calibre-Web-NextGen/issues/162), reporter @magdalar) | **Nightly `processed_books/fixed_originals/*.zip` written as `root:root` instead of `PUID:PGID`**. The `cwa-auto-zipper` s6 longrun service called `python3 /app/calibre-web-automated/scripts/auto_zip.py` directly while every other periodic service (`cwa-ingest-service`, `cwa-checksum-backfill`, `cwa-preview-cache-cleanup`) wraps its application Python with `s6-setuidgid abc`. The auto-zipper ran as root, so the `.zip` archives it produced had the wrong owner — breaking host-side cleanup, rsync, and backup workflows for any deployment where the container PUID isn't 0. The `.epub` files in the same directory landed correctly under PUID:PGID because they come from `cwa-ingest-service`, which already drops privileges. Fix is a one-line wrap of the loop's auto_zip.py invocation with `s6-setuidgid abc`. New `tests/unit/test_s6_service_privilege_drop.py` source-pins the regression (negative test verified — fails on the pre-fix line). End-to-end reproduced inside `cwn-local`: ran `auto_zip.py` against a /tmp test tree first as root (produces `root:root` zip) and then under `s6-setuidgid abc` (produces `abc:dialout` zip, mapping to PUID=501 PGID=20). Bind-mounted `/config` couldn't be used for the live test because Docker Desktop on macOS squashes virtiofs ownership to root regardless of process uid — `/tmp` (container layer) bypasses that quirk and exercises the real privilege-drop. Broader audit of similar services tracked in `notes/s6-privilege-drop-audit.md` (e.g. `metadata-change-detector` is structurally root-elevated but `cover_enforcer.py` chowns its own outputs — needs per-service analysis, not bundled here). | `84a7c0ba` | v4.0.55 |
| #167 | (fork issue [#147](https://github.com/new-usemame/Calibre-Web-NextGen/issues/147), reporter @iroQuai) | **Container won't start on v4.0.49+, gevent_wsgi NoneType crash**. `cps/gevent_wsgi.py` overrides `pywsgi.WSGIHandler.format_request` (introduced upstream in commit `1d7dcce98d`, 2024-07; byte-identical in our codebase across v4.0.44–v4.0.53). gevent calls `format_request` for *all* requests including those that fail HTTP parsing — TLS ClientHello on the plain-HTTP port (Traefik v3 sending TLS bytes), malformed headers, etc. For those, `get_environ()` is never invoked, so `self.environ` stays `None` and `self.environ.get(...)` raises `AttributeError: 'NoneType' object has no attribute 'get'`. The reporter's "worked on .44, broke on .49" memory turned out to be partial — what changed in their environment was either healthcheck probe frequency or the Dockerfile `HEALTHCHECK`'s `https://localhost:8083` TLS fallback starting to fire, not anything in our code. Fix is a one-line None guard on `self.environ.get(...)`. New `tests/unit/test_gevent_wsgi_format_request.py` pins the 4 cases (environ=None → client_address fallback, X-Forwarded-For present, empty environ, all-None edge). Live repro on cwn-local: `printf '\x16\x03\x01\x00\x05hello\n' \| nc localhost 8086` produced the exact traceback before the fix; post-fix returns HTTP 400 with no traceback. Stress: 30 parallel TLS-bytes requests → 0 tracebacks, container stays healthy. Cold restart healthy in 7s. | `126f22aa` | v4.0.54 |
| #191 | (fork release follow-up to v4.0.60 rebrand) | **Existing installs still showed the old brand in the navbar and browser tab after v4.0.60.** The v4.0.60 rebrand (#189) changed the column default in `cps/config_sql.py:69` to `Calibre-Web NextGen`, but SQLAlchemy column defaults only apply on row insert — existing `settings` rows carried whatever default was in place when they were first written (`Calibre-Web` for users migrating from stock CW, `Calibre-Web Automated` for users coming from CWA). One-time idempotent migration in `scripts/migrate_brand_title.py` invoked from `cwa-init/run`. Normalizes the current title to `[a-z0-9]` and rebrands iff the normalized form matches one of the two known legacy defaults (`calibreweb`, `calibrewebautomated`); `NULL`, empty, and whitespace-only are treated as legacy. Anything else (custom titles like `Alice's Library`, or already on the new brand) is left alone. The cwa-init wrapper logs but does not abort on non-zero exit, so a migration error never blocks startup. **32 unit tests** cover the legacy-variant matrix (case + hyphen agnostic — `Calibre-Web`, `calibre web`, `CALIBRE-WEB AUTOMATED`, etc.), custom-title preservation (`Alice's Library`, `Sample Family Library`, `Calibre-Web Automated v2`, etc.), `NULL` / empty / whitespace handling, idempotency, and the CLI exit-code + stdout contract. Live-verified end-to-end on the v4.0.60 teenyverse container: legacy `Calibre-Web` → `Calibre-Web NextGen` update, `Sample Family Library` preserved, second run on already-migrated DB is a no-op. | `2432db20` | v4.0.61 |
| #199 | (fork issue [#192](https://github.com/new-usemame/Calibre-Web-NextGen/issues/192), reporter @magdalar; same class as upstream CWA [#1082](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1082)) | **Concurrent auto-import of large epubs on `NETWORK_SHARE_MODE=true` crashed the web service to persistent 404s, required `docker compose restart`.** Four contributors, each addressed: (1) post-ingest `/cwa-internal/reconnect-db` enqueued `TaskReconnectDatabase` via `WorkerThread` — the worker disposed the shared SQLAlchemy engine while in-flight request greenlets were using it. Replaced with a synchronous `CalibreDB.refresh_for_new_data()` classmethod that expires loaded objects + rolls back the held read transaction across every instance; engine is never touched. (2) No application-level coordination between the Flask app and the `ingest_processor` subprocess on `metadata.db` writes. SQLite's fcntl locks are unreliable on mergerfs / SMB / NFS / Docker Desktop virtiofs. New `cps/services/calibre_db_lock.py` exposes a process-shared advisory flock at `/config/.cwa-metadata-write.lock` (local Docker volume, always supports flock). Applied around `calibre_db.session.commit()` in `editbooks.do_edit_book` and around `calibredb add` in `ingest_processor.add_book_to_library`. (3) `setup_db` + `check_valid_db` did not set `PRAGMA busy_timeout`; SQLAlchemy's `connect_args timeout=30` only applies at connect time. Added explicit `PRAGMA busy_timeout=60000`. (4) `calibredb add` failure was terminal — first transient lock moved the book to `/processed_books/failed`. New `_run_calibredb_add_with_retry` detects `'database is locked'` / `'BusyError'` in stderr and retries 4 attempts with 2s/4s/8s exponential backoff; non-lock errors surface immediately. **13 RED→GREEN regression tests** in `tests/unit/test_metadata_db_write_coordination.py` including a two-Python-process subprocess test of the flock cross-process semantics. Live-verified on `cwn-local` with `NETWORK_SHARE_MODE=true`: 13 large epubs across 4 concurrent ingest waves + an Edit Book POST during a wave + 24 parallel HTTP requests during another wave — all ingests succeeded, web `/health` stayed 200 throughout, final state 0 in `/processed_books/failed`. | `840c52da` | v4.0.62 |
| #200 | (fork issue [#198](https://github.com/new-usemame/Calibre-Web-NextGen/issues/198), reporter @goatdancer4000-sudo) | **Admin panel "NextGen Discord Server" button was still linking to the upstream CWA Discord (`discord.gg/EjgSeek94R`) — leftover from before the rebrand.** Repointed at our community-maintained NextGen Discord server (`discord.gg/B8NXZmcp32`, permanent invite, no expiry, unlimited uses). Same sweep across the rest of the repo: `tests/README.md` (developer "Ask in Discord" link) and the V3-era CWA snapshots in `changelogs/V3.0/` (README_V3.md two occurrences, V3-reddit-release-post.md one) repointed too, so anyone landing on a historical doc via search isn't sent to the old server. Net: zero references to `discord.gg/EjgSeek94R` anywhere in `repo/` after this release. Verified end-to-end: source-pin (`grep` on repo returns 5 hits for new link, 0 for old), Docker rebuild + container restart, admin session GET `/admin/view` renders `href="https://discord.gg/B8NXZmcp32"`. Discord server itself was built out via the `CWNG Bot` (Application ID `1505060729600868474`) — 5 categories, 13 text channels, 3 hoisted roles, welcome message pinned, server settings (verification=Medium, default-notifications=Only-mentions). | `b8397c96` | v4.0.63 |
| #210 | (fork issue [#134](https://github.com/new-usemame/Calibre-Web-NextGen/issues/134), reporter @Onkeyuk; follow-up trace by @kanjieater, CWA [#1256](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1256) original reporter; regression vector identified by @navels) | **Web UI 504s under sustained ingest on large libraries (~10k+ books) — default duplicate-scan debounce was too aggressive.** CWA upstream commit `fe60df7` (2026-01-29) lowered `duplicate_scan_debounce_seconds` from 30 → 5 to make duplicate-detection notifications appear faster after multi-book ingest. For users with 10k+ book libraries the 5s default produces stacking SQL prefilter queries (`find_duplicate_candidate_ids_sql`) that contend for the GIL with concurrent SQLAlchemy session-init paths (which call `conn.create_function` to register the `title_sort`/`lower` UDFs on the SQLite connection). The collision deadlocks the gevent hub. Default reverted to 30 in all five mirrored sites — schema literal (`scripts/cwa_schema.sql`), three Python fallbacks (`cps/cwa_functions.py` web process, `cps/editbooks.py`, `scripts/ingest_processor.py`), and the Jinja template fallback (`cps/templates/cwa_settings.html`). Runtime lower-bound clamp raised from 5 → 10 across all three clamp sites so users typing 5 through the admin UI get clamped to 10. One-time DB migration `_migrate_duplicate_debounce_5_to_30_v1` in `scripts/cwa_db.py`'s `CWA_DB.__init__` bumps existing installs from 5 → 30 once, gated by a marker file in `CONFIG_DIR/.cwa_migrations/duplicate_debounce_5_to_30_v1.applied`. Users who customised the value to anything other than 5 (e.g. 15, 60, 120) are not touched. 13 regression tests in `tests/unit/test_duplicate_debounce_default_regression.py` pin the schema literal, four Python fallbacks, three clamp sites, fresh-install behaviour, three migration variants (5 → 30, 15 preserved, 60 preserved), and the marker-idempotency contract. Live-verified on cwn-local: pre-restart DB had `duplicate_scan_debounce_seconds=5`; post-restart DB shows 30 + marker written; second restart is a no-op. Runtime clamp endpoint POST `delay_seconds=3` returns 10 (floor), `delay_seconds=700` returns 600 (ceiling), no delay returns 30 (default). Playwright UI pilot on `/cwa-settings` shows `value=30, min=10, max=600` with no console errors. This is a tactical regression revert — the underlying GIL+SQLite deadlock pattern still exists, just much less likely to trip under default settings. The architectural fixes live in upstream CWA PRs [#1349](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1349) (defer post-import reconnect + duplicate-cache invalidation until batch settles) and [#1353](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1353) (incremental duplicate index) and will be evaluated in follow-up cycles. | `71c799b5` | v4.0.65 |
| #212 | (fork issue [#134](https://github.com/new-usemame/Calibre-Web-NextGen/issues/134) follow-up to v4.0.65; root cause behind CWA [#1256](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1256)) | **Root-cause fix for the GIL+SQLite deadlock vector.** v4.0.65 reduced the collision frequency (debounce 5 → 30); this release eliminates the deadlock pattern structurally by moving `conn.create_function()` out of every request-handler hot path. With `StaticPool` sharing one SQLite connection across all threads, the pre-fix code called `create_functions` mid-request from `cps/web.py:get_matching_tags` (tag autocomplete — extremely frequent), `cps/editbooks.py` ×2 (edit-book entry + after format upload), `cps/admin.py:912` (admin config save when `config_title_regex` changes), `cps/helper.py:364` (set_read_status), `cps/search.py:250`, plus three internal db.py hot paths (`get_typeahead`, `check_exists_book`, `search_query`) and `init_session` for every new SQLAlchemy session. Each call held the GIL while invoking SQLite's C `create_function` — concurrent worker thread mid-`func.lower` query needed the GIL to invoke the UDF, and SQLite's connection mutex blocked both threads. New module-level `_register_sqlite_udfs(dbapi_connection, _connection_record)` in `cps/db.py` registers `lower`/`uuid4`/`title_sort` once via SQLAlchemy `event.listen(engine, 'connect', ...)` — wired on both `check_engine` (in `check_valid_db`) and the main `cls.engine` (in `setup_db`). `title_sort` reads `CalibreDB.config.config_title_regex` via closure at call time, so admin updates to the regex take effect on the next call without re-registration; the admin route at `cps/admin.py:912` now calls `db.CalibreDB.update_config(config)` to propagate the new regex without touching connection state. All 6 request-handler callsites + 3 internal db.py hot paths + the `init_session` call removed. `create_functions(self, config=None)` preserved as a backward-compat no-op shim. `title_sort` hardened: returns `''` on `NULL`, falls back to `strip_whitespaces` on malformed regex, never raises. 29 new regression tests across `tests/unit/test_sqlite_udf_registration_listener.py` (17 — source-pin every callsite removed, behavioural verification UDFs work without explicit registration, title_sort closure adapts to live config, concurrency smoke with 32 + 16 mixed threads) and `tests/unit/test_duplicate_debounce_migration_edge_cases.py` (12 — boundary value parametrised, empty table handling, marker write failure non-fatal, two concurrent CWA_DB constructors converge to 30, init-order pin). Live-verified on a fresh `cwng-udf-verify` container based on v4.0.65 image with patches applied via `docker cp`: 50 concurrent `/get_matching_tags` requests during an active duplicate scan all returned HTTP 200 in 1 second total, log scan shows the cwa-duplicates SQL prefilter ran cleanly with no "database is locked", no "no such function", no traceback. The deadlock vector is now structurally absent — no production code path calls `conn.create_function()` on a hot path. | `7b31d7e1` | v4.0.66 |
| #215 | (fork issue [#147](https://github.com/new-usemame/Calibre-Web-NextGen/issues/147) third gap; reporter @iroQuai persistence) | **Container falsely reported HEALTHY by Docker / k8s / Compose when the Calibre library was unmounted, empty, or unreadable.** The Dockerfile HEALTHCHECK probes `/health` with `curl -fsS`, but `admin.before_request` was 302-redirecting `/health` to `/admin/db_configuration` whenever `config.db_configured` was False — `curl -fsS` does not treat 3xx as failure, so the probe exited 0 and orchestration never paged on-call, never rolled back, never failed over. Two changes: (1) `cps/admin.py` adds `'web.health_check'` to the redirect-bypass allowlist (joining `web.login` + the DB-config-wizard endpoints already there) so `/health` reaches its own body in the unconfigured state. (2) `cps/web.py:health_check` early-returns HTTP 503 with `{"status": "unconfigured", ...}` (distinct from `'degraded'` for DB-down) before touching `metadata.db`, so probes correctly see the broken state and operators reading logs can distinguish the two failure modes. 4 regression tests in `tests/unit/test_health_unconfigured_503.py`: AST-walks `admin.before_request` for the `not in` tuple and pins `'web.health_check'`; source-pins `config.db_configured` + `503` + `'unconfigured'` in the `/health` handler; source-pins the `db_configured` check happens before `sqlite3.connect`; behavioral test with monkeypatch confirms 503 + status='unconfigured' via Flask test client. Live-verified in two parallel local containers — `cwng-repro-147` (read-only empty library mount, `db_configured=False`) flipped UNHEALTHY post-fix (was falsely HEALTHY pre-fix); `cwng-repro-147-healthy` (writable library, autoconfig succeeds) stayed HEALTHY. 50 concurrent `/health` probes returned 503 in the unconfigured container and 200 in the healthy one — zero partials, no 500s, no races. Adjacency: `/admin/dbconfig`, `/login`, `/`, `/opds/` unchanged. Closes the third and final gap on #147 after #167 (gevent_wsgi None-guard in v4.0.54) and #197 (HEALTHCHECK retarget in v4.0.61). | `2723202a` | v4.0.68 |
| #213 | [#1351](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1351) | @raphi011 | **Kobo multi-device sync: books synced on one device became invisible to other devices on the same Calibre-Web account.** `HandleSyncRequest` was filtering changed entries through the user-keyed `KoboSyncedBooks` table — once any device had synced a book, the filter excluded it from subsequent sync responses to any device on the same account. Per-device sync state lives in the `x-kobo-synctoken` cursor (the `last_modified` timestamp comparisons), so filtering by user-keyed sync table was both incorrect (locked out additional devices) and redundant. Also removed the `magic_shelf_book_ids` injection from the inner OR — that path bypassed the timestamp cursor and produced an infinite `cont_sync` loop on paginated sync. Magic-shelf membership is still correctly enforced by the OUTER filter (`kobo_sync` shelf OR magic-shelf) further down. CI green; admin-merged after retriggering an earlier stuck workflow that had been in_progress for 2h+ due to a GitHub Actions runner issue. | `ae3e1936` | v4.0.67 |
| #227 | (operator pain report 2026-05-17 + live MITM capture against book UUID `0b800030-997b-4d71-9a1d-0d162c26c867`; companion to #226 B1/B2/B4 cluster) | **Books deleted from CW persisted on Kobo devices forever — the operator manually long-pressed → archived them on the device every time the library changed.** Wire-confirmed during the borrow-day capture: the reading-state handler kept firing `redirect_or_proxy_request()` every sync for the orphan book. **Root cause:** the Kobo protocol needs a book's UUID to address a `DeletedEntitlement` to the device, but `editbooks.delete_whole_book` destroyed the calibre `metadata.db` row (line 1394 pre-fix) before any sync handler could capture the UUID, and `kobo_synced_books` only stored `(user_id, book_id)`. The device interpreted absence-from-sync-response as "still on device, nothing changed," not as deletion. **Fix has four parts.** (1) New `ub.KoboDeletedBook` tombstone table — `(user_id, book_uuid, deleted_at)` snapshot with `UNIQUE(user_id, book_uuid)` for idempotent capture + `(user_id, deleted_at)` index for the sync query path. (2) `migrate_kobo_deleted_book` migration in `cps/ub.py` — creates the table if missing, gated by `CONFIG_DIR/.cwa_migrations/kobo_deleted_book_v1` marker; idempotent on re-run via `CREATE TABLE IF NOT EXISTS` regardless of marker state. (3) `kobo_sync_status.record_book_deletion(book_id, book_uuid)` — enumerates affected users via `KoboSyncedBooks`, inserts per-user tombstones, clears the stale `KoboSyncedBooks` rows so the two-way deletion logic doesn't trip over them. No-op on missing UUID (defensive); idempotent per `(user_id, book_uuid)` via the UNIQUE. Called from `editbooks.delete_whole_book` BEFORE any metadata row is touched (also covers `delete_book_from_table` since that calls through to `delete_whole_book`). Wrapped in try/except so a tombstone-capture failure can't block the user's delete action — worst case is pre-fix orphan behavior, no data loss. (4) `HandleSyncRequest` emits `{"DeletedEntitlement": {"BookEntitlement": {"Id": uuid, "RevisionId": uuid, "CrossRevisionId": uuid}}}` for tombstones where `deleted_at > sync_token.archive_last_modified` (the device's cursor, NOT the local `new_archived_last_modified` which would mask legitimate tombstones whose `deleted_at` lies between the cursor and the local watermark). Advances `new_archived_last_modified` past each emitted tombstone so each device sees each one exactly once. Paginates via `SYNC_ITEM_LIMIT` + `cont_sync` so mass-deletes don't blow past the device's response cap. **15 regression tests** in `tests/unit/test_kobo_deleted_book_tombstone.py` — source-pinned (model + UNIQUE + migration wired into `migrate_Database` + helper exists + `delete_whole_book` calls it + sync emits `DeletedEntitlement` filtered against device cursor); behavioral SQLite (`record_book_deletion` creates per-affected-user tombstones not cross-user, idempotent on double-invocation, no-ops on missing UUID, clears stale `KoboSyncedBooks` rows); sync emission (pending rows in `deleted_at` order, cursor advance prevents re-emit on second sync, user_id filter prevents cross-user leak); migration (creates table from blank schema, marker written, second run no-op). **Live container verification:** cwn-local rebuilt from this branch — migration ran on cold boot (table + indexes + marker present in `/config/`); manually inserted tombstone for `cwng84test` user surfaced as 1 `DeletedEntitlement` with `{Id, RevisionId, CrossRevisionId}` all set to tombstone UUID alongside normal sync entitlements; returned synctoken had `archive_last_modified` advanced to tombstone's `deleted_at`; second sync with returned token returned 0 `DeletedEntitlement` (cursor correct, no re-emit); no new ERROR/WARN/Traceback in container logs. Closes the B3 portion of `notes/KOBO-BUG-CLUSTER-PROMPT-2026-05-17.md`. Design doc that informed this PR: `notes/B3-kobo-deleted-book-tombstone-DESIGN.md`. | `4daccbb5` | v4.0.71 |
| #226 | (fork issue [#220](https://github.com/new-usemame/Calibre-Web-NextGen/issues/220), reporter @Hoite; 2026-05-17 live MITM capture against a Libra Colour FW 4.45.23684) | **Three live-captured Kobo sync bugs sharing one anti-pattern — sync contract emits absences instead of tombstones, and sibling handlers reply 404 to unknown items so the device retries forever.** **B1 — ghost-shelf 404 retry loop:** `HandleTagUpdate` (`/v1/library/tags/<id>` DELETE/PUT) and `HandleTagRemoveItem` both `abort(404)` when the shelf is unknown unless `config_kobo_proxy` is on. The device interprets 404 as transient and retries DELETE every sync forever — wire-confirmed in the capture against two specific ghost-shelf IDs. The sibling `HandleStateRequest` at line ~989 documents the correct pattern ("device retries forever and the user sees sync failures") and calls `redirect_or_proxy_request()` unconditionally. Both tag handlers now mirror that pattern, dropping the `config_kobo_proxy` gate. **B2 — two-axis cursor latent fork #220 loop:** `HandleSyncRequest`'s filter at line ~258 matches a book when EITHER `Books.last_modified` OR `BookShelf.date_added` is past the sync cursor, but the cursor advance at line ~318 only ever reads `book.Books.last_modified`. A book added to a `kobo_sync` shelf after its own `last_modified` re-matches every sync forever with `x-kobo-sync: continue` as the trap signal. Wire-confirmed live: 112 sync requests in 60s, ~436 KB/min wasted bandwidth, identical 4118-byte responses each cycle. Fix: fold `book.date_added` into `new_books_last_modified` via `max()`, guarded with `getattr` because the else branch's SELECT lacks the column. **B4 — magic-shelf orphan after server-side toggle off:** the DeletedTag emission block was wholly nested under `if config.config_kobo_sync_magic_shelves:`. When admin flipped the global flag off, no tombstones were ever emitted, leaving orphan shelves on previously-synced devices (combines with B1 to amplify into the 404 loop). Wire-confirmed against `Test_E2E_Discovered` orphan magic shelf. Fix: emit DeletedTag for orphan magic shelves regardless of the global flag — when on, filter to per-shelf `kobo_sync=False` (existing behavior); when off, emit for all the user's magic shelves so the device can clean up. **10 regression tests** in `tests/unit/test_kobo_bug_cluster_2026_05_17.py` — source-pinned (assert `abort(404)` can't return to either tag handler, cursor-advance reads `date_added`, magic-shelf DeletedTag not wholly gated) + behavioral SQLite (cursor advance simulation, magic-shelf deletable query). **Live container repro post-fix:** B1 DELETE on bogus tag returns HTTP 200 instead of 404 (loop ends); B2 sync with cursor at 2026-05-15 + `BookShelf.date_added` bumped to 2026-05-20 advances cursor and second sync returns empty (`x-kobo-sync: ` instead of `continue` — pre-fix would loop forever); B4 sync response with magic-shelves config off includes 5 DeletedTag entries including the live-captured `Test_E2E_Discovered` UUID (pre-fix would be 0). B3 (orphan book on device after CW delete) shares the same root cause but needs a schema migration to capture book UUID before delete — deferred per goal-file allowance to follow-up PR; design doc at `notes/B3-kobo-deleted-book-tombstone-DESIGN.md`. Adjacency sweep: `HandleTagAddItem` (POST `/items` on unknown shelf) deliberately left at `abort(404)` — different semantics (user-initiated add to non-existent shelf should fail visibly). Regular shelf delete via web UI already adds ShelfArchive → sync_shelves emits DeletedTag → combined with B1 fix the device's confirmation-DELETE no longer loops. Closes #220 (B2 portion). | `ed154a87` | v4.0.70 |
| #221 | [#1343](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1343) | @shavitmichael | **Four small Kobo sync-protocol fixes the Android Kobo app needs that Kobo devices tolerate.** (1) `get_download_url_for_book` switches to `kobo.redirect_download_book` when the request carries `x-kobo-deviceos: Android` — the Android app expects an HTTP redirect step that Kobo devices don't. (2) `DrmType: "None"` always present on book metadata (removed the "Not required" comment) — the Android app rejects entries that omit DrmType while Kobo devices fall back to a default. (3) `Series: {}` always present on book metadata — the Android app refuses sync entries without a Series object; empty dict satisfies the schema for books not in a series. (4) Capitalized `{Width}` / `{Height}` in cover image URL templates (both `HandleInitRequest` resource templates) — the Android app's URL-template substitution is case-sensitive; lowercase tokens left unresolved produced broken cover URLs. Production diff: 32+/11- in `cps/kobo.py` (single file). **5 regression tests** in `tests/unit/test_kobo_android_app_compat.py` covering the four user-visible behaviors plus the `x-kobo-deviceos` gate logic. All four changes are additive or pin behavior the Android app needs; no effect on existing Kobo device sync. | `dd94fb19` | v4.0.72 |
| #229 | (operator-flagged 2026-05-17 borrow-day session; B8 in `notes/KOBO-A1-A2-B8-PROMPT.md`) | **Shelves created on a Kobo device (long-press a book → "Create new collection") never round-tripped to other devices on the same account, and disappeared on factory reset.** `HandleTagCreate` (`POST /v1/library/tags`) constructed `ub.Shelf(user_id, name, uuid)` without setting `kobo_sync`, so the column default of `False` made device-created shelves invisible to subsequent syncs: the row existed on the device + on the server but no `NewTag` was ever emitted for it. **Fix**: add `kobo_sync=True` to the `ub.Shelf(...)` constructor in the device-create branch. Device-created shelves are Kobo-managed by definition; users can untoggle via the shelf-edit UI later. The CW web-UI shelf-creation path (`cps/shelf.py:create_shelf`) is unchanged — web-created shelves still default `kobo_sync=False` and require explicit opt-in via the "sync this shelf to Kobo" toggle in the shelf-edit form. **3 regression tests** in `tests/unit/test_kobo_device_shelf_default_sync.py` source-pin (a) `kobo_sync=True` appears in `HandleTagCreate`, (b) specifically inside the `ub.Shelf(...)` call's argument list (not just somewhere else in the function), and (c) the `Shelf.kobo_sync` column default remains `False` so the CW-UI path keeps its opt-in semantics. **Live container verification:** `POST /kobo/<token>/v1/library/tags` with a fresh shelf name returns 201 + persists `kobo_sync=1`; the subsequent `GET /v1/library/sync` emits `NewTag` for the new shelf (verified against cwn-local's `cwng84test` user, two `NewTag` entries returned including the newly-created shelf). | `3e429c8f` | v4.0.73 |
| (direct push) | (follow-up polish for v4.0.74 backport of CWA #1349 by @navels) | **Preserve @navels' fast-exit design intent — defer fork PR #122 + PR #199 cps.services imports.** v4.0.74's #231 backport left `from cps.services import calibre_user_plugins` and `from cps.services.calibre_db_lock import metadata_db_write_lock` at the top of `scripts/ingest_processor.py`. Both touch `cps.services.*`, which triggers `cps/__init__.py` to load on import — emitting `[cps:92] ProxyFix configured to trust 1 proxy(ies)...` even on the fast-exit path that @navels designed in CWA #1349 to skip when there's nothing to ingest. @navels' unit test `test_import_and_missing_target_skip_heavy_startup` passed because it only checks `process_lock is None`, but his stated intent ("Fast-exit missing ingest paths before initializing the full application stack") was being silently violated by our fork-original eager imports. **Fix**: move both imports into a new `_load_fork_cps_imports()` helper called by `_load_optional_cps_modules()` — fires only after `initialize_runtime()` is past the missing-target gate. Module-top declares `_calibre_plugins = None` and `metadata_db_write_lock = _noop_metadata_db_write_lock` as no-op fallbacks so callsite semantics still work in test paths that never reach `initialize_runtime()`. **Verified on cwn-local** that the same missing-path invocation now emits only `[ingest-processor] Skipping missing ingest target: ...` — no `ProxyFix` log, no cps imports fired. All 110 adjacent regression tests still pass; PR #199's `/config/.cwa-metadata-write.lock` is still correctly created during real ingest (the lazy load happens past the gate). | `be94b626` | v4.0.75 |
| #238 | (fork issue [#219](https://github.com/new-usemame/Calibre-Web-NextGen/issues/219), reporter @thealmostmighty, confirmed by @droM4X, workaround attempted by @Hoite) | **Fresh installs logged `Failed to store checksum for book N: no such table: book_format_checksums` on every book ingest** — `book_format_checksums` was never created in `metadata.db`, but three sibling writers in `scripts/` wrote to it on every import regardless of feature-flag state. **Root cause has two parts.** (1) `cps/db.py` + `cps/ub.py` gated `ensure_calibre_db_tables` / `ensure_app_db_tables` on `is_koreader_sync_enabled()`. KOReader sync is off by default — on fresh install the tables are never created, and `cwa-checksum-backfill` printed `WARNING: Database schema not ready after 30s, proceeding anyway`. (2) PR #94 (v4.0.28) fixed the same write-without-guard pattern for `cps/helper.py` but never propagated to `scripts/ingest_processor.py:generate_book_checksums`, `scripts/cover_enforcer.py`, or `scripts/kindle_epub_fixer.py` — all three still called `store_checksum` on every ingest. **Fix has two halves.** Schema invariant: drop the `is_koreader_sync_enabled()` gate from both `ensure_*` callers — the table is structural, not feature-flag-controlled (idempotent, empty when sync is off, zero cost). Sibling write gating: add the PR #94 pattern (`if not is_koreader_sync_enabled(): return`) at the top of each of the three writers so disabled-sync instances skip the partial-MD5 work entirely. The pure utilities in `cps/progress_syncing/checksums/manager.py` stay unguarded so explicit-backfill paths still work (preserves PR #94 intent). **11 regression tests** in `tests/unit/test_book_format_checksums_table_creation.py` — source-pin both halves (gates removed in db.py/ub.py, gates added in the three writers) plus a behavioral end-to-end test (`store_checksum` against a fresh in-memory SQLite + `ensure_calibre_db_tables` writes a row). **Live container verification on a true fresh install** (empty config + library volumes): `book_format_checksums` exists in `metadata.db` and `kosync_progress` exists in `app.db` from t=0; sync-OFF ingest of an EPUB produced zero `Failed to store checksum` errors and no `Generating KOReader sync checksums` message (gate fired); sync-ON path then ingested a second EPUB which wrote a row — `cwa-checksum-backfill` reported `Database schema ready (attempt 1)` instead of the 30s timeout (also picked up the earlier sync-off book at the same time, so `SELECT COUNT(*) FROM book_format_checksums` = 2). Adjacency: `cps/cwa_functions.py` on-toggle-enable path now functionally redundant but left as defense-in-depth; `cps/helper.py` already correctly gated; `scripts/generate_book_checksums.py` (the cwa-checksum-backfill script itself) already gated; `cps/progress_syncing/protocols/kosync.py` blueprint-level gate protects `kosync_progress` writes. @Hoite's workaround creates the table in `app.db` — wrong location (writers connect to `metadata.db`), which is why @thealmostmighty saw the error persist after applying it. | `f7c6c38c` | v4.0.79 |
| #248 | (community contribution by [@ikerios](https://github.com/ikerios); complementary to fork [#220](https://github.com/new-usemame/Calibre-Web-NextGen/issues/220) cursor-advance fix in v4.0.54) | **Kobo Forma stuck in infinite sync loop after factory reset and re-registration** — ~3 sync requests per second sustained for 15+ hours against a 393-book library, with `kobo_synced_books` fully populated and a recent device-side cursor. Each response carried 2 rows yet emitted `x-kobo-sync: continue`, so the device refused to persist the advanced synctoken and re-requested the same data forever. **Root cause** in `cps/kobo.py:HandleSyncRequest`: the books branch set `cont_sync = bool(book_count)` — True for any non-zero count. The Kobo protocol treats `x-kobo-sync: continue` as a paging signal ("more pages exist, keep your cursor pinned"), not a freshness signal. When the current batch is exhaustive (`book_count <= SYNC_ITEM_LIMIT`), emitting `continue` is a contract violation: the firmware suppresses synctoken persistence, the device re-requests with the same cursor, the server returns the same rows with `continue`, and the loop self-perpetuates. The reading-states branch seventeen lines below already used the correct `cont_sync \|= bool(changed_reading_states.count() > SYNC_ITEM_LIMIT)`. **Fix**: one-character semantic correction at line 353 — `bool(book_count)` → `bool(book_count > SYNC_ITEM_LIMIT)` to mirror the reading-states branch. **4 RED→GREEN regression tests** in `tests/unit/test_kobo_cont_sync_batch_cap.py` source-pin both branches use the `> SYNC_ITEM_LIMIT` shape; explicitly forbid the broken `cont_sync = bool(book_count)` pattern; cover the books-branch + reading-states-branch + all-bool-count-sites invariant. 194/194 kobo-touched unit tests pass. **Distinct from but complementary to fork #220**: that PR fixed the cursor-advance side (`BookShelf.date_added` folded into `new_books_last_modified`); this PR fixes the signal side. Both are required for `else`-branch users (no `kobo_only_shelves_sync`) to escape the loop. **Bug pre-dates the fork** — `git blame` points to upstream `janeczku/calibre-web`, so this is also a candidate for upstream backport. Live verification on cwn-local: code loads, source inspection confirms both `cont_sync = bool(...)` and `cont_sync \|= bool(...)` sites carry the `> SYNC_ITEM_LIMIT` comparison; the third `cont_sync = True` site is correctly guarded by an outer `if len(pending_deletions) >= SYNC_ITEM_LIMIT` block (tombstone-pagination, intentional). Credit @ikerios for the 15-hour production trace and the upstream-mirror fix structure. | `1ce7c60e` | v4.0.89 |
| #255 | (fork issue [#224](https://github.com/new-usemame/Calibre-Web-NextGen/issues/224), reporter @droM4X) | **OPDS error responses now return Atom XML instead of the stock Flask HTML error page.** @droM4X asked for consistent responses across 401 / 403 / 404 / 500 on `/opds/*` so OPDS readers (Readest, KOReader, generic Atom clients) don't show users "broken feed" when something fails. Pre-fix, `abort(404)` from inside an OPDS route, `abort(403)` from denied-tag filters, and any unhandled 500 all rendered Flask's stock HTML error page — readers can't parse HTML and silently skip or show an opaque error. The existing fork-#183 catch-all only covered unmatched URL paths, not aborts from inside routes. **Fix**: register `@opds.errorhandler(401|403|404|500)` on the `opds` blueprint. Each returns a small Atom feed body (`<feed xmlns="http://www.w3.org/2005/Atom"><id>opds-<class></id><title><label></title></feed>`) with the correct HTTP status code and `Content-Type: application/atom+xml; charset=utf-8`. The 401 handler also sets `WWW-Authenticate: Basic realm="OPDS"` so HTTP-Basic readers prompt for credentials. The fork-#183 `<path:_unknown>` catch-all stays in place as defense-in-depth. **7 RED→GREEN tests** in `tests/unit/test_opds_error_responses_atom.py` source-pin each errorhandler, the Atom content-type, and the Atom namespace presence. **Live curl verification on cwn-local**: `/opds/totally-bogus` returns HTTP 404 with `application/atom+xml; charset=utf-8` and a well-formed Atom body. **Adjacency** (`notes/feat-224-adjacency.md`): Kobo + KOReader sync blueprints have the same "machine client gets HTML" pattern but expect JSON, not Atom — separate fix when a user reports. | `eb5c162c` | v4.0.92 |
| #254 | (fork issue [#222](https://github.com/new-usemame/Calibre-Web-NextGen/issues/222), reporter @droM4X) | **Removed the inert "Switch Theme" header link.** The navbar rendered a `<li class="cwa-switch-theme">` element styled as disabled (opacity 0.5, `cursor: not-allowed`, `onclick: return false`, tooltip "Temporarily disabled until v5.0.0"). It was visual clutter that did nothing. Reporter @droM4X flagged it as unnecessary. **Fix**: remove the `<li>` block from `cps/templates/layout.html`. The supporting JS handlers in `cps/static/js/caliBlur.js`, CSS rules in `cps/static/css/caliBlur.css`, and backend route in `cps/cwa_functions.py` remain as dead code (no UI surface) — to be cleaned up when v5.0.0's theme system either ships or is dropped. **4 RED→GREEN source-pin tests** in `tests/unit/test_theme_switcher_link_removed.py` pin that the wrapper `<li>`, `id="cwa-switch-theme"` anchor, "Temporarily disabled until v5.0.0" tooltip text, and `{{_('Switch Theme')}}` label are all absent. **Playwright pilot** on cwn-local: navbar renders without the inert link, profile dropdown + advanced search items still present, zero console errors. | `6f4cc386` | v4.0.91 |
| #249 | (fork issue [#217](https://github.com/new-usemame/Calibre-Web-NextGen/issues/217), reporter @sjthespian) | **Metadata-search "Fetch Metadata" failed to find any book whose title contained an apostrophe** — reporter typed `At Winter's End`, got zero results; same title without the apostrophe returned the book on the first hit. Affected every provider whose URL-encodes the user-typed query: the ASCII apostrophe became `%27` and catalog indexes (Amazon, Google, Kobo, OpenLibrary, etc.) store the apostrophe-free form. **Root cause has two halves.** (1) `cps/services/Metadata.py:get_title_tokens` didn't normalize apostrophes — it stripped most punctuation but the apostrophe class survived through to URL encoding. (2) `cps/metadata_provider/amazon.py:178` passed the raw `query` directly into Amazon's `field-keywords` parameter, never invoking `get_title_tokens`, so even after fixing the tokenizer Amazon would still have shipped `Alice%27s+Adventures` to amazon.com/s. **Fix.** New first regex `r"['‘’ʼ`´]"` → `""` (strip, not space-replace, to keep `Don't` → `Dont` as one token) covering ASCII U+0027, curly U+2018/U+2019, modifier-letter U+02BC (Hawaiian okina), backtick U+0060, acute U+00B4. Amazon `search()` now does `" ".join(self.get_title_tokens(query, strip_joiners=False)) or query` before populating the request params. Bundled adjacent latent bug: the second tokenizer regex had CJK quotes `《》「」"”` written *outside* the character class — they were a literal suffix that never matched anything, silently disabling colon/semicolon/etc. token splitting; moved inside `[ ]` so the docstring claim "Replace other special chars with a space" actually holds. **22 RED→GREEN regression tests** — `tests/unit/test_metadata_title_tokens.py` (19) covers all five apostrophe-class chars, existing-behavior preservation (joiners, year-in-parens, double-quote wrapper, leading/trailing apostrophes), CJK quotes, and URL-encoded-query invariant (`%27` never reaches the wire); `tests/unit/test_amazon_provider_tokenize_query.py` (3) source-pins that Amazon invokes `get_title_tokens`, the broken `'field-keywords': query` pattern is gone, and the join expression has the right shape. **Live verification on cwn-local with Playwright pilot of the metadata edit modal on book 16 (Alice's Adventures in Wonderland)**: DNB Query URL `query=tit=%22Alices%20Adventures...%22` (no `%27`); Amazon URL post-fix `field-keywords=Alices+Adventures+in+Wonderland` (no `%27`); in-container tokenize call `Metadata.get_title_tokens("At Winter's End")` → `["At", "Winters", "End"]` identical to the apostrophe-free form. **Blast radius:** 9 providers consume `get_title_tokens` (openlibrary, google, dnb, scholar, lubimyczytac, kobo, ibdb, douban, comicvine) + Amazon now also. Hardcover (GraphQL) and Litres (Russian catalog, raw query) are documented as v2-track in `notes/feat-249-followups.md` — apply the same Amazon pattern if a user reports the symptom there. | `61d3a7aa` | v4.0.88 |
| #250 | (fork issue [#193](https://github.com/new-usemame/Calibre-Web-NextGen/issues/193); backports the first two commits of [@FRaccie](https://github.com/FRaccie)'s downstream [FRaccie/Calibre-Web-NextGen#1](https://github.com/FRaccie/Calibre-Web-NextGen/pull/1)) | **Container went unhealthy and `/duplicates` crashed on large libraries** with `sqlite3.OperationalError: too many SQL variables`. Symptom reported by @FRaccie on a ~200k-book production library; the duplicate prefilter returned ~170,231 candidate IDs and `find_duplicate_books_python` issued `db.Books.id.in_(list(candidate_ids))` as a single statement — exceeding SQLite's `SQLITE_MAX_VARIABLE_NUMBER` (32766 on 3.32+, 999 on older builds). The after-import cache refresh hit the same error, so the incremental baseline never persisted; every subsequent import fell back to a near-full Python scan in the in-process `WorkerThread`, holding the GIL and starving the web accept loop. Self-perpetuating. **Fix**: new `_fetch_books_in_chunks` helper splits the IN filter into 900-id batches (safe under the pre-3.32 999 floor) and concatenates the per-batch results; applied at both `Books.id.in_` sites in `find_duplicate_books_python` and `find_duplicate_books_sql`. Per-batch queries can only order within a batch, so each callsite re-sorts in Python using the tz-safe `_timestamp_or_default(b.timestamp, _AWARE_MIN)` key — Calibre libraries mix offset-naive and offset-aware `Books.timestamp` (DB `ORDER BY` tolerated it, Python's sort otherwise raised `can't compare offset-naive and offset-aware datetimes`). **9 new tests** across `tests/unit/test_duplicates_chunked_in.py` (chunk size below the 999 floor, empty-ids no-op, 170k-id full coverage, exact-multiple-of-chunk, set input accepted) and `tests/unit/test_duplicates_timezone.py` (mixed-tz total ordering without raise, source-pin that both callsites use the tz-safe key, source-pin that the raw `(b.timestamp is not None, b.timestamp)` crash pattern cannot return). RED on main, GREEN on this branch; 99/99 across every duplicate-touching unit-test file. **Live container verification on cwn-local with synthetic 170,231-id payload against real SQLite 3.50.4**: unchunked path raised `OperationalError: too many SQL variables` (exact production symptom); chunked path returned 3040 books in 0.58s, no error. Playwright pilot of `/duplicates`: page rendered cleanly, manual scan completed (`groups=3 max_book_id=17`), no traceback in logs. **Adjacency sweep** (full sweep in [`notes/feat-250-adjacency.md`](notes/feat-250-adjacency.md)) — `cps/duplicate_index.py` and `cps/tasks/database.py` already chunk via their own helpers; `cps/services/cover_preview_cleanup.py:50` has the same overflow pattern but is currently unreachable (no scheduler wired). Third commit in @FRaccie's PR (background-scan architectural change) deferred — carries broader design implications and warrants a separate discussion. Credit @FRaccie for the production diagnosis and both halves of the fix. | `29a10af3` | v4.0.87 |
| #247 | (fork issue [#234](https://github.com/new-usemame/Calibre-Web-NextGen/issues/234), reporter @thunderbolt93; inspired-by @navels in CWA [#1359](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1359)) | **Rendering the book grid during a concurrent ingest crashed with `AttributeError: 'NoneType' object has no attribute 'id'` inside `cps.db.CalibreDB.order_authors`** — wire-symptom: 500 on `/` mid-ingest with `CWA_WATCH_MODE=poll`. **Root cause:** `order_authors()` re-queried `Authors` by `Books.author_sort` on every page render and assumed every result row plus every linked `book.authors` entry was a valid Authors object; a torn-down LEFT JOIN row (concurrent ingest/delete) could surface a `None` entry in the loaded collection, and the downstream `r.id` access blew up the whole page render. Two adjacent fragilities in the same function: `entry.author_sort.split('&')` raised when `author_sort` was NULL (Calibre allows it), and the `.first()` id-fallback could append None into `ordered_authors` and propagate into the template. **Fix:** consult the already-loaded `book.authors` collection instead of issuing fresh SQL during render — no render-time SQL means no session-state race window. None entries in `book.authors` are filtered before lookups; None / empty `author_sort` is treated as empty (no `.split` crash); stale sort entries log once per process via the existing fork-#108 `_AUTHOR_SORT_DRIFT_WARNED` rate-limit and fall through to id-fallback so every linked author still appears in the rendered order; last-resort safety net: if every sort entry is stale and no ids remain, return the linked authors so the book renders with its author list rather than appearing author-less. Structure adopted from CWA upstream PR #1359 (@navels) — same fix proposed upstream; this lands the relief in our build today rather than waiting for upstream review. Preserves our fork-original additions: the `_AUTHOR_SORT_DRIFT_WARNED` rate-limit (fork #108) and the linked-authors safety net. **8 new unit tests** in `tests/unit/test_order_authors_none_guard.py` cover the failure modes from the report (None author, None author_sort, stale sort, mixed linked entries, combined entry shape, id=None linked author, empty book.authors). RED on `main`, GREEN on this branch. Existing fork-#108 dedup test (`tests/unit/test_author_sort_drift_log_dedup.py`) updated to the new in-memory mechanism — all 6 still pass, confirming fork #108 dedup contract preserved. **Live smoke verified on cwn-local** — hot-swapped patched `cps/db.py`, restarted, hit `/` and `/opds` — both returned 200, zero errors in container logs, index page renders 15 books normally. Blast radius: 9 callers of `order_authors` across `cps/web.py`, `cps/editbooks.py`, `cps/duplicates.py`, `cps/duplicate_index.py`, `cps/search.py` — return-value semantics unchanged when called with `list_return=False` (returns list of Author objects); attribute-set semantics unchanged when called with `list_return=True`. | `bc2cb141` | v4.0.85 |
| #279 | (CW issue [#3648](https://github.com/janeczku/calibre-web/issues/3648), reporter @NathanIfinity — reproduced in our build before fixing per the new "verify upstream issues against our divergent code" discipline) | **Amazon metadata search now returns results again** instead of "No Result(s) found! Please try another keyword!" for queries that obviously have matches. ComicVine and the Chinese provider returned results fine for the same query — the failure was Amazon-specific. **Root cause**: `cps/metadata_provider/amazon.py::Amazon.headers` declared only `User-Agent`, `Accept`, `Accept-Encoding`. Amazon's bot detection now 503s on requests missing the full browser-shape header set; the provider's empty result list propagated to the modal as the no-results message. Verified directly from cwn-local: minimal `{UA, Accept, Accept-Encoding}` → status=503, body=2.6KB; full browser headers → status=200, body=1.1MB with the real product list. **Fix**: extend `Amazon.headers` with `Accept-Language: en-US,en;q=0.5`, `DNT: 1`, `Sec-Fetch-Dest: document`, `Sec-Fetch-Mode: navigate`, `Sec-Fetch-Site: none`, `Sec-Fetch-User: ?1`, `Upgrade-Insecure-Requests: 1`. `Accept` value also widened to the real browser shape (`text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8`). `session.headers` already wired at class definition, so per-request `headers=` kwarg is no longer needed (commented-out line removed at the request site). **6 RED→GREEN source-pin tests** in `tests/unit/test_amazon_metadata_search_headers_3648.py` pin each browser-shape header, session-level assignment, and anchor comment so a future trim doesn't silently regress. 17/17 amazon-adjacent tests still pass. | `e1a2ce6d` | v4.0.111 |
| #291 | (revisits closed [#281](https://github.com/new-usemame/Calibre-Web-NextGen/pull/281); reproduced live during fork #276 work) | **`User.view_settings = NULL` no longer 500s the entire web UI for affected users.** `view_settings` is `Column(JSON, default={})`, but SQLAlchemy's `default=` only applies on INSERT through the ORM — rows imported from older schemas (pre-2025-01-14 when this column was added upstream) or inserted via raw SQL / external admin tools landed with NULL. Any affected user then hit `AttributeError: 'NoneType' object has no attribute 'get'` at `cps/ub.py:235` in `User.get_view_property`. Because `layout.html`'s cover-settings cog calls `get_view_property` on every render, the affected user got a 500 on `/`, `/book/<id>`, `/table`, and every other page — effectively a per-user web UI lockout. **PR #281** proposed `vs = self.view_settings or {}` guards inside `get_view_property` / `set_view_property` and was closed on the wrong threshold ("no teenyverse production NULL rows"); I reproduced the exact crash later that day on cwn-local by manually setting `view_settings=NULL`. **Fix**: new `migrate_user_view_settings_null(engine, _session)` runs from the existing `migrate_Database` orchestrator between `migrate_user_table` and `migrate_shelf_table`. Single `UPDATE user SET view_settings = '{}' WHERE view_settings IS NULL`. Idempotent (zero rows match on re-run); no-op when no legacy NULLs exist; rowcount logged so operators can see whether any legacy NULLs were normalized. Also flips `Anonymous.__init__` initial value from `view_settings = None` to `view_settings = {}` for defense-in-depth — `loadSettings()` immediately overwrites it from the DB row, but with the migration that DB row is always `{}` so the Anonymous path can never bottom out at `None`. **6 source-pin tests** in `tests/unit/test_view_settings_null_migration.py` pin function definition, idempotent UPDATE shape (WHERE NULL filter + `'{}'` literal), rowcount log, orchestrator wiring, rollback-on-exception, Anonymous `__init__` uses `{}`. **Live end-to-end verified on cwn-local**: seeded `null-vs-test` with `view_settings=NULL`, restarted container → log shows `[view-settings-null-migration] normalized 1 user(s) with NULL view_settings to '{}'` → DB row now `{}` → user logs in successfully → `/` and `/book/5` render cleanly (pre-fix both 500'd with the AttributeError). | `bafd309a` | v4.0.116 |
| #293 | (fork issues [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) mobile pass 1 + [#225](https://github.com/new-usemame/Calibre-Web-NextGen/issues/225) v4.0.118 regression) | **Server announcement banner now actually visible on the dark theme + mobile.** v4.0.118 shipped the banner using Bootstrap `class='alert alert-info'`. **caliBlur.css** (the CWNG dark theme, which is the default) rewrites every `.alert` element into a fixed-position 10-second auto-hiding toast at the bottom of the viewport — so the banner was invisible-then-broken on every page on the default theme. On iPhone SE viewport (375 px) the banner shrank to 24 px wide (only the close-button width) floating at y=624. **Fix**: drop the Bootstrap `.alert .alert-info` class set entirely; use a dedicated `cwng-server-announcement` class with inline styles that force flow-layout positioning regardless of what the theme stylesheet does to ambient `.alert` rules. Same `alert-info` palette (`#d9edf7` background, `#31708f` text, `#bce8f1` border), none of the toast behavior. Also moved the banner from before `{% block flash %}` (above the main content container) to INSIDE `<div class='container-fluid'>` so it inherits the same navbar-clearing offset the rest of the content already has. `margin: 56px 0 0.5em 0` clears the navbar across viewports. `position: relative; z-index: 5` is belt-and-suspenders against any theme-specific `transform` creating a containing block (caliBlur sets `transform: translateZ(0)` on several layout selectors). Test pin in `tests/unit/test_225_server_announcement.py::test_layout_banner_does_not_inherit_caliBlur_toast_alert_styles` requires the banner div to NOT carry the `alert` class set AND to have explicit `position: static|relative`. 8/8 #225 tests + 30/30 .po-compile tests GREEN. CI green first try (no xdist flake). **Live verified on cwn-local at iPhone SE 375x667 AND desktop 1280x800**: banner renders cleanly, no overlap with navbar on either viewport (iPhone: banner top=94 bottom=179 vs nav top=0 bottom=48; desktop: banner top=116 bottom=164 vs nav top=0 bottom=60), text fully readable with proper background contrast, dismiss × button works on both, re-edit announcement via SQL → restart → new content hash → banner reappears. | `77dc6da8` | v4.0.119 |
| #295 | (fork issue [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) mobile pass 2) | **Flash toasts no longer obscure bottom-of-form buttons on mobile.** Mobile audit at iPhone SE width caught the wrong-credentials error toast (`Wrong Username or Password`) completely covering the `LOG IN WITH MAGIC LINK` button on `/login` — making it impossible to switch to the magic-link flow after a failed password attempt. **Root cause**: `caliBlur.css` styles every `.alert` element as `position: fixed; bottom: 20px` — fine on desktop where there's plenty of vertical room, but on narrow viewports the toast lands on top of bottom-of-form controls. **Fix**: new `cps/static/css/mobile-alert-toast-position.css` with a single media-query rule scoped to the Bootstrap 3 SM breakpoint (`@media (max-width: 767px)`) flipping `.alert` to `top: 80px !important; bottom: auto !important`. 80px clears the fixed navbar with visual margin. Loaded in `layout.html` AFTER `caliBlur.css` + `caliBlur_override.css` so the `!important` wins on specificity tie. Desktop unchanged — verified at 1280x800 that toast computed style still resolves to `top: 721px; bottom: 20px` (the original caliBlur position). **4 source-pin tests** in `tests/unit/test_288_mobile_alert_toast.py` cover file existence, media-query breakpoint scope, .alert top + bottom:auto with !important, layout.html `<link>` inclusion. CI hit the xdist worker-IPC hang twice before going green on the second rerun (same documented flake class — `notes/xdist-worker-ipc-hang-followup-2026-05-21.md`). **Live verified on cwn-local with Playwright**: at iPhone SE 375x667 a failed-credentials POST renders the toast at `top: 80px` (rect 80–139), with all 3 stacked form buttons (Login / Home / Magic Link) at viewport bottom fully clear; at desktop 1280x800 same failed-credentials POST renders the toast at `top: 721px; bottom: 20px` (caliBlur default, no regression). | `cc8a31e7` | v4.0.121 |
| #298 | (fork issue [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) mobile pass 3 + xdist hang real root cause) | **Send-to-eReader modal Send/Cancel buttons now reachable on phones + xdist hang root cause traced (for real this time).** Two changes traveled together: **(1) Mobile pass 3**: at iPhone SE 375x667 the email-select modal on `/book/<id>` rendered the Send + Cancel buttons at y=785-821, below the 667-tall viewport. Bootstrap 3 doesn't ship a `.modal-dialog-scrollable` class (BS4+ added one). New `cps/static/css/mobile-modal-scrollable-body.css` with a single media query (`max-width: 767px`) caps `.modal-body` to `max-height: 50vh; overflow-y: auto` (with `-webkit-overflow-scrolling: touch` for iOS momentum) and tightens `.modal-dialog` margins to 10px to recover vertical space. Header + footer stay pinned; body scrolls between them. Desktop verified unchanged at 1280x800 (modal-body max-height=none, overflow-y=visible). **(2) xdist hang root cause v2**: PR #297 closed the sqlite3 connection in `test_store_checksum_after_ensure_calibre_db_tables` but the same test STILL hung on this PR's first CI run. Deeper bisect: `store_checksum` had `from ... import calibre_db` ABOVE the `if db_connection is None:` branch — every call triggered the full `cps/__init__.py` Flask app singleton import (database wiring, gevent setup, etc.). Under pytest-xdist subprocess contexts that init hangs on resources that don't exist in the worker. Moving the import INSIDE the if-branch eliminates the `cps` package import for test paths that pass a connection explicitly. Production paths that don't pass a connection still get the lazy import on first use — zero behavioural change. **Empirical confirmation**: PR #298's second CI run passed first try with this fix in place. **4 source-pin tests** in `tests/unit/test_288_mobile_modal_scrollable.py` (modal CSS file exists, max-height + overflow-y rules, modal-dialog margin-top rule, layout.html `<link>` inclusion). 11/11 related #283/#288 tests stay GREEN. **Live verified on cwn-local with Playwright**: iPhone SE 375x667 → modal-body clientHeight=334 scrollHeight=560 (internal scroll works), Send button at top=569 bottom=605 fully inside viewport; desktop 1280x800 → modal-body max-height=none, modal-dialog margin-top=0px (no regression). | `85470d7b` | v4.0.124 |
| #299 | (fork issue [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) mobile pass 4) | **Sort dropdown label readable on phones — 'DATE ADDED, ...' instead of 'DATE A...'.** At iPhone SE 375px the book-list sort dropdown rendered 'Date added, newest first' as the unreadable 'DATE A...' because three decorative leading glyphs (book + calendar + sort-lines) inside the toggle button consumed ~80px of the 164px-wide toggle, leaving the label only 62px. Two-part CSS fix inside the existing `@media (max-width: 480px)` block in `cps/static/css/book_organizer.css`: (1) `.book-organizer-sort-toggle .book-organizer-icons { display: none }` hides the decorative glyphs on mobile (the dropdown caret already implies 'click to sort'); (2) `.book-organizer-label max-width` bumped from 140px to 200px so common short labels (~190px uppercase) fit without ellipsis. Result at iPhone SE: label renders 'DATE ADDED, ...' (116px) — still ellipsised because the parent shelf-actions panel constrains the bar to 270px (deeper refactor tracked as v2), but the active sort is now recognizable to users instead of the cryptic 'DATE A...'. **1 source-pin test** in `tests/unit/test_288_mobile_sort_dropdown_width.py` asserts mobile max-width ≥200px. **Live verified on cwn-local with Playwright**: iPhone SE 375x667 → icons display=none, label width=116px scrollWidth=193 (controlled ellipsis); desktop 1280x800 → icons display=flex, label width=193px truncated=false (no regression). | `a50b3179` | v4.0.125 |
| #294 | (fork issue [#283](https://github.com/new-usemame/Calibre-Web-NextGen/issues/283) follow-up, reporter @droM4X) | **Books List Comments editor: drop formatBlock dropdown + stack Cancel/Submit below textarea + swap to Cancel-left/Submit-right.** Reporter @droM4X verified the v4.0.112 fixes (contrast, toolbar wrap, dead Link/Image buttons) and posted three more rough edges in the same `/table` Comments wysihtml5 popup. **Fix 1**: hide the 'Font Size' dropdown — `data-wysihtml5-command='formatBlock'` actually picks paragraph style (H1/H2/H3/Paragraph), but the metadata editor doesn't distinguish heading levels in rendered output. Hidden via the same `:has()` selector pattern that already hides createLink + insertImage. **Fix 2**: x-editable renders `.editable-input` + `.editable-buttons` as inline-block siblings in the same row, which overflowed at narrow popup widths. Force the form to `flex-direction: column` so buttons stack BELOW the input. Scoped via `:has(.wysihtml5-sandbox)` so only wysihtml5 popups get the column layout — other editable popups (text input, select) keep their default inline shape. **Fix 3**: x-editable's default button order is Submit/Cancel; common UX is Cancel-left, Submit-right. `.editable-cancel { order: 1 }` + `.editable-submit { order: 2 }` inside the same scoped flex container. All three fixes in `cps/static/css/wysihtml5-fork.css` — pure CSS, no template change. **3 new source-pin tests** in `tests/unit/test_283_followup_toolbar.py` (14/14 total #283 tests GREEN). CI green first try. **Live verified on cwn-local with Playwright + DOM evaluate** at desktop 1280x800: `ul.wysihtml5-toolbar > li:has(a[data-wysihtml5-command='formatBlock'])` computed `display=none`; `.editableform:has(.wysihtml5-sandbox)` `display=flex direction=column`; `.editable-input` `display=block width=566px` (full popup); `.editable-buttons` `display=flex direction=row margin-top=10px`; `.editable-cancel order=1`; `.editable-submit order=2`. `:has()` selector requirement: Chrome 105+, Safari 15.4+, Firefox 121+ (evergreen since late 2023). | `1906d4a5` | v4.0.120 |
| #301 | (fork issue [#283](https://github.com/new-usemame/Calibre-Web-NextGen/issues/283) round 3, reporter @droM4X) | **Books List Comments editor: popover stays in view + Submit button is the orange one.** @droM4X tested v4.0.125 (PR #294 fixes) and posted three more concrete asks with a screenshot: (a) popover clipped at top by the fixed navbar on upper rows; (b) ask for auto-flip / open-below when no space above; (c) button colors inverted — Cancel currently orange, Submit currently muted dark, opposite of Bootstrap convention. **Root causes**: x-editable's bootstrap-editable defaults `placement='top'` so the popover always renders ABOVE the cell — when the row is near the top of the viewport its top edge ends up behind the fixed navbar; and caliBlur.css inverts Bootstrap's button-color convention (maps `.btn-default` to `var(--color-secondary)` accent orange and `.btn-primary` to a muted dark) — x-editable correctly generates Cancel as `btn-default` and Submit as `btn-primary` per the library spec, but the theme then paints orange on Cancel. **Three fixes** (all scoped, no global theme touch): (1+2) `book_table.html` gains `data-editable-placement='bottom'` on the Comments column header AND the custom_column wysihtml5 mirror — bootstrap-table editable extension forwards this to x-editable's options, forcing the popover BELOW the cell. Simpler than auto-flip which would need popper.js/BS4+. (3) `wysihtml5-fork.css` scoped overrides inside `.editableform:has(.wysihtml5-sandbox)` force `.editable-submit` background to `var(--color-secondary, #e89b18)` (CWNG orange accent) and `.editable-cancel` background to muted `#4a5563`, both with `!important` to overcome caliBlur specificity. Hover states matched. Scoped tightly so other editable surfaces (text inputs on other columns) keep their existing theme colors. **4 source-pin tests** in `tests/unit/test_283_round3_zindex_colors.py` pin Comments + custom-column placement attributes + submit-uses-accent + cancel-uses-non-accent. CI passed first try (xdist hang final fix from PR #300 holding). **Live verified on cwn-local desktop 1280x800**: opening Comments editor on row 1 renders popover at `top=535` with className 'bottom' (was at top behind navbar with className 'top'); `.editable-submit` computed `background-color: rgb(204, 123, 25)` (CWNG orange); `.editable-cancel` computed `background-color: rgb(74, 85, 99)` (muted gray). Thanks @droM4X for the thorough round-three test report. | `0c16c8ad` | v4.0.127 |
| #306 | (fork issue [#302](https://github.com/new-usemame/Calibre-Web-NextGen/issues/302), reporter @droM4X) | **Navbar search button no longer leaks 'SEARCH' text on hover + casing matches between navbar and /advsearch.** Two unrelated dark-theme glitches reported on v4.0.127 with screenshot: (a) hovering the navbar search button revealed 'CH' (tail of 'SEARCH') in white behind the magnifying-glass icon — the button is meant to be icon-only via a `:before` glyph + `color: transparent` for the text label, but the `:hover` rule flipped `color: #fff`, revealing the hidden glyphs at exactly the wrong moment; (b) `.advanced_search .btn { text-transform: none }` overrode the theme-wide `.btn { text-transform: uppercase }`, so the same i18n string `_('Search')` rendered 'SEARCH' in the navbar and 'Search' on `/advsearch`. **Two one-line CSS fixes in `cps/static/css/caliBlur.css`**: (1) change navbar search `:hover` color from `#fff` to `transparent` so the text stays invisible at all interaction states; the existing background change is the actual hover affordance. (2) Remove the `text-transform: none` declaration from `.advanced_search .btn` — keep the page-specific font-family/font-weight; result is both pages render the same i18n string with the global uppercase rule. **2 source-pin tests** in `tests/unit/test_search_button_hover_no_bleedthrough_fork302.py` regex-anchor the exact selector text + assert no visible color in the hover rule body + assert `.advanced_search .btn` no longer contains `text-transform: none`. **CI passed first try** (xdist hang fully eliminated per PR #300). **Live verified on cwn-local dark theme**: navbar search hover-rule computed `color: transparent` (was `#fff`); `/advsearch` submit button computed `text-transform: uppercase` rendering 'SEARCH' (was `none` rendering 'Search'). Thanks @droM4X for catching this — the bleed-through is dark-theme-specific because caliBlur is what uses the transparent-text-behind-icon pattern. | `e1dc2dd6` | v4.0.128 |
| #307 | (fork issue [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) substantive mobile sweep, triggered by operator iPhone 14 screenshot) | **Substantive mobile layout sweep — sort dropdown gets full row, book card titles wrap to 2 lines, alert-cwa banner stops overlapping book content.** The operator pulled up production v4.0.127 on their iPhone (414px viewport) and showed three mobile UX issues that my earlier mobile passes (v4.0.119–v4.0.125) had missed because they were too narrowly targeted at iPhone SE 375px. **Root causes (three unrelated)**: (a) sort dropdown 'DATE ADDED, NEWES...' still truncated at 414px because the `.shelf-actions` parent panel constrains the `.book-organizer-bar` to ~324px regardless of viewport width, and the bar-left side only got ~218px of that — the dropdown then couldn't fit the label even after the v4.0.125 max-width:200px bump; (b) book card titles aggressively single-line truncated (`(ebook - german) Niet...` instead of two readable lines) because `caliBlur.css` declares `white-space: nowrap !important` on `.container-fluid .book .meta .title` + `.author`; (c) `.alert-info.alert-cwa` persistent banners (duplicate-scan-setup, cwa-update, translation-missing) use caliBlur's `position: fixed; bottom: 20px; left: 50%; width: 50%` treatment — a centered floating overlay that blocks the book grid content on narrow viewports. **Three scoped fixes**: (1) `cps/static/css/book_organizer.css` mobile block (`@media max-width:480px`) adds `flex-wrap: wrap` on `.book-organizer-bar` so the sort dropdown gets a full row + `flex: 1 1 100%` on bar-left/right so each takes a full row + `max-width: none` on the sort label (since the full row gives plenty of horizontal space). (2) New `cps/static/css/fork-mobile-cards.css` with mobile rules (`@media max-width:767px`) — `.book .meta .title` + `.author` get `white-space: normal !important; -webkit-line-clamp: 2 !important; display: -webkit-box !important` to override caliBlur's nowrap with 2-line clamp. (3) Same file — `.alert-info.alert-cwa` + `.alert-danger.alert-cwa` re-anchored to `top: 64px !important` (below navbar) with `bottom: auto !important; width: auto !important; left/right: 8px !important` so the banner spans the full viewport width and stops blocking books. **7 source-pin tests** in `tests/unit/test_mobile_substantive_layout_sweep.py` + updated `tests/unit/test_288_mobile_sort_dropdown_width.py` (rule changed from `max-width: 200px` to `max-width: none` since the wrap is the real fix). **Live verified on cwn-local at THREE mobile viewports** (iPhone SE 375x667, iPhone 14 414x896, iPhone 14 Pro Max 430x932) + desktop 1280x800 unchanged. JPEG screenshots captured at each viewport. The next mobile pass will test at 375 + 414 + 430 minimum + inspect the constraint chain end-to-end, not just the immediate selector — caught a class of issue the operator was right to call out as 'not enterprise standard'. | `0f39bed9` | v4.0.129 |
| #305 | (4-part annotation hardening — H1 follow-up; closes scope of #228 series) | **Annotation decouple + live Kobo capture + PDF/CBZ overlays.** Massive 4-sub-project rework picked up from a parallel agent's session, reviewed + verified to enterprise standard before merge. **Sub-project 1 (decouple source/sync target)**: rename `kobo_annotation_sync` → `annotation`; new `annotation_sync_target` table (per-(annotation, target) destination with status state machine `pending → synced/failed → tombstone`); fix the H1-era source bug where `source='hardcover'` was backfilled on pre-H1 rows + continued to be written by the live PATCH path; 8-step transactional migration with sanity-check gate, idempotent + rollback-on-failure; bit-exact SHA-256 preservation tested across 5000-row populated fixture (41ms total). Hardcover push logic extracted from `cps/readingservices.py` into `cps/services/annotation_sync/` (handler ABC + HardcoverHandler + dispatcher, 522 LoC across 3 files). **Sub-project 2 (live Kobo capture independent of Hardcover)**: auth gate decoupled — annotations persist locally whenever Kobo sync is on + user authenticated, even when Hardcover is off; full position field capture from PATCH payload; soft-delete via DELETE PATCH; recovery via re-create PATCH. **Sub-project 3 (PDF annotation overlay)**: new polymorphic columns `position_type`, `pdf_page`, `pdf_quad_json`, `comic_page` (idempotent migration). `cps/static/js/reading/annotations_pdf.js` listens to PDF.js `pagerendered` events + draws colored rect overlays per annotation `pdf_quad`. Uses normalized 0..1 coords so zoom-independent (verified live: 2x scale → 2x overlay w/h). **Sub-project 4 (CBR/CBZ page-level annotations)**: rides on (3)'s polymorphic schema with `position_type='comic_page'`. `cps/static/js/reading/annotations_comic.js` installs a floating badge, MutationObserver watches kthoom's `.page` indicator, badge updates with N-notes + first-annotation color when current page has annotations. **288 unit tests green** across annotation + kobo + hardcover suites. **Live verification across 5 angles each**: (a) live PATCH capture proven by 9 unit tests + agent's e2e log; (b) 3/3 PDF overlays render with correct rgba (yellow 240,196,25 / green 92,184,92 / red 217,83,79) + `mix-blend-mode: multiply`; (c) zoom invariance 1.25→2.5 = 2.0x w/h ratio confirmed; (d) 5/5 comic badge page transitions (no-annot hidden / 2-notes blue / 1-note green / malformed text hidden / back-to-2 restored with correct color); (e) 8/8 edge-case data round-trips (unicode 中文 한국어 العربية / emoji 📚🎨 / special chars `'"<>&\n` / NULL / 5000-char text / empty strings). **Module sizes well-bounded — not bloat**: 522 LoC across 3 service files, 64+38 LoC for 2 migrations, JS files 4-6 KB each. **Open items intentionally deferred to follow-ups**: text-select-to-create UI for PDF (3b), click-to-tag UI for comics (4b), async sync worker, real Kobo device verification (manual checklist at `notes/feat-annotation-decouple-kobo-verification.md`). Original PR design + plan in `notes/2026-05-21-annotation-decouple-source-target-DESIGN.md` + `-PLAN.md`. CI green first try. Credit: 17 commits authored by the parallel agent (7255+/339- across 35 files). | `46f75e93` | v4.0.130 |
| #309 | (production hotfix for #305 / v4.0.130) | **Annotation polymorphic migration uses PRAGMA instead of the stale SQLAlchemy inspector.** v4.0.130's `migrate_annotation_polymorphic_position` failed on the teenyverse deploy with `sqlite3.OperationalError: duplicate column name: position_type` even though the live `PRAGMA table_info(annotation)` showed the column absent — the `annotation` table shipped missing all 4 polymorphic columns (`position_type`, `pdf_page`, `pdf_quad_json`, `comic_page`), so the PDF + comic annotation overlays from #305 sub-projects 3+4 were inert on production. **Root cause**: SQLAlchemy's reflection cache (`inspect(engine)`) is stale after the preceding decouple migration's CREATE/DROP/RENAME sequence — `add_missing_tables` runs `Base.metadata.create_all` first (creating an `annotation` placeholder with the full current model schema, polymorphic columns included), then the decouple migration drops that placeholder + renames `kobo_annotation_sync` → `annotation` (H1-era columns only); the cached inspector kept returning the placeholder's column set, so the existence guard diverged from the real post-rename table + the `ADD COLUMN` fired against a mismatched view. **Fix**: query the live SQLite catalog via `PRAGMA table_info(annotation)` INSIDE the same transaction that runs the `ADD COLUMN`, so the existence check + DDL see the identical DB view; plus per-statement `try/except` catching `duplicate column` as belt-and-suspenders idempotency (one already-present column no longer aborts the rest). **4 regression tests** in `tests/unit/test_annotation_polymorphic_stale_inspector.py` reproduce the exact teenyverse failure (table already has `position_type`), happy path (bare table → all 4), full-noop, and no-table cases. 292 annotation/kobo/hardcover tests green. No data at risk — nullable `ADD COLUMN`s, teenyverse had 0 annotation rows. **Live verified on teenyverse**: `[annotation-polymorphic-position] added 4 columns` in logs, all 4 columns present in the live schema. | `9ad240b2` | v4.0.131 |
| #310 | (fork issue [#288](https://github.com/new-usemame/Calibre-Web-NextGen/issues/288) — two mobile bugs caught on a real iPhone) | **Book titles no longer overlap covers + pagination arrow vertically aligned on mobile.** Two bugs the operator caught on a real iPhone that did NOT reproduce on localhost (Chromium or headless WebKit) — the sim-vs-device split was the diagnostic clue. **Bug 1 (titles overlap covers)**: the home grid uses isotope masonry (`main.js`, `layoutMode: fitRowsCentered`) which absolutely-positions each `.book` card from its measured height at document-ready. On a cold mobile-Safari load the Open Sans web font arrives AFTER isotope lays out, so cards are measured with fallback-font metrics; when the real font swaps in, multi-line titles reflow taller but the absolute positions are stale → titles overlap the covers above. There was no relayout on `document.fonts.ready` and phones never fire `window.resize`, so it never self-corrected (localhost serves the font instantly, hiding the race). **Fix**: (a) `main.js` relayouts the discover isotope grid on `document.fonts.ready` + `window load` (guarded for browsers without the Font Loading API); (b) `fork-mobile-cards.css` reserves a deterministic 2-line title height (`min-height: 2.5em`) so every card is a CONSTANT height regardless of title length or font-load timing — isotope can't mis-measure, making overlap structurally impossible even if a relayout is missed. Proven in actual WebKit with the font response delayed 4s (forcing the fallback-metrics layout): 0 overlaps + uniform 285px card heights both during the delay and after the font + relayout settle. **Bug 2 (pagination arrow misaligned)**: caliBlur sizes the prev/next arrow `<a>` + `:before` glyph with desktop dimensions (`line-height: 60px; padding: 20px 25px; height: 60px`) tuned for its 60px-tall desktop pagination bar; against ~33px mobile page buttons the arrow centered ~9px low. **Fix**: mobile flex-row pagination (`align-items: center`) + collapse the arrow box (`line-height: 1; height: auto; padding: 0 10px; margin-right: 0`) to match the button height — arrow center now 69px == page-button center 69px (was 78 vs 69). **7 regression tests** in `tests/unit/test_mobile_isotope_overlap_and_arrow.py`; 12/12 mobile-layout tests green. Desktop verified unchanged at 1280px (pagination block/fixed, title nowrap/no-min-height — all scoped to `@media max-width: 767px`). | `3c5f3b8f` | v4.0.132 |
| #313 | (real-device fix for #305 sub-project 2 — live Kobo annotation capture) | **Kobo annotation capture now actually routes to CWNG when Kobo sync is on, independent of Hardcover.** Caught by a real-device test (the tester's Kobo, 2026-05-24): the device synced, CWNG received library-sync requests + the self-signed cert was trusted, but CWNG captured 0 annotations — no annotation PATCH reached CWNG at all. **Root cause**: Kobo annotations sync to the *reading-services* host (`readingservices.kobo.com`), a different host than the library/store API. CWNG's `/v1/initialization` response (`cps/kobo.py HandleInitRequest`) rewrites `reading_services_host` to point at CWNG so the device sends annotations to us — but both branches (proxied + unproxied) gated that rewrite on `config.config_hardcover_annotations_sync and bool(hardcover)`. #305 sub-project (2) decoupled the annotation *capture handler* (`cps/readingservices.py handle_annotations`) from Hardcover, but missed this *routing* gate in the init handshake. With Hardcover off (the default), the Init response left `reading_services_host` at `readingservices.kobo.com`, so the device sent annotations straight to Kobo's cloud and CWNG's capture handler was never reached — live Kobo capture was a no-op on the wire. **Fix**: gate the redirect on `config.config_kobo_sync` (the master Kobo sync toggle) in both branches. Safe because `proxy_to_kobo_reading_services()` unconditionally forwards every reading-services request on to Kobo's real servers — CWNG is a passive tap: reading state + annotations still reach Kobo's cloud, CWNG just also captures a local copy. **2 regression tests** in `tests/unit/test_kobo_init_library_sync_rewrite.py` pin that both branches gate `reading_services_host` on `config_kobo_sync` + the solely-Hardcover guard is gone; 283 kobo+annotation tests green. **Why it shipped despite #305's 288 tests + claimed 5-angle e2e**: every non-device test exercised the capture handler directly (synthetic PATCH); none verified the init handshake that decides whether a real device routes there. This is the class of bug only a real-device test catches — final confirmation is the device re-test on this release. | `d66b3d02` | v4.0.133 |
| #314 | (web-reader overlay fix for #305 sub-project 5 — follow-on from the #313 real-device test) | **Captured Kobo highlights now resolve a CFI and render as overlays in the in-browser reader, including live-captured ones with NULL child_index.** After #313 made live Kobo capture route to CWNG, the captured highlights still didn't render as colored overlays in the web reader. **Root cause**: `cps/services/kobo_position.py compute_cfi_range` only used the KoboSpan *selector* path when `start/end_container_child_index == -99` (`KOBO_SELECTOR_SENTINEL` — the value Kobo writes in `KoboReader.sqlite`). But the live reading-services PATCH omits `StartContainerChildIndex` entirely, so live-captured kepub annotations store it as **NULL**. With NULL: selector path skipped → child-index path returned None (no index) → highlight resolved to no CFI → the web reader had nothing to anchor an overlay to. Affected every live-captured Kobo highlight. **Fix**: take the selector path whenever both KoboSpan IDs are present (`start_id and end_id`), independent of `child_index`. The span IDs are the reliable kepub anchor; the child-index DOM walk remains for plain EPUBs with no span IDs (unchanged, still gated by absence of IDs). The `-99` sentinel case is now a subset of 'IDs present', preserving existing behavior. **Verified against the real Nineteen Eighty-Four kepub** (book 523 on the production library): all four of the tester's annotations — her 2 real Kobo highlights (NULL child_index) + 2 test annotations — now resolve to valid `epubcfi()` strings (e.g. `epubcfi(/6/8!/4[kobo.0.15]:0,/4[kobo.0.15]:56)` for the 'BIG BROTHER IS WATCHING YOU' line). The `/annotations/<book>/data.json` endpoint computes + persists `cfi_range` on first read (via `_ensure_cfi_range`), so existing annotations heal on next load with no data migration. The `/annotations/<book>` view page + Markdown/CSV/JSON exports were separately verified to render all annotations (text, note, color) against the real prod data. **New regression test** `test_live_capture_null_child_index_uses_selector` pins the NULL-child-index selector path; existing `-99` sentinel tests unchanged (now a subset); 284 kobo+annotation tests green. CI hit the xdist worker-IPC hang twice, green on 2nd rerun. | `b29a41a0` | v4.0.134 |
| #317 | (web-reader overlay **render** fix for #305 — follow-on from #314) | **Captured Kobo highlights now actually paint as colored overlays in the web reader.** v4.0.134 made the converter return a CFI, but the highlights still never drew on the page — the sidebar listed them, the page stayed blank. **Root cause (two bugs).** (1) The CFI the server computed was unresolvable by epub.js: `epubcfi(/6/8!/4[kobo.0.15]:0,…)` asserts the KoboSpan *is* the document body (`/4`) with a character offset attached directly to an element step. epub.js resolves highlight CFIs via a position-constrained XPath (`stepsToXpath` → `./*/*[position()=2 and @id='kobo.0.15']`) against its rendered DOM, where the span is actually nested several levels deep (`/4/2[book-columns]/2[book-inner]/…`), so it returned `No startContainer found` and drew nothing. (2) `annotations.js` built the highlight class as `"cwa-annotation-overlay cwa-annotation-overlay-" + color`; real Kobo devices store a hex color (`#F6F3B3`, not `"yellow"`), so `classList.add()` threw `InvalidCharacterError` on the space + `#`. **Fix (root cause): a highlight CFI is reader-specific — only the reader can produce one it can resolve.** The KoboSpan id is the one anchor stable across rendering, so the reader now resolves the span in the live document via `getElementById`, builds a DOM Range from the stored offsets (TreeWalker handles nested inline markup), and calls epub.js's own `contents.cfiFromRange(range)` to generate the canonical CFI before highlighting — applied per section on the `rendered` event so it survives navigation. `data.json` now carries the KoboSpan anchors (`start_kobospan`/`end_kobospan` + offsets + `content_id`); colors map (named **or** hex) to an rgba fill + a sanitized class token. `read_book` serves the KEPUB to authenticated users with annotations on an EPUB-family book (the spans only exist in the kepub), with the reading position falling back across the epub/kepub pair so it isn't orphaned. The server-side converter (export/portability) now walks the chapter DOM to emit a structurally valid 3-part source CFI (offsets on text nodes) instead of the invalid element-offset form, and prefers the KEPUB when locating the file on disk. **Why #314's verification missed it**: #314 confirmed the converter *returned* a CFI string + that `data.json` served it; it never drove epub.js to actually resolve+paint that CFI in a browser. This fix was found and verified with Playwright against a live reader. **Verified live**: all four test highlights (two hex `#F6F3B3`, two named `red`; single-span + cross-paragraph) render with exact text coverage (3/3 single-span text matches; cross-span covers precisely its anchor extent), survive paging away and back, position correctly on a 390px mobile viewport and in dark mode, console clean of both prior errors. **Tests**: converter assertions updated to the valid CFI form + a round-trip test that walks the generated CFI back to the highlighted text; `_data_json_row` contract test pins the KoboSpan anchor fields + hex-color pass-through; 89 kobo+annotation tests green. | `2c79bc4` | v4.0.135 |
| #285 | (fork issue [#283](https://github.com/new-usemame/Calibre-Web-NextGen/issues/283), reporter @droM4X) | **Three UI bugs in the Books List (`/table`) bulk-editor's Comments dialog.** (1) Saved or newly typed text rendered as **light gray on white** — almost invisible. (2) Toolbar overflowed the dialog with awkward stacked wrapping. (3) Insert Link + Upload Image buttons did nothing on click. **Root causes**: (1) wysihtml5 copies `getComputedStyle(textarea)` into the iframe body inline style — on the CWNG dark theme that picks up `color: rgb(245,245,245)` and pastes it onto the white sandbox background. (2) Vendor `bootstrap-wysihtml5-0.0.3.css` uses `float: left` on toolbar `<li>` items — wraps poorly when out of horizontal room. (3) Vendor `bootstrap-wysihtml5-0.0.3.min.js` tags its modals with Bootstrap 2 classes (`modal hide fade`); Bootstrap 3+ keeps anything with the inline `hide` class invisible. The metadata editor doesn't render saved HTML links or accept image uploads anyway, so the buttons were vestigial. **Fix**: new `cps/static/css/wysihtml5-fork-iframe.css` is injected INTO the editor sandbox via the x-editable wysihtml5 plugin's `stylesheets` option — sets `body.wysihtml5-editor { color: #333 !important; background-color: #ffffff !important }` so the wysihtml5 inline-style copy can't override, preserves `#a9a9a9` placeholder, picks a sensible link color. New `cps/static/css/wysihtml5-fork.css` loaded into the host page after vendor css — toolbar becomes `display: flex; flex-wrap: wrap` with `gap` controlling spacing, `float: none` cancels vendor's float; `:has()` hides the `<li>` containing `a[data-wysihtml5-command=createLink]` and `[command=insertImage]`. `:has()` supported in all evergreen browsers since late 2023. `book_table.html` wires the host-page css and runs the x-editable defaults override between the wysihtml5 plugin script and `table.js`. **11 source-pin tests** in `tests/unit/test_283_wysihtml5_bulk_editor.py` pin each fix surface. **Live verified on cwn-local via Playwright + JS evaluate**: pre-fix `body_color: rgb(245,245,245)` → post-fix `rgb(51,51,51)`; toolbar pre-fix `float: left` → post-fix `display: flex; flex-wrap: wrap`; both broken buttons' parent `<li>` `display: none` post-fix. | `d4959d5c` | v4.0.112 |

| #328 | (fork issue [#322](https://github.com/new-usemame/Calibre-Web-NextGen/issues/322), reporter @droM4X) | **Shelf names are legible in the manual reorder view.** The drag rows in `shelf_reorder.html` are bare `<div class="list-group-item">`. Bootstrap's readable text color (`#555`) only targets `a.list-group-item`/`button.list-group-item`, so a bare div inherits the layout body color; on the caliBlur dark theme that body color is light, rendering near-white shelf names on the white `.list-group-item` background. Same mechanism as #285. **Theme-safe**: caliBlur scopes its shelf-order overrides to `.shelforder #sortTrue > .list-group-item`, but this #237 reorder template uses `#sortShelves`, so the rows keep Bootstrap's white background in both default and caliBlur themes — a pinned dark text color is correct everywhere. **Fix**: `color:#333` on the row (~12.6:1 contrast on white, clears WCAG AA). 3 regression tests in `test_237_shelf_reorder.py` pin an explicit row text color + WCAG AA contrast against white (RED when the template fix is stashed, GREEN with it). | `bc2c10e8` | v4.0.136 |
| #311 | (fork issue [#276](https://github.com/new-usemame/Calibre-Web-NextGen/issues/276), reporter @magdalar) | **Admin relay-only eReader sends no longer count as the admin's own download.** Continuation of the v4.0.115 multi-recipient "Send to eReader" feature (#276 core). `send_to_selected_ereaders` (`cps/web.py`) called `ub.update_download(book_id, current_user.id)` unconditionally after a successful send, so an admin managing a household's eReaders who sent a book solely to *other* users' addresses had it recorded as their own download — polluting their download history and the hot-books / "downloaded by you" surfaces with books they only relayed. **Fix:** gate the self-download on a new pure helper `send_includes_own_address(own_kindle_mail, selected_emails)` in `cps/services/ereader_send.py` — records the self-download only when one of the sender's own `kindle_mail` addresses is in the sent set (case-insensitive, whitespace-tolerant). The single-target `/send/<id>` route (always sends to the user's own kindle_mail) is unchanged. Addresses the "bonus" half of @magdalar's v2 ask; the headline "users who have this book" view on the book detail page is a separate design-decision feature deferred to `notes/276-recipient-view-design.md`. **16 regression tests** in `tests/unit/test_ereader_send.py`: 14 pin the helper's set semantics, 1 pins the two magdalar-named cases give opposite answers, 1 source-pin asserts the route keeps `update_download` behind the guard (RED on `main`, GREEN here). Existing #276 core test (`test_276_send_to_other_users.py`, 6 tests) still green. No deps, no schema change, no i18n. | `165536a1` | v4.0.136 |

| #335 | (fork issues [#326](https://github.com/new-usemame/Calibre-Web-NextGen/pull/326) audit by @jbelascoain + [#308](https://github.com/new-usemame/Calibre-Web-NextGen/issues/308) reporter @Glennza1962 + [#331](https://github.com/new-usemame/Calibre-Web-NextGen/issues/331) reporter @Gusdezup) | **Kobo sync no longer silently fails behind nginx + admin spinner constrained at the CSS layer.** Two independent fork-side reports converge on the same root cause: nginx's default `proxy_buffer_size` (4 KB) and `proxy_buffers` (8 × 4 KB) are too small for CWA's `/v1/library/sync` response headers (auth, sync tokens, library state). nginx silently drops the response. The CWA log shows no error; the Kobo shows *"Sync failed, please try again."* Both reporters independently traced it to bumping the buffer directives. **Fix**: README gets a "nginx buffer sizes (important for Kobo sync)" callout in the Kobo sync section with the three load-bearing directives (`proxy_buffer_size 32k; proxy_buffers 4 32k; proxy_busy_buffers_size 64k;`), Synology DSM + Nginx Proxy Manager-specific notes, and a link to a new `examples/nginx-reverse-proxy.conf` reference snippet. Troubleshooting entry for *"Sync failed, please try again"* adds buffer-cause as a numbered item so symptom-searchers land here. **Bundled #326 audit by @jbelascoain** — community PR proposed swapping the loading spinner gif to a nicer-looking 250×250 version, but the five `<img id="img-spinner">` callsites (admin Restart/Status modals + config_db + config_edit + cwa_settings) carry no width/height attrs, so the new gif would have rendered at full native size everywhere. Constrain the rendered size at the CSS layer (`#img-spinner, #img-spinner2 { width: 48px; height: 48px; box-shadow: none; }`) so a future gif swap can't regress, and centralise the box-shadow cleanup @jbelascoain inlined in admin.html. **13 source-pinning regression tests** across `tests/unit/test_331_308_nginx_buffer_docs.py` (12) + `tests/unit/test_326_spinner_sizing.py` (3): README mentions both proxy directives + explicitly warns about the silent-failure mode (the #1 diagnostic mistake), troubleshooting entry includes buffer cause, README links to reference config, reference config exists with ≥32K floor + X-Forwarded-* headers + Kobo-buffer motivation comment + modern `http2 on;` directive (not the deprecated `listen ... http2;` combined form), CSS rule exists with ≤96px width/height + box-shadow:none, every template referencing loading-icon.gif uses the constrained id. **Greptile 5/5** after one round of revisions (caught the deprecated http2 syntax + technically-wrong "inherited" wording in the CSS comment — both addressed). | `fb4134a3` | v4.0.139 |

| #333 | (fork issue [#318](https://github.com/new-usemame/Calibre-Web-NextGen/issues/318), reporter @thealmostmighty) | **Manual duplicate-scan + auto-resolve buttons no longer refused on stale follow-up marker.** Both endpoints in `cps/duplicates.py` (`trigger_scan` for the *Run Full Scan* button and `execute_resolution` for the *Execute Auto-Resolution* button) gated on `ingest_batch_follow_up_pending()`, which returns True if any of `cwa_ingest_batch_active`, `cwa_ingest_batch_dirty`, or `cwa_ingest_batch_dirty.running` marker files exist. The dirty/running markers signal "s6 post-batch follow-up hasn't run yet" — NOT "ingest is currently writing books". A stale dirty marker can persist after a clean ingest finishes or after an abnormal container shutdown, **permanently blocking the user's button until the marker is removed by hand**. The task-level gate in `cps/tasks/duplicate_scan.py` is the single source of truth: full scan + non-manual trigger + ingest-pending → skip; manual triggers are exempt by design. The endpoint-level gates shared the same condition but missed the manual carve-out. Fix: remove the redundant endpoint-level gates from both routes. **10 regression tests** in `tests/unit/test_duplicate_trigger_scan_not_gated_by_pending_followup.py` source-pin (a) both endpoints no longer reference the followup-pending check, (b) both endpoints no longer emit the `ingest_in_progress` blocked response, (c) `trigger_scan` still queues `TaskDuplicateScan` with `trigger_type='manual'`, (d) task-level gate is preserved (still calls the check, still exempts manual triggers). Transient-pending detection elsewhere in `duplicates.py` is unchanged. | `59a25cc9` | v4.0.138 |

| #332 | (fork issue [#312](https://github.com/new-usemame/Calibre-Web-NextGen/issues/312), reporter @uschi1) | **Silent KOReader sync failures after migration are now diagnosable + admin → View Logs works.** Three problems collapsed into one user-visible symptom for @uschi1: (1) the kosync gate `_require_kosync_enabled()` (default-off post-migration toggle from upstream Feb 2026) returned 503 with **zero log lines** — her plugin froze and there was nothing to look at; (2) admin → View Logs button had been commented out because `cps/config_sql.py` force-overwrote `config_logfile = /dev/stdout` on every settings load, so the on-disk file was always empty; (3) the existing Download Debug Package zip therefore contained no log content either. **Fix (Tier 1)**: `cps/logger.py` dual-writes to stdout (for `docker logs`) AND a rotating file (5 MiB × 5 backups, up from 100 KB × 2). `cps/config_sql.py` drops the force-reset block and one-shot migrates legacy stdout-only installs back to the file default. View Logs button re-enabled in `admin.html`. `cps/progress_syncing/protocols/kosync.py` gate emits `WARN ... KOReader sync_disabled: rejecting /kosync/users/auth from <ip>. Enable in Admin → CWA Settings → KOReader Sync.` per request; auth-failure + book-checksum-no-match paths promoted DEBUG→INFO with username/checksum context; `update_progress` DB-error log includes user + document. `cps/progress_syncing/settings.py` adds rate-limited (5 min) WARN when `cwa_settings` unreadable. **Fix (Tier 2)**: `cps/debug_info.py` adds `_redact_for_export()`, `_sanitize_log_line()`, `_build_debug_zip(sanitize=…)`. New `/admin/debug/support-pack` route produces a zip safe to attach to a public GitHub issue — secrets stripped from `settings.txt` (known fields + substring match + JWT defense-in-depth, with type preservation so booleans/lists/dicts/password-policy ints aren't corrupted), IPs/paths/auth-headers scrubbed from logs (IPv4-mapped loopback `::ffff:127.0.0.1` preserved, bare `::` from Flask's `[::]:8083` startup line preserved), 10 MiB tail-cap per file, `SANITIZED.txt` marker. New "Get Support Pack" admin button next to the existing two. **60 regression tests** across 6 new test files. Container-verified on cwn-local + Playwright UI pilot of all three admin buttons. Self-review caught 3 real bugs (type-corruption, over-aggressive long-token regex on non-secrets, bare-`::` over-match) all fixed + regression-tested before ship. | `9634ffab` | v4.0.137 |

| #327 | (fork issue [#319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319), reporter @SethMilliken) | **Per-user hidden books were unrecoverable — a book hidden by accident could not be unhidden.** The hide feature (#64 / fork PR #105) shipped with three independent breakages. (1) The `/hidden` listing rendered a blank page: `render_hidden_books` set `page="hidden"`, `layout.html` emits `<body class="{{ page }}">`, and Bootstrap ships `.hidden{display:none!important}`, so the whole body was `display:none`. Changed to a non-colliding `page_name="hidden_books"`. (2) The detail page was unreachable for hidden books: `show_book` calls `get_book_read_archived`, which never forwarded a hidden flag, so `common_filters` excluded hidden books and `/book/<id>` bounced with "unavailable" — and the Unhide toggle lives only on the detail page, so there was no reachable way to unhide. Added an `allow_show_hidden` kwarg threaded from `get_book_read_archived` into `common_filters` (default `False`, so every other caller is unchanged); `show_book` passes `True`. (3) The bare `/hidden` URL (pointed to in #64's own comments) 404'd; only `/hidden/<sort_param>` was routed. Added a `/hidden` redirect to the canonical `/hidden/stored` listing. Scoped to the recoverability bug; the broader UX asks in #319 (hide off by default, distinct icon, feature flag) are a separate enhancement. **5 regression tests** in `tests/unit/test_user_hidden_books.py::TestHiddenBooksRecoveryFlow` — RED on pre-fix code (TypeError on the missing kwarg, missing `/hidden` route, `page="hidden"` collision), GREEN with the fix; the 35 adjacent db/filter unit tests (incl. the 12 `common_filters` policy tests) stay green because the new kwarg defaults `False`. Blast radius: `get_book_read_archived` has exactly one caller. | `d729afa7` | v4.0.136 |
| #337 | (fork issue [#319](https://github.com/new-usemame/Calibre-Web-NextGen/issues/319), reporters @SethMilliken + @droM4X) | **Per-user hide: recoverability bug-class closed AND user-hostility UX causes addressed.** Two halves shipped together. **Bug-half (droM4X verification):** clicking the metadata icon, reading icon, or download icon on a user's own hidden book all bounced with "Selected book is unavailable"; covers were missing from `/hidden/stored` and the detail page; the `/hidden` URL had no discovery surface. Root cause: `get_filtered_book` is the helper every action route uses; v4.0.136 only extended `get_book_read_archived`. Fix: extend `CalibreDB.get_filtered_book` with `allow_show_hidden=False` kwarg (mirroring `allow_show_archived`, default False — every existing caller's semantics unchanged), thread `True` from the eight detail-page action callsites a user can reach for their own hidden book (`get_book_cover` + `get_download_link` in `helper.py`; `serve_book` + `read_book` + audio-reader inner lookup in `web.py`; `render_edit_book` + `do_edit_book` + `get_sorted_entry` in `editbooks.py`). Discoverability: `/me` profile gets a "Hidden Books (N)" button, gated on `hidden_book_count > 0`. **UX-safety half (SethMilliken's original report):** the hide feature shipped on-by-default with the same eye icon as Read/Unread (adjacent buttons), so it was easy to accidentally hide. Three fixes: (1) new `config_user_hide_enabled = Column(Boolean, default=False)` on `_Settings` (idempotent ALTER via `_migrate_table` reflection — legacy DBs gain the column on first boot post-upgrade), admin toggles it in Feature Configuration; the hide button is now suppressed until admin opts in. (2) Read-status icon swapped from `glyphicon-eye-open` / `glyphicon-eye-close` to `glyphicon-ok` / `glyphicon-unchecked` (droM4X's exact wording: "should be changed to a checkmark"); hide button keeps the eye, eliminating the visual collision. (3) Hide button moved to the END of the action row (after Delete) — now 232px from the read-status button on rendered pages. **Defense-in-depth recovery** (admins must not be able to strand users' already-hidden books): `/ajax/togglehidden/<id>` always allows unhide of an existing row but refuses new hides (HTTP 403 + INFO log) when the flag is off; detail-page hide button gate is `(g.user_hide_enabled or is_hidden)` so unhide stays available; `/me` Hidden Books link is count-gated, not flag-gated. `g.user_hide_enabled` is populated in `admin.py`'s `@admi.before_app_request` (mirroring `g.allow_anonymous`) because Jinja's `config` global is Flask's `app.config`, not `cps.config` (PR #335 lesson). **38 source-pin regression tests** in `tests/unit/test_user_hidden_books.py` + new `test_user_hide_ux_safety_319.py` — RED on the v4.0.139 baseline, GREEN with this fix; full unit suite 1203 passing. **Hungarian + German translations** for the two new admin-form strings; all 28 .mo files recompile per `tests/unit/test_translations_compile.py`. **Live `cwn-local` end-to-end with Playwright + direct HTTP**: default-off (no hide button), admin toggle persists (`Felhasználók elrejthetik a könyveket a saját könyvtárukból`), hide click → DB row + icon flip + `/me` shows "HIDDEN BOOKS (1)" + `/hidden/stored` shows cover + all action icons work on the hidden book; admin disables flag → already-hidden book detail page: unhide button still renders and works; direct POST bypass with flag off → HTTP 403 + log line. **Greptile P2 catch addressed**: the `change_profile()` validation-error re-render now also passes `hidden_book_count`, so the recovery link doesn't silently disappear after a bad save. Out of scope per "implement what's asked" rule: confirmation modal on hide-click (no reporter requested), admin-vs-user-level hide split (droM4X's architectural proposal, not a fix-this ask), auto-shelf for hidden books (SethMilliken's alternative to docs — `/me` link satisfies the discoverability ask). | `a5200d21` | v4.0.140 |
| #1209 | (fork issue [#920](https://github.com/new-usemame/Calibre-Web-NextGen/issues/920), reporter @iroQuai) | **A failed KOReader sync discarded the reason, leaving the one failure mode with no server-side trace also with no device-side trace.** @iroQuai's v4.1.21 log (the release that added push/pull logging) shows a highlight-delete cycle emitting one `KOReader auth:` line and a pull, then nothing — no second auth line, no push line, no rejection line. Since #1101 routes every refusal through `_reject`, which logs unconditionally, the push is never made: the fault is device-side, which rules out the whole server-side payload space `notes/920-delete-push-reproduction-attempts.md` had cleared. The naming exception was dropped twice — `logger.dbg`, which KOReader suppresses unless debug logging is on, and `callback(false, res.body)`, nil on a raise because `pcall` returns the error not a response. Every sync call now funnels through one `finish`: warn-level log, reason passed to the callback, reason shown in the toast (getting `crash.log` off an e-reader is a chore). Device half of #1101. NOT a fix for the underlying raise — unreproduced, gate held. Red/green on two CI-gated source pins, one forbidding `logger.dbg(` on any failure path; 4 Lua cases; 488 adjacent tests green. Plugin bumped to 4.1.24. | f61ee8a6c | TBD |

### Security

| Fork PR | Upstream issue | Description | SHA | Release |
|---|---|---|---|---|
| #371 | [fork #370](https://github.com/new-usemame/Calibre-Web-NextGen/issues/370) (community report + patch by @8bitgentleman) | **`POST /duplicates/invalidate-cache` requires authentication.** The endpoint had `@csrf.exempt` but no auth decorators — unauthenticated clients (including the public internet on exposed installs) could force duplicate-cache invalidations (low-grade DoS; least-privilege violation). Copy-paste omission: the adjacent `trigger-scan` route already carried `@login_required_if_no_ano` + `@admin_or_edit_required`. Merged the contributor's 2-line fix + structural regression tests as-is under the `community-contribution` validate-author exemption (authorship preserved). Independent cross-check before merge: the ingest service no longer calls this endpoint (`scripts/ingest_processor.py` uses `/cwa-internal/*` since the #1353-era rework), so adding auth breaks no internal caller. First community security contribution to the fork. | `3d8628627` | v4.0.156 |
| #18 | [#1303](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1303) | Kobo IDOR: `/kobo_auth/generate_auth_token` + `/deleteauthtoken` reject `current_user.id != user_id` and not admin | `9f50bb2` | v4.0.7 |
| #19 | (fork audit; private disclosure to upstream) | 14 CWA routes (cwa_logs, convert_library, epub_fixer) gated with `@login_required_if_no_ano + @admin_required` | `09bf581` | v4.0.7 |
| #20 | (fork audit; private disclosure to upstream) | `cover_enforcer.py` shell-injection: `os.system(f'cp "{title}" ...')` → `shutil.copy` / `shutil.rmtree` | `b70fb53` | v4.0.7 |
| #201 | inspired by [janeczku/calibre-web#3625](https://github.com/janeczku/calibre-web/pull/3625) (@jvoisin) | XSS hardening on book-comment render path. Janeczku's PR #3625 routed only the custom-column `comments`-type `column.value\|safe` render through `clean_string`. We extend the same hardening to the regular `entry.comments[0].text\|safe` render too, in both `detail.html` and `listenmp3.html` — four template sites total. New `clean_string` Jinja filter in `cps/jinjia.py` delegates to existing `cps.clean_html.clean_string` (bleach/nh3) so the filter wrapper is small and the sanitiser is shared with editbooks. Defense-in-depth complement to the save-time sanitiser in `editbooks.py:1603,1666`: a crafted comment imported directly into `metadata.db` from Calibre desktop bypasses editbooks, but cannot bypass the render-time filter. 13 regression tests pin: filter is registered via `@jinjia.app_template_filter('clean_string')`; the import statement matches `from .clean_html import clean_string` so the filter delegates to the real sanitiser; all four template render sites match `\|clean_string\|safe`; the sanitiser strips live `<script>`, `<img onerror>`, `<svg onload>`, `<iframe>`, `<b onclick>`, and `javascript:` vectors (either by stripping or by HTML-entity-escaping — both inert in the browser); benign `<p>/<b>/<i>` markup is preserved. | `bff5f88d` | v4.0.65 |

### Infrastructure

| Fork PR | Description | SHA | Release |
|---|---|---|---|
| [#1118](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1118) | (fork issue [#627](https://github.com/new-usemame/Calibre-Web-NextGen/issues/627)) **Books delivered by "Send to Reader" were never fingerprinted, so KOReader progress from them matched nothing.** KOReader identifies a book by hashing the file it holds. `do_download_file()` registers the fingerprint of the exact file it serves; `TaskEmail._get_attachment()` registered nothing, so an emailed book was a file the server had never hashed — the device's push arrived carrying an unknown checksum, the position was stored orphaned, and it never surfaced in the library (`No book found for checksum`). The same book pulled over OPDS synced fine, which is why this read as a device problem for three weeks across ~36 comments, and why every suggestion in that thread half-worked. **Not simply "hash the library file":** with `config_embed_metadata` on, `_get_attachment` emails a freshly staged `do_calibre_export()` file whose bytes differ from the library copy, and both branches delete that staged file immediately after reading it — so the fingerprint has to be taken inside the send path, because afterwards there is nothing left to hash. **And the filename digest is a second trap:** the device sees the MIME attachment name, while our staged export carries a uuid-ish temp name that lives for milliseconds; registering that basename would map a meaningless name onto the book and would break the plugin's `filename` matching mode, which is exactly the mode this reporter had been asked to use. `calculate_and_store_checksum()` grows an optional `filename_for_matching`, defaulting to the basename so every existing caller is behaviourally unchanged (pinned by a test). Registration happens in **both** branches of `_get_attachment` (Google Drive and local), gated on `is_koreader_sync_enabled()` like the download path — the table does not exist otherwise and every write would raise `no such table` (upstream CWA #1183) — and best-effort, since losing the email would be worse than losing the sync hint. **7 behavioural tests** (`tests/unit/test_627_send_to_reader_checksum.py`), deliberately not source-pinned: the real hashing runs and only the DB write is captured, so each asserts the digest that would actually be persisted, computed independently from a real file with the same function the client uses. Red/green proven by reverting the source and keeping the tests — `.FFF...` → `.......`, the three red ones exactly as predicted (content digest, filename-not-temp-name, embedding-off). Regression sweep **384 passed / 13 skipped** across `-k "kosync or checksum or mail or email or progress"`. Not exercised against live SMTP + a physical PocketBook (reporter's hardware); the seam below that is covered. Reported by [@uschi1](https://github.com/uschi1). No new dependency, license, external URL or route. | `05e7882c9` | v4.1.21 |
| [#1107](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1107) | (fork issues [#958](https://github.com/new-usemame/Calibre-Web-NextGen/issues/958), [#1105](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1105), [#1084](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1084)) | **971 tests — 23% of the suite — were collected by CI and then silently dropped, and the gate reported green while it happened.** The Fast Tests job selects `pytest -m "smoke or unit"`. Marker selection is opt-in, and `tests/conftest.py`'s `pytest_collection_modifyitems` only ever *added skips* — it never applied `unit` to anything — so a file that forgot `pytestmark = pytest.mark.unit` was collected, deselected, and ran in no job at all. Nothing in the output says a deselected test exists; the job is green either way. Measured on `93652eb30`: **937 of 4148 under `tests/unit/` (118 of 434 files), plus 36 under `tests/smoke/`.** Among them were the regression guards for shipped fixes — `test_320_reorder_legacy_glyph`, `test_1089_firefox_scrollbar_overlay` (shipped hours earlier), `test_896_metadata_keys_resolver`, `test_521_unicode_collation`, `test_annotation_migration_preservation` and ~110 more. **This had been diagnosed twice and patched one file at a time** — #957's row in this table notes the #736/#918 theme guard "never gated a PR until now", and #948's notes a marker "set deliberately … an unmarked guard is silently deselected (the #958 class)". #958 filed the general problem and went untouched; #1105 rediscovered it independently. **Root fix: the lane comes from the directory, not from remembering.** `tests/unit/**` → `unit`, `tests/smoke/**` → `smoke`, applied in collection to any item that declares no lane of its own, so a new file cannot be born invisible. A test that declares a lane (`integration`, `slow`, …) keeps it — that is what holds the two deliberately-slow files under `tests/unit/` out of the fast gate. **Collect-only, `tests/unit` + `tests/smoke` under the gate's own selector: 973 deselected → 2**, both documented opt-outs. **`--maxfail=3` made this load-bearing rather than cosmetic**, because turning the lane on turns their failures on too. Three things came out of the dark and are fixed here: **(1)** `test_320_reorder_legacy_glyph` (#1084) scanned the 708 KB `caliBlur.css` with `([^{}]+)\{([^{}]*\\e155[^{}]*)\}` — catastrophic backtracking, since every glyph-free block makes the engine retry each split point of the two runs before failing, replayed across start offsets. Over six minutes locally; **reproduced blowing CI's own `--timeout=120`, stack pinned on the `re.finditer` line**. Now matches the block and tests its body in Python: **0.04s**. The CSS had *not* drifted — exactly one `\e155` block, correctly scoped — so this was a timeout, not the regression it guards. Added a non-vacuity assertion plus a fixture with a real block and a `\e156`/`shelfreorder` decoy, because #1084 warned a rewritten scan could silently match nothing and leave the #320 guard green forever. **(2)** `test_flash_toast_stacking` anchored on the bare string `cwa-toast-stack`, which `str.index` found in a `<style>` rule ~200 lines above the actual `<div>`; both ordering assertions compared against the wrong offset and one reported inline flashes as being inside the floating stack. The template is correct — `layout.html:468/483` render error/success before the container at `:495`. Anchored on the opening tag. **(3)** A regression this change itself introduced, found by control-running the full gate on `main` with identical flags rather than trusting the branch in isolation: **branch 4 failed / main 3 failed**, three identical and local-env-only (`Missing critical directory: /config`, two script `SystemExit`s — they already run in CI and pass). The fourth, `test_ingest_config_full_load`, passed alone and failed in the suite. Bisected to `test_calibre_init.py`, which fakes the whole `cps` package into `sys.modules` and never restored it, so the next file's `from cps import config` got `ImportError: … (unknown location)`. Latent only because the file had never shared a worker with anything. Restored via an autouse fixture and pinned by an ordered two-file subprocess test. **Quarantine instead of silence for the rest:** 37 tests were already red behind the gap, in three clusters — 30 `spec_from_file_location` shims whose hand-stubbed relative imports went stale, 4 in `test_oauth_session.py` that assert against `oauth_bb.{github,google,generic}_logged_in` as module attributes when all three are nested inside `init_oauth_blueprints()` (`cps/oauth_bb.py:922`) and so **cannot ever have passed**, and 3 where `get_language_name('ger')` returns `Unknown` — the one cluster that may be a real user-visible bug and is flagged to be checked against a container before either side is changed. They are listed in `tests/quarantine.py`, one line each with the reason and issue [#1106](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1106); a guard fails if any entry lacks an issue reference or names a file that no longer exists. Nothing that currently passes was quarantined to make this green. **Guards** (`tests/unit/test_ci_test_lanes.py`, 15 tests, `pytestmark` declared explicitly so the guard for the mechanism does not depend on the mechanism to be selected): a test directory with no lane fails; a test file at the `tests/` root fails, since no directory implies a lane there; the workflow's `-m` expression drifting away from the lanes conftest assigns fails; the set of fast-lane files declaring a slower lane is pinned, so opting out is a reviewable diff; and an end-to-end check collects previously-invisible files with the gate's real selector and asserts nothing is dropped. **Cross-family review (Terra) returned BLOCK on two HIGHs and was right on both.** (1) The hook carried no ordering hint, so it relied on pluggy calling the later-registered conftest before `_pytest.mark`'s deselection — true today, guaranteed by nothing; `@pytest.hookimpl(tryfirst=True)` now states it, since a silent flip would re-hide ~970 tests behind a green board. (2) `tests/test_simple_ingest.py` sat at the `tests/` root — deselected by the fast gate for having no marker, and never named by the Integration job's explicit `tests/docker/ tests/integration/` paths. It drives a real container via the `cwa_container` fixture and **ran in no job at all**: the same hole one level up, which the directory guard was exempting by construction. Moved into `tests/integration/`, marked `docker_integration`, and the exemption replaced with a guard that fails on any root-level test file. Also from that review: the two opt-out guards now assert against *collected* markers via the gate's real selector instead of source spelling (a hook that added the lane unconditionally would have passed the regex check), a guard rejects a fast-lane file that declares `requires_docker`/`requires_calibre` but no lane, and the `sys.modules` restore uses a sentinel so a pre-existing `None` failed-import marker is restored rather than deleted. Terra's read that the regex rewrite is equivalent and does not weaken the #320 guard matches an independent check here. The `ger` cluster was split out to [#1109](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1109) with the root cause established — `LANGUAGE_NAMES` is keyed on ISO 639-2/T (`deu`, `fra`) and carries no 639-2/B variant — so it is tracked as the product bug it is rather than as test triage. **Full gate, CI flags:** 4159 passed / 93 skipped, 4m07s (main: 3221 passed, 3m51s) — +938 tests for +16s. **Real CI on the PR: Fast Tests green in 2m48s**, with the three locally-failing tests passing on the runner as predicted. Test-only; no `cps/` runtime change, no route, dependency, license or external URL. | `008c907ef` | v4.1.21 |
| [#1016](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1016) | (fork issue [#875](https://github.com/new-usemame/Calibre-Web-NextGen/issues/875); adopted from [#1014](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1014) by [@chloeroform](https://github.com/chloeroform)) **Calibre's `/usr/bin` entry points are baked into the image, so `calibre_postinstall` stops running on every container start.** The Calibre binaries always shipped in the image (`COPY --from=dependencies /app/calibre`), but the `/usr/bin` symlinks did not, so `calibredb --version` failed on a cold boot, `calibre-binaries-setup` concluded Calibre was "not installed", and it ran `calibre_postinstall` on **every** start, forever, on a container that already had Calibre in it. Cost is hardware-dependent: the reporter measured ~12s of a ~60s boot; the controlled A/B here (same library, same flags, pre-fix image vs patched) measured **2.50s → 0.57s**. The claim in the changelog is scoped accordingly rather than quoting the reporter's number as universal. The links are now created at build time and the boot check passes into the existing `Skipping setup, Calibre already installed` branch. The service is deliberately retained as the repair path for an image without the links (e.g. a non-Ubuntu base). Adopted rather than merged as-is: the contributed `find … -type f -exec ln -s {} /usr/bin \;` also put `calibre_postinstall`, `calibre-complete` and the runtime `.qt6_processed` marker onto `PATH` (22 links where `calibre_postinstall` creates 19), and had no build-time verification, so a future Calibre layout change would silently restore the per-boot cost. Shipped form filters to `-perm -u+x` minus the two non-entry-point helpers and ends in `test -x /usr/bin/calibredb`, which resolves the symlink **without executing it** so the assertion also holds on the cross-emulated arm64 leg. Nothing else moved to build time: the Qt6 / kernel ABI check and the PUID/PGID ownership pass live in `cwa-init`, a separate unit that runs before this one and is gated on its own sentinel — verified by reading the s6 dependency graph, not assumed. 5 regression tests in `tests/unit/test_dockerfile_calibre_symlinks_875.py` **extract the shipped shell expression out of the Dockerfile and execute it** against a synthetic `/app/calibre` (word-level source-pins would not have caught the `PATH` pollution): 4 red on `main`, and 2 red against the contributed expression. Verified on a real image build + cold boot. No new dependency, license, or external URL. | `835d05924` | v4.1.19 |
| [#1060](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1060) | i18n: Russian refreshed — adopted from community PR [#1058](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1058) by [@standhaftsohnsergius](https://github.com/standhaftsohnsergius). The duplicate-scan controls shipped in v4.1.19 and the annotation-sync task labels had no Russian translation and fell back to English; both are translated here, plus a spelling correction in the Kobo delete warning. Verified against the compiled `.mo` (not the `.po`) so fuzzy entries are not counted as coverage — see #1086. `.po` only, no code. | `9688d8fd2` | v4.1.20 |
| [#1082](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1082) | i18n: Brazilian Portuguese refreshed — adopted from community PR [#1072](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1072) by [@pedronora](https://github.com/pedronora). pt_BR 1180/2606 → 1289/2606 translated, 375 → 369 fuzzy; six wrong leftover fuzzies corrected (`Book {} not found` rendered "Token não encontrado", `Recently added Books` → "Livros Baixados", `Books currently being read` → "Versão atual", `Hidden Books`, `Formats:`, `Failed to log in with Generic OAuth`). Contributor's work applied verbatim, then 13 corrections on top under the anti-slop bar: a typo (`Formatdos:` → `Formatos:`), a singular/plural regression (`Read` "Lidos" → "Lido" — it is a single-book cover badge), a noun/verb mistranslation (`Duplicate books`, the Duplicates page `<h1>`, was "Duplicar livros" → "Livros duplicados"), gender agreement (`When locked…` "bloqueado" → "bloqueada", agreeing with "capa"), number agreement (`Top Rated`, `{n} found`), `necessitar` + "de", dropped ellipsis/period, em-dash consistency, and shelf terminology normalised to "estante" (incl. one pre-existing "prateleira"). Cross-family linguistic review (Terra, native-pt_BR reviewer brief) independently confirmed 11 of the 13 and caught the gender-agreement one. Verified with `msgfmt --check --check-format`, the 88-gate i18n suite (28-locale compile + 28-locale `--check`), a full `compile_translations.sh` run (28/28 locales), and a real `gettext` resolution + runtime `.format()` smoke on every corrected msgid. No new route/dependency/URL. | `831dcd800` | `v4.1.20` |
| [#1090](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1090) | i18n: Russian catalog completed — adopted from community PR [#1088](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1088) by [@standhaftsohnsergius](https://github.com/standhaftsohnsergius). The Ko-fi support link added to the classic user menu in #1071 (`cps/templates/layout.html:351,384`) shipped two new msgids, `Support` and `Support Calibre-Web NextGen`, which were the only untranslated entries left in `ru`. ru 2603/2605 -> 2605/2605 translated, 0 fuzzy — the only locale in the repo at full coverage (next best is `hu` at 1646/2605 with 123 fuzzy). Contributor diff adopted verbatim, no corrections needed. Verified by diffing the **compiled `.mo` catalogs** of base and PR rather than the `.po`: exactly 2 msgids added, **0 of the 2603 existing msgstrs changed**, which proves the other 15 changed lines are pure `msgmerge` re-wrap churn (the wrap points moved inside URLs, e.g. `console.cloud.`/`google.com` -> `console.`/`cloud.google.com`) and are semantically inert; re-running `msgmerge --no-fuzzy-matching --update` against `messages.pot` leaves the file byte-stable, so it will not fight the next Update Translations run. Also `msgfmt --check --check-format`, the 125-test i18n suite, real `gettext` resolution of both msgids in `ru`, and a grep pin that `layout.html` actually consumes them. Cross-family linguistic review (Terra) approved as-is: `Поддержать` (perfective infinitive) is the right register for a donation CTA and avoids the `Поддержка` reading of "technical support". `.po` only, no code, no dependency/route/URL. | `45f925758` | v4.1.21 |
| [#1091](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1091) | **The published translation-status table counted fuzzy entries as translated, overstating 27 of 28 locales by 4.7-16.8 points.** `scripts/generate_translation_status.py` counted an entry as translated whenever `msgstr` was non-empty. A fuzzy entry has a non-empty `msgstr` — that is what `msgmerge` guesses — but `msgfmt` excludes fuzzy entries from the compiled `.mo`, so the user sees the English source. The README therefore advertised Norwegian at 33% when 16.5% actually shipped, pt_BR at 64% against a real 50%, and Czech at 34% against 18%; only `ru` (0 fuzzy) was accurate. This is the reporting half of the same failure class as #879 and #1086 — coverage looking fuller than it ships — and it pointed volunteer translators at the wrong languages. **Fix**: `translated` now excludes fuzzy, so it means "reaches the user" and equals what `msgfmt --statistics` reports. The fuzzy count keeps its own column, so nothing is hidden. **Tests**: a synthetic 1-real/1-fuzzy/1-empty catalog pins 1/3 rather than 2/3, and a ground-truth test asserts the generated count equals `msgfmt --statistics` for **every** shipped locale, so the published number can never drift from what the build compiles. Both fail on the pre-fix script (the second naming all 27 mismatched locales). README table regenerated. No dependency, route or external URL. | `398d5003` | v4.1.21 |
| [#1012](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1012) | i18n: Russian catalog completed after v4.1.17 by [@standhaftsohnsergius](https://github.com/standhaftsohnsergius) (ru Last-Translator) — 12 empty msgstrs filled, ru 2582/2594 → 2594/2594. Covers the Kobo shelf-sync (#866) and SPA error-boundary (#855) strings that Russian users were seeing in English. Merged as-is; verified with `msgfmt --check`, the 28-locale compile gate, and a live red/green on `GET /api/v1/i18n/ru.json` (catalog 2412 → 2594, ETag changes so upgraded clients revalidate). | `a935806fb` | v4.1.18 |
| [#1002](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1002) + [#1003](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1003) | (fork issue [#868](https://github.com/new-usemame/Calibre-Web-NextGen/issues/868)) **Every s6 startup unit now announces its entry, so slow boots are attributable.** A user reported a ~5-minute unexplained gap in `docker logs --timestamps` between `[calibre-binaries-setup] Service completed` and the first `[cwa-auto-library]` output. The gap could not be pinned to a service, because the units running in that window emitted nothing when they *started* — only when they finished, or from inside a Python helper. With no entry marker there is no way to separate "s6 had not entered this unit yet" from "the unit was running and working silently", which is the first question any startup-time investigation has to answer. Two units were silent on their default path: `cwa-auto-library` fell straight through to `python3 auto_library.py` (its only tagged `echo` sat inside the `DISABLE_LIBRARY_AUTOMOUNT` branch, so it never fired in the normal case), and `svc-calibre-web-automated` had no log line anywhere in its run script — it went from `export` to `exec s6-notifyoncheck`. #1002 fixes the first, placed *before* the `DISABLE` guard so the skip path is marked too; #1003 fixes the second (the line also repeats on each supervisor restart, which is what makes a crash-loop visible) and pins the invariant. **23 regression tests** in `tests/unit/test_s6_service_entry_announcement.py` parametrised over every discovered s6 `run` script: the first executable top-level statement must be an `echo` (assignments, `export`, `set` and control keywords are transparent; nested `echo`s don't count, since a conditional line is exactly what made `cwa-auto-library` look instrumented while being silent), and that line must name its unit — accepting both the dominant `[<service>]` convention and the older `========== STARTING <NAME> ==========` banner, so existing user log greps aren't broken. 2 tests red on `main` before the fixes (`cwa-auto-library`, `svc-calibre-web-automated`), all 23 green after. **Verified by execution**, not just source-pin: both patched scripts run under `bash` with stubbed `python3` / `s6-notifyoncheck` — `cwa-auto-library` emits the entry line on both the default and `DISABLE_LIBRARY_AUTOMOUNT=true` paths, `svc-calibre-web-automated` emits it and still hands an intact argv to `exec` with `CWA_PORT_OVERRIDE` honoured in both the log line and the readiness probe; `bash -n` clean on both. Community-PR gate on #1002: maintainer review plus an independent cross-family pass (GPT-5.6-Terra), no findings. Reported and originally patched by @chloeroform. No new dependency, license, or external URL. | `8eec88eb8` + `fcd3e4516` | v4.1.18 |
| [#1000](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1000) | (fork issue [#943](https://github.com/new-usemame/Calibre-Web-NextGen/issues/943)) **`docker build .` works without credentials again.** Python and kepubify are mirrored into the private `ghcr.io/new-usemame/pbs-cache` package so image builds don't depend on the GitHub release CDN (which intermittently 404s the Actions egress). That mirror was wired as a hard requirement — the first two `FROM` lines — so every build outside the org died at stage 1 with `error from registry: unauthorized`. A contributor could not build, therefore could not test; #940 is the concrete cost, a plausible-looking init-script change that would have broken fresh installs and every `PUID != 911` install, submitted by someone with no way to run it. New global `ARG PBS_SOURCE` selects the source: it defaults to `upstream` (public release CDN, no credentials) and the three CI image builds pass `PBS_SOURCE=ghcr`. BuildKit only resolves stages the target reaches, so a default build never touches the private package. Both sources follow the same version pins. Verified with real builds on a cold, credential-free container builder: the default path emits **zero** `pbs-cache` references and yields a byte-identical Python tarball (md5 `425746b002b42cbf9cff5eee1cd1a971`, 91,182,150 B) to the mirror's, extracting to a working Python 3.13.14 with `ssl`+`sqlite3`, plus `kepubify v4.0.4` mode 0755; the `PBS_SOURCE=ghcr` path still uses the mirror and never hits the CDN. 7 regression tests in `tests/unit/test_dockerfile_contributor_build.py` (all 7 red on the pre-fix tree). Reported by @chloeroform. No new dependency, license, or external URL — both upstream URLs already existed in `scripts/ensure-python-mirror.sh`. Fork-original. | `33e035fa7` | v4.1.18 |
| [#975](https://github.com/new-usemame/Calibre-Web-NextGen/pull/975) | (fork issue [#657](https://github.com/new-usemame/Calibre-Web-NextGen/issues/657); requested repeatedly through the in-app feedback form) | **Series name and position are shown under book covers in the new UI again.** Book cards in the library, search results, and shelves now render the series and index ("Dune #2") beneath title and author, restoring parity with the classic view — the series-heavy view already showed the position badge but not the series name. Books outside a series are unchanged. Cross-family review (Kimi K3) verdict MERGE, findings dispositioned on the PR. No new route/dependency/URL. Fork-original. | `9b21944c` | v4.1.17 |
| #969 | i18n tooling: stop the translations bot from conflicting community translation PRs. `pybabel extract` restamped `POT-Creation-Date` from the wall clock on every push to main; `msgmerge` fanned it into all 28 locales and the bot committed it — ~99 `.po` commits/month for a field with no consumers. Because gettext emits `POT-Creation-Date` and `PO-Revision-Date` on **adjacent** lines, that bot bump collided with the `PO-Revision-Date` a translator's editor stamps on save: git cannot split adjacent changed lines into separate hunks, so the 3-way merge conflicted. Measured on PR [#938](https://github.com/new-usemame/Calibre-Web-NextGen/pull/938) against merge base `703c96f4f` — main changed *only* `POT-Creation-Date`, the PR *only* `PO-Revision-Date`, and that pair was its **only** conflict, while ~1345 lines of `#:` location churn in the same file merged cleanly (unchanged msgid lines give git separating context the header pair lacks). This is why the same contributor's work had to be re-authored by hand five times in eight days (#718→#721, #820→#822, #844→#860, #895→#929, #938). New `scripts/freeze_pot_creation_date.py` carries the previously committed date into the freshly extracted POT, wired into `update_translations.sh` after `fix_pot_format_flags.py` and **before** the `msgmerge` loop (order is load-bearing — msgmerge is what copies the header into every locale). Safe because the field is unread: absent from every consumer in `cps/`/`scripts/`/`.github/`, `msgfmt --check` passes without it, and gettext takes plurals/charset from `Plural-Forms`/`Content-Type` in the compiled `.mo`; `TestNoConsumersJustifyingTheFreeze` pins that so a future reader fails loudly. Verified on the real pipeline, not just unit tests: a no-op `pybabel extract` now yields a **byte-identical** `messages.pot` (the workflow's existing `git diff --cached --quiet` guard then commits nothing), and after a one-time convergence each locale's header survives `msgmerge` unchanged even when `#:` refs genuinely move (`index.html:9120` → `42120`, header byte-stable). 15 regression tests in `tests/unit/test_pot_creation_date_freeze.py` reproduce the conflict via `git merge-file` (the same 3-way engine GitHub uses for mergeability) and pin the freeze, the wiring, the ordering, and the no-consumer assumption. **Scope limit:** does not retroactively un-conflict #938 or #949 — main already carries the churned date, so #938 still needs one manual resolution, and #949 has genuine content divergence. Kills exactly one class: "went CONFLICTING despite zero real divergence." | `dd6fcee5` | v4.1.16 |
| #378 | test: the Fast Tests job no longer boots the docker compose stack for the two unit-marked tests inside `tests/integration/test_ingest_checksums.py`. The module-autouse `_enable_koreader_sync` fixture (PR #238 work) requested `cwa_container`, costing 33s of dead setup per Fast run when compose boot worked and failing the whole job with 2 setup ERRORS when it flaked (PR #376's third CI failure — after its first two were the #377 freeze). Fixture is now opt-in via `usefixtures` on the three `docker_integration` classes; unit selection runs in 0.06s with no docker. | `a7bca286e` | v4.0.157 |
| #338 | chore: gitignore `.playwright-mcp/` capture artifacts so future Playwright MCP page snapshots (which may contain credentials open in the browser at capture time) cannot be accidentally committed via `git add .`. Defensive only — no tracked files removed, no runtime behavior changes. | `eb4bc33d` | v4.0.146 |
| #6 | CI: rewrite gating so auto-merge actually waits for green CI | `d7e22c6` | v4.0.6 |
| #10 | CI: resolve PRs on `workflow_run` via `.pull_requests[]` | `f8fb1ed` | v4.0.6 |
| #21 | Production-ready GHCR multi-arch release pipeline | `7a26106` | v4.0.7 |
| #27 | Drop deadsnakes PPA, install Python 3.13 from python-build-standalone | `2e5d781` | v4.0.7 |
| #28 | Updater: point release check at this fork; only flag true upgrades | `5997519` | v4.0.8 |
| #36 | Dependabot: route bumps to `needs-review` rather than `safe-tier-1` | `efc9c02` | v4.0.16 |
| #170 | CI: extract tier-policy classify + validate to `scripts/lib/tier_policy.py`. Single source of truth shared by the auto-merge workflow and the autopilot triage script; previously the same logic lived as a Python heredoc in `scripts/triage-prs.sh` and a separate bash block in `auto-merge.yml` and could drift between them. Module exposes `load_policy()`, `classify_upstream_pr()`, `validate_fork_pr()` plus a CLI (`python -m scripts.lib.tier_policy ...`). 22 pytest unit tests cover classify (translation/forbidden/large/deps/small/medium), validate (forbidden path / tier-2 caps / sensitive-diff / header-line negation), CLI shapes, and explicit-path / env-var / fallback loading. No behaviour change for end users; foundation for the auto-merge.yml refactor in a follow-up PR. | `d378d634` | v4.0.55 |
| #171 | CI: tier-label-guard — strip `safe-tier-1` / `safe-tier-2` when applied by an account not in `TIER_LABEL_TRUSTED_LABELERS`. New workflow `.github/workflows/tier-label-guard.yml` (pull_request_target / labeled), reads the trusted set from `.github/policy/tier-policy.config` (sourced from main, not the PR head, so a PR cannot widen its own allow-list). Establishes the label-as-signed-contract semantic that lets a follow-up PR shrink auto-merge.yml's re-validation. | `929bcdb5` | v4.0.55 |
| #172 | CI: auto-merge.yml refactor. Three changes that together drop ~120 LOC of bash and a class of CI bug. (a) Remove the `workflow_run` trigger and its PR-discovery branch (the source of the 2026-05-02 silent-no-op); auto-merge now reacts only to `pull_request: [labeled, synchronize, reopened]`. (b) Replace the inline bash regex validation (forbidden paths / sensitive-diff / size caps) with a single call to `python -m scripts.lib.tier_policy validate-fork-pr` — the shared module is the single source of truth. (c) Replace required-checks polling + direct `gh pr merge` with `gh pr merge --auto --squash --delete-branch`; GitHub itself waits for required checks via branch protection. Tier-2 PRs gate on Integration Tests through tests.yml's `Test Suite Summary` (now tier-2-aware: it fails when integration-tests fails on a `safe-tier-2` PR) — branch protection requires Test Suite Summary, so safe-tier-2 effectively gates on integration-tests via that path. The label-as-contract is upheld by tier-label-guard.yml (preceding PR), so the workflow doesn't have to re-verify the labeler. | `c7109e42` | v4.0.55 |
| #173 | CI: pin auto-merge safety invariants as a unit test (`tests/unit/test_workflow_safety_invariants.py`). Six properties go red the moment a future PR tries to weaken them: (1) `auto-merge.yml` refuses fork PRs, (2) policy-consuming workflows checkout `ref: main` (not PR head), (3) no `pull_request_target` workflow checks out PR head, (4) no workflow grants `actions: write` / `id-token: write` / `deployments: write` without an explicit documented exemption (with reason) in the test, (5) no workflow pushes / mutates the upstream CWA or CW repos (hard rule #2), (6) every workflow with mutating `gh` calls has an explicit `permissions:` block. Existing legitimate broad-perm uses are exempted with justifications (sigstore OIDC on the release workflow; workflow re-dispatch on update-translations). Test runs as part of Fast Tests, so the invariants block any future PR that drifts. | `6df0a43a` | v4.0.55 |
| #175 | CI: backport CI hygiene from CWA #1254 (don't fail on missing DockerHub creds) — closes fork issue [#152](https://github.com/new-usemame/Calibre-Web-NextGen/issues/152) (reporter @jgoguen). `dockerhub-description.yml` and `discord-release-bot.yml` are both manual-dispatch only and had no prior secret-presence gate, so an operator who dispatched either of them without `DOCKERHUB_USERNAME` / `DISCORD_RELEASE_WEBHOOK_URL` configured would see a loud red run for missing creds. Both now lead with a probe step that no-ops cleanly (`::notice::`-level log line, exit 0) when the secret isn't set; the actual `peter-evans/dockerhub-description` and `sillyangel/releases-to-discord` steps are gated on the probe output. Also: bumped `actions/checkout@v3` → `@v4` in `discord-release-bot.yml` (Node 16 runtime in @v3 has been removed from GitHub-hosted runners; the workflow would have failed at the checkout step). Avatar URL + repository default updated to reference this fork. Upstream's #1253 (Dockerfile layer-caching restructure) is **not** ported — our Dockerfile already has the cache-friendly layout (`requirements.txt` at line 122 inside the `dependencies` stage; full app code at line 253 in the final stage), and upstream's restructure also changed base image (ubuntu:noble → debian:trixie) and Python source (deadsnakes PPA → apt-pkg python3.13), neither of which we want — PR #27 deliberately moved off deadsnakes onto python-build-standalone for reliability. The action-version bumps from #1261 that affect us most are out-of-scope here (our other workflows are already on @v4 / @v6 / pinned SHAs). | `cd263645` | v4.0.55 |
| #176 | CI: fix auto-merge.yml case-pattern order so CHANGES-vs-upstream.md actually counts as a CHANGES update. Bash `case` matches positionally — the `*.md\|README*) ;;` branch was placed BEFORE the explicit `CHANGES-vs-upstream.md) has_changes_update=1 ;;` branch, so every tier-2 PR's CHANGES.md row was being swallowed by the docs wildcard. Effect: tier-2 PRs that DID update CHANGES-vs-upstream.md (the correct behaviour) still got demoted on "tier-2 PRs with code changes must update CHANGES-vs-upstream.md" — observed on PR #175 in the May-13 wave; both PRs in that wave had to be admin-merged because of this. Fix is a 4-line reorder + a comment explaining the precedence requirement. New `tests/unit/test_auto_merge_changes_file_classifier.py` extracts the live case-block from auto-merge.yml at test time and runs it under real bash against 6 representative file shapes (workflow YAML + CHANGES.md → must pass; code without CHANGES.md → must demote; pure CHANGES.md / pure docs / translations-only → all classified correctly). A 7th source-pin test enforces the textual precedence so a future PR can't accidentally re-order the patterns. The behavioral 6 + source-pin 1 all fail against the pre-fix workflow and pass against the fix — verified locally. | `66a65c26` | v4.0.55 |
| #194 | Add `.cwa_migrations/` to `.gitignore`. The directory is created at runtime by `cps/ub.py.run_cwa_migrations()` under `CONFIG_DIR` as an idempotency marker store for one-time data migrations (e.g. `duplicates_sidebar_v1`). When a developer mounts `/config` at the repo root for local container runs, these markers surface as untracked files on every container restart, which trips the autopilot preflight `repo/ has uncommitted changes` gate every tick. Pure ignore rule — no behavior change. | `627cd488` | v4.0.61 |
| #197 | (closes the route-collision behind #196 from @droM4X) **Docker HEALTHCHECK probed the wrong endpoint.** `Dockerfile:311` ran `curl -fsL http://localhost:${CWA_PORT_OVERRIDE:-8083}/` which redirects to `/login` and returns 200 regardless of whether the Calibre `metadata.db` is reachable. Container orchestration (Docker / Compose / Kubernetes) thought everything was healthy while OPDS, the book grid, and downloads were broken underneath. Retargets HEALTHCHECK at the existing `/health` endpoint (`cps/web.py:1051`) which opens `metadata.db`, runs `SELECT 1` with a retry on `database is locked`, and returns 503 on failure — exactly what container orchestration needs to make routing decisions. Also switches `localhost` → `127.0.0.1` to avoid the IPv6 AAAA-first resolution path on hosts where the WSGI server only binds v4. 3 regression tests in `tests/unit/test_dockerfile_healthcheck.py` pin: the probe target (`/health`), the `CWA_PORT_OVERRIDE` substitution, and a forbid-regression assertion on probing bare `/`. Inspired-by @droM4X — the right diagnosis in #196; this ships the Dockerfile half cleanly without the duplicate-route bug in that PR's app code (re-registering `/health` from `cps/__init__.py` would crash startup with Flask's `View function mapping is overwriting an existing endpoint function` since `cps/web.py:1051` already defines it). | `895481be` | v4.0.61 |
| #177 | Pin the privilege-drop contract for `scripts/cover_enforcer.py`. Follow-up from the May-13 s6 audit (`notes/s6-privilege-drop-audit.md`, autopilot-side) prompted by issue #162. `metadata-change-detector/run` invokes `cover_enforcer.py` as root by design (calibredb / ebook-polish need root), then the script's `_reset_book_dir_ownership` helper explicitly chowns `book_dir` and every entry inside back to PUID:PGID so the Flask app (running as `abc`) retains write access for subsequent cover-from-URL saves. Audit verified end-to-end on cwn-local: `app.db` and `metadata.db` (both WAL journal mode) plus their `-wal` / `-shm` sidecars are all `abc:dialout`-owned, because Flask creates them before cover_enforcer ever runs. No service-level change needed. But if a future contributor removes the chown helper assuming privileges are dropped elsewhere, the user-visible symptom is silent — Flask just starts failing cover-from-URL saves with permission denied. New `tests/unit/test_cover_enforcer_chown_contract.py` source-pins: (1) `_reset_book_dir_ownership` exists; (2) it reads `PUID` and `PGID` from `os.environ`; (3) it actually calls `os.chown` (AST-walk); (4) it walks `book_dir` contents via `os.listdir` with at least 2 chown calls (book_dir + per-entry); (5) `metadata-change-detector/run` still invokes `cover_enforcer.py` — inverse pin so a refactor that routes around the script invalidates the audit on the next test run rather than silently. 5 tests, all green. | `8abe360e` | v4.0.55 |
| #178 | (fork issue [#160](https://github.com/new-usemame/Calibre-Web-NextGen/issues/160), reporter @droM4X) | **OPDS locale resolver chain for anonymous clients.** Continuation of #121 ([#137](https://github.com/new-usemame/Calibre-Web-NextGen/pull/137) + [#138](https://github.com/new-usemame/Calibre-Web-NextGen/pull/138)) — those landed the `N_()` wrappers + the Hungarian translations, but droM4X's follow-up reported that clients sending no `Accept-Language` (Readest, KOReader, some Kobo clients) still got English because the Guest user has `locale='en'`. The four-quadrant HTTP behaviour audit (anon-on/off × creds valid/invalid) was already correct after v4.0.46; the remaining gap was operator-tunable + per-request locale resolution. New precedence chain in `cps/cw_babel.py:get_locale()`: (1) `?lang=xx` query-param override, validated against shipped translations (droM4X's specific ask — lets users force a locale per-request via OPDS URL); (2) authenticated user `user.locale` (existing); (3) `Accept-Language` HTTP header negotiation (existing); (4) new `config_opds_default_locale` ConfigSQL field, **only** consulted when `request.path.startswith('/opds')` so the web UI isn't affected; (5) `'en'` final fallback (existing). New ConfigSQL column `config_opds_default_locale` (`String(8)`, default `""` — empty default keeps existing deployments byte-identical) surfaced in Admin → Configuration → Default Visibilities for New Users → "OPDS Default Locale for Anonymous Clients" with `(none — fall back to English)` as the first option. `_coerce_locale()` helper rejects unparseable input + unshipped locales cleanly so an unknown `?lang=zzzz` falls through to the next step instead of 500. **Live verified on cwn-local**: anon `Accept-Language: hu` → Hungarian; `?lang=de` → German (Regale = Shelves); `?lang=hu` overrides `Accept-Language: de` → Hungarian; `?lang=zzzz` falls through to `Accept-Language: fr` → French (Livres alphabétiques); `OPDS_DEFAULT=hu` + no `Accept-Language` → Hungarian; `OPDS_DEFAULT=hu` + `Accept-Language: de` → German (Accept-Language wins); web UI (`/`) with `OPDS_DEFAULT=hu` → English (gating works). 10 regression tests source-pin the precedence order + the `/opds` path gating + ConfigSQL column shape + admin save handler wiring + locale coercion semantics. | `b7292240` | v4.0.56 |
| #179 | [#1258](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1258) | @jgoguen | (closes fork issue [#153](https://github.com/new-usemame/Calibre-Web-NextGen/issues/153)) **Add per-user OPDS shelf exposure storage** — backend-only data model. New `User.opds_only_shelves_sync` flag (Integer, default 0), two new tables `opds_shelf_exposure` + `opds_magic_shelf_exposure` (each `UNIQUE(user_id, shelf_id)`, indexed FKs), four module-level helpers (`is_opds_shelf_exposed_for_user`, `set_opds_shelf_exposed_for_user`, magic-shelf variants). Wired into `add_missing_tables()` so the migration runs idempotently on existing installs. No user-visible change on its own. Mirrors the `kobo_only_shelves_sync` pattern verbatim. | `6009fbb2` | v4.0.56 |
| #179 | [#1259](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1259) | @jgoguen | (closes fork issue [#153](https://github.com/new-usemame/Calibre-Web-NextGen/issues/153)) **Enforce per-user OPDS shelf filtering in backend** — the filter layer that consumes the CWA #1258 storage. New helpers in `cps/opds.py`: `opds_only_selected_shelves`, `get_opds_visible_shelves`, `get_opds_visible_magic_shelves`, `is_opds_entity_exposed`, `authorize_opds_entity`, `get_opds_book_filter` (UNION query across exposed shelves), `fill_opds_indexpage`, `is_opds_book_exposed`. `CalibreDB.common_filters()` gains an additive `extra_filter` kwarg (defaults `true()` — no behavior change for non-OPDS callers). `cps/magic_shelf.py` refactored: new `get_book_ids_for_magic_shelf` + `build_book_query_for_magic_shelf` helpers extracted from `get_books_for_magic_shelf`; the OPDS layer calls `build_book_query_for_magic_shelf` to UNION magic-shelf book IDs into the `extra_filter`. **Fork-specific preservation**: `allow_show_hidden` (from #64 hidden-books) merged with `extra_filter` in `common_filters` signature so both features compose; series-outerjoin sort fix (from #38, CWA #1233 backport) preserved in `build_book_query_for_magic_shelf` so ORDER BY series.name still returns non-empty results. 16 upstream regression tests imported (`tests/unit/test_opds_sync_restrictions.py`) — all pass against our cherry-picked state. **Live verified on cwn-local**: 2 public shelves created → all visible by default; flip `opds_only_shelves_sync=1` → 0 shelves visible in `/opds/shelfindex`; insert one `opds_shelf_exposure` row → exactly that shelf surfaces. | `6009fbb2` | v4.0.56 |
| #179 | [#1260](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1260) | @jgoguen | (closes fork issue [#153](https://github.com/new-usemame/Calibre-Web-NextGen/issues/153)) **Add UI for per-user OPDS shelf exposure settings** — surfaces the data model + backend from CWA #1258/#1259 in the user, shelf, and magic-shelf edit pages. `user_edit.html` gains the per-user "OPDS expose-only-selected" toggle; `shelf_edit.html` and `magic_shelf_edit.html` gain a per-shelf "Expose this shelf in my OPDS feed" checkbox shown only when the user has the master toggle on. `user_table.html` adds the column to the admin user grid. Form handlers in `cps/admin.py`, `cps/shelf.py`, `cps/oauth_bb.py`, `cps/usermanagement.py` persist the new settings via the storage helpers. 5 upstream regression tests imported (`tests/unit/test_opds_settings_persistence.py`) — all pass. Combined with CWA #1258 + #1259, this closes fork #153. | `6009fbb2` | v4.0.56 |
| #186 | (fork issue [#160](https://github.com/new-usemame/Calibre-Web-NextGen/issues/160) follow-up, reporter @droM4X) | **OPDS `?lang=` propagation through navigation/pagination/search/OSD.** Continuation of #178 / v4.0.56 — that PR shipped the four-step locale resolver in `cps/cw_babel.py:get_locale()`, but every link in the OPDS XML response was built with bare `url_for(...)` and dropped `?lang=hu` on the floor. Set `?lang=hu` on `/opds`, get Hungarian root, click any nav entry, revert to English. @droM4X's "only the second half of the task was implemented" caught this — the resolver was in, the response side of the loop wasn't. Four parts: (1) new `@opds.url_defaults` callback injects `lang` from `request.args` into every `url_for` in the OPDS blueprint when not already set, so following any nav link preserves the language; (2) pagination `rel=first|next|previous` links now use new `_opds_paged_url` helper that preserves every inbound query arg except `offset` — was: `request.path + '?offset=N'` which clobbered everything; (3) `/opds/search/<path:query>` strips the URL query string from `RAW_URI` before splitting out the search term — without this, `url_defaults` adding `?lang=hu` to every search URL made `.split('/opds/search/')[1]` capture `term?lang=hu` as the search term, and every path-form search would have returned zero matches; (4) `/opds/osd` advertises the resolved request locale via `get_locale()` instead of hardcoded `en-EN`, and its OpenSearch URL templates compose `{searchTerms}` correctly relative to any propagated query string (path-form keeps the marker in the URL path; query-form joins with `&` when a query string is already present). 16 regression tests in `tests/unit/test_opds_lang_propagation_160.py` source-pin every fix (so a refactor can't silently regress) and behaviorally drive Flask `url_for` and `_opds_paged_url` under varied request contexts. **Live-verified on cwn-local against a 21-book library**: `/opds?lang=hu` titles Hungarian + all nav hrefs carry `?lang=hu`; `/opds/new?lang=hu&offset=5` has `rel=next` = `…&offset=10` and `rel=first` strips offset; pagination tooltip = "Következő" (Hungarian for "Next"); `/opds/search/crime?lang=hu` returns 1 match identical to no-lang variant (search term not contaminated); `/opds/osd?lang=de` advertises `<Language>de</Language>` with clean URL templates; mixed 60 concurrent requests (30 anon + 30 admin) all 200, zero errors in container logs; malformed `lang=` values (empty, garbage, SQLi-shaped, `<script>`) → 200 with en fallback, no 500s; locale normalization `pt-BR` ≡ `pt_BR` ≡ pt_BR translation, `zh-Hans-CN` → Chinese Simplified; 4-quadrant auth matrix unchanged from PR #137/#178; #121 regression check shows 0 WARN log lines for anonymous probes. | `d04be560` | v4.0.57 |
| #161 | Community contribution from @Amjad50 — **drag-and-drop book merge with per-format conflict resolution**. Drag one book cover onto another in the grid to merge their formats; the source book is deleted after merge, the keeper retains all formats. When the source and keeper share a format, a confirmation modal surfaces a per-format radio (Keep existing / Replace with source). Server-side: existing `/ajax/mergebooks` endpoint extended with optional `overwrite_formats` list (backwards-compatible — empty list = original keep-only behavior); `@user_login_required` + `@edit_required` decorators block anonymous and view-only users (verified: viewer-role curl POST → 403). Client-side: CSS / modal HTML / JS bundle gated behind `{% if current_user.is_authenticated and current_user.role_edit() %}` so read-only users never receive the surface. CSRF via global Flask-WTF middleware + `X-CSRFToken` header. JS adds drag-flicker prevention via shield divs, focus on Cancel by default, multiple close affordances (Cancel button / Esc / backdrop click), MutationObserver for dynamically-loaded books, no-op-during-multi-select-mode gating. Live-verified end-to-end on cwn-local with Playwright: single-conflict modal (book 3 → 6) shows 1 EPUB conflict row + EPUB+PDF result preview; two-conflict modal (book 7 → 6) shows both EPUB + PDF rows; replace-then-confirm flow merges correctly with source deletion + file replacement on disk. PR landed under the operator-applied `community-contribution` label which exempts validate-author. Credit @Amjad50 (Amjad Alsharafi). | `550595c5` | v4.0.56 |
| #156 | **Deflake `test_cwa_db` + `test_generate_checksums`** (the two long-documented "pre-existing CI flakes" finally root-caused and fixed). Two deterministic bugs that have failed intermittently for months depending on CI worker load + test ordering. **Root cause #1**: `scripts/cwa_db.py:22` hardcoded `self.db_path = "/config/"` regardless of the `CWA_DB_PATH` env var the test fixture (`tests/conftest.py:331`) was already setting via `monkeypatch.setenv`. Every parallel pytest-xdist worker opened the same shared `/config/cwa.db`, surfacing as `sqlite3.OperationalError: database is locked` under write contention and wrong-timestamp readbacks where one worker's INSERT for `book_id=1` overwrote another's. Fix reads `CWA_DB_PATH` (defaults to `/config/` so production is byte-identical) and `os.makedirs(..., exist_ok=True)` before sqlite3 connect for defense-in-depth (also unblocks the pre-existing buggy `test_handles_missing_database_gracefully` that pointed at a non-existent tmp_path subdir expecting auto-creation). **Root cause #2**: `scripts/generate_book_checksums.py:36-37` imported `cps.progress_syncing.checksums` + `cps.progress_syncing.settings` at module top, forcing a full `cps/__init__.py` boot (Flask + SQLAlchemy + plugins + logger) on every subprocess invocation. Measured ~1.5s locally; 30+s under CI parallel-worker contention, tripping the 30s `subprocess.run(..., timeout=30)` in the test. Fix moves the heavy imports inside `generate_checksums()` body and adds a `_check_koreader_sync_enabled_lightweight()` helper that reads the `cwa_settings.koreader_sync_enabled` column via direct sqlite3 (honoring `CWA_DB_PATH`, fail-closed semantics matching production) so the early-exit path doesn't pay any cps cost. Disabled-path subprocess: 1.5s+ → **0.039s**. Test timeouts also bumped 30s→120s with explanatory comment as belt-and-suspenders against future regression. 3 new regression tests pin: env-var-respect, `/config/` default-when-unset, parallel-instance-with-distinct-paths isolation. **Verified**: CI passed cleanly on first run (Fast Tests + Test Suite Summary green, no `--admin` override). After this PR, future PRs ship through CI without flake-driven retries — the `--admin` override used to merge #149 (Phase 2 cover-preview) is no longer necessary for the documented "pre-existing flakes" reason. | `80b7bc7c` | v4.0.52 |
| #208 | **Move single-instance lock acquisition out of module-import time** in the three scheduled scripts (`scripts/ingest_processor.py`, `scripts/kindle_epub_fixer.py`, `scripts/convert_library.py`). All three historically did `open(path, 'x')` + `atexit.register(removeLock)` + `sys.exit(2)` on collision **at the module top level** — which meant `importlib.import_module(...)` from a pytest-xdist worker (workers share `/tmp` across processes) could crash a sibling worker that had already taken the same lock. That's the root cause behind the test-isolation flake patched narrowly by #204 (test-only mitigation, deeper refactor deferred). Each script now has a `_acquire_lock_or_exit()` (or `_acquire_process_lock_or_exit()`) helper called from `main()` — importing the module is a pure load with no filesystem side-effects. `convert_library.py` also hardened for CI / non-container environments (missing `/config` dir, missing `abc` user) with `try/except` around its `FileHandler` setup and the `pwd.getpwnam('abc')` lookup, so a test runner can subprocess-import the script without root and without the LinuxServer image's user table. New regression test `tests/unit/test_script_import_side_effects.py` (20 tests) pins the contract via three layers: (1) subprocess imports of each script return rc=0 and create no lockfile in `/tmp`, both with no prior lockfile and with a stale sibling-script lockfile present; (2) AST source-pin tests assert no module-level `sys.exit()` on lock collision and no module-level `atexit.register()` — a future refactor that re-introduces import-time side effects fails the test; (3) AST wire-up test asserts each script's `main()` actually calls its `_acquire_lock_or_exit()` helper, so a refactor that silently removes the call (losing single-instance protection in production while the import-side-effect tests still pass) is caught. Verified locally: all 20 tests green, `python3 -c 'import <script>'` for each creates no lockfile in `/tmp`. Eliminates the entire class of "PR landed red Fast Tests because of cross-xdist-worker lockfile pollution" that #204 patched narrowly. | `b141b672` | v4.0.64 |
| #209 | Community contribution from @RichardGubijev — **"Apply Default Settings" admin button is now red (`btn-danger`) to differentiate it visually from the adjacent grey "Submit" button.** Reporter's rationale: they'd hit the destructive button by accident multiple times while looking for "Submit" next to it. The red colour matches the existing convention for destructive admin actions (`Restore Calibre Database` etc.). One-line CSS class swap in `cps/templates/cwa_settings.html:1218` (`btn-default` → `btn-danger`); button position preserved (some users have muscle memory for the current layout). No JS or Python touched. PR landed under the operator-applied `community-contribution` label which exempts validate-author. Credit @RichardGubijev. | `e4367df8` | v4.0.67 |
| #246 | (fork issue [#244](https://github.com/new-usemame/Calibre-Web-NextGen/issues/244)) **Integration test suite `test_ingest_checksums.py` was failing on every PR opened against `main` since v4.0.79 / PR #238 shipped** — six tests assertion-failed with `len(checksums) == 0`. After 6 consecutive autopilot ticks blocked at the repo-WIP precondition gate, this lands the proper fix. **Root cause:** PR #238 / fork #219 gated `book_format_checksums` writes on `cwa_settings.koreader_sync_enabled` so disabled-sync instances skip the slow partial-MD5 work. The schema default in `scripts/cwa_schema.sql` is 0, so a fresh test container left the suite asserting against zero rows. **Fix:** module-scoped autouse fixture flips the flag to 1 on the session container before any test in the module runs, then resets it to 0 on teardown so the session-scoped container is clean for later modules. `is_koreader_sync_enabled()` in `cps/progress_syncing/settings.py` re-reads `cwa_settings` via a fresh `CWA_DB()` on every call, so the in-container ingest_processor picks up the flip without a service restart. **Rejected alternative:** the WIP version of this branch added `pytest.skip()` when the flag was 0 — that would have made CI green but stopped exercising the actual ingest -> checksum-write path. Coverage theater, not a fix. CI green on the fix: Integration Tests (Docker) passed cleanly on the first run. | `706fd74d` | v4.0.85 |
| #282 | (diagnostic follow-up to `notes/pr-281-ci-hang-diagnosis-2026-05-21.md`) **CI fast-tests now surfaces hung tests as named failures instead of opaque step timeouts.** PR #281's Fast Tests job timed out after 10 minutes with no indication of which test or which pytest-xdist worker hung — visible stdout ended at `[gw0] [97%]` then 9 minutes of silence. Same opacity blocked PRs #279, #280, #266. Root cause for the opacity (separate from the underlying flake): `pytest -n auto` parallelism with no per-test timeout means a stuck test on one worker only manifests as a step-level 10-minute timeout, with no signal pointing to which test or worker stalled. Adds `--timeout=120 --timeout-method=thread` to the pytest invocation in `.github/workflows/tests.yml`. Future hangs surface as named test failures with stack traces. `thread` method is required because pytest-xdist parallel mode breaks the default `signal`-based timeout. `pytest-timeout>=2.2.0` is already installed via `requirements-dev.txt` — no dependency change. Step-level `timeout-minutes: 10` retained as last-resort kill. No app behavior change. | `7a66b060` | v4.0.110 |
| #296 | (CI flake mitigation, follow-up to PR #282 + notes/xdist-worker-ipc-hang-followup-2026-05-21.md) | **Two CI workflow mitigations for the xdist worker-IPC hang flake.** PR after PR through v4.0.110–v4.0.121 hit a documented xdist worker-IPC hang on ~1/3 of Test Suite runs: pytest-xdist's master waits forever for a worker between assignments, no currently-executing test for `pytest-timeout` to interrupt, only the step-level 10-minute kill catches it. Two complementary changes in `.github/workflows/tests.yml`: **(1) Scope `push.branches`** from `['**']` to `[main, dev]`. Previously a fork-branch push fired BOTH the `push` event AND the `pull_request` event for the same SHA → two parallel Test Suite runs competing for the same xdist resources, doubling the hang surface. Now PR branches get CI exclusively via `pull_request:`. Verified self-applying on PR #296's own CI: only 1 Test Suite run triggered on push (was 2 previously). Cuts CI minutes by ~50% too. **(2) Add `--dist=loadfile`** to the Fast Tests pytest invocation. Default xdist `--dist=loadbalance` work-steals individual tests between workers — the work-stealing race is exactly the failure mode the prior investigation fingered. `loadfile` pins each test file to a single worker → fewer handoffs → fewer races. Keeps `-n auto` so parallelism remains. **3 source-pin tests** in `tests/unit/test_ci_xdist_hang_mitigation.py` prevent silent regression: push.branches doesn't include `'**'`, pytest invocation includes `--dist=loadfile`, `--timeout=120 --timeout-method=thread` (PR #282) still present. **Empirical**: PR #296's own CI failed once at the same 10-min step timeout on the same `test_store_checksum_after_ensure_calibre_db_tables` test, then went green on rerun. So `--dist=loadfile` reduces but doesn't eliminate the hang — there's a residual issue (possibly a sqlite C-level mutex that `--timeout-method=thread` can't interrupt). Residual hang tracked in the same followup note for v2. The push-trigger fix alone is unambiguous progress: halves CI surface regardless of whether the test-specific bug ever gets diagnosed. | `2f2ecdc9` | v4.0.122 |
| #297 | (root-causes the residual half of the xdist worker-IPC hang flake — followup to #296 / v4.0.122) | **Sqlite connection cleanup in `test_store_checksum_after_ensure_calibre_db_tables`** — root cause of the residual xdist hang traced and eliminated. PR #296 (v4.0.122) added two surface-area mitigations (push-trigger scope + `--dist=loadfile`) but one of #296's own CI runs still timed out at exactly the same `test_store_checksum_after_ensure_calibre_db_tables` step. **Root cause**: the test called `conn = sqlite3.connect(str(db_path))` but never `conn.close()`. When the test function returned, the connection became unreachable → gc'd → `__del__` ran `sqlite3_close` at the C level — which can block on the sqlite mutex. Under pytest-xdist this gc happens DURING the worker → master IPC handoff: if sqlite's `__del__` blocks waiting on a mutex while xdist is also waiting for the worker's 'finished' signal, the master deadlocks. The test's assertions all pass but the runner never receives the completion signal — master waits the full 9 minutes until the step-level 10-minute kill fires. **Fix**: wrap the test body in `try/finally` with explicit `conn.close()`. Connection closes synchronously at the end of the test, before the worker hands the result back to the master. sqlite mutex released before IPC. Pure test-hygiene change — no production code change. **Empirical confirmation**: PR #297's CI passed first try, single run, no rerun-on-flake. After v4.0.110–v4.0.122 needed reruns on roughly half of all PR submissions due to this hang, the first PR after the fix sailed through cleanly. Addresses `notes/xdist-worker-ipc-hang-followup-2026-05-21.md` from the original investigation. | `a005762e` | v4.0.123 |
| #300 | (final xdist hang fix — closes the four-PR series; follow-up to PRs #296 / #297 / #298) | **Reclassified the cps-import-triggering test as integration so it no longer runs in the Fast Tests xdist pool.** PR #297 closed an unhandled sqlite connection and PR #298 deferred a heavy `cps` import inside `store_checksum`, both reducing the xdist worker-IPC hang incidence but not eliminating it. Tracing the next-shallowest source: the test does `from cps.progress_syncing.models import ensure_calibre_db_tables`, and `cps/progress_syncing/models.py` imports `cps.db` (full SQLAlchemy Base + heavy calibre-library wiring) and `cps.ub` (user database with all migrations) at module level. Both trigger the heavyweight cps package init — under pytest-xdist subprocess context those imports hang on resources that don't exist in the worker. Refactoring the module-level imports would require touching modules used throughout cps (out of scope for a CI flake fix). The test isn't a unit test by definition: it does real sqlite I/O AND triggers heavyweight cps init. **Fix**: change the class decorator from `@pytest.mark.unit` to `@pytest.mark.integration`. Moves the test out of the Fast Tests xdist pool entirely — Integration Tests runs in a separate CI job under Docker without the same parallelism issues. PR #300's own CI passed first try, no rerun needed — first datapoint of a clean run since the hang started cropping up across v4.0.110+. **Four-PR series summary**: #296 (drop dual-trigger CI surface + `--dist=loadfile`) → #297 (close sqlite connection in `try/finally`) → #298 (defer cps import inside `store_checksum`) → #300 (move test to integration). Each PR reduced the hang surface; #300 eliminates it from Fast Tests entirely. Addresses `notes/xdist-worker-ipc-hang-followup-2026-05-21.md`. **Coincidentally GitHub PR #300** — fork milestone. | `6e5184eb` | v4.0.126 |
| #321 | CI: `validate-author` skips merge commits (`--no-merges`) so a web-UI "Update branch" doesn't block auto-merge. The committer scan flagged any commit whose committer email wasn't new-usemame's; GitHub's "Update branch" button produces a merge commit committed by `GitHub <noreply@github.com>` (the clicker is the author, not the committer), so every PR that sat long enough for `main` to advance and need a branch update failed the gate — a recurring catch-22 unrelated to who authored the code (observed blocking docs PR #316 after a 14:03Z web-UI update). Merge commits carry no original authored content; their parents are validated independently. Substantive single-parent commits by a foreign committer are still caught (no over-relaxation). Wall 7 in `tests/unit/test_workflow_safety_invariants.py` (3 tests) source-pins `--no-merges` and behaviorally verifies a web-UI merge commit is skipped while a foreign non-merge commit is still flagged. | `e5597943` | v4.0.136 |
| #338 | Add `.playwright-mcp/` to `.gitignore`. The Playwright MCP server writes page/console capture artifacts to that directory during local UI verification; those snapshots can incidentally carry auth headers, CSRF tokens, or API keys from whatever page was open at capture time, so ignoring the directory keeps them from being committed. Mirrors the existing `.cwa_migrations/` ignore rule (#194). Pure ignore rule — no tracked files affected, no behavior change. | `eb4bc33d0` | v4.0.146 |

| #960 | (fixes the delivery half of [fork #400](https://github.com/new-usemame/Calibre-Web-NextGen/issues/400), reported missing by @KucharczykL) **The KOReader plugin is published to `new-usemame/cwasync.koplugin` by the release itself instead of by a step someone has to remember.** PR #864 (`6fb33f74b`) moved plugin distribution off per-app-release assets and onto a dedicated repository (correct: app tags that don't touch the plugin must not look like plugin updates), but replaced the workflow with a manual `scripts/publish-cwasync-plugin.sh --publish` tracked as prose in `notes/org/LEDGER.md`. It was never re-run: v4.1.12, v4.1.13 and v4.1.14 all shipped while the dedicated repository stayed on the v4.1.11 plugin, so the three plugin-changing fixes merged since (#699 highlight sync, #906 deletion sync, #924 delete guard) reached no device through Updates Manager or appstore.koplugin. New `.github/workflows/plugin-release-publish.yml` runs the same script (one source of truth for the release recipe) with a new `--auto` mode on every published `vX.Y.Z` release. `--auto`'s owed-check compares the plugin against what the dedicated repository already ships and **runs before** the version check — that ordering is the fix: version-first would fail every ordinary release on a lockstep mismatch and get muted, owed-first skips an unchanged plugin silently and fails loudly on a changed-but-unbumped one. Manual `--publish` still errors on a no-op, so the human contract is unchanged. Cross-repo push needs `GH_PAT` (`GITHUB_TOKEN` is scoped to this repo); tag shape is pinned to `vX.Y.Z` before it reaches `actions/checkout`'s `ref`. 3 new guards (workflow-fires-on-release, `--auto` skip mode, and the ordering invariant, mutation-tested) plus `pytest.mark.unit` on `test_cwasync_updates_manager_compat.py` and `test_kosync_plugin_no_book_handling.py` — both were unmarked, so CI's `-m "smoke or unit"` had been deselecting every #400 guard and the `EXPECTED_PLUGIN_VERSION` release-tag anchor since they were written. v4.1.14 published to the dedicated repo out-of-band to unstick users now. | `b1ace0782` | v4.1.15 |

### Features (fork-original, not yet upstream)

| Fork PR | Description | SHA | Release |
|---|---|---|---|
| [#1092](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1092) | (community PR by [@Kyraminol](https://github.com/Kyraminol), extending their own [#998](https://github.com/new-usemame/Calibre-Web-NextGen/pull/998)) **Calibre identifiers in the KOReader progress export.** Each `GET /kosync/export` row now carries an `identifiers` map (`{type: value}`, `{}` when the book has none), so an ingesting tracker can match on ISBN or a store id instead of title and author alone. Implemented as a second query scoped to the ids the visibility-filtered book lookup already matched, rebuilt per 500-id chunk, rather than an outer join that would fan out n*m rows — hidden books cannot contribute identifiers and the `IN()` stays bounded. Hardening in [#1093](https://github.com/new-usemame/Calibre-Web-NextGen/pull/1093): identifier type keys are ASCII-folded to match the `NOCASE` collation the table is unique under (Python's `str.lower()` also folds U+212A KELVIN SIGN onto `k`, silently dropping one of two rows SQLite keeps distinct), and author names are un-escaped from Calibre's `\|` comma form, which this endpoint had missed when [#732](https://github.com/new-usemame/Calibre-Web-NextGen/pull/732) fixed that class fork-wide. No new dependency/URL. |  `a547e6d7d`, `fe1701342` | v4.1.21 |
| [#998](https://github.com/new-usemame/Calibre-Web-NextGen/pull/998) | (community PR by [@Kyraminol](https://github.com/Kyraminol), [#978](https://github.com/new-usemame/Calibre-Web-NextGen/issues/978)) **Bulk export of a user's KOReader reading progress.** A read-only `GET /kosync/export` returns every book the caller has progress for — Calibre book id, title, authors, percentage, and the first-synced and last-updated timestamps — so progress can be fed into an external tracker without scraping per-book endpoints. Authenticates through the same HTTP Basic path as the other KOReader sync routes (app passwords supported), returns only the caller's own rows, and is scoped to books the caller may see. No new dependency/URL. | `f2519ec04` | v4.1.18 |
| [#781](https://github.com/new-usemame/Calibre-Web-NextGen/pull/781) | (fork issues [#741](https://github.com/new-usemame/Calibre-Web-NextGen/issues/741) @magdalar, [#778](https://github.com/new-usemame/Calibre-Web-NextGen/issues/778), [#689](https://github.com/new-usemame/Calibre-Web-NextGen/issues/689)) | **The new-UI book editor suggests existing tags/authors/series/publishers/languages as you type again.** The redesigned editor rendered these five fields as bare text inputs, so a typo silently created a near-duplicate tag (`sci-fi` vs `scifi`) or series — the classic editor's typeahead was lost, and its absence is cited in the "back to classic" feedback. **Backend** (`cps/api/edit.py`): new read-only `GET /api/v1/metadata/typeahead/<field>`, edit-role gated, field allowlisted (`400` on unknown), reusing `calibre_db.get_typeahead` — the exact query behind the legacy `/get_*_json` routes — so SPA and classic editors agree on existing values (SSOT); languages mirror the localized start-first ranking; results capped at 25. **Frontend** (`MetadataTypeahead.tsx` + wiring in `EditBook.tsx`): a token-aware combobox that queries the segment under the caret, hides values already in the field, inserts a pick while preserving surrounding tokens, and keeps free-text entry for genuinely new values; WAI-ARIA editable combobox pattern; series single-value, the rest delimiter-separated. New SPA msgid `No suggestions` anchored in `spa_strings.py`. **Red/green** 7 tests (`test_741_metadata_typeahead.py`: role gate, unknown→400, per-field dispatch, tags `tag_filter`, authors `\|`→`,`, language ranking, cap). **Live e2e** cwn-local: all five fields return correct suggestions via curl (unknown→400, unauth→401); Playwright select-and-insert on Publishers + Tags, Save round-trips and the tag persists (verified via API, then reverted); desktop + mobile dropdown aligned + in-viewport. No new dependency/URL. New GET route → `/security-review` before merge. Fork-original (SPA editor surface). | `1f2f1c17b` | v4.1.9 |
| [#713](https://github.com/new-usemame/Calibre-Web-NextGen/pull/713) | (community PR [#701](https://github.com/new-usemame/Calibre-Web-NextGen/pull/701) @kurtlieber) | **User-selectable UI fonts (new interface), persisted per user.** Account settings gains **UI body font** + **UI display font** pickers (System Sans-Serif / Bookish Serif / Monospace / Default); the choice saves to the DB so it follows the user across devices, and each dropdown option previews in its own font. Adopted from @kurtlieber's #701, **reworked key-based**: stores a short preset *key* (not a full CSS stack), with the stacks in one SSOT (`frontend/src/lib/fonts.ts`) and the backend validating only keys — the original duplicated the full CSS stacks byte-for-byte across `account.py` and `Account.tsx`, so any drift would silently 400-reject a valid choice. New `ui_font_body`/`ui_font_display` columns on `user` (migration follows the proven `migrate_user_table` pattern); `App.tsx` overrides the `--font-body`/`--font-display` design tokens (already consumed app-wide) from the stored key; empty → theme default. **Dropped** the original PR's accidental `docker-compose.yml` change (it repointed the shipped compose at a local `:patched` image). **24 tests** (`test_api_v1_account.py`: valid persists, unknown/non-string 400, display allowlist stricter than body, empty→default, + a guard that the backend key allowlist matches the frontend SSOT); frontend build green; migration applied to a copy of the real cwn-local `app.db`; pybabel extracts the new SPA strings (anchored in `spa_strings.py`). Greptile 5/5, its one P2 (falsy non-string coerced to default) fixed. No new dependency/URL/font-asset. Credit: @kurtlieber (#701). | `93e6bfd97` | v4.1.6 |
| [#686](https://github.com/new-usemame/Calibre-Web-NextGen/pull/686) | [fork #585](https://github.com/new-usemame/Calibre-Web-NextGen/issues/585) (@Glennza1962, @alva-seal) | **Customize sidebar UX rework (operator design): top liquid-glass capsule + inline edit.** Follow-up to #680 — the bottom Customize button + modal was undiscoverable; replaced with a frosted-glass **Customize** capsule pinned at the top of the rail. Tapping it turns the sidebar into an inline editable list (per-row drag handle to reorder + ✕ to hide/delete, with restore); Done saves, Reset/Cancel available; focus moves to Done on enter and back to the capsule on exit. New `SidebarEditList.tsx`; `SidebarCustomize` modal removed. Reuses the existing `POST /api/v1/account/sidebar` (visibility bits + `view_settings` order) — **no backend/route/DB change**, so no new `/security-review` surface. 15 unit tests (backend endpoint unchanged + updated SPA source-pins); live Playwright desktop+mobile (capsule→tap→13-row edit list, keyboard reorder, delete Series hides it in the normal sidebar, all persisted, no console errors). | `c0dedfcd5` | v4.1.6 |
| [#680](https://github.com/new-usemame/Calibre-Web-NextGen/pull/680) | [fork #585](https://github.com/new-usemame/Calibre-Web-NextGen/issues/585) (@Glennza1962, @alva-seal) | **Customize the sidebar from the new UI — set visibility AND reorder.** v1 (#624, v4.1.4) made the SPA *honour* the classic `sidebar_view` visibility config (read-only); users then asked to change it from the new UI and to reorder entries (e.g. Shelves to the top so they don't scroll). New **Customize sidebar** button in the rail opens an editor that toggles section visibility and reorders the browse-by + discovery entries and the Shelves block. **Backend:** new `POST /api/v1/account/sidebar` writes the existing per-user `sidebar_view` bitmask (visibility — the same store the classic UI + OPDS read, so it stays one config) and `view_settings['sidebar']['order']` (order — same per-user JSON store as shelf reorder #237); allowlist-validated against `SIDEBAR_VISIBILITY_BITS`/`ORDERABLE_SIDEBAR_KEYS`, all-or-nothing, scoped to `current_user`, session+CSRF-guarded; **no schema change**. `serialize_user` gains `sidebar_order`. **Frontend:** `SidebarCustomize.tsx` modal with reorder by mouse (up/down + pointer drag), touch, and keyboard (arrow keys) + screen-reader position announcements + reset-to-default; `Sidebar.tsx` renders the customizable region in saved order (contiguous nav runs kept as valid `role=list` groups; Shelves block emitted inline), composing with the v1 visibility filter. Shared `lib/sidebarEntries.ts` is the single source of truth for keys. **18 red-on-main/green tests** (`tests/unit/test_585_sidebar_customize.py`): serializer order field, endpoint bit-flip + order persistence + key/dup/non-list validation + anonymous-401, SPA source-pins. **Verified:** live API round-trip + validation on a venv instance, multi-user isolation, Playwright desktop+mobile (keyboard + button reorder, visibility toggle, save→live re-render, mobile modal), zero new unit regressions vs main. **`/security-review` clean** (new route — auth via `_require_real_user` + blueprint gate, CSRF app-wide, no IDOR, allowlist validation, bitmask flips display bits only). Greptile P1 (seed effect clobbering unsaved edits on background `me` refetch) fixed + verified. SPA-only msgids anchored in `spa_strings.py`. | `e30d3f01d` | v4.1.6 |
| [#676](https://github.com/new-usemame/Calibre-Web-NextGen/pull/676) | (spec [#556](https://github.com/new-usemame/Calibre-Web-NextGen/pull/556); operator-requested) | **In-app "What's New" feature log (SPA `/whats-new`).** Readers not following the GitHub changelog had no in-app way to see what changed after an update. New page reachable from the Help ("?") menu: plain-English, newest-first log grouped by release, category-tinted chips, a "Try it" deep-link per entry, and a subtle Help-menu dot that lights once after an update and clears on open. `data/whatsNew.ts` (typed `WHATS_NEW`, seeded v4.1.5→v4.1.0 from the released CHANGELOG; `LATEST_WHATS_NEW_VERSION` derives from `WHATS_NEW[0]` so the `whats-new-populate` skill's prepend re-arms discovery). `lib/whatsNew.ts` unread dot keyed to the newest *logged* version (no runtime installed-version constant exists in the SPA) via localStorage + a same-tab event so the dot clears live. `App.tsx` route + `TopBar` Help entry; `spa_strings.py` anchors the page/menu/chip msgids so babel extracts them (SPA-only strings must live in a `.py` — the #577 failure mode). v4.1.2 intentionally omitted (it re-published v4.1.1's fixes unchanged). **6 source-pin tests** (`tests/unit/test_whats_new_spa.py`, RED on main): msgid + chip anchoring, route/menu wiring, dot keying + mark-seen, LATEST-from-WHATS_NEW[0], every deep-link resolves to a real route. **Live cwn-local** (my build confirmed loaded): `tsc -b && vite build` clean; page renders desktop 1280 + mobile 390; all 8 deep-links resolve; dot full lifecycle (lights fresh → persists v4.1.5 → clears on open). Fork-original, SPA-only, no new deps/routes; `/security-review` not required. | `beeb514a4` | v4.1.6 |
| [#520](https://github.com/new-usemame/Calibre-Web-NextGen/pull/520) | [fork #509](https://github.com/new-usemame/Calibre-Web-NextGen/issues/509) (@barukh27) | **"Currently reading" now shows in the web UI for KOReader/Kobo in-progress books.** A book a user was partway through synced as `ub.ReadBook.read_status = STATUS_IN_PROGRESS` (2), but every browsing surface flattened the tri-state to a FINISHED-only boolean — `web.py` coercion (`== STATUS_FINISHED`) and the `cover_badges` macro call `is_read=(entry[2] == True)` (and `2 == True` is `False`) — so an in-progress book rendered identically to unread everywhere. `cps/kobo.py` already mapped the state to "Reading" for Kobo devices; only the web UI dropped it. **Fix:** carry the raw tri-state to the templates and render a distinct amber "Currently reading" state — a passive marker under the title on `detail.html` (keyed on new `entry.read_status_raw`) and a `.cover-badge-in-progress` badge in the `image.html` `cover_badges` macro (now takes the int) consumed by index/shelf/search/author. The read/unread toggle stays 2-state; both the detail toggle and the caliBlur cover quick-toggle drop the in-progress badge when the user explicitly marks a book read (no stale "Currently reading" next to "Mark As Unread"). The smart-shelf "Currently Reading" filter already existed and now agrees visually. **Tests:** `tests/unit/test_in_progress_read_state_509.py` Jinja-renders the macro (status 2 → in-progress badge, 1 → read, 0 → none) + source-pins the coercion/call sites + the detail toggle pill-removal (RED on main, GREEN here). **Verified:** zero unit-suite regressions vs main (set-diff empty); live app + Playwright on caliBlur (the only supported theme), desktop 1280 + mobile 390 — amber pill on detail + grid covers, finished books unaffected, mark-read removes the pill live with no reload. Greptile findings (detail-pill-not-cleared, dead `read_status_raw` in `list_books`) both applied. The Kindle "auto-sync only on manual tap" half is a config answer (`wifi_enable_action` must be "turn_on"), see `notes/fork-509-adjacency.md`. `/security-review` not required (no auth/route/path/subprocess/crypto). | `919960f05` | v4.0.172 |
| #455 | [fork #400](https://github.com/new-usemame/Calibre-Web-NextGen/issues/400) (@filiporlo) | **KOReader sync plugin is now distributable through Updates Manager (advokatb/updatesmanager.koplugin).** Two gaps closed: (1) `_meta.lua` gains the `version` field Updates Manager reads for installed-version detection — without it every check reported "unknown" and flagged a false update; kept in lockstep with `main.lua` (both bumped to 4.0.166, mirroring the release tag) and pinned by `tests/unit/test_cwasync_updates_manager_compat.py` (lockstep + release-tag-shape + workflow pins, red-on-main/green). (2) New `.github/workflows/plugin-release-asset.yml` attaches `cwasync.koplugin.zip` (archive root = the plugin folder, same layout as the Dockerfile STEP 7.3 in-app zip) to every published GitHub release via `actions/checkout` + `gh release upload` (no third-party action, scoped `contents:write`). Zip recipe + layout exercised locally; workflow fires first at this release's publish. Remaining halves (dedicated `cwasync.koplugin` repo so UM's latest-release-tag comparison isn't confused by daily app tags, + PR to UM's `DEFAULT_PLUGIN_REPOS`) are outward-facing operator decisions — see `notes/koreader-updatesmanager-DESIGN.md`. | `76e64c556` | v4.0.166 |
| [#478](https://github.com/new-usemame/Calibre-Web-NextGen/pull/478) | [fork #472](https://github.com/new-usemame/Calibre-Web-NextGen/issues/472) (@huperaisan) | **Type just a year in the Published Date field.** The edit-form Published Date only accepted a full `YYYY-MM-DD` chosen through the click-heavy picker; most books only carry a year. Now `2020` and `2020-05` are accepted and normalised to January 1st / the 1st of the month (matching Calibre). New pure `cps/services/pubdate_parse.py` → `parse_partial_pubdate()` (accepts `YYYY` / `YYYY-MM` / `YYYY-MM-DD`, whitespace-tolerant, raises `ValueError` on malformed input so the existing flash-error path is unchanged); `editbooks.py:954` calls it instead of the strict `strptime`. Frontend: bootstrap-datepicker init gains `forceParse:false` so a typed year survives blur (click path unchanged — picking a day still formats to ISO), and the localized `#fake_pubdate` mirror tolerates partial dates instead of going stale. Provider/file parse paths (add-book, `reload_metadata`) stay strict — out of scope, they parse ISO not free-text. 16 regression tests RED on main / GREEN on branch (`test_pubdate_parse_472.py` 14 cases + `test_pubdate_datepicker_js_472.py` pinning `forceParse:false` + the partial mirror regex). **Live e2e on cwn-local** (Playwright, real keyboard, desktop + mobile 390×844): `2020`→DB `2020-01-01`, round-trip shows the date; `2020-05`→`2020-05-01`; `notadate`→flash error + DEFAULT pubdate (strict behaviour preserved); mobile typed `1999` survives. `/security-review` not required (no auth/route/path/subprocess). | `d4cc2753e` | v4.0.166 |
| #444 | [fork #428](https://github.com/new-usemame/Calibre-Web-NextGen/issues/428) (@iroQuai) | **Admin-set custom email body text.** The mails the server sends with a book (send-to-eReader, test mail) carried a fixed "This Email has been sent via Calibre-Web NextGen." line; reporter wanted to personalize it (own language, link to library). New `mail_body_text` String column on `_Settings` (default `""`) — the `mail_` prefix auto-migrates via `config_sql._migrate_table` and flows through `get_mail_settings()` into Edit Email Server Settings with no extra wiring. New `helper.get_email_body_text()` returns the admin value when non-blank else the shipped translated default; the three `TaskEmail` call sites (`convert_book_format`, `send_test_mail`, `send_mail`) call it instead of the literal. Saved in `update_mailsettings` via `_config_string` before `config.save()` (all submit branches). Whitespace-only treated as unset. Plain-text email (`set_content(..., "text", "plain")`) + admin-only route + Jinja autoescape on the textarea (no `|safe`) → no breakout guard needed. Registration/welcome mail keeps its own body (out of scope, pinned). 13 tests: real `_resolve_email_body_text` run in isolation + source pins on every wiring site. **Live on cwn-local**: column auto-migrated on boot, settings page renders the textarea, two-line custom body POSTs → 200 → persisted to DB → reload renders it back (proving live `config.mail_body_text` populates `get_email_body_text()`), no new errors in logs. v2-track: metadata/templating placeholders + HTML formatting (reporter ranked low). | 1fa19d748 | v4.0.162 |
| [#442](https://github.com/new-usemame/Calibre-Web-NextGen/pull/442) | [fork #323](https://github.com/new-usemame/Calibre-Web-NextGen/issues/323) (@olskar) | **Admin-set custom CSS injected site-wide.** A new Custom CSS textarea under Admin → Edit UI Configuration persists `config_custom_css` on `_Settings` (auto-migrated in `config_sql.py` + defensive `ALTER TABLE` in `ub.py`, mirroring the #225 `config_server_announcement` pattern). `render_template.render_title_template` passes it through new helper `_style_safe_css` and `layout.html` renders it as the last `<style>` in `<head>` via `|safe`, so admin rules override the shipped themes; rendered raw (not HTML-escaped) so valid CSS like the `>` child combinator survives. Trust-the-admin model (no per-rule sanitization); the only guard neutralizes every `</` to `<\/` so the value can't close the RAWTEXT `<style>` element early — `</` never appears in valid CSS, so it is a no-op for real input. 13 regression tests (`tests/unit/test_custom_css_injection.py`): AST-extracts and exercises the real `_style_safe_css` for the breakout invariant (`</style><script>` neutralized, `>` combinator preserved, empty/None → `""`) plus source-pins on the column, migration, save handler, textarea, and the `{% if custom_css %}` + `|safe` render. **Live-verified on cwn-local**: column migrated on boot, admin config page renders the textarea; with a value carrying an embedded `</style><script>alert(1)</script>` payload, the authenticated home page and the anonymous login page both inject `<style id="cwng-custom-css">` with the legit `.navbar > .navbar-brand` rule intact and the payload rendered as inert `<\/style>` text (0 raw breakouts on the wire). Theme-independent injection (no `g.current_theme` branch), so caliBlur and default theme emit the identical block. | e3d75fb5f | v4.0.162 |
| #392 | **Add books to a shelf directly from the shelf page.** A new "Add Books" button opens a searchable picker — type to find books in the library, tick the ones you want, and add them all at once; books already on the shelf show as "Already on this shelf" so duplicates can't be added. Works on phone and desktop; especially handy for filling a brand-new empty shelf. Verified live (Playwright, desktop + mobile). | `b8ba6fb58` | v4.0.159 |
| #380 | [fork #320](https://github.com/new-usemame/Calibre-Web-NextGen/issues/320) (@SpookyUSAF; save-on-drop design @droM4X) | **Shelf reorder is the same responsive cover grid as the shelf view — drag (mouse), long-press drag (touch), or arrow keys (keyboard), saving automatically on every change.** The old screen was a vertical list so cramped the reporter browsed at 10% zoom to drag books. Design call documented in-thread: in-shelf-view dragging (droM4X's first suggestion) was evaluated and amended — the shelf grid's existing cover-drag is the destructive book MERGE, and disambiguating drop-on (merge) from drop-between (reorder) by pixels puts data loss one mis-drop away, worst on touch; the dedicated page instead becomes the exact same grid. Modalities each deliberate: Sortable grid mode (mouse), `delayOnTouchOnly` 150ms long-press so swipes scroll (touch), tabbable covers + arrow-key moves + aria-live "Moved to position N of M" announcements (keyboard); failures turn the status line into click-to-retry. Server: JSON `{order:[...]}` normalized via `normalize_shelf_order` — unknown ids dropped, dups collapsed, stale-payload-missing books keep relative order at the end; the legacy handler did `to_save[str(book_id)]` and **500'd with KeyError** whenever the shelf changed between page load and save (fixed; form shape kept one release, parsed defensively); JSON 403 for non-editors. **Caught live:** main.js isotopes every `.discover .row` with `.book` cards — absolute positioning desynced from Sortable's DOM moves (a resize snapped covers back to the pre-drag picture while the DB held the new order; found by screenshot-vs-DOM-vs-DB triangulation). Page owns its layout now (regression-pinned); side win: mobile renders the true 2-up grid isotope had been overriding. **Verified live** across drag/keyboard/mobile/resize with a DB check at each step, hostile payloads (`[3,3,99,"junk"]`), malformed JSON no-op, and an OBSERVED 403 with the role bit cleared. 18 unit tests; JPEGs notes/feat-320-*.jpeg. Greptile declined to review (manual trigger + window); /security-review clean. | `6e7ac4c3f` | v4.0.157 |
| #379 | [fork #334](https://github.com/new-usemame/Calibre-Web-NextGen/issues/334) (@Glennza1962 — asked upstream at CWA long ago, never landed) | **Add a whole series to a shelf in one click, in series order.** Series pages get an "Add Series to Shelf" dropdown (same `postAction` plumbing as the search page's bulk add): every visible book of the series, `series_index` order, dedup against the shelf, append after current max order; `common_filters()` so restricted users can't bulk-add books they can't see; public shelves offered only with the edit-public-shelves role. **Series-subsystem audit shipped along:** (A5) series pages now DEFAULT to series order when no sort is stored — the OPDS series feed always did, the web UI disagreeing was the tell (explicit sorts still stick); (A1) `add_to_shelf`'s Hardcover sync sat behind the non-XHR early return so plain form adds silently skipped it — now precedes the response split; (A3) `add_selected_to_shelf` claimed `partial_success` with a positive count after a commit failure whose rollback persisted nothing — now an honest 500 with added_count 0; (A4) per-book `max(order)` re-queries hoisted to one query. Greptile P1 confirmed + swept: `InvalidRequestError` has no `.orig`, so every shelf.py DB-error flash could itself 500 — all nine accesses now `getattr(e,'orig',e)` + a pin banning bare `.orig` in the file. Documented-not-shipped: bulk-path Hardcover parity → [fork #381](https://github.com/new-usemame/Calibre-Web-NextGen/issues/381) (worker-task shape); `Books.series_index` declared String over calibre's REAL column — production orders numerically by schema-affinity luck; left (blast radius), but the test fixture mirrors the REAL schema and pins the 10-after-2 numeric case. **Verified live** (series 1.0/2.0/3.5/10.0): page renders in series order with chip "Series number, ascending"; POST → rows in series order; dedup; partial re-add appends at max+1; Playwright desktop+mobile JPEGs notes/feat-334-*.jpeg. 13 unit tests; /security-review clean. | `5fef454e3` | v4.0.157 |
| #374 | [fork #218](https://github.com/new-usemame/Calibre-Web-NextGen/issues/218) follow-up (@yodatak: "it don't reload all the metadata") | **Reload Metadata now also reloads authors, tags, and series (with series number) from the book file.** The three fields ride the same helpers the edit-book save path uses (`handle_author_on_edit`, `edit_book_tags`, `edit_book_series`) so author_sort regeneration, tag dedup + orphan cleanup, and series create-on-demand are identical by construction; title/author changes converge the on-disk `Author/Title (id)` folder + filename via `update_dir_structure` (the v4.0.149 title-only reload skipped that — same-class gap, fixed). Absent-field safety: missing `<dc:creator>` ('Unknown' sentinel) or missing subjects/series ('') never stomp curated data — including the adversarial-review catch that `parse_epub_series` fabricated `series_id='1'`, which made reloads of name-only files reset curated series indexes (fixed at the parser, absence='', all three consumers audited). File renames run OUTSIDE `metadata_db_write_lock` (single-commit contract, #192) and rename failures roll back rather than committing a layout the move didn't produce. Caught live on double-reload: numeric (not string) series_index compare, else every reload re-marked the book modified and churned Kobo-sync cursors. **Live-verified** (cwn-local, OPF rewritten inside a real EPUB): forward + reverse reloads with both-direction folder moves, creator-stripped no-stomp, curated-index preservation, idempotency. 21 reload unit tests. | `39697357f` | v4.0.157 |
| #375 | [fork #346](https://github.com/new-usemame/Calibre-Web-NextGen/issues/346) (@BakaPhoenix + @magdalar) | **Book detail + edit pages show the original imported filename.** Ingest renames files to match their (possibly wrongly auto-matched) metadata, so users recognizing misidentified books had nothing stable to go by. New `book_original_filename` app.db table (auto-created at boot); `NewBookProcessor` snapshots the ingest-folder basename at `__init__` and records it post-add via the processor's first direct app.db WRITE (WAL + 30s busy timeout + best-effort except — a failure never blocks an import); `ON CONFLICT(book_id) DO NOTHING` so the creating import wins; **fallback-inferred book ids are never recorded** (adversarial-review catch — under concurrent ingest the most-recently-modified guess can be another processor's book, and a wrong 'Imported as' is worse than none); defensive getattr so partially-constructed processors (tests use `object.__new__`) can't route imports to the failure branch (CI catch); `delete_whole_book` cleans the row. Read-only 'Imported as' chip on detail + muted line on edit. New-imports-only (originals for existing books are gone). **Live e2e through the real watcher**: junk-named EPUB → auto-ingest → row → both pages render (JPEGs in notes/) → delete removes row. 12 unit tests. | `353c1922d` | v4.0.157 |
| #29 | Bump cover resolution for high-DPI e-readers (Libra Color etc.) | `630ee22` | v4.0.9 |
| #30 | Metadata: Open Library + Google Books API key + per-provider status + in-modal API-key panel | `428530c` | v4.0.9 |
| #31 | `isoLanguages.get_language_names`: tolerate `None` / string locales (unblocks DNB) | `491af54` | v4.0.10 |
| #32 | DNB: drop synchronous cover-validation; switch to `<img onerror>` graceful fallback | `dab1589` | v4.0.11 |
| #33 | Metadata: cover-resolution booster + sort-by-cover-size in fetch dialog | `e5b7666` | v4.0.12 |
| #34 | docs(readme): user-facing fork front-matter | `aa89fd7` | (docs only) |
| #49 | Fix "Cover-file is not a valid image file" on URL covers (Hardcover/Google/iTunes): chown back to PUID:PGID after enforcer + diagnostics on cover-save failures | `4df03f0` | v4.0.13 |
| #51 | Fix "Generate Kobo Auth Token Fails" blank-page (mirrors upstream issue #1328 — reporter @blahblah57): replace `.join(Data).all()` + N+1 lazy-load with `joinedload(Books.data)`, gate on `config_kepubifypath`, and guard per-book convert in try/except | `e82fdc5` | v4.0.14 |
| #52 | Fix infinite ingestion loop on `NETWORK_SHARE_MODE=true` / Docker Desktop / inotify-ENOSPC fallback (mirrors upstream issue #1326 — reporter @mysterfr): polling watcher's mtime-age fallback was re-emitting `CLOSE_WRITE` every poll cycle for any file older than `--stabilize`, despite the `stable_count` sentinel being set after first emit. Gate the emit on the sentinel + extract `scan_once` for testability; new regression suite under `tests/integration/test_watch_fallback.py`. | `8a3a424` | v4.0.15 |
| #65 | Cover quality: Amazon image-CDN booster path (ISBN-keyed, edition-correct covers up to ~2000px from public CloudFront, no scraping) + Apple Books as a first-class metadata provider + tighter iTunes match guard (no more wrong-edition substitution when ISBN-lookup misses). 33 unit tests. Solves the "Wuthering Heights Wordsworth Classics cover not available from any source" class of issue. | `a6d7b55` | v4.0.19 |
| #66 | Focused cover-picker UI at `/book/<id>/cover` (separate from the metadata-search modal) + reliable inline live preview on the existing `cover_url` field on the edit page. Includes embedded-cover-from-book-file as a candidate and a per-book cover-lock that resolves [janeczku/calibre-web#2165](https://github.com/janeczku/calibre-web/issues/2165) (8 hearts). 36 unit tests. See `notes/COVER-PICKER-DESIGN.md`. | `025534a` | v4.0.19 |
| #68 | Fix cover-picker visibility on caliBlur dark theme + unblock external cover images: (1) add `cover_picker.cover_picker_page` to the `img-src *` CSP exception in `cps/web.py` so all 14 providers' cover URLs render in the picker grid, not just the embedded `data:` URL; (2) rewrite `cover_picker.css` + the inline-preview block in `cwa.css` with CSS custom properties + `body.blur` overrides for proper contrast on both themes; (3) `cw_advocate.head(...)` → `cw_advocate.request("HEAD", ...)` since the vendored library doesn't export top-level `head` (the AttributeError was being caught as a generic "unreachable", failing every URL validation). Also added a labeled "Cover not reachable" placeholder for candidate cards whose images fail to load. End-to-end browser-verified on the deployed instance. | `a1833a2` | v4.0.20 |
| #70 | Kobo cover aspect-ratio padding: pads book covers to the device's native aspect ratio (Libra Color/2 1264×1680 default; Clara family alt) before serving over the Kobo sync API, so devices stop white-letterboxing tall publisher covers on the home and sleep screens. Five fill modes — edge_mirror (default; mirrors outer edges into the pad area for a natural artwork extension), edge_blur (soft bokeh border), most-common color, average color, manual hex. Padded variants are cached in the existing `/config/thumbnails/` dir keyed by `book_uuid + resolution + src_mtime + settings_hash`; `CoverImageId` includes `-p<settings_hash>` so devices re-fetch on settings change. Off by default; new admin section under Configuration → Kobo sync. Original cover files on disk are never modified. See `notes/KOBO-COVER-PADDING-DESIGN.md`. | `3b10ccd` | v4.0.21 |
| #73 | Kobo cover padding follow-up + UI polish: (1) padding now defaults to ON when a user first enables `config_kobo_sync` (off→on flip in admin save handler auto-enables padding; user can still untoggle padding independently afterward, and the auto-enable does not re-fire on subsequent saves); (2) the "Custom border color" input on the same admin section is `readonly` + dimmed at 50% opacity when the selected fill mode is anything other than `manual`, preserving the typed value across mode toggles without shifting layout (new `_kobo_padding_color_sync` JS handler in `cps/static/js/main.js`); (3) Save / Cancel button pairs across seven settings templates (Configuration, DB Configuration, View Configuration, Schedule, Shelf editor, Book editor, User editor) reordered to right-aligned with Save rightmost + Cancel as a secondary style (white background, brand-orange `#cc7b19` text + border) — new shared `.cwa-form-actions` + `.btn-secondary` rules in `cwa.css`. End-to-end browser-verified on the deployed instance. | `c2e9e47` | v4.0.22 |
| #75 | Equalize Save / Cancel button heights in `.cwa-form-actions` row. The v4.0.22 reorder rendered Cancel (anchor + `.btn-secondary`, 1px border) at 41 px and Save (`.btn-default` on caliBlur, borderless) at 36 px, visually mismatched. Force a fixed 38 px box on every `.btn` inside `.cwa-form-actions` with `line-height: 1`, zero vertical padding, inline-flex centering, and a transparent 1px border on `.btn-default` so it occupies the same space as `.btn-secondary`'s visible border. Browser-verified on the deployed instance: both buttons report 38 px, sameTop=true. CSS-only single-file change. | `197c48c` | v4.0.23 |
| #92 | Cover-picker Kobo preview ([#84](https://github.com/new-usemame/Calibre-Web-NextGen/issues/84)): when `config_kobo_cover_padding_enabled` is on, the `/book/<id>/cover` page gets a "Kobo preview" panel with a toggle plus mirrored aspect / fill-mode / custom-color controls. Toggling it on swaps every visible cover (current, candidate grid, confirm modal, URL thumb) to a server-rendered padded variant via a new `POST /book/<id>/cover/kobo-preview` endpoint that reuses `cover_padding.pad_blob`. Picker controls are session-local and do not mutate global admin defaults. Server hardening: `http(s)` scheme allowlist, pre-stream Content-Length cap, `BoundedSemaphore(4)` on Wand work to prevent worker saturation. JS hardening: per-burst `AbortController` (toggle off / settings change / panel close cancel in-flight server work), in-flight de-dup, debounced grid `MutationObserver`, same-origin `/cover/` URL short-circuit, loading status pill. 29 unit tests; browser-verified end-to-end on the deployed instance. See `notes/cover-picker-kobo-preview-DESIGN.md`. | `316c9feb` | v4.0.27 |
| #94 | Fix CWA upstream issue [#1183](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1183) (reporter: @Bugg6): `helper.py` was calling `calculate_and_store_checksum()` on every metadata-embed regardless of whether KOReader sync was enabled. On instances that never enabled sync, the `book_format_checksums` table doesn't exist, so each save logged "Failed to store checksum for book N: no such table". Gate the call on `is_koreader_sync_enabled()` — same gate `cps/db.py` and `cps/ub.py` use. The pure-utility `calculate_and_store_checksum()` function stays unguarded so explicit-backfill paths (used right after a user enables sync mid-instance) still work. Single-file change, 3 regression tests. | `2312a8c5` | v4.0.28 |
| #104 | Fork issue [#95](https://github.com/new-usemame/Calibre-Web-NextGen/issues/95) / mirrors CWA [#1269](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1269) (reporter: @charredchar). OAuth users (Authentik / Authelia / Keycloak) could log into the web UI but get rejected on OPDS and KOReader Sync because both surfaces use HTTP Basic auth and OAuth users have no usable local password. New `user_app_password` table + `_verify_app_password()` branch in `cps/usermanagement.py:verify_password()` lets every user mint per-device labeled tokens via `/me`. The cleartext is shown once at create-time via Flask flash; only the werkzeug hash is persisted. Revoke endpoint scopes to caller's user_id (no cross-user privilege escalation). 9 unit tests pin the auth path against an in-memory SQLAlchemy session: match returns user + stamps `last_used_at`, no-match returns None without stamping, revoked rows ignored, cross-user lookups blocked, empty-password short-circuits without DB query. See `notes/oauth-opds-app-passwords-DESIGN.md`. | `b339949d` | v4.0.31 |
| #105 | Fork issue [#64](https://github.com/new-usemame/Calibre-Web-NextGen/issues/64). Per-user hide-books feature so users with large libraries (e.g. Project Gutenberg hoards) can declutter their personal view without affecting other users or deleting the book. New `user_hidden_book` table (mirrors `archived_book` shape but separate semantics: archive is sync-pause, hide is personal-library declutter). `common_filters()` extended with `allow_show_hidden` kwarg, defaulting to False so the hidden books are excluded from index, search, OPDS, and shelf listings for the calling user. New `POST /ajax/togglehidden/<book_id>` endpoint mirrors `togglearchived`. New `/hidden` listing route bypasses the exclusion via `allow_show_hidden=True` so users can see and unhide. Hide / Unhide button on the book detail page (eye-open / eye-close glyphicons). 10 unit tests pin the model invariants (per-user uniqueness, multi-user same-book OK), the `common_filters` signature + source + actual SQL output (NOT IN clause carries the hidden ids), and the route shape (POST-only, dispatch wiring, `allow_show_hidden=True` in the listing). | `e96326fa` | v4.0.32 |
| #90 | Community contribution by @FRaccie / @ZanibytesDev — mirrors upstream [crocodilestick/Calibre-Web-Automated#817](https://github.com/crocodilestick/Calibre-Web-Automated/issues/817) (reporter @jderuiter). `cps/kobo_auth.py:generate_auth_token()` walked every book in the library on each visit to "Create/View Kobo Auth Token" and queued a kepub conversion task for every EPUB. On large libraries (200k+ books) this floods the worker queue, doubles disk usage, starves `/v1/library/sync`, and trips the container healthcheck. Fix scopes the query to books on the user's `Shelf.kobo_sync == True` shelves via a `BookShelf` join. Books outside those shelves still convert on-demand at sync time. `tests/smoke/test_kobo_auth_shelf_scope_smoke.py` AST-pins the shelf scope so a future refactor can't reintroduce the unbounded walk. | `1bc7fc07` | v4.0.33 |
| #106 | Fork issue [#103](https://github.com/new-usemame/Calibre-Web-NextGen/issues/103) / mirrors janeczku [#3274](https://github.com/janeczku/calibre-web/issues/3274). `do_download_file()` did `download_name + "." + book_format` in five places without validating that `data.name` (which becomes `book_name` and propagates into `download_name` in three of the four code branches) or `book_format` is a usable string. A calibre.db `data` row with NULL `name`, or a caller with `book_format=None`, fell into `None + "."` deep inside the function and raised an unhandled `TypeError` — Flask surfaced it as a 500. Fix adds a precondition guard at the top of the function that aborts(400) with a diagnostic log line naming the offending book id + format. Strictly additive — valid inputs flow through unchanged. 8 unit tests pin: data.name None / empty rejected, book_format None / empty / non-string rejected, data=None rejected, valid inputs pass through, source-pin AST check that the guard precedes any string concatenation. Root-cause-first per the operator's standing rule: not a try/except mask — a clean rejection at the boundary that surfaces *which* row is corrupt so the operator can fix the underlying calibre.db. | `440f4744` | v4.0.34 |
| #110 | Fork issue [#109](https://github.com/new-usemame/Calibre-Web-NextGen/issues/109) (reporter: @droM4X) — **regression in v4.0.31**. The app-passwords UI shipped in PR #104 was placed inside the existing profile-save outer `<form>` in `cps/templates/user_edit.html` (which spans lines 176–607). HTML5 forbids nested `<form>` elements; browsers silently ignore the inner `<form>` tag and submit the OUTER form when the inner button is clicked. Result: user clicks "Create app password", browser POSTs to `/me` (profile-save endpoint, not `/me/app-passwords`), label text gets ignored, page reloads with no flash + no token — exactly @droM4X's symptom. Fix moves the entire app-passwords block outside the outer profile `<form>`. 4 unit tests pin the invariant: `<form>` depth-tracker walk over the template asserts no nested forms, plus a stronger pin that asserts the app-password create `<form>` opens at outer-form-nesting-depth 0 specifically. | `08a516fb` | v4.0.35 |
| #111 | Fork issue [#108](https://github.com/new-usemame/Calibre-Web-NextGen/issues/108) (reporter: @droM4X). `cps.db.CalibreDB.order_authors` was logging `ERROR Author '<name>' not found to display name in right order` on every page render where `Books.author_sort` had drifted from `Authors.sort`. With OPDS pollers + open browser tabs, three Hungarian author names spammed the Docker log every ~30 seconds indefinitely. Three changes: (1) module-level `_AUTHOR_SORT_DRIFT_WARNED` set rate-limits to one log line per process per drifted sort string; (2) demote ERROR → WARNING (the function recovers gracefully via the Authors.id fallback loop); (3) replace `break` with `continue` so a single drifted author no longer short-circuits the whole book's author ordering into id-order — only the drifted one falls through. 6 unit tests pin: AST walk asserts `continue` not `break` in `order_authors`, source-pin on the dedup set + WARNING level, behavioral test runs `order_authors` 10× over 3 drifted authors and asserts the dedup set has exactly 3 entries (proving rate-limit), behavioral test on a 2-author book pins that the valid author still gets correctly sort-ordered when one author drifts. | `69c87789` | v4.0.36 |
| #122 | Closes upstream CWA [#243](https://github.com/crocodilestick/Calibre-Web-Automated/issues/243) (11 reactions, 15 comments — long-standing user request). Opt-in support for user-installed Calibre plugins during ingest, format conversion, cover enforcement, and metadata embed. New env var `CWA_CALIBRE_USER_PLUGINS` (default: off). When the operator sets it to a truthy value, every Calibre subprocess invocation runs with `HOME=/config` so the embedded Calibre process loads any plugin .zip files placed under `/config/.config/calibre/plugins/`; the directory is auto-created at container startup with the right ownership (skipped on `NETWORK_SHARE_MODE=true`). When unset (the default), HOME is inherited from the parent and no third-party Python loads — preserves the existing trust model for deployments that don't ask for it. New helper module `cps/services/calibre_user_plugins.py` (~80 LOC, single-concern, no Flask dependencies) exposes `is_enabled`, `apply_to_env(env)`, `ensure_plugins_dir`, `env_var_name`, `home_path`. Four call sites refactored to delegate to the helper instead of hardcoding `HOME=/config`: `scripts/ingest_processor.py`, `scripts/convert_library.py`, `scripts/cover_enforcer.py`, `cps/embed_helper.py`. Bootstrap call added to `scripts/auto_library.py:main()`. 17 regression tests pin: helper truthy/falsy parsing across the standard set (matches NETWORK_SHARE_MODE convention), apply_to_env mutation in/out, plugins_dir path is `/config/.config/calibre/plugins` exactly, ensure_plugins_dir idempotent + permission-error-tolerant, source-pin AST checks that no call site re-hardcodes `HOME = "/config"` (the regression vector — would silently force plugin loading even when opted out), source-pin that each site references the helper module name. | `a6d8e62c` | v4.0.40 |
| #123 | Follow-up to #122 — found via local Docker e2e test that v4.0.40 was incomplete: just dropping `.zip` files in `/config/.config/calibre/plugins/` didn't make Calibre load them at conversion time. Calibre maintains a registry at `customize.py.json` populated by `calibre-customize -a <zip>`. Without that step, the .zips are inert. This was the user-visible failure mode reported all over upstream CWA #243 ("I copied the plugin folder, nothing happens"). v4.0.41 fixes it: bootstrap now scans the plugins dir for `.zip` files and runs `calibre-customize -a` per file with HOME=/config on first boot, then short-circuits on subsequent boots when the registry is non-empty. New helper function `auto_register_plugins(calibre_customize_binary=...)` returns the list of newly-registered plugin display names. Permission errors / missing binary / nonzero exit codes log + return cleanly so container start is never gated on registration. 9 new regression tests cover: disabled returns empty, missing dir returns empty, populated registry short-circuits, first boot calls binary once per .zip in sorted order, failed subprocess doesn't raise, missing binary doesn't raise, registry parser handles missing/corrupt JSON. Verified end-to-end on local Docker with the canonical plugin set (DeDRM_plugin.zip + Obok_plugin.zip + DeACSM_plugin.zip): all three auto-register on fresh container, persist to customize.py.json, second boot skips re-registration, non-DRM books continue to ingest cleanly with the plugin chain active. Full DRM round-trip not exercised here because the plugins read decryption keys (Kindle serial / ADE / Kobo device key) from `dedrm.json` next to the .zips — those are operator-private and out of scope for autopilot test coverage. | `b0aed3ec` | v4.0.41 |
| TBD | Two production-only bugs caught by deploying #123 to teenyverse and copying real plugin .zips through the bootstrap. **Bug 1 — source-equals-destination collision:** when an operator's source filename matches the plugin's display name (e.g. `DeACSM.zip` → calibre target `<plugins_dir>/DeACSM.zip`), calibre-customize's internal `shutil.copy(source, target)` collapses on the same path and the registration silently fails (source file gets truncated/removed, `customize.py.json` stays unchanged). Fix: stage every source through `tempfile.NamedTemporaryFile(suffix=".zip")` so the destination path can never equal the source path. Side benefit: keeps the operator's original .zip exactly as they dropped it. **Bug 2 — chown gated on NETWORK_SHARE_MODE for the local config volume:** the post-registration chown to `abc:abc` was wrapped in `if not nsm:`, but `/config/.config/calibre` is always a local Docker volume — `NETWORK_SHARE_MODE` is for `/calibre-library` and `/cwa-book-ingest` (NAS-mounted paths). With NSM=true on teenyverse, calibre-extracted plugin contents stayed owned by root after the bootstrap, and the abc service user couldn't read them at conversion time. Fix: chown unconditionally on `/config/.config/calibre`, leave NSM gating only for the NAS-mounted library/ingest dirs. 2 new regression tests pin: tmp-staged path is under the system tmp dir (never under plugins_dir), and the staged tmp file is cleaned up after registration (no per-reboot leak into /tmp). All 43 unit tests green. Verified end-to-end on local Docker: collision filename `DeACSM.zip` registers cleanly, all 3 plugins land in customize.py.json, all files end up `abc:abc`-owned. | `fcb0822a` | v4.0.42 |
| #120 | Cover-picker Kobo preview throughput overhaul. Three issues observed live on `/book/<id>/cover` after v4.0.38 shipped: (1) `threading.BoundedSemaphore(4)` was already wrong from PR #92 onward — the WSGI server is gevent and we don't `monkey.patch_all`, so a synchronous semaphore acquire blocks the entire gevent loop; with 18 candidate covers, login + `/static/` + metadata-search all stall while each Wand job runs serially. PR #119 swapped to `concurrent.futures.ThreadPoolExecutor` which had the same problem because `Future.result()` is a synchronous threading.Event wait that doesn't yield to the gevent hub — confirmed live with `py-spy`: MainThread stuck in `concurrent.futures._base.wait` while a worker did the C call. (2) `_fetch_url_bytes` for external candidate covers (Amazon, OpenLibrary, Google Books, etc.) was running on the gevent main thread inside the picker handler, so a slow upstream stalled every other in-flight request. (3) Each settings change re-fetched all ~18 candidate URLs from scratch, so toggling Edge blur → Gradient took as long as the initial toggle-on. Fixes: switch the pool to `gevent.threadpool.ThreadPool` (yields cleanly via `pool.apply` so the gevent loop stays responsive); fall back to the stdlib executor when gevent isn't importable so unit-test runners stay green. Run the entire fetch-then-pad pipeline on the pool — the gevent main thread never blocks on SSL handshakes. Add a 64 MB LRU URL-bytes cache so subsequent settings changes hit cache for the URL fetch and pay only the Wand cost. Drop the candidate fetch timeout from `(10, 30)` to `(5, 8)` since the picker is interactive (a slow upstream blocks the user's grid; better to drop fast and let them retry). Optimize `edge_blur` itself: blur at quarter resolution and upscale (the upscale acts as its own low-pass filter), 3× faster wall-clock with no perceptible visual difference (0.54 s → 0.24 s on a real cover). Lift the client-side `KOBO_MAX_CONCURRENT` cap from 4 to 8 to match the new server pool. Mark the helper reentrant via `threading.local` so the picker handler can dispatch the whole pipeline to the pool while `render_kobo_preview_data_url` (which also dispatches when called directly) doesn't double-allocate a slot. Live-verified: 18 covers re-render in **~1 second** (down from "never completes in 20 s"); subsequent settings changes (edge_blur → gradient) hit the URL cache and complete in 1 s. **Why the previous releases looked correct in unit tests but wedged in production**: every unit test imports the module without gevent, so the gevent codepath was never exercised; the threading.Semaphore + concurrent.futures regressions were invisible to the test harness. New behavioral check pins the gevent path. 33 unit tests. | `e83d5791` | v4.0.39 |
| #119 | Cover-picker Kobo preview reliability + new `gradient` fill mode. Two issues observed live on `/book/<id>/cover`: (1) changing the **Target aspect ratio** or **Border fill style** dropdowns while "Show how each cover would look on a Kobo" was on did not refresh the previews — the closure-shared `inFlightCount`/`currentBurst` got corrupted when an aborted older burst's `.finally` decremented the counter belonging to the freshly-started new burst, so subsequent fetches looked "in flight" and visible img.src never updated; (2) toggling on with ~36 candidate cards fired 36 simultaneous fetches, each holding a gunicorn worker through the server-side `BoundedSemaphore(4)`, starving login / static / metadata-search routes until the burst drained. Fix replaces the closure-state with a generation counter — every refresh increments `generation`, each fetch closes over `myGen`, and completion handlers only mutate live UI state when `generation === myGen`. Adds a client-side `KOBO_MAX_CONCURRENT = 4` work-queue so the toggle-on burst never exceeds the server's per-process Wand cap. Discrete-event controls (aspect / fill-mode dropdowns) refresh immediately; the manual-color text input keeps the 250 ms debounce. New **`gradient`** fill mode added alongside `edge_mirror` / `edge_blur` / `dominant` / `average` / `manual`: samples the dominant color from the cover's top 10% band and bottom 10% band and renders the pad area as a smooth top→bottom gradient between them via Wand's `pseudo='gradient:'`. Looks "designed" — palette-matched without the edge-replication seams of mirror/blur. New option exposed in both the picker and the admin Settings dropdown. 33 unit tests (4 new JS surface tests for refresh wiring + generation safety + concurrency cap, 2 new Wand round-trip tests for gradient mode, 1 new template-surface test). | `27580c0b` | v4.0.38 |
| #148 | **Phase 1 of cover-normalization** ([design](notes/COVER-NORMALIZATION-DESIGN.md)). Pure refactor + preset-catalog extension that lays the foundation for normalizing book covers to e-reader aspect ratios across every browse surface (library grid, shelves, detail page, search). This PR ships only the rename + catalog so subsequent phases (per-user-per-book overrides, library-render endpoint, cover-editor lock + apply-to-all, settings checkbox, multi-select bulk-apply) can build on a device-neutral foundation. Renames `cps/services/cover_padding.py` → `cover_preview.py`, `PaddingSettings` → `CoverPreviewSettings`, `render_kobo_preview_data_url()` → `render_preview_data_url()`. Preset catalog grows from 3 Kobo entries to **22** across Kobo (Libra Color / Libra 2 / Clara Color/BW/2E/HD / Sage / Elipsa 2E / Forma), Kindle (Paperwhite / Oasis / Basic / Scribe), PocketBook (Era / InkPad / Color), Boox (Page / Leaf / Note Air), and generic 6"/7"/10" e-ink classes. Old Kobo preset keys preserved verbatim (admin DB persistence) so existing Kobo-sync setups are uninterrupted on upgrade. New `PRESET_LABELS` + `PRESET_GROUPS` sibling dicts power UI grouped dropdowns. Cover-picker route `/book/<id>/cover/kobo-preview` → `/book/<id>/cover/ereader-preview` — the new URL is `ereader-preview` (not `preview`) because `/book/<id>/cover/preview` is already the URL-validator endpoint at line 151 of `cps/cover_picker.py`. Old URL kept as a **308 Permanent Redirect** for one release. Cover-picker modal heading + element ids/classes + JS identifiers + endpoint URL all renamed Kobo → ereader; a template `BuildError` from the route rename was caught + fixed during verification. 8 new regression tests: `TestPresetCatalogCoverage` (4 tests — historical Kobo keys preserved, `PRESET_ASPECTS`/`PRESET_LABELS`/`PRESET_GROUPS` stay in sync, every preset yields a portrait ratio) + `TestLegacyKoboPreviewUrlRedirect` (4 tests — route registration, distinct function names, code=308 + Location target, shim body contains no business logic). 71 total unit tests pass / 24 Wand-gated skips / 0 failures. Live `cwn-local` container healthcheck green; cover-picker page → 200 with "E-reader preview" heading rendered; zero `BuildError` in container logs. **Behavior preservation**: Kobo-sync admin column names (`config_kobo_cover_padding_*`) deliberately preserved — Kobo sync IS Kobo-specific at the protocol layer. The `kobopad-` cache filename prefix inside the engine is preserved for now (TODO flagged for cleanup in a later cache-invalidation event). | `71a063f5` | v4.0.50 |
| #159 | **Phase 3 of cover-normalization** ([design](notes/COVER-NORMALIZATION-DESIGN.md)). The **hot read path** — `GET /cover/<int:book_id>/preview` now serves the cover padded to the user's chosen e-reader aspect ratio, cached on disk with stampede protection, ETag-driven 304s, and an hourly LRU sweeper keeping the cache under a configurable size cap. Still no user-visible UI change — Phase 4 wires up the cover-editor controls and Phase 5 adds the library/shelf toggle that puts this endpoint to work. Operators can hit it directly today; templates don't until Phase 5. **`cps/services/cover_preview_cache.py`** (new pure module, no Flask deps): SHA-256 cache key truncated to 16 hex chars including `cover_mtime` so a cover update auto-busts the cache; git-style 2-char prefix path layout under `/config/.cwa-preview-cache/<aa>/<rest>.jpg` caps directory size; atomic write-then-rename with `fsync` prevents partial files surfacing during a crash; per-key `stampede_lock()` returns the same `threading.Lock` for the same key — folds ~20 simultaneous misses on the same tile into one render. 22 regression tests including a 20-thread barrier-synchronized concurrency test and a live atomic-write race test (writer + 4 readers on a 16 KB payload for 500ms asserting no reader ever sees a partial file). Handles `noatime` mounts via mtime fallback. **`cps/cover_preview_blueprint.py`** (extended): new `GET /cover/<int:book_id>/preview` endpoint. Auth + book-visibility check mirror `/cover/<id>` exactly via `calibre_db.get_filtered_book(book_id, allow_show_archived=True)` — same call chain as `helper.get_book_cover_internal`. Cover-path resolution mirrors the Calibre-directory branch of `get_book_cover_internal` (`Path(config.get_book_path()) / book.path / "cover.jpg"`). Query params (optional, for cover-editor live preview): `p` preset, `f` fill, `c` color, `v` cache-buster. Resolution precedence is **query-param > per-book override > user default** via Phase 2's `resolve_effective_settings()`. ETag (weak validator `W/"<cache-key>"`) on every response; `If-None-Match` matching returns 304. Cache miss runs under `stampede_lock` with a re-check inside the lock so we don't render twice in a race. `Cache-Control: private, max-age=86400`. 503 if the engine raises (logged with book id). 14 regression tests including a 20-thread concurrent-miss test that pins `pad_blob` is called exactly once. **`cps/services/cover_preview_cache_sweeper.py`** (new): reads `CWA_PREVIEW_CACHE_MAX_MB` (default 1024); walks `CACHE_ROOT`, totals file sizes, evicts oldest-atime files until under cap; mtime fallback on noatime mounts; skips `.tmp-` orphans to avoid racing in-flight `write_to_cache` calls; per-file `unlink` errors counted but don't abort; race-tolerant (file vanished between walk + unlink — accounted, not error). 15 tests cover under-cap no-op, oldest-first eviction, dry-run preserves files, multi-prefix walk, .tmp- skip, permission-error tolerance, noatime fallback, bad/negative/zero env values defaulting to 1024. **`root/etc/s6-overlay/s6-rc.d/cwa-preview-cache-cleanup/`** (new s6 longrun service): runs the sweeper as the `abc` service user every `CWA_PREVIEW_CACHE_SWEEP_INTERVAL` seconds (default 3600); startup delay `CWA_PREVIEW_CACHE_INITIAL_DELAY` (default 120s) so it doesn't compete with cwa-init on fresh boot; registered in the user bundle. Matches `cwa-auto-zipper` longrun pattern. **51 new unit tests** total, all green. **Live container verification** (full report at `notes/PHASE-3-VERIFICATION-2026-05-13.md`): cwn-local rebuild → healthy in 36s; first `GET /cover/2/preview` returns 200 / `image/jpeg` / 350 422 bytes / ETag `W/"f478ccf60f8e8083"` / `Cache-Control: private, max-age=86400`; body is valid JPEG; cache file appears at `/config/.cwa-preview-cache/f4/78ccf60f8e8083.jpg`; `If-None-Match` returns 304 with empty body; **cache hit is 8 ms vs 186 ms cold miss — 23× speedup**; raw `/cover/<id>` still serves untouched bytes; manual sweeper run reports `before=350422 after=350422 evicted=0 cap=1073741824`; zero new `BuildError`/`TemplateError`/`sqlalchemy` errors in container logs. CI passed on the first run (no `--admin` override needed) — the v4.0.52 deflake paying off as the first PR to ship through cleanly. | `f3358289` | v4.0.53 |
| #149 | **Phase 2 of cover-normalization** ([design](notes/COVER-NORMALIZATION-DESIGN.md)). Backend storage layer + 5 write endpoints. **No frontend changes** — endpoints are consumed by Phases 4 + 5; existing users are unaffected on upgrade. **DB schema**: 4 new `user` columns (`show_ereader_previews`, `preview_preset`, `preview_default_fill`, `preview_default_color`) and a new `book_cover_preview` table (composite PK on `(user_id, book_id)`, CASCADE FK on user; `book_id` cannot be SQL-FK since it lives in a separate SQLite file). **Migrations**: idempotent via the established `exists()`-probe pattern in `migrate_user_table`; one-time UPDATE sets `show_ereader_previews=0` for users that existed before the migration (silent rollout — new users default True via column-level default). New `migrate_book_cover_preview_table()` wired into `migrate_Database` after `migrate_kobo_unique_constraints`. **Resolution helper** at `cps/services/cover_preview_resolution.py` consolidates the precedence rule **query-param > per-book override > user default** with 10 pinning tests — this is the load-bearing contract that every Phase-3 cover render will use. **5 POST endpoints** in `cps/cover_preview_blueprint.py`: `/me/cover/preview-defaults` (user defaults), `/book/<id>/cover/preview-settings` (per-book upsert), `/book/<id>/cover/preview-lock` (lock toggle — creates row from effective settings if none exists), `/me/cover/preview-apply-to-all` (library-wide; preserves locked rows; compacts unlocked rows that match new defaults), `/books/cover/preview-bulk-apply` (multi-select cap 5000). All CSRF-protected via CW's global Flask-WTF middleware, `@user_login_required`, mutating-per-book endpoints additionally `@edit_required`. Validators reject unknown `fill_mode`/`preset`/malformed color hex; bulk-apply integer check excludes `bool` (since `isinstance(True, int) is True`). **Orphan cleanup** at `cps/services/cover_preview_cleanup.py` — `sweep_orphaned_cover_previews()` removes rows whose `book_id` no longer exists in `metadata.db` (empty-Calibre-DB and DB-unavailable cases both no-op to avoid wiping valid rows); scheduler wiring into `cps/schedule.py`'s `CalibreTask` framework deferred to a follow-up. **52 new regression tests** all green (10 resolution-precedence + 26 endpoint auth/payload/SQL/limits + 6 migration column-add+idempotency+backfill + 5 orphan-cleanup). Live `cwn-local` container verification (full report at `notes/PHASE-2-VERIFICATION-2026-05-12.md`): migration ran cleanly against a real app.db with 2 pre-existing users — both got `show_ereader_previews=0`, log line `[cover-preview-migration] Defaulted show_ereader_previews=0 for 2 existing user(s)` confirmed; each of the 5 endpoints returned **200** with correct SQL side effects via authenticated curl (locked row preserved by apply-to-all, bulk-apply touched 3 books, lock toggle worked); zero new `BuildError`/`TemplateError`/`sqlalchemy` errors. CI Fast Tests hit pre-existing flakes (`test_cwa_db` sqlite3 `database is locked` under pytest-xdist + `test_generate_checksums` subprocess timeouts) on this PR's runs — both documented in CLAUDE.md as unrelated; the deflake is queued as a separate follow-up so it doesn't block the Phase 3 dependency chain. Merged with `--admin` per session authorization. | `834bf886` | v4.0.51 |
| #188 | (fork issues [#184](https://github.com/new-usemame/Calibre-Web-NextGen/issues/184), [#185](https://github.com/new-usemame/Calibre-Web-NextGen/issues/185)) | **OPDS responses intermittently dropped relationship fields (summary, author, language, tags) for a subset of books per request.** Identical sequential requests against a static library returned different counts — `<summary>` / `<author>` / `<dcterms:language>` / `<category>` would render fully for most books in the feed and empty for one or two, with the affected book varying request-to-request. Live SQL trace via `engine.echo=True` showed nine subquery post-loads against a parent query whose `FROM` clause had been polluted with `LEFT OUTER JOIN books_series_link ... LEFT OUTER JOIN series` despite the OPDS code path never adding those joins; `lazy='subquery'` reproduces the parent's compiled FROM/WHERE/ORDER/LIMIT as the inner subselect of its post-load, and that compile-state leak across the process-captured `CalibreDB.session` (shared across gevent greenlets, running `fill_indexpage`'s two-parent-queries-per-request shape) caused the inner subselect to miss rows for a subset of parent IDs — those rows then had empty relationship collections on the `Book` instance handed to the template. **Fix**: switch all nine `Books` relationships (`authors`, `tags`, `comments`, `data`, `series`, `ratings`, `languages`, `publishers`, `identifiers`) from `lazy='subquery'` to `lazy='selectin'`. `selectin` emits one extra `WHERE parent_id IN (?, ?, ...)` per relationship, atomic, depending only on the parent primary keys batched as an IN clause — never on the parent query's FROM/WHERE/ORDER/LIMIT or compile state. Still an eager loader, so PR #40's `DetachedInstanceError` case (upstream #1067 / #1130 / #1139) remains covered. **Supersedes [PR #40](https://github.com/new-usemame/Calibre-Web-NextGen/pull/40)** (`bc9386c`, v4.0.16, backport of upstream PR #1279 by @dbraendle) — that PR chose `lazy='subquery'` because it solved the original `DetachedInstanceError`; `lazy='selectin'` solves the same DetachedInstanceError while avoiding the compile-state coupling that made #184 + #185 surface. Both #184 and #185 share this single fix because they're the same bug — the random-panel branch in `fill_indexpage` produces the two-parent-queries-per-request shape that the OPDS path always exercises, regardless of which OPDS endpoint the client hits. Affects every consumer of `Books`: OPDS feeds (visible in #184, #185), Kobo sync (`cps/kobo.py`), KOReader cwasync (`cps/readingservices.py`), Magic Shelves, web UI grid/list/detail. **Repro before fix** (30 sequential `/opds/new` requests against a static 21-book library, v4.0.58 GHCR image): counts varied — iter=1 `sum=1 auth=4 cat=21`, iter=2 `sum=1 auth=6 cat=31`, iter=8 `sum=0 auth=6 cat=15`, iter=16 `sum=1 auth=4 cat=12`, etc. Different books dropped in different iterations; mostly all-or-nothing across the four subquery-loaded relationships but not strictly so (iter=4 once showed Oz with `auth=0 lang=1 cat=0 sum=1` — language + summary present, author + tags gone — independent per-relationship post-load failures, ruling out a top-level "Books not loaded" hypothesis). **Post-fix evidence** (same library, same image-with-patch): 100 sequential `/opds/new` requests all returned `sum=1 auth=6 cat=31` (constant); 50 parallel via `xargs -P 16` all returned identical relationship counts. Plus regression suite: `tests/integration/test_books_relationship_loading.py` source-pins `Books.__mapper__.attrs[name].lazy == 'selectin'` for each of the nine relationships (fails fast if anyone reverts the loader) and behaviorally runs 100 sequential + 50 parallel queries asserting populated collections match the underlying DB row counts. Full SQL trace, compile-state-leakage analysis, and why we did not adopt the symptom-level alternatives (per-request session, per-query `joinedload(...)`, disable random panel) all in [`notes/184-185-DESIGN.md`](notes/184-185-DESIGN.md). **Upstream impact**: CWA's PR #1279 is still OPEN (not merged), so upstream `main` doesn't have this bug — but anyone who applies #1279 will inherit it. Upstream comment queued for that PR. | `e8023170` | v4.0.59 |
| #187 | (fork issues [#180](https://github.com/new-usemame/Calibre-Web-NextGen/issues/180), [#181](https://github.com/new-usemame/Calibre-Web-NextGen/issues/181), [#182](https://github.com/new-usemame/Calibre-Web-NextGen/issues/182), [#183](https://github.com/new-usemame/Calibre-Web-NextGen/issues/183), reporter @droM4X) | **OPDS polish bundle — rating digits, sentinel pubdate omission, fork URL in feed headers, and 404 catch-all for forged paths.** Four small bugs in the OPDS surface bundled because they're in the same layer and verify together against the same library. **#180**: `/opds/ratings` titles rendered as `3.0000000000 Stars` because SQLAlchemy's division of the INTEGER `ratings.rating` column coerced to `Decimal('3.0000000000')` and bare `"{}".format()` printed the trailing zeros; new `_format_opds_rating(value)` helper coerces via `float()` and uses `":g"` formatting so whole stars render as `"3"` and half-stars as `"3.5"`. **#181**: `<published>` emitted Calibre's `datetime(101, 1, 1)` sentinel (`0101-01-01T00:00:00+00:00`) for books without a real pubdate, which Readest and other OPDS clients showed as "Invalid Date"; new `_is_real_pubdate()` helper compares against `db.Books.DEFAULT_PUBDATE` and is exposed to `feed.xml` via the OPDS context processor so `<published>` is omitted only for the sentinel — real historical pubdates (Don Quixote 1605, etc.) still emit per OPDS spec. **#182**: `feed.xml`, `index.xml`, and `osd.xml` still advertised `github.com/crocodilestick/calibre-web-automated` in `<author><uri>` and `<Contact>`; replaced with the fork URL and extended `tests/unit/test_template_fork_urls.py` to walk `.xml` so a future cherry-pick can't reintroduce it. **#183**: `/opds/<anything-not-real>` (e.g. `/opds/test`) returned 308 → `/opds/test/` which matched stock Calibre-Web's `@web.route('/<data>/<sort_param>/')` and rendered the full HTML books grid; new blueprint-scoped `@opds.route('/opds/<path:_unknown>')` catch-all returns 404 with an Atom feed body and `application/atom+xml` content-type. The `<path:>` converter catches multi-segment forged paths (`/opds/a/b/c`) too; Flask's URL map prefers literal-prefix routes so defined OPDS endpoints still win for their specific paths. **28 regression tests** in `tests/unit/test_opds_bugs_180_181_182_183.py` source-pin each helper + the catch-all rule shape, plus behaviorally drive the four-endpoint URL map under varied request contexts. **Live-verified on cwn-local against a 21-book test library (freshly rebuilt image, Readest UA)**: `/opds/ratings` shows `2.5 Stars`, `3 Stars`, `4 Stars`, `5 Stars` cleanly; The Picture of Dorian Gray (the one sentinel-pubdate book) emits no `<published>` element while the other 20 books still do; `/opds` returns the fork URL; nine OPDS endpoints (`/opds`, `/opds/books`, `/opds/new`, `/opds/discover`, `/opds/ratings`, `/opds/author`, `/opds/category`, `/opds/series`, `/opds/osd`) all return 200; `/opds/test`, `/opds/foo/bar`, `/opds/totally-bogus` all return 404 with `application/atom+xml; charset=utf-8` and a small Atom error body; 20 concurrent forged-path requests all 404 cleanly; zero new ERROR / WARN / Traceback lines in container logs since the rebuild. | `62f6f2f8` | v4.0.58 |
| #195 | (fork issue [#190](https://github.com/new-usemame/Calibre-Web-NextGen/issues/190), author + reporter @droM4X) | **OPDS feed entries now emit `<content>` alongside `<summary>` per OPDS 1.2 §5.1.2.** Some OPDS readers (Readest in droM4X's case) prefer to display `<content>` when present; the fork previously emitted only `<summary>` carrying the full description text, which left Readest showing no synopsis. New `first_sentence` Jinja filter in `cps/jinjia.py` (splits on `.`, returns first sentence + period) and updated `feed.xml` so every entry with a description emits `<summary type="text">` with the first sentence and `<content type="text">` with the full text. Both tags carry `type="text"` per spec. **Live verification on the v4.0.60 teenyverse container with the staged PR diff**: 60 entries, 42 have descriptions, all 42 get both tags, XML well-formed (Python ElementTree parse, summary's first chunk appears inside content as expected), no Flask/Jinja errors in container logs, OPDS endpoint returns 200 with `application/atom+xml`. | `f9085039` | v4.0.61 |
| #236 | **Phase 1 of H1 — schema foundation** ([design](notes/KOBO-WEB-READER-ANNOTATIONS-DESIGN.md) §1.1, [prompt](notes/KOBO-H1-PROMPT.md)). Foundation for the headline Kobo highlight import / view / web-reader sync feature (closes CW #2155, 26 reactions, once P1–P5 ship). Extends the existing `kobo_annotation_sync` table — originally added for Hardcover annotation sync via CWA #1166 + #1324 — with 12 nullable columns the import pipeline + CFI converter + view + render code in P2–P5 will populate: `content_id`, `start_container_path`, `start_container_child_index`, `start_offset`, `end_container_path`, `end_container_child_index`, `end_offset`, `context_string`, `chapter_progress`, `cfi_range`, `source` (`'kobo'`/`'webreader'`/`'koreader'`/`'hardcover'`), `hidden` (soft-delete flag). All nullable so pre-H1 Hardcover-sync rows continue to work untouched. Migration is idempotent — column existence checked per-column before each `ALTER TABLE ADD COLUMN`, source backfill scoped to rows with `synced_to_hardcover=1` so a second restart is a no-op. Also tags new Hardcover-sync rows with `source='hardcover'` at insert time in `cps/readingservices.py` so the column stays meaningful between container restarts (not just at migration time). **No user-visible change in this release** — the import endpoint, view page, and web-reader render arrive in P2–P5. 9 regression tests pin model declaration, nullability, position-column types, migration column-add, pre-H1 row survival, source backfill scope, idempotency under re-run, ORM round-trip across all H1 fields, and the Hardcover sync constructor source-pin. RED→GREEN proven cleanly (test would fail on `main` without the model + migration). Live `cwn-local`: schema column count went 11 → 23, no migration errors in logs, second restart no-op, `/health` 200. | `94f48511` | v4.0.78 |
| #239 | **Phase 2 of H1 — Kobo position → epub.js CFI converter** ([design](notes/KOBO-WEB-READER-ANNOTATIONS-DESIGN.md) §3, [prompt](notes/KOBO-H1-PROMPT.md)). Production port of the 99.3%-round-trip-proven prototype from the 2026-05-17 borrow-day session. New `cps/services/kobo_position.py` module — single public function `compute_cfi_range(epub_path, KoboPosition)` returning an `epubcfi(...)` string ready for `rendition.annotations.highlight()` in epub.js. P3's import endpoint will call it per highlight at ingest time and persist the result into the `cfi_range` column shipped in v4.0.78. **Production hardening over the prototype:** (1) `lxml.html` DOM parser replaces the brittle regex span scanner that produced the prototype's lone 0.7% nested-span failure — the known failure case is now handled cleanly; (2) `functools.lru_cache` on parsed spine + chapter trees keyed by `(epub_path, mtime_ns)` so re-uploads invalidate automatically and a book's 100+ highlights don't re-parse the chapter HTML 100 times; (3) `ContextString` re-anchoring fallback when CFI lookup fails (EPUB restructured / plain-EPUB with no usable child index); (4) plain-EPUB path via the `StartContainerChildIndex` DOM-walk — Kobo writes `-99` as a sentinel meaning "use the CSS selector"; any other value triggers DOM-index walking. **Real-data verification:** ran the new module against all 145 highlights in the same Animal Farm kepub the prototype used — **145/145 = 100.0% round-trip** (vs. 99.3% on the prototype). **18 unit tests** in `tests/unit/test_kobo_position_converter.py` covering spine parse, KoboSpan id extraction (including the three-digit suffix that bit the prototype), single-span / multi-span / mid-offset highlights, plain-EPUB child-index walking, malformed input → None (no raise), mtime-driven cache invalidation, and ContextString fallback. Synthetic kepub fixture in `tests/fixtures/kepub_fixture.py` so the suite never depends on real device data (PII concern). **Plus 5 belt-and-suspenders preservation tests** for the P1 schema migration (already shipped in v4.0.78): bit-exact column preservation on a 100-row populated pre-H1 DB, SHA-256 fingerprint match across `(id, highlighted_text, note_text, highlight_color)`, row count preservation, `source='hardcover'` lands only on `synced_to_hardcover=1` rows (never cross-leaks to unsynced rows), fresh ORM session reads all 100 pre-H1 rows cleanly. No new dependencies — `lxml` (5.3.2) + `beautifulsoup4` already in `requirements.txt`. **Behaviorally inert in this release**: service module only, no user-visible surface; the import endpoint, view page, and web-reader render arrive in P3–P5. | `f5a04707` | v4.0.80 |
| #241 | **H1 safety net — annotation backup with rolling-3 per-book snapshots** (closes fork [#240](https://github.com/new-usemame/Calibre-Web-NextGen/issues/240)). Passive safeguard behind the upcoming H1 import + view + web-reader-write paths: every INSERT or UPDATE to `kobo_annotation_sync` is captured as a per-`(user_id, book_id)` gzipped JSON snapshot under `/config/annotation-backups/<user_id>/<book_id>/<UTC-iso>.json.gz`, with SHA-256 content-hash dedup so identical state never doubles disk usage and rolling-3 retention so the cap is bounded. Two-phase SQLAlchemy wiring on `Session`: `after_flush` collects `(user_id, book_id)` keys onto a per-session attribute, `after_commit` drains them to a daemon worker so the worker never queries before the source thread's commit lands, `after_rollback` discards pending so a rolled-back tx never produces a phantom backup. New `KoboAnnotationBackup(id, user_id, book_id, created_at, content_hash, file_path, size_bytes, annotation_count)` table for retention queries (`ORDER BY created_at DESC LIMIT 3`). Worker is a single daemon thread + `queue.Queue`; backup work is I/O-bound and never blocks the sync API or any HTTP handler. **Default-on**: any future write path (Hardcover sync today; P3's import endpoint and P5's web-reader create path tomorrow) gets the safety net automatically without each caller having to remember. **19 regression tests** cover model + schema declaration, fresh-install table creation via `add_missing_tables`, single-write produces 1 file + 1 index row, full H1 payload round-trips through gzip+JSON without loss, content-hash dedup short-circuits, mutation produces a new backup, 4 distinct states → exactly 3 files + 3 index rows (oldest unlinked), per-book retention isolation, no-annotation no-op, orphan `book_id IS NULL` skipped at collect-time, two-phase collect / dispatch / rollback wiring, and `enqueue_baseline_for_user` enumerates distinct `book_id` once per book (not once per annotation). **Live `cwn-local` end-to-end**: INSERT triggers `after_flush` + `after_commit` → 357-byte gzip file on disk + index row; UPDATE produces a second; 5 distinct states result in exactly 3 surviving files (rolling-3 enforced); ROLLBACK after mutation clears pending so the queue stays at 0. **Out of scope (follow-up PRs)**: restore endpoint UI, per-user toggle UI (default-on is correct for v1), per-user disk-quota sweeper (rolling-3 is the floor). | `dee31869` | v4.0.81 |
| #242 | **Phase 3 of H1 — `POST /annotations/import` endpoint** ([design](notes/KOBO-WEB-READER-ANNOTATIONS-DESIGN.md), [prompt](notes/KOBO-H1-PROMPT.md)). First user-visible piece of H1: a user uploads their Kobo device's `KoboReader.sqlite` and CWNG ingests every highlight, note, and color into their account. New `cps/annotations.py` blueprint exposes `GET /annotations/import` (upload form) + `POST /annotations/import` (multipart upload → JSON `{imported, skipped_existing, skipped_orphan, skipped_hidden, total_seen}`). New `cps/services/kobo_import.py` parses the Bookmark table read-only (`mode=ro&immutable=1` URI — never writes, never fires triggers); new `tests/fixtures/kobo_reader_sqlite.py` builds synthetic test DBs so the suite never depends on real device data (PII concern). **Pipeline**: multipart upload streamed into a `NamedTemporaryFile` with a 100 MB hard cap (size check pre-read + mid-read so a lying `Content-Length` can't sneak past) → SQLite magic-bytes check → `parse_kobo_bookmarks()` walks `Bookmark` rows with `Text != ''` → `ingest_bookmarks()` resolves each `VolumeID` against the CW library via `calibre_db.get_book_by_uuid` → `(user_id, annotation_id)` dedup short-circuits re-imports → hidden + sideloaded `file://` + unknown-UUID rows skipped + counted → temp file unlinked in `finally` before the JSON response returns (file contents are PII, never persisted). The #241 `after_commit` backup hook captures each INSERT automatically, so a re-import that would overwrite anything has a recoverable snapshot already on disk. **Security**: CSRF protected (Flask-WTF default, **not** `@csrf.exempt`); `@user_login_required`; 100 MB streamed-read cap; SQLite read-only sandbox; parameterized UUID lookup. **22 P3 tests** (14 parser + 8 ingest) — RED→GREEN proven, canonical-fixture counts (`imported=3, skipped_hidden=1, skipped_orphan=2, total_seen=6`), full H1-column round-trip, idempotency (re-import = 0 imported, 3 skipped_existing), multi-user isolation, sideloaded-URI orphan handling, commit-failure rollback. **Live `cwn-local` end-to-end via curl**: login as `cwng84test` → POST with CSRF + multipart → HTTP 200 with the expected JSON; DB shows 3 new rows scoped per-user with full H1 payload populated; annotation-backup wrote a 600-byte gzipped snapshot to `/config/annotation-backups/3/2/<UTC-iso>.json.gz`; re-import short-circuits to `{imported=0, skipped_existing=3}` and backup count stays at 1 (content-hash dedup intact). **Skipped (with reason)**: Playwright UI pilot — the form is a simple HTML upload + tiny `fetch()` handler with no JS state machine; the live curl flow exercised the actual user-visible path end-to-end. **CFI computation deferred to P5** — schema columns are populated with raw Kobo position data so the eventual converter call has everything it needs. | `65edf6dc` | v4.0.82 |
| #243 | **Phase 4 of H1 — view + Markdown/CSV/JSON export** ([design](notes/KOBO-WEB-READER-ANNOTATIONS-DESIGN.md), [prompt](notes/KOBO-H1-PROMPT.md)). The surface that turns P3's imported highlights into something users can actually look at + download. **Routes**: `GET /annotations/<book_id>` renders a per-book view page with color-coded blockquotes (yellow/red/green/blue per Kobo's color palette) + note + chapter progress + source + date; `GET /annotations/<book_id>/export.md` downloads a Markdown document (H1 with book title, one blockquote per highlight, metadata line with color/note/progress/source); `GET /annotations/<book_id>/export.csv` downloads a CSV with stable column order + RFC-4180 escaping; `GET /annotations/<book_id>/export.json` downloads a JSON envelope **identical in shape to the v4.0.81 backup snapshot format** so a power user can use either format interchangeably. **Implementation**: pure-function helpers (`render_markdown`, `render_csv`, `render_json`, `_safe_filename_part`, `_load_user_annotations`, `_row_to_dict`) are unit-testable without a Flask context; Flask request handlers are thin adapters. All routes `@user_login_required` + per-user-per-book scoped via `ub.session.query` filtered on `(user_id, book_id)`. Hidden rows excluded. Stable sort by `chapter_progress` (nulls last) → `created_at` → `id`. **16 P4 tests** cover per-user-per-book isolation, hidden exclusion, sort order, Markdown structure (title + blockquote per highlight + metadata line + multi-line each line quoted), CSV stable column order + embedded-comma/newline `QUOTE_MINIMAL` round-trip + empty-book header-only, JSON envelope matches backup shape + zero-count empty case, and `_safe_filename_part` slug (path-traversal chars stripped, CJK fallback to `book`, dots and underscores preserved). 281/281 in broader related-test suite pass. **Live `cwn-local` end-to-end via curl**: imported 3 highlights via P3 endpoint → `GET /annotations/2` returns HTTP 200 with "Annotations: The Picture of Dorian Gray" + 3 blockquotes + correct color-coded CSS classes → Markdown export downloads with `Content-Disposition: attachment; filename="The-Picture-of-Dorian-Gray-highlights.md"`, body has H1 + 3 blockquotes + metadata lines, multi-format highlight emits with note + 2% progress → CSV header + 3 rows, embedded comma in the multi-format text properly quoted, `csv.DictReader` round-trip recovers exact text → JSON envelope shape matches backup format. **Mid-verify fix**: caught + corrected a duplicate `charset=utf-8` in `Content-Type` headers (mimetype was carrying it, Werkzeug appended again; now mimetype= without explicit charset, Werkzeug appends once). **Skipped (with reason)**: Playwright UI pilot — view is a static-render template with no JS state machine; the curl flow above already exercised the user-visible HTML structure, color classes, export links, download headers, and content body. | `fe0cbc1c` | v4.0.83 |
| #245 | **Phase 5 of H1 — web reader overlay** ([design](notes/KOBO-WEB-READER-ANNOTATIONS-DESIGN.md) §2 + §6.1, [prompt](notes/KOBO-H1-PROMPT.md)). Headline visual piece of H1. Imported highlights (P3) now render in the in-browser EPUB reader as color-coded overlays + a sidebar "Annotations" tab with clickable jump-to-CFI entries. Closes the core promise of the design doc: server → web reader render, web reader's sidebar wired to the imported set. **New route** `GET /annotations/<int:book_id>/data.json` returns the user's per-book annotations as JSON; for rows where `cfi_range` is NULL (P3-imported rows arrive position-only), the endpoint computes CFI on the fly via P2's `compute_cfi_range` using the EPUB on disk, then persists back so subsequent reads cache-hit. **Reader template** uncomments + renames the previously-disabled Notes panel in `cps/templates/read.html` → "Annotations" with an `id="annotations-list"` `<ol>`; passes `calibre.annotationsApiBase` to the JS layer. **New `cps/static/js/reading/annotations.js`** fetches `data.json` on `reader.rendition` ready, iterates the annotations, calls `reader.rendition.annotations.highlight(cfi, data, callback, className, styles)` for each row with a resolvable CFI, populates the sidebar list with clickable list items that `reader.rendition.display(cfi)` on click. Color mapping: yellow / red / green / blue (Kobo's palette) → rgba fill with 40% opacity + `mix-blend-mode: multiply` so highlights read cleanly on light + dark + sepia + black themes. **Deliberate scope cap**: text-select-to-create popup + DELETE endpoint + sidebar delete button land in a small follow-up — keeps this PR small enough to ship cleanly. Users can see + jump to highlights they imported via P3, but can't edit them in the reader yet; the `/annotations/<book_id>` view + the three export formats remain the surfaces for inspection + export. **7 P5 helper tests** in `tests/unit/test_annotations_data_endpoint.py` (RED→GREEN): `_resolve_epub_path` (finds kepub on disk; returns None when no EPUB format; returns None when file missing) + `_ensure_cfi_range` (short-circuits when cfi_range already set; computes via P2 when missing + persists back; returns None on malformed content_id; returns None when no EPUB on disk). **288/288 in broader related-test suite pass.** **Live `cwn-local` end-to-end**: `GET /annotations/2/data.json` returns the 3 P3-imported highlights; `GET /read/2/epub` renders HTTP 200 with `id="show-Annotations"` tab + `id="annotationsView"` view + `id="annotations-list"` `<ol>` + `annotationsApiBase` global + `annotations.js` script tag. **Playwright pilot**: reader page loads, "Annotations" sidebar tab visible alongside TOC + Bookmarks, no console errors from `annotations.js`, no failed XHRs. The pre-existing `view_settings` bug on the root `/` (visible as a 500 in browsers) is unrelated to P5. | `13d19209` | v4.0.84 |
| #257 | **App password creation now anchor-scrolls to the inline box** (follow-up to v4.0.90 #253, fork issue [#223](https://github.com/new-usemame/Calibre-Web-NextGen/issues/223), reporter @droM4X). After v4.0.90 shipped the inline green box, @droM4X noted that the post-redirect page landed at the top of `/me` — users had to scroll down to find the freshly-created token. `app_password_create` now redirects to `url_for('web.profile', _anchor='pending-app-password')` (HTTP 302 `Location: /me#pending-app-password`), and the browser auto-scrolls to the matching `id="pending-app-password"` div shipped in v4.0.90. The error path (label validation failure) keeps its plain `/me` redirect — no new password exists, so the anchor would be a dangling link. 2 source-pin tests in `tests/unit/test_app_password_redirect_anchor.py` (success branch carries the anchor, error branch doesn't); all 7 v4.0.90 tests still pass. **Live curl + Playwright pilot** on cwn-local: Location header is `/me#pending-app-password`; post-create URL renders with the anchor; inline box is in viewport. | `5ef109c6` | v4.0.93 |
| #260 | (fork issue [#224](https://github.com/new-usemame/Calibre-Web-NextGen/issues/224) follow-up, reporter @droM4X) | **OPDS 401 on Basic-auth rejection now returns Atom XML** (follow-up to v4.0.92 #255). The v4.0.92 fix registered `@opds.errorhandler(401\|403\|404\|500)` on the OPDS blueprint, which fires for explicit `abort(...)` calls from inside route handlers. But the 401 from missing/wrong Basic-auth credentials goes through `auth.auth_error_callback(401)` directly, which (by default) returns a fully-formed HTML `Response` without raising an `HTTPException` — so the blueprint errorhandler pipeline never fired and OPDS readers still got HTML. E2E against the deployed image confirmed `/opds/` with no credentials returned `text/html` + body `Unauthorized Access` + `WWW-Authenticate: Basic realm="Authentication Required"`. **Fix**: register a custom `@auth.error_handler` on the global `HTTPBasicAuth` instance in `cps/usermanagement.py` that branches on `request.path` — Atom XML + `WWW-Authenticate: Basic realm="OPDS"` for `/opds` and `/opds/*` (exact-match or `/opds/` subpath, not a substring match — `/opds-fake` keeps HTML), default HTML response for every other Basic-auth consumer (kosync, app passwords). **6 RED→GREEN tests** in `tests/unit/test_opds_auth_error_atom_401.py` — 4 positive (Atom Content-Type, OPDS realm, parseable `<feed>` body, subpath `/opds/books` also Atom) + 2 negative covering the non-OPDS-preserves-HTML invariant and the substring-match gotcha. All 6 pass on the fix; 4 fail on pre-fix main with the exact `text/html` / `Authentication Required` symptoms users reported. | `6966b584` | v4.0.95 |
| #261 + #263 | (community contribution from [@ikerios](https://github.com/ikerios), reported + diagnosed + patched + live-verified on podman/NixOS host) | **Cover-preview disk-cache LRU sweeper now actually runs.** The `cwa-preview-cache-cleanup` s6 longrun exec'd `python3 -m cps.services.cover_preview_cache_sweeper` from the supervisor's default CWD of `/`. Since `python3 -m` only adds the current directory to `sys.path[0]`, the `cps` package (at `/app/calibre-web-automated/cps/`) was unreachable, every sweep iteration died with `ModuleNotFoundError: No module named 'cps'`, the `\|\| echo …WARNING…` clause swallowed the failure, and the `CWA_PREVIEW_CACHE_MAX_MB` cap turned advisory — `/cover/<id>/preview` kept writing padded JPEG tiles into `/config/.cwa-preview-cache/` while nothing evicted them. On heavily-browsed libraries the disk grew unbounded until the operator noticed. **Fix** (#261): `cd /app/calibre-web-automated` before the `while true` loop — matches the pattern already used in `svc-calibre-web-automated/run` and `cwa-init/run` — with a `\|\| { echo "...FATAL..."; exit 1 }` guard so a misconfigured image surfaces a loud failure instead of looping silently. **3 RED→GREEN source-pin tests** in `tests/unit/test_s6_preview_cache_cleanup_cwd.py` (#263) — the cd is present, anchored before the while-true loop, and carries the FATAL guard. **Live container repro on cwn-local**: pre-fix `python -m cps...` from CWD=/ → `ModuleNotFoundError`; post-fix from CWD=/app/calibre-web-automated → `before=0 after=0 evicted=0 errors=0 cap=1073741824 root=/config/.cwa-preview-cache` summary from `main()`. Adjacent suite (preview cache + sweeper + s6 privilege drop) 42/42 pass. | `47cac8ed`, `1a8e9bf8` | v4.0.96 |
| #269 | (CWA issue [#1361](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1361), reporter @AnthonyGress) | **OPDS catalog now surfaces book series metadata.** Reader apps that support series grouping (Marvin, KOReader, FBReader, Calibre Companion) saw no series signal because the entry block never emitted it. Janeczku's plain CW puts a literal `SERIES: NAME [N]` line into the summary text — readable but unstructured; readers can't parse it for grouping or sort order. Per-entry now emits the structured form: `<calibre:series>NAME</calibre:series>` + `<calibre:series_index>N</calibre:series_index>` (Calibre namespace, what Calibre itself emits and what most reader apps recognize) plus `<dcterms:isPartOf>NAME</dcterms:isPartOf>` (generic Dublin Core fallback). The feed root now declares `xmlns:calibre="http://calibre.kovidgoyal.net/2009/metadata"`. Both names go through Jinja's `\|e` filter so series like `Foundation & Friends` don't break the XML. **5 RED→GREEN tests** in `tests/unit/test_opds_series_metadata.py` cover namespace declaration, both element shapes, the `{% if entry.Books.series %}` guard (otherwise IndexError on books without a series), and the escape filter. **Live container test on cwn-local** with `Foundation & Friends` series (ampersand stresses escape): XML parses cleanly via ElementTree, `&` round-trips through `&amp;`, elements consistent across `/opds/books/letter/`, `/opds/new`, `/opds/discover`, `/opds/search` paths. Books without a series correctly emit no series-related elements. 110 adjacent OPDS tests green. | `bae34e25` | v4.0.101 |
| #264 | (fork issue [#205](https://github.com/new-usemame/Calibre-Web-NextGen/issues/205), reporter @goatdancer4000-sudo, +1 @droM4X, +1 @Onkeyuk) | **Cover Settings cog button now does something — a per-user toggle to hide shelf badges on covers.** The cog icon on the book-organizer toolbar opened a dropdown whose only item ("Cover Settings") had been wired as a deliberate no-op pending another tick. Three users asked for the same thing: a way to hide the shelf badges that overlay book covers when a book is on several shelves. **Fix**: the dropdown now contains a `role="menuitemcheckbox"` toggle "Hide shelf badges on covers"; clicking it persists `user.view_settings.cover.hide_shelf_badges` via the existing `/ajax/view` endpoint (no DB migration — JSON column). When set, `<body>` carries `cover-hide-shelf-badges` and CSS scopes `display: none` to `.cover-badge-shelf` + `.cover-badge-shelf-extra-pill`. The Read badge (`.cover-badge-read`) stays visible — the toggle is scoped to shelf badges only by deliberate design (the source-pin tests assert that). JS does an optimistic UI flip with a revert-on-failure path so storage and viewport never drift. **6 RED→GREEN source-pin tests** in `tests/unit/test_cover_settings_hide_shelf_badges.py`. **Playwright pilot on cwn-local** (admin, 3 books pinned to a public shelf): initial state cog dropdown shows the toggle with `aria-checked="false"` + 3 visible shelf badges → click toggle → body class + aria-checked flip, all 3 badges `display:none`, `POST /ajax/view → 200`, DB confirms `view_settings.cover.hide_shelf_badges = true` → reload preserves state on `/`, `/discover/stored/`, `/shelf/1` → toggle OFF reverses the state and DB. Zero console errors. | `f99ce7c5` | v4.0.97 |
| #258 | **Pagination now also appears at the end of book lists** (closes fork issue [#256](https://github.com/new-usemame/Calibre-Web-NextGen/issues/256), reporter @droM4X). The caliBlur theme pins `.pagination` at the top-right via `position: fixed`. After scrolling through a long book list, users had to scroll back up to navigate. A second paginator copy now renders in normal flow at the end of the list. `cps/templates/layout.html` duplicates the paginator markup after the body block with `class="pagination pagination-bottom"`; `cps/static/css/caliBlur.css` scopes a `position: static` override to that modifier so the top copy keeps its fixed-top behavior while the bottom copy flows with the document. **3 RED→GREEN source-pin tests** in `tests/unit/test_pagination_bottom.py` pin both paginators present, the bottom-modifier class, and the CSS override. **Playwright pilot on cwn-local** with `config_books_per_page=5`: two paginators render — top at `position:fixed top:58px`, bottom at `position:static top:586px`; clicking the bottom Next link navigates to `/page/2`. | `f5bd50d8` | v4.0.94 |
| #278 | (CWA issue [#1360](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1360), upstream reporter on Docker Desktop with HDD-backed ingest — reproduced in our code path before fixing) | **Polling watcher no longer hammers HDD-backed ingest folders.** Reporter: "the container keeps touching the folder repeatedly instead of sleeping on real file events. The result is constant drive activity, extra seeks, and sustained read load." Verified in `scripts/watch_fallback.py::scan_once`: unconditionally called `iter_files(root, recursive=True)` every poll interval (default 5s), doing `os.walk(root)` over the entire tree + `os.stat()` on every file. On a 3000-book HDD library that's thousands of disk seeks every 5s. **Fix**: new `_dir_mtime_signature(root, recursive)` produces a cheap `(mtime_ns_hash, size)` tuple of root + immediate subdir mtimes; `scan_once` short-circuits the full walk when prior sig matches current AND index is populated. Linux + macOS bump dir mtime on add/remove — exactly the events ingest cares about. The stabilize-emit path still runs from the gated branch so files mid-flight at gate-close time aren't lost. **7 RED→GREEN tests** in `tests/unit/test_watch_fallback_mtime_gate_1360.py` — headline test patches `iter_files` + asserts `call_count == 0` on a no-change poll, pins the HDD-saving behavior. **Live exercise on cwn-local**: empty-dir → no-change-skip → file-drop reopens gate → `CLOSE_WRITE` emits after stabilize. Zero behavior change under inotify mode (fallback only runs on `CWA_WATCH_MODE=poll` or inotify ENOSPC). | `8b03cc79` | v4.0.109 |
| #277 | (CWA issue [#1364](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1364), today's upstream report — reproduced on our build before fixing per the new "verify upstream issues against our divergent code" discipline) | **Web EPUB reader now persists user position + records read history.** Reporter on the upstream tracker: "opens at the correct cached position then immediately snaps back to the beginning ... book_read_link fields are never updated." Two distinct root causes, both reproduced on cwn-local v4.0.107: (1) the `epub-progress.js` locationchange handler wrote `localStorage[key] = calculateProgress()` unconditionally; before `epub.locations.generate()` resolves that returns 0, wiping the user's prior valid position — the qFinished restore path then read 0 and `display(cfiFromPercentage(0))` landed them at the beginning; (2) `cps/web.py::read_book` never bumped `book_read_link.last_time_started_reading` / `times_started_reading` — only the Kobo + KOSync paths did, so users who only read in the browser had no read history recorded. **Fix 1**: gate the localStorage save on `epub.locations._locations.length > 0` so we never write the fake 0. **Fix 2**: UPSERT `ub.ReadBook` in `read_book`, with a debounce that only bumps `times_started_reading` on transition INTO IN_PROGRESS (refresh/Back/Forward doesn't inflate); always update `last_time_started_reading`. Wrapped in try/except so a DB error doesn't break page render. **7 RED→GREEN source-pin tests** in `tests/unit/test_cwa_1364_web_reader_progress.py`. **Live Playwright pilot** (Picture of Dorian Gray, book 2): baseline empty → open reader → row inserted (status=IN_PROGRESS, count=1, ts=now); jump to 35% → localStorage=`"35"`; navigate away + reopen → localStorage stays `"35"` (not corrupted to `"0"`), CFI restored to chapter 7, calculateProgress=35; count stays at 1, last_time_started_reading updated. Auto-save to server-side Bookmark table (reporter's third complaint about unreliable bookmarks) is a larger surface, tracked as follow-up — localStorage fix already addresses the headline "snap to beginning" symptom. | `f71271de` | v4.0.108 |
| #275 | [CW PR #3370](https://github.com/janeczku/calibre-web/pull/3370) | @ryan-c-scott | **Web EPUB reader now shows section progress alongside book progress** — display went from `XX%` to `XX% (YY% in book)`, so you can see at a glance how close you are to the end of the current chapter. Adapted to our split `epub-progress.js` architecture (upstream PR inlined the logic in `epub.js`; we have a separate file driven by `locationchange`). Uses the `epub` global throughout for consistency — `reader.book.locations` stays empty in our setup because `reader.book` is a separate ePub instance whose `generate()` is never called. `findLast` is ES2023 with manual reverse-iteration fallback for older runtimes; defense-in-depth null-state checks fall back to book-only display before `locations.generate()` resolves. `localStorage` save still passes the single book-percentage integer (composite shape would break older sessions). **6 RED→GREEN source-pin tests** in `tests/unit/test_epub_section_progress_3370.py`. **Playwright pilot on cwn-local** (Picture of Dorian Gray, 2981 locations generated): position 0 → `0% (0% in book)`; jumped to 50% of book → `77% (51% in book)` at chapter 9 (loc_index=11) — section and book percentages diverge as expected. Credit @ryan-c-scott. "in book" string not yet localized per upstream PR's own scope cap. | `942a5e13` | v4.0.107 |
| #274 | (CW issue [#3437](https://github.com/janeczku/calibre-web/issues/3437), reporter @rschiefer — 283-day-old upstream report, highest-reach un-drained CW issue at 8 reach) | **Book upload onto a restricted filesystem (CIFS without explicit `file_mode`/`dir_mode`, LinuxServer-style PUID/PGID bind mounts, NFS squash-root shares) no longer surfaces `[Errno 1] Operation not permitted` as a user-visible error even though the bytes already landed at the destination.** Root cause: `cps/helper.py::move_files_on_change`'s cross-device fallback used `shutil.copy2 + os.remove`. `shutil.copy2` is `copyfile` followed by `copystat`; on filesystems that disallow `chmod` for the running uid, `copystat` raises `PermissionError(EPERM)` AFTER `copyfile` has already written the destination. Pre-fix code re-raised the EPERM, masking the data-side success. **Fix**: new `cps/services/file_move.py::copy_with_metadata_fallback` helper tries `shutil.copy2` first (preserves mode + atime/mtime + xattr); on `OSError`, retries with `shutil.copyfile` (data only). Destination gets default umask permissions — acceptable for the upload path because the file is about to be re-processed by the Calibre ingest pipeline. Three call sites in `cps/helper.py` route through the helper: `rename_all_files_on_change` + 2 in `move_files_on_change` (upload path + multi-file merge loop). **13 RED→GREEN tests** in `tests/unit/test_file_move_metadata_fallback.py` (10) + `tests/unit/test_move_files_chmod_eperm.py` (3) — helper happy path, EPERM fallback, EACCES same, ENOSPC propagates, real-data round-trip, source-pins for the 3 call sites + import + no remaining direct `shutil.copy2`; integration through `move_files_on_change` (EPERM tolerated, `localbook.path` updated on success, genuine copy failure surfaces). 189/190 helper-adjacent tests pass. Credit @rschiefer for the original report. | `eab384c0` | v4.0.106 |
| #273 | (fork issue [#222](https://github.com/new-usemame/Calibre-Web-NextGen/issues/222) verification feedback, reporter @droM4X) | **Orphaned "Theme switching is temporarily disabled until v5.0.0" flash banner no longer fires on first page load.** v4.0.91 removed the Switch Theme icon from the top bar but the once-per-day banner kept firing — with the icon gone, the banner became orphaned context. @droM4X's exact quote: "This notification is no longer needed." **Fix**: `cps/render_template.py::theme_migration_notification()` becomes a no-op. Function shape (zero-arg, returns None) is preserved so the call site in `render_title_template` doesn't change and we can re-purpose for a future migration banner without rewiring. The underlying theme-migration DB shim in `cps.ub` (forces users from theme=0 to theme=1) still runs — that's the actual state change; this was only the user-facing notice. **4 source-pin + behavioral tests** in `tests/unit/test_theme_migration_notification_suppressed.py` pin (a) no `flash()` in body, (b) fork-#222 anchor comment present, (c) zero-arg signature preserved, (d) mocked-flash count == 0. **Playwright pilot on cwn-local**: deleted `/app/theme_migration_notice` to force a fresh-start condition, restarted, hit `/`, accessibility snapshot showed 0 theme-related alerts (vs the banner that fired pre-fix). Also fixed `scripts/auto-close-awaiting-verification.sh` anchor: uses `min(SELF_LAST, LABELED_AT)` so reporter replies that landed BETWEEN ship-out and a later autopilot re-label are caught (this missed @droM4X's engagement on #222 until I noticed manually). | `1e8a3abe` | v4.0.105 |
| #272 | [CWA PR #1330](https://github.com/crocodilestick/Calibre-Web-Automated/pull/1330) | @I-Would-Like-To-Report-A-Bug-Please | **Metadata-search modal no longer crashes silently when a provider returns null/missing fields.** Originally reported against lubimyczytac.pl where some books legitimately have no tag list (returned as `null`), but the same shape mismatch existed in the Amazon providers (`authors = ""` instead of `[]`) and in the JS that consumed the responses. The user saw "no results" in the modal even though the backend returned valid records — the JS exploded on `book.tags.length` / `book.authors.join(...)` / `Object.keys(book.identifiers)`. **Provider contract fixes** (3 files): `amazon.py` + `amazonjp.py` constructors + except-fallbacks use `authors = []`; `lubimyczytac.py::_parse_tags` returns `[]` on empty (was `None`). **Frontend null-guards** (2 files): `get_meta.js` wraps `$.each(book[attribute_name] \|\| [], ...)` + `Object.keys(book.identifiers \|\| {})`; `book_edit.html` template guards `(book.authors \|\| []).join(...)`, `book.tags && book.tags.length !== 0`, `book.identifiers && Object.keys(book.identifiers).length !== 0`. **8 RED→GREEN source-pin tests** in `tests/unit/test_cwa_1330_metadata_modal_defensive.py` cover all 5 file changes. 65/65 metadata-adjacent tests still pass. Credit @I-Would-Like-To-Report-A-Bug-Please. | `1ace93dc` | v4.0.104 |
| #271 | (CWA issue [#1339](https://github.com/crocodilestick/Calibre-Web-Automated/issues/1339), reporter @Altycoder, +1 @ragaskar) | **Home grid no longer shows wrong covers after a library wipe + re-ingest.** `ub.Thumbnail` rows are keyed by `entity_id = book.id` (no UUID, no content hash). When a library is wiped and a different one is ingested, old Thumbnail records + their on-disk files survive and serve for the re-used `book.id` values — `get_book_cover_internal` only checked "is the file present on disk?", never compared `book.last_modified` vs `thumbnail.generated_at`. The book detail page used the `og` resolution (no Thumbnail rows → falls through to on-disk `cover.jpg` → correct cover) while the home grid used `sm/md/lg` via `get_cover_srcset` → hit the stale rows → wrong cover. **Fix**: in `get_book_cover_internal`, treat thumbnails whose `generated_at` predates the book's `last_modified` as missing. The existing background-regenerate branch fires AND the request falls through to on-disk `cover.jpg` — user sees the right cover immediately, a fresh thumbnail is written for subsequent loads. Inline `_stale` helper is defensive: returns False on missing data, normalizes tz-aware/tz-naive datetime mismatch. **6 RED→GREEN tests** in `tests/unit/test_cover_thumbnail_staleness_1339.py` source-pin the comparison + exercise the helper via `exec`. **Live container repro on cwn-local**: swapped book_2's thumbnail file with book_3's content + aged `generated_at` to 2025-01-01 → pre-fix `/cover/2/sm` → `image/webp` md5=`dd3b2c84` (wrong book) → post-fix call 1 → `image/jpeg` md5=`a019ed9d` (matches on-disk Dorian Gray cover) → call 2 after 5s background regen → `image/webp` md5=`8699aec7` (regenerated correctly). 264 cover/thumbnail-adjacent tests pass in isolation. Also addresses @Altycoder's gripe that "Scheduled Thumbnail Refresh" doesn't fix it — even if the scheduled task is killed mid-batch, the read path now self-heals stale records as users browse. | `42478883` | v4.0.103 |
| #270 | (fork issue [#256](https://github.com/new-usemame/Calibre-Web-NextGen/issues/256) verification feedback, reporter @droM4X) | **Bottom paginator chevrons now vertically align with the numbered buttons + ≥20px bottom margin.** After v4.0.94 shipped, @droM4X noted in his verification reply that the chevron prev/next items rendered visually above the boxed numerals (default `vertical-align: baseline` on inline-block items aligns text baselines — shorter-content chevrons float to the top of the row) and the bottom margin was too tight. **Fix**: `vertical-align: middle` on every `.page-item` inside `.pagination.pagination-bottom`, plus explicit `margin-bottom: 24px` (caliBlur's root font-size is ~10px so `1.5rem` resolves to only 15px — the rem unit would silently fall below droM4X's 20px request). **2 new regression tests** pin both invariants. **Playwright pilot on cwn-local** with `config_books_per_page=4`: all 6 page-items (prev chevron, 4 numbered buttons, next chevron) share `center=y616` to the pixel; computed `margin-bottom: 24px`; top paginator unchanged (`position: fixed; top: 58.1px; right: 0`). | `591e4658` | v4.0.102 |
| #253 | **New app passwords now display inline on the profile page** instead of in a 10-second bottom toast (closes fork issue [#223](https://github.com/new-usemame/Calibre-Web-NextGen/issues/223), reporter @droM4X). The cleartext appears in a green-highlighted box at the top of the App passwords section on `/me`, persistent across reloads of the profile page, cleared when the user navigates to any other top-level page. `app_password_create` writes `{label, token}` to `session['pending_app_password']` instead of `flash(...token...)`; the profile route passes it to `user_edit.html`; a `before_request` hook in `cps/__init__.py` pops the key on real navigations gated on `Sec-Fetch-Dest: document` (sub-resources like CSS / JS / XHR / OPDS / Kobo sync don't accidentally clear it mid-render). Cleartext is never persisted — only the werkzeug hash. **7 source-pin tests** in `tests/unit/test_app_password_inline_display.py`. **Playwright pilot end-to-end** on cwn-local: create → POST → /me renders the inline box; reload /me preserves it; navigate to /admin/view then back to /me clears it. Edge case caught during implementation: the initial endpoint-only filter cleared on every CSS/JS sub-resource (`endpoint=static`); the fix uses `Sec-Fetch-Dest` to scope clearing to real navigations only. | `b7d817d8` | v4.0.90 |
| #230 | (operator-flagged 2026-05-17 borrow-day session; A2 in `notes/KOBO-A1-A2-B8-PROMPT.md`) | **Admin per-book "Resend to Kobo" action** on admin → Edit User, alongside the existing *Force Full Kobo Sync* button. Admin enters a book ID, clicks **Resend**, and on the user's next sync the device receives that one book as `NewEntitlement` (forcing a fresh download). Avoids the nuclear "re-sync everything" path when a single book gets corrupted on device, has a stale cover, or needs to pick up a post-sync format conversion. **Mechanism, twofold:** (1) delete `(user_id, book_id)` from `kobo_synced_books` so the next sync emits `NewEntitlement` rather than `ChangedEntitlement`; (2) bump `Books.last_modified = datetime.now(timezone.utc)` so the sync filter `Books.last_modified > sync_token.books_last_modified` picks the book up regardless of where the device's cursor sits. Either step alone is a partial fix — deletion alone leaves a book stuck behind an advanced cursor; the timestamp bump alone produces a `ChangedEntitlement`, not a re-delivery. **Endpoint** `POST /ajax/kobo_resend/<int:userid>/<int:bookid>` — `@user_login_required` + `@admin_required`, integer-coerced at the routing layer (`<int:...>` rejects non-numeric paths at routing), CSRF-protected via the global `ajaxSetup` `beforeSend` hook. **UI** block on `user_edit.html` under the existing Kobo Sync Token section: book-ID number input + Resend button + inline feedback span; hidden behind `{% if current_user.role_admin() %}` so non-admin viewers never see it; show/hide in lockstep with the Force Full Kobo Sync button (token-deletion / token-creation modal hooks updated). Inline feedback shows success in green, errors in red (invalid book ID, empty/zero input, network failure). **8 regression tests** in `tests/unit/test_kobo_admin_resend_book.py` covering route shape, full decorator stack, both writes, both commits, integer-only path converters, and security shape. **Live container verification**: HTTP POST clears the (3,2) row and bumps last_modified; the next `GET /v1/library/sync` returns `NewEntitlement` (not `ChangedEntitlement`) for the targeted book. Playwright UI pilot covered the happy path, invalid book ID (red feedback), empty input (client-side validation), and zero input — all four behave correctly. Adjacency: token-deletion correctly hides the resend block alongside the existing fullsync button. | `68e8880a` | v4.0.73 |
| #287 | (fork issue [#237](https://github.com/new-usemame/Calibre-Web-NextGen/issues/237), reporter @new-usemame) | **Drag-to-reorder regular shelves in the sidebar.** Magic Shelves already supported user-ordered display via the existing `sort_magic_shelves_for_user` work; regular shelves were locked to alphabetical (`render_template.py:141` `order_by(ub.Shelf.name)`). This closes the gap using the same `view_settings` storage shape. **New** helpers `normalize_shelf_order(order_list, available_ids)` (drops unknown/stale IDs, de-duplicates first-seen-wins, coerces string IDs to int, appends available IDs not yet in the list) + `sort_shelves_for_user(shelves, user)` (applies `user.view_settings['shelves']['order']` when set, else casefold alphabetical; None-safe against `Anonymous` whose `view_settings` is `None` per `cps/ub.py:345`) in `cps/shelf.py`. New endpoint `GET/POST /shelf/reorder` (`@user_login_required`) — GET renders `shelf_reorder.html` with Sortable.min.js drag-list of all accessible shelves (own + public); POST accepts JSON `{order:[id,...]}`, normalizes against the accessible-shelf set (security: stale or other-user IDs dropped), persists to `view_settings['shelves']['order']` with `flag_modified` on the JSON column. Sidebar (`layout.html`) surfaces a `Reorder Shelves` link below `Create Shelf` for authenticated users with >1 shelf. **11 source-pin tests** in `tests/unit/test_237_shelf_reorder.py` covering normalize behavior (8 input cases), `sort_shelves_for_user` shape, route registration + login gate, `render_template` integration, sidebar link, template Sortable hookup. **5 new i18n strings** extracted via `scripts/update_translations.sh`; Hungarian (reporter's locale) + German translations written into the .po files; all 28 .mo files recompile cleanly per `tests/unit/test_translations_compile.py`. **Live end-to-end on cwn-local with Playwright + JS evaluate**: created 3 shelves → alphabetical render → POST `{order:[2,4,1,3]}` returned 200 with `success:true` → reload `/shelf/reorder` shows `[Zebra, Middle, KOBO, Alpha]` → sidebar on `/` shows the same order (cross-page consistency). Hungarian UI confirmed: title "Polcok átrendezése", save button "Mentés", sidebar link translated. | `1c5c160e` | v4.0.113 |
| #289 | (fork issue [#237](https://github.com/new-usemame/Calibre-Web-NextGen/issues/237) follow-up, reporter @new-usemame) | **Sort-mode picker on the Shelf reorder page** — full parity with magic-shelf order modes. v4.0.113 shipped manual drag-to-reorder; this adds 8 named modes alongside manual: `name_asc` (default), `name_desc`, `book_count_desc`, `book_count_asc`, `created_desc`, `created_asc`, `modified_desc`, `modified_asc`. New `SHELF_ORDER_MODES` set + `DEFAULT_SHELF_ORDER_MODE = 'name_asc'` in `cps/shelf.py`. `sort_shelves_for_user` dispatches on the configured mode; tie-break across all modes uses casefold name for deterministic equal-key sorting. Switching back to manual restores the user's previously-stored drag order (the order list is kept in view_settings even when a named mode is active). New `_shelf_book_count(shelf)` helper tolerant of detached relationships (returns 0 on failure). POST `/shelf/reorder` reads `{order_mode, order}` JSON; validates order_mode against the set (400 on unknown); back-compat: payload without `order_mode` defaults to `manual` (matches v4.0.113 clients). `shelf_reorder.html` gains an `<select id=orderModeSelect>` with all 9 i18n-wrapped labels and a change-handler that toggles `display:none` on the manual drag-list. **6 new source-pin tests** added to `tests/unit/test_237_shelf_reorder.py` (17 total) covering SHELF_ORDER_MODES set membership + default validity, dispatch on each named mode, `_shelf_book_count` helper existence, POST handler validation + persistence of order_mode, template picker + POST field. **11 new i18n strings** with Hungarian (reporter's locale) + German translations; all 28 .mo files recompile cleanly per `tests/unit/test_translations_compile.py`. **Live verified on cwn-local with Playwright + JS evaluate** across 4 shelves with varied created / last_modified timestamps: `created_desc` → `[Zeta(5/20), Charlie(5/18), Bravo(5/16), KOBO(Jan)]`; `modified_desc` → `[Charlie(30min), Bravo(1h), Zeta(2h), KOBO]`; `book_count_desc` → `[KOBO(1), zero-count shelves with name tiebreak]`; `name_desc` → `[Z, K, C, B]`. Hungarian picker labels confirmed end-to-end (e.g. `Név (A → Z)`, `Könyvek száma (legtöbb → legkevesebb)`, `Létrehozva (legújabb → legrégebbi)`). | `ceaf6cf6` | v4.0.114 |
| #290 | (fork issue [#276](https://github.com/new-usemame/Calibre-Web-NextGen/issues/276), reporter @magdalar) | **Admin checkboxes in the Send-to-eReader modal for sending to other users' eReaders.** Reporter manages eReaders for their family and copy-pastes addresses from /admin/view → user-edit each time they want to send a book to a different device. The modal already supported sending to multiple addresses from `self.kindle_mail` (comma-separated); this adds an admin-only section listing OTHER users with a configured `kindle_mail` as checkboxes. **Privacy gate**: `current_user.role_admin()` guards the build in `show_book` (non-admin sessions get an empty `other_users_with_kindle` → template `{% if %}` skips the entire fieldset, no header rendered). **Backend defense-in-depth**: `send_to_selected_ereaders` POST handler extends the allow-set used to validate `selected_emails` with other users' `kindle_mail` only when `role_admin()` is true; non-admins continue to be gated against `self.kindle_mail` + `allow_additional_ereader_emails` flag. The other-users list filters (a) current_user themselves (avoids duplicate display) and (b) empty `kindle_mail`. **6 source-pin tests** in `tests/unit/test_276_send_to_other_users.py`. **1 new i18n string** ('Other users' eReaders:') with Hungarian + German translations; all 28 .mo files recompile cleanly per `tests/unit/test_translations_compile.py`. **Live end-to-end verified on cwn-local with Playwright + JS evaluate**: admin session at /book/5 → modal shows 3 fieldsets ('Choose email addresses:' with admin@me.com checked, 'Other users' eReaders:' with cwng84test + family-member as unchecked rows, 'Additional email addresses:' textarea); non-admin (cwng84test with `allow_additional_ereader_emails=1`) at the same URL → modal shows only the self section + additional, `innerHTML.includes('other-user-email-')` = false confirming no other-users surface leaks to non-admins. Hungarian UI confirmed (`Választ e-mail címeket:`, `Más felhasználók e-olvasói:`, `További e-mail címek:`). V2-track from magdalar's wishlist (per-user tracking of which user(s) received which book) deferred. | `4b081e1c` | v4.0.115 |
| #292 | (fork issue [#225](https://github.com/new-usemame/Calibre-Web-NextGen/issues/225), reporter @froggybottomboys) | **Server-wide announcement banner.** Reporter runs CWNG for ~10 friends in a book club and asked for a way to contact users — admin email broadcast or post-login banner. Operator pushed back on email (privacy, hard to self-host); the dismissible banner satisfies both the user request and the privacy concern. **New column** `config_server_announcement = Column(String, default='')` on `settings`. **Migration** in `migrate_config_table` adds the column to legacy DBs via the existing `SELECT/ALTER` idempotent pattern. **Admin form**: `/admin/config` (Basic Configuration) gains a 500-char textarea labeled `Server announcement banner` with help text explaining the dismiss model. **render_title_template** populates a dedicated `server_announcement` kwarg from `config.config_server_announcement` (with a `getattr` None-safe fallback) — Flask's template global `config` is `app.config` not `cps.config`, so the dedicated kwarg avoids the confusion. **layout.html** renders `<div id='serverAnnouncementBanner' class='alert alert-info'>` above main content when the kwarg is truthy, with a close button. **Per-user dismiss is client-side via localStorage** keyed by a 32-bit hash of the announcement content. Updating the announcement (different hash) re-shows the banner for every user. No server-side dismiss state, no per-user table — simple + privacy-respecting. **7 source-pin tests** in `tests/unit/test_225_server_announcement.py` pin Column declaration, ALTER TABLE migration + orchestrator wiring, admin form `_config_string` call, template textarea presence, layout `{% if server_announcement %}` guard + banner id, render_template kwarg pass, localStorage key prefix. **4 new i18n strings** (`Server announcement banner` + help text + placeholder; `Dismiss` already existed in duplicates context) with Hungarian (reporter's locale) + German translations; all 28 .mo files recompile cleanly per `tests/unit/test_translations_compile.py`. **Live end-to-end verified on cwn-local with Playwright**: set announcement via SQL → restart → banner visible at top of `/` with exact text + close button; click dismiss → banner `display:none` + `localStorage.cwng_announcement_dismissed_<hash>=1` set; reload → dismiss persists; edit announcement via SQL → restart → banner re-appears with new text (different hash → different localStorage key, so old dismiss doesn't apply); `/admin/config` textarea pre-filled with current value + label + help text translated to Hungarian when locale is hu. **V2-track from issue thread**: push notifications (operator's preferred channel — VAPID + ServiceWorker + per-device subscription store is substantially bigger); multi-announcement / timed expiry / severity (info/warning/error); markdown formatting. | `154bf819` | v4.0.118 |
| #280 | (fork issue [#218](https://github.com/new-usemame/Calibre-Web-NextGen/issues/218), reporter @yodatak) | **Reload metadata from on-disk file.** New endpoint `POST /admin/book/<int:book_id>/reload_metadata` (auth: `@login_required_if_no_ano` + `@edit_required`) re-reads embedded metadata from the book's on-disk EPUB/KEPUB and updates the Calibre catalog without going through full re-ingest. Use case: user edits an EPUB's OPF / description / publisher / pubdate / languages via an external tool (Calibre desktop, grimmory, an OPF rewriter), wants CWNG to pick up the new fields. Updates title, comments, publisher, pubdate, languages via the existing `edit_book_*` helpers (so relationship-table bookkeeping stays consistent with the normal save path). **Authors / tags / series are deferred** — they involve more relationship-table bookkeeping (author_sort regeneration, tag dedup, series create-on-demand) better handled through the full edit-book save path. All writes wrapped in `metadata_db_write_lock()` so the endpoint is safe under concurrent admin calls. Returns JSON `{success, updated_fields, source_format, message}`. 4xx on missing book / missing file / no formats; 5xx on parse / commit failures with the underlying error surfaced. **9 source-pin tests** in `tests/unit/test_reload_metadata_from_disk_218.py` pin route registration, decorator stack, 404-on-missing-book, get_epub_info delegation, edit_book_* helper use, metadata_db_write_lock coordination, updated_fields response shape, deferred-fields invariant. **Live verified on cwn-local**: modified an EPUB's OPF metadata on disk (injected new `<dc:publisher>` + `<dc:description>`); POST returned 200 with `{success: true, updated_fields: [comments, publisher], source_format: EPUB}`; the new fields landed in `metadata.db` immediately. UI button on `book_edit.html` is a follow-up — the endpoint is operator-callable today via curl or any scripted client. | `318267f4` | v4.0.110 |
| #340 | **A new cover now shows up everywhere immediately — web UI and Kobo.** Applying a cover via the focused cover-picker (`POST /book/<id>/cover/apply`) saved `has_cover` and busted the picker's own `?ts=` preview but never bumped `book.last_modified`. Every cover URL in the app cache-busts on `c=last_modified` (the `jinjia` filter feeding detail page, home-grid srcset, edit page, and og:image), so an unchanged timestamp meant the browser kept serving the cached cover until a manual refresh; Kobo native sync selects changed books with `Books.last_modified > sync_token.books_last_modified`, so the device never re-pulled the new cover. **Root cause confirmed on teenyverse**: *Carmilla*'s `cover.jpg` was rewritten 2026-05-28 17:06Z while its `last_modified` was stuck at 2026-05-27 03:01Z. **Fix** mirrors the edit-book path in `_apply_response` — bump `last_modified`, `set_metadata_dirty`, commit, then `remove_synced_book(all=True)`; the `CoverImageId` already embeds the cover mtime, so devices re-fetch once the book is re-selected. The post-commit best-effort steps were split from the critical DB write so a commit failure now returns 500 instead of a false `ok:True`. **Discoverability**: the cover image itself is now the entry to the picker on the book detail page — an always-visible corner camera badge (discoverable on touch) plus a hover/focus "Change cover" overlay; the edit-page button was relabeled from "Open focused cover picker" to "Change cover" (no overlay on the edit page, to avoid discarding unsaved form edits on an accidental click). Shared overlay CSS in `cwa.css` scoped to a new `.cover-editable` class; reuses the existing "Change cover" message id (no new translation strings). **4 regression tests** pin the three sync/cache invariants (last_modified bump, `remove_synced_book(all=True)`, `set_metadata_dirty`) for both the success and failure paths plus a source-pin refactor guard. **Live-verified on cwn-local**: real authenticated multipart upload → `last_modified` bumped, seeded `kobo_synced_books` row cleared, `metadata_dirtied` set, detail page served the new cover (new `c=` param, new image bytes), `CoverImageId` recomputed to `<uuid>-<mtime>`; both entry points exercised via Playwright with 0 console errors. Greptile reviewed (4/5); both P2 findings addressed. | `150e83582` | v4.0.141 |
| #349 | **Edit highlights in the web reader + a KOReader↔server annotation bridge (Phase 1 + 2 of the Kobo highlight feature).** Phase 1: select text in the epub reader → popup (4 colour swatches + optional note) → Save creates a `source='webreader'` highlight that paints as an overlay and persists; click a highlight to recolor / edit the note / soft-delete. Routes on the `annotations` blueprint, UI in `annotations.js`, Hardcover fan-out when that sync target is enabled. Phase 2: `GET/PUT /kosync/syncs/annotations` on the `kosync` blueprint (reuses its Basic auth + checksum→book resolution, no new credentials), a `device_origin_id` column + idempotent migration to suppress feedback loops, a portable-annotation projection as the wire shape, and a `cwasync.koplugin` KoboReader.sqlite provider that writes highlights into a Kobo so the stock reader renders them. The plugin's device-write toggle ships **default-off** until the on-device round-trip (`notes/feat-annotation-koreader-bridge-device-verification.md` Tests 1–4) passes — the SQL→Nickel render was already proven on real hardware (2026-05-26); only the plugin's runtime sqlite-FFI/wifi glue is ungated. Phase 3 (server→stock-Nickel push) excluded — disproven on hardware. **i18n**: the popup strings move from hardcoded English to the reader's `window.calibre.annotationsI18n` bridge (each `_()`-wrapped); Save/Cancel/Delete + the empty-state reuse existing msgids, verified live under a Hungarian session (`Mentés`/`Mégsem`/`Törlés`). **Verified**: 38 unit + a provider schema-fit guard replaying the exact INSERT/SELECT against the real `Bookmark` schema (and a real 1,940-bookmark device-DB copy) + busted Lua + live HTTP CRUD and pull/push (503/401/200) + full Playwright create→paint→reload→edit→delete. Also de-stales two plugin tests already red on `main` (plugin version `4.0.54`→`4.0.136`; CWA→NextGen prompt rebrand). | `(pending squash)` | `(pending release)` |

| #1154 | (fork issue [#1153](https://github.com/new-usemame/Calibre-Web-NextGen/issues/1153), self-reported) | **A book's page no longer 500s when the library's custom-column definitions can't be read.** `_detail_custom_columns()` in `cps/api/books.py` documents itself as "degrading safely if DB metadata is unavailable" but caught only `AttributeError`/`KeyError`/`TypeError`. The error an actually-unreadable calibre schema raises is a SQLAlchemy `OperationalError` (`no such table: custom_columns`) — not in that list — so it escaped and took the whole detail payload down over a supplementary field. Reachable whenever the metadata DB opens but its schema isn't there: library mid-write, moved/renamed library path, `custom_columns` mid-migration, partially-restored library. **Pre-existing in production**; it was masked in tests only because `calibre_db.session` could be `None` and `None.query(...)` raises `AttributeError`, so the *no-session* case was caught while the *unreadable-schema* case was never exercised. #1149 made `session` a property that always materialises, which removed that accidental cushion and turned the latent gap into two red tests on `main` (`test_587_koreader_progress`, plus the same class in `test_kobo_bookmark_created_at` / `test_api_v1_books` under `--dist=loadfile` worker grouping). **Fix**: degrade inside `CalibreDB.get_cc_columns` itself (catch `SQLAlchemyError`/`AttributeError` → log + return `[]`), not at one callsite. Driving the real routes on `cwn-local` showed the API-only fix was incomplete: `/book/<id>` (`cps/web.py` `show_book`) still 500'd, because four of the five callers — book detail page, books table, and both search surfaces — had no guard at all. All five want "the custom columns, if any" and none can act on a DB error, so one source of truth beats five copies of the same try/except. `_detail_custom_columns` keeps its own guard as defense-in-depth. **4 regression tests** in `tests/unit/test_book_detail_custom_columns_degrade.py` — the contract pin (unreadable schema degrades to `[]`) is red without the fix and green with it; two supporting pins assert the fixture reproduces the real raise and that `session` materialises rather than returning `None`, so the contract test can't pass vacuously. Fixture saves/restores the class-level `session_factory` — the bug class here is cross-test pollution. | `238a12e5b` | v4.1.22 |

## Backports from janeczku/calibre-web (2026-04 wave)

CWA detached from the original Calibre-Web upstream (`janeczku/calibre-web`) at some point before this fork started. The original upstream is alive and pushing fixes — most notably a security wave in April 2026 by @jvoisin and @haraldpdl that CWA never picked up. We sync these fixes directly into our `main` so users on `ghcr.io/new-usemame/calibre-web-nextgen` get them without waiting for upstream's next release tag (releases on calibre-web tend to be 5–6 months apart).

### Security

| Fork commit | Janeczku commit | Author | Description |
|---|---|---|---|
| `a387c5c7` | [`4bb553f5`](https://github.com/janeczku/calibre-web/commit/4bb553f5) | @jvoisin | XXE: lxml parsers in `cps/epub.py`, `cps/epub_helper.py`, `cps/fb2.py` use `XMLParser(resolve_entities=False, no_network=True)` |
| `0ec5f440` | [`19da54a7`](https://github.com/janeczku/calibre-web/commit/19da54a7) | @jvoisin | LDAP injection: escape RFC 4515 metacharacters in `bind_user` before passing into LDAP filter |
| `0cb70e61` | [`e21f9437`](https://github.com/janeczku/calibre-web/commit/e21f9437) | @jvoisin | OAuth relink: reject the bind when an OAuth identity is already attached to a different authenticated user |
| `4248fe7a` | [`a6c55dea`](https://github.com/janeczku/calibre-web/commit/a6c55dea) | @jvoisin | `/show/<book_id>` access bypass: switch from `get_book` to `get_filtered_book` so per-user content-hiding rules apply |
| `a04435f1` | [`0615637f`](https://github.com/janeczku/calibre-web/commit/0615637f) | @jvoisin | `debug_info`: exclude any config key containing `token` or `secret` from the admin debug dump |
| `01649f8e` | [`4c1d7a9f`](https://github.com/janeczku/calibre-web/commit/4c1d7a9f) | @jvoisin | Shelf reorder POST now requires edit permission, not just view permission |
| `b716b0bd` | [`94899056`](https://github.com/janeczku/calibre-web/commit/94899056) | upstream | `chmod 0600` on freshly generated `.key` Fernet key file |
| `119e591c` | [`fd744af7`](https://github.com/janeczku/calibre-web/commit/fd744af7) | @jvoisin | 500 page: log the full traceback server-side, but only render it to authenticated admins |
| `e2b569ef` | [`c784149d`](https://github.com/janeczku/calibre-web/commit/c784149d) (upstream PR [#3624](https://github.com/janeczku/calibre-web/pull/3624)) | @jvoisin | `serve_book` defense-in-depth: set `Content-Disposition: inline`, `X-Content-Type-Options: nosniff`, and `Content-Security-Policy: script-src 'none'; object-src 'none'` on the PDF/generic response built from `send_from_directory`, so the browser cannot MIME-sniff book content into an executable type or run inline scripts when rendering inline. |
| `d3530149` | [`c23d35db`](https://github.com/janeczku/calibre-web/commit/c23d35db) (upstream PR [#3623](https://github.com/janeczku/calibre-web/pull/3623)) | @jvoisin | Remote-login magic-link token entropy bumped from 32 bits (4 bytes / 8 hex chars) to 128 bits (16 bytes / 32 hex chars) in `cps/ub.py:RemoteAuthToken.__init__`. `/ajax/verify_token` has no rate limiting and tokens are valid for 10 minutes, so the 32-bit space was brute-forceable. Now matches the Kobo permanent-device token (`cps/kobo_auth.py:99`) which already used `urandom(16)` on the same column. Pinned by `tests/unit/test_remote_auth_token_entropy.py` (AST source-pin on `urandom(16)` + behavioral 32-hex-char + uniqueness sanity). |

`fixes/fix_kobo` (Kobo IDOR on `generate_auth_token` / `deleteauthtoken`) was independently fixed in our PR #18 (`9f50bb2`); upstream and fork landed on identical guards.

### Stability and hygiene

| Fork commit | Janeczku commit | Author | Description |
|---|---|---|---|
| `cdaa94d2` | [`ab17992e`](https://github.com/janeczku/calibre-web/commit/ab17992e) | @haraldpdl | `Books.atom_timestamp` returns `last_modified` (with fallback to `timestamp`) so OPDS sync clients see post-import metadata and cover changes |
| `5187a274` | [`674b47bd`](https://github.com/janeczku/calibre-web/commit/674b47bd) | @haraldpdl | `do_download_file`: clean up `/tmp/calibre_web` staged copies via `after_this_request` hook so embed-metadata downloads don't fill the disk |
| `5bacfcc0` | [`e0de5bd6`](https://github.com/janeczku/calibre-web/commit/e0de5bd6) (upstream PR [#3555](https://github.com/janeczku/calibre-web/pull/3555)) | @lpinner | `render_discover_books` now hides books the user has already marked as read. Two backends: when `config.config_read_column` is unset, filter on `coalesce(ub.ReadBook.read_status, 0) != STATUS_FINISHED` (per-user state in app.db); when a custom column is configured, filter on `coalesce(db.cc_classes[col].value, False) != True` (Calibre custom-column). Invalid column ID logs + flashes an i18n error and falls back to no filter so Discover still renders. Resolves janeczku [#2588](https://github.com/janeczku/calibre-web/issues/2588) (filed 2021-12, multiple "me too" commenters who said Discover stopped being useful once their library exceeded a few hundred books — the page kept surfacing books they'd already read). |
| `b7eebe1d` | [`570371cc`](https://github.com/janeczku/calibre-web/commit/570371cc) | @haraldpdl | `do_calibre_export`: add `--dont-save-cover` so we don't write an unused `<uuid>.jpg` next to every download |
| `1dedd55b` | [`9c87f0fb`](https://github.com/janeczku/calibre-web/commit/9c87f0fb) | upstream | `gdrive.py`: call `.hexdigest()` on the md5 hash before comparing to the gdrive checksum string |
| `db677b1c` (fork PR [#265](https://github.com/new-usemame/Calibre-Web-NextGen/pull/265), v4.0.98) | upstream PR [#3504](https://github.com/janeczku/calibre-web/pull/3504) | @lb803 | `cps/comic.py`: natural-sort archive entries before scanning for a cover image. CBZ/CBT/CBR/CB7 iterated `cf.namelist()` / `cf.getnames()` in archive-library order — for many implementations that's insertion order, so the "cover" picked was whichever entry came first, often an interior page rather than page1. `from natsort import natsorted as sort` (with a `sorted` stdlib fallback) + wrap all four iteration sites in `sort(...)`. Multi-digit page filenames (page2 vs page10) now compare in natural order rather than lexicographically. Adjacency fix: hoisted `import zipfile` + `import tarfile` to module top (they were defined only inside the comicapi-ImportError branch — latent NameError if `_extract_cover_from_archive` was ever called with comicapi present). 3 behavioural + source-pin tests in `tests/unit/test_comic_natsort.py` including a synthetic CBZ where `page10/page2/page1` are inserted out-of-order — natsort correctly returns `page1.png`, bare `sorted` would still pick `page10.png` so the test catches a regression to lexicographic sort. |
| `d088909` (fork PR [#268](https://github.com/new-usemame/Calibre-Web-NextGen/pull/268), v4.0.100) | upstream PR [#3369](https://github.com/janeczku/calibre-web/pull/3369) | @chtzvt | `cps/reverseproxy.py`: env-var fallbacks for opaque reverse proxies. Deployments behind Tailscale Funnel / Cloudflare Tunnel / other opaque TLS terminators couldn't inject the `X-Script-Name` / `X-Scheme` / `X-Forwarded-Host` headers the middleware needed, so cookie + asset URLs broke. Four optional env vars now provide the same configuration: `PROXY_SCRIPT_NAME`, `PROXY_SCHEME`, `PROXY_HOST`, `PROXY_PORT`. Headers always win when both are set. `PROXY_PORT` only appends when the host value doesn't already carry a port (so `X-Forwarded-Host: example.com:1234` + `PROXY_PORT=8443` doesn't double-up). Also adds `X-Forwarded-Proto` (de-facto standard) as a recognized scheme source alongside the historic `X-Scheme`. 8 RED→GREEN tests in `tests/unit/test_reverseproxy_env_overrides.py` cover all configuration combinations + header-overrides-env precedence + port-already-present case + no-config preserves `is_proxied=False` so direct-access callers (Kobo download URL builder) still detect direct-access mode. |
| `44a32bf` (fork PR [#267](https://github.com/new-usemame/Calibre-Web-NextGen/pull/267), v4.0.99) | upstream PR [#3358](https://github.com/janeczku/calibre-web/pull/3358) | @dotknott | `cps/kobo.py`: surface Subtitle from a Calibre custom column labeled "subtitle" in the Kobo sync metadata block. Calibre libraries commonly store per-book subtitles in a custom column (especially non-fiction, academic, series); the Kobo sync protocol has a `Subtitle` key the device renders below the title but neither stock CW nor this fork was populating it. New `get_subtitle(book)` helper looks up `db.CustomColumns` for `label == 'subtitle'`, reads the value off `book.custom_column_N`, returns the string or `""`. `get_metadata` includes `"Subtitle": get_subtitle(book)` so every sync entry carries the key (some Kobo firmware rejects entries that omit it). Rewrote from the upstream patch — which had three null-handling bugs (`.all()[0]` IndexError on missing column, unreachable else branch, TypeError on None attribute) — with explicit empty-result handling at every fork: column missing, no value, missing attribute, None cell value all return `""`. 8 RED→GREEN tests in `tests/unit/test_kobo_subtitle_custom_column.py`. 202 adjacent kobo-suite tests green. |
| [`1c48531`](https://github.com/new-usemame/Calibre-Web-NextGen/commit/1c485313a9fae160807ae55c25439b232af0ab66) (fork PR [#762](https://github.com/new-usemame/Calibre-Web-NextGen/pull/762), TBD) | upstream PR [#3668](https://github.com/janeczku/calibre-web/pull/3668) | @AshayK003 | `cps/web.py:download_link` + `cps/opds.py:opds_download_link`: a download request that omits the `User-Agent` header 500'd. Both endpoints did `"Kobo" in request.headers.get('User-Agent')` — `.get` returns `None` when the header is absent, so `"Kobo" in None` raised `TypeError: argument of type 'NoneType' is not iterable` before any book logic ran. Any client that doesn't send a UA (curl/scripts, some OPDS readers, some e-readers) got a 500 instead of the file. Fixed on both diverged lines with the None-safe `.get('User-Agent', "")` default already used in `cps/editbooks.py` + `cps/render_template.py`. Reproduced live on cwn-local (`/opds/download` 500→200, no traceback); web path is character-identical + unit-pinned. 8 RED→GREEN tests in `tests/unit/test_download_user_agent_missing_3668.py` (evaluates both shipping expressions against a UA-less werkzeug `Headers`). |

The ComicVine/Douban None-iteration crash that janeczku fixed in `0ca6e861` does not occur in this fork — `cps/search_metadata.py:350` already coalesces provider results with `future.result() or []`, and `cps/metadata_provider/comicvine.py:50` already returns `[]` on HTTP error.

Regression coverage for the testable subset of these backports lives in `tests/unit/test_janeczku_backports.py` (21 tests covering XXE, LDAP escape, debug_info redaction, atom_timestamp priority, embed-helper argv, and key-file permissions).

## Container image

Published to `ghcr.io/new-usemame/calibre-web-nextgen` instead of upstream's `crocodilestick/calibre-web-automated`. Same data layout, same compose file shape — drop-in swap.

## Patch hygiene

Every entry in the "Backports" section is a clean cherry-pick of an upstream PR with the original author preserved as committer in the squash-merge message. Original-fork patches in the bottom section are landed as their own commits with focused titles. The squash-merge SHAs above are stable references on this fork's `main`.

For "Original fork patches": the diffs are small and isolated; PR descriptions in this fork link the line-by-line rationale. Upstream is welcome to take them.
