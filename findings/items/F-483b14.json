{
  "area": "kobo",
  "body": "OBSERVED after #2128 and #2131 merged: `_log_sync_observability()` printed the structured `suppressed_replay` value twice, once under its real name and once under `suppressed_unchanged`. No `suppressed_unchanged` counter existed. The tests reinforced the ambiguity by asserting equal labels even though the sync handler already tracked live held-book suppressions and removed-entitlement suppressions separately.\n\nOBSERVED resolution: the structured observability payload now carries both meanings explicitly. `suppressed_replay` counts every exact same-reader fingerprint replay kept off the wire, including removal tombstones. `suppressed_unchanged` counts only live held books whose unchanged fingerprint prevents re-emission; `suppressed_removed` remains the tombstone subset. Mixed regression coverage proves the distinction as total = live + removed, while private capture receives the same truthful counters as the INFO line.\n\nASSUMED: existing external log consumers treat these fields by label rather than fixed field count. The line keeps both established labels and order, so the correction changes only the formerly false second value.\n\nCORRECTION. This finding was first closed citing a production line reading `suppressed_replay=5 suppressed_unchanged=5 suppressed_removed=5` as proof the counters had become independent. That evidence was invalid: the instance it came from was still running a build from before this fix, whose formatter passed `suppressed_replay` into both the replay and the unchanged position, so two equal values are exactly what the defect prints. Three equal counters were the anomaly to explain, not the confirmation.\n\nOBSERVED on an instance actually running the fix: one sync summary reads `suppressed_replay=5 suppressed_unchanged=0 suppressed_removed=5`. `suppressed_unchanged` holds a different value from `suppressed_replay` in the same line, which the defect could not produce, and the three agree with the documented meaning \u2014 the replay total is the live unchanged count plus the removed-tombstone count, here 5 = 0 + 5. The line keeps its established field order. Source verified at the same time: the three format arguments are now three distinct lookups rather than the same one twice.",
  "created": "2026-09-02",
  "id": "F-483b14",
  "refs": [
    "#2128",
    "#2131"
  ],
  "severity": "correctness",
  "source": "audit",
  "status": "closed",
  "title": "Kobo INFO summary reports replay total twice under different labels"
}
