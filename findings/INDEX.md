# Findings

Internal audit findings from maintainer passes over the codebase. **This is not the
bug tracker** — if you hit a problem, please open an issue instead:
<https://github.com/new-usemame/Calibre-Web-NextGen/issues>.

These are things a maintenance pass noticed in passing and recorded so they are not
lost. They are unprioritised against user reports, and a user report always outranks
anything here. Generated by `scripts/findings.py index` — edit the JSON, not this file.

135 open, 2 closed.

## Open


### data-integrity

| id | area | finding |
|---|---|---|
| `F-1b2fdd` | ingest | Ingest can overwrite a good existing format when automerge is set to overwrite |
| `F-1fdb7c` | ingest | Ingest retry queue: at-least-once after a crash, and a non-atomic rewrite that can discard work |
| `F-7a1bf7` | ingest | Ingest conversion timeout is flat, not proportional to book size |
| `F-b521d3` | kobo | Kobo sync overwrites a further reading position with a device's older one (progress goes backwards) |
| `F-fe2b1c` | progress-sync | Lost update on the shared kosync row |

### correctness

| id | area | finding |
|---|---|---|
| `F-062a91` | spa | Reader save-retry: coalesced re-entry races the armed retry timer, and retries outlive the reader |
| `F-07fd40` | ingest | [bug] The whole app stops answering while a book is being ingested |
| `F-08f29f` | metadata | [bug] A metadata change log can sit unprocessed until an unrelated edit arrives |
| `F-0bd78b` | ingest | Books already in the ingest folder when the container starts are never imported |
| `F-0eed67` | kobo | Cover changes do not bust the browser cache outside the edit page |
| `F-151f59` | api | Read-status toggle routes still report success on a rolled-back write (#1318 contract gap) |
| `F-15488f` | spa | Reader save failures are invisible to sighted users; classic reader never retries in-session; SPA loses position on tab close |
| `F-194ce4` | metadata | Web-reader savepoint can roll back app.db while metadata.db stays committed |
| `F-1b7a81` | spa | [bug] SPA a11y gate is red on main: color-contrast (serious) on every light-theme route |
| `F-1f6174` | db | Duplicate visibility revalidation is recomputed per page render and per status poll |
| `F-205a8e` | kobo | Kobo: failed on-demand KEPUB conversion silently falls back to plain EPUB (book loses highlighting/annotations) |
| `F-22bb78` | kobo | CWA_COVER_PICKER_TIMEOUT is a dead knob — the cover-picker fan-out has no overall deadline |
| `F-2515ca` | koreader | Plugin publish is only partly resumable: a push that lands with no release still reports success |
| `F-2a34af` | core | X-Forwarded-Prefix reaches SCRIPT_NAME unvalidated: every consumer of request.script_root inherits it |
| `F-2be7fb` | spa | A perPage change after paging has advanced can query the old page number at the new size |
| `F-31a248` | ci | auto-revert workflow can't open PRs, so the red-main safety net produces nothing actionable |
| `F-3443ba` | metadata | Two more request-path time.sleep retries can freeze the gevent hub (web.py) |
| `F-347c16` | api | Bookmark routes can raise before any commit guard, leaving the shared session failed |
| `F-34a698` | ingest | test_ingest_batch_dirty fails on a clean main |
| `F-3aef89` | kobo | SPA and classic readers can land on different bookmark rows when the served format switches to kepub |
| `F-3eec38` | koreader | KOReader sync: "No book found for checksum" is undiagnosable from the log alone |
| `F-428248` | ci | CI: the Docker integration suite is dead — tests/docker/ shadows the Docker SDK |
| `F-43d21e` | autopilot | sync-wiki aborts on a wrong gh active user that the adjacent step reads correctly |
| `F-442557` | ingest | Ingest: a converter's child processes can outlive the conversion deadline |
| `F-447ab1` | kobo | Marking a book Read part-way through freezes a stale progress percentage beside the Read checkmark |
| `F-448e80` | ingest | test_script_import_side_effects is racy under xdist: the lockfile assertion fails on a different parameter each run |
| `F-48fca9` | spa | Typing the literal word "None" into a custom column clears it instead of saving it |
| `F-4bd825` | ingest | Ingest: a failure after a successful conversion can still drop the book |
| `F-5024cc` | spa | Sorting: Å, Ä and Ö belong after Z for Swedish, Danish, Norwegian and Finnish readers |
| `F-5647b8` | spa | a11y.spec.ts reports a different failure set on every run, so the a11y gate cannot be trusted |
| `F-571ee7` | api | GET /robots.txt returns 404 in every deployment — needs a crawl-policy decision |
| `F-5ac530` | spa | [bug] Light theme fails WCAG contrast across the sidebar, and the SPA a11y gate is skipped on main |
| `F-5f6795` | kobo | Kobo deferred KEPUB download advertises the EPUB's byte size |
| `F-5f8b9c` | koreader | The KOReader plugin's Lua test suite has no CI runner |
| `F-5fa5a0` | spa | [a11y] Light theme fails colour contrast on 31 nodes — the a11y gate is red across most pages |
| `F-6d6dfd` | koreader | KOReader: the "couldn't read the local set" guard is inert on the default provider path |
| `F-738bba` | ci | Wiki sync has no CI gate, and repo/wiki-src is a divergent unpublished copy |
| `F-78cd70` | koreader | KOReader sync plugin: move releases to a dedicated repository |
| `F-8260d1` | koreader | KOReader sync can un-finish a book marked Read on a default install (the #1340 guard is web-reader-only) |
| `F-84e991` | api | refresh_for_new_data() only refreshes the calling request's session, not other in-flight ones |
| `F-85844d` | api | Integration tests on fork PRs: can a fork's GITHUB_TOKEN pull the private pbs-cache mirror? |
| `F-89b567` | koreader | KOReader bulk pull costs one blocking request plus a file hash per book, with no yield |
| `F-8bdb42` | spa | New UI: moving a shelf in the sidebar drags the separator line with it |
| `F-9073e5` | kobo | Reading-position 'furthest wins' is decided in Python, not by the UPDATE |
| `F-970750` | kobo | Custom-column mark-read never reaches the Kobo for a book with no prior reading state |
| `F-9c171e` | metadata | Surface Hardcover token status in admin |
| `F-9fdee5` | spa | Finishing a book in the web reader marks it read for every user on a custom-read-column install |
| `F-9ff0a6` | admin | [a11y] View-settings popup promises a menu it does not provide |
| `F-a0d61c` | i18n | Community translation PRs still go CONFLICTING when they remove a fuzzy flag |
| `F-a24ccb` | metadata | Bulk metadata edits leave stale cards in the library, and report success even when every write failed |
| `F-a3bf89` | i18n | auto-revert is a dead safety net, and would be dangerous if revived |
| `F-a7004f` | metadata | Metadata enforcement reports DONE and stores a checksum even when the calibre subprocess failed |
| `F-b095e3` | ci | CI: a failed (not skipped) changed_paths silently disables the build gate |
| `F-b363fe` | i18n | SPA i18n: fuzzy-flagged translations silently fall back to English across all locales |
| `F-b6019b` | spa | "Edit default view" opens a blank Advanced search form |
| `F-bcb5de` | koreader | KOReader: no pull prompt when the device wakes with the book already open |
| `F-cb615d` | spa | Comics/manga reader still pages left-to-right for right-to-left titles |
| `F-cbf3dc` | i18n | pt_BR: 415 fuzzy entries fall back to English, including "Book not found" and "No books found." |
| `F-cc7377` | metadata | Metadata edits are a silent no-op when enforcement is off — no indication anywhere in the UI |
| `F-cc8450` | kobo | Read state has more writers than the read toggle, and the two-carrier write is not atomic |
| `F-d1f997` | kobo | Version probes can hang the server if a binary is chatty (process_wait reads pipes after wait) |
| `F-d2a62a` | admin | Duplicates status: cache-missing response can't distinguish a running scan from 'needs a manual scan' |
| `F-d82680` | kobo | Ranker buries a shipped-then-broken issue: 'handled' outranks everything and 'pushback' is read by nothing (#324 is bucket 5) |
| `F-d96519` | i18n | extract_spa_strings.py --write drops ~30 msgids that are still live in .tsx |
| `F-db85a3` | ci | Triage the 37 tests that were red behind the CI marker gap |
| `F-dcc473` | kobo | Verify checksums on the Python and kepubify downloads |
| `F-dccd6c` | ci | CI: an integration suite that cannot run at all still reports the workflow green |
| `F-dde17d` | spa | Book covers fail silently when the session is gone: /cover/<id> redirects to the login page instead of answering an image |
| `F-de9cfb` | spa | SPA e2e: count-pinned specs break on a drifted seed, and mutating specs aren't isolated |
| `F-e23f3c` | koreader | Integration: 36 kosync tests skip because cwa_api_client cannot log in |
| `F-e3bdd7` | kobo | Clearing a series or tag still doesn't reach the .kepub (ebook-meta merges, ebook-polish replaces) |
| `F-e4da4d` | koreader | KOReader: deleting a large batch of highlights can time out — the delete path is unbounded on both sides |
| `F-e63807` | metadata | Hardcover token status + env-token note are hidden until "Enable Hardcover Sync" is on |
| `F-e72fe3` | api | cps/redirect.py::remove_prefix strips the mount prefix without a segment boundary |
| `F-e9aee5` | koreader | The KOReader plugin's Lua tests never run in CI |
| `F-ed2450` | i18n | tier-1 auto-merge validation does not enforce the "no code" clause of its own tier definition |
| `F-f38008` | kobo | KOReader sync: a mismatched content_id makes a zero-row Kobo read look like an emptied device |
| `F-f3c4df` | ci | test_1094 conversion-deadline test asserts against ambient wall-clock, flakes under load |
| `F-ff3809` | ingest | Count messages have no plural form: English reads "1 books", and inflected languages cannot render them at all |
| `F-065a9a` | cover-picker | Cover picker fan-out omits copy_current_request_context, silently dropping per-user Hardcover tokens and all DNB language data |
| `F-123292` | oauth | Cancelled or failed OAuth loops back to the provider instead of the login page |
| `F-27fa63` | container-init | Non-root container start leaves three real residual failures (ImageMagick policy, cps/cache, dirs.json) and the 0-EPERM claim is a Docker Desktop fakeowner artifact |
| `F-391ce8` | cwa-functions | download_current_log siblings rewrite their own abort(403)/abort(404) as 400 |
| `F-4229fc` | autopilot | verification-sweep discards gh's stderr, so the regression radar's failure reason is unrecoverable |
| `F-5d9298` | container | metadata-change-detector starts two watchers as root while its sibling drops to abc |
| `F-6e4ea6` | ci | findings/INDEX.md is generated and committed, so concurrent findings PRs always conflict |
| `F-79d746` | ci | TIER1_PATHS_REGEX is declared as tier policy but enforces nothing |
| `F-b6a4af` | container | cwa-auto-library swallows its own exit code, so a failed boot step reads as success |

### ux

| id | area | finding |
|---|---|---|
| `F-098b15` | i18n | New UI is largely untranslated in every language except Russian |
| `F-151255` | spa | New UI: a save error throws away what you typed and restores the old value |
| `F-188930` | kobo | Set a custom Kobo sleep-screen / background from the web UI |
| `F-209826` | core | sp1-ux.spec.ts:3 is flaky (nav ordering assertion) |
| `F-3605ee` | metadata | Centralize metadata-provider HTTP timeout budget (consistency) |
| `F-3d40e4` | spa | New UI: List smart shelves on /shelves + consistent back-links |
| `F-415292` | spa | New UI reader: no swipe-to-turn-page on touch devices (classic has it) |
| `F-5384f4` | spa | [New UI] Make the series line on book cards a link to the series |
| `F-5c7d88` | api | Scoped, revocable API tokens for third-party clients |
| `F-63e69a` | ci | CI: a Dockerfile-only PR doesn't run the Docker integration tests |
| `F-64f2bd` | spa | Convert on book page v2: common conversions first, 'Other…' for custom targets |
| `F-794ff3` | koreader | Import highlights from a Kindle/KOReader `My Clippings.txt` file |
| `F-a3bcff` | spa | Ambient animated backgrounds for the library and reader |
| `F-ab1edf` | metadata | Move writable runtime directories (cps/cache, metadata_change_logs, metadata_temp) out of the application tree |
| `F-b198f4` | metadata | Hardcover has two enable flags in two databases; reconcile them, then allow enabling by env var |
| `F-b22824` | kobo | Consider defaulting new users to "sync only selected shelves" for Kobo |
| `F-b82071` | metadata | Hardcover: log token presence + source, and distinguish HARDCOVER_TOKEN from HARDCOVER_TOKEN_FILE |
| `F-c78e8c` | kobo | New UI: Kobo-sync toggle for smart shelves (backend already wired) |
| `F-d60c09` | spa | New UI: Duplicate action for ordinary shelves (parity with smart shelves) |
| `F-d9392a` | metadata | Book detail page scrolls sideways on mobile when a metadata value has no spaces |
| `F-e12078` | metadata | Add Anna's Archive as a metadata provider |
| `F-e46f22` | spa | New UI: show highlight count badge on the Highlights button |
| `F-e64448` | ingest | Ingest: actually fulfill .acsm tickets instead of only explaining why conversion failed |
| `F-feec51` | metadata | Hardcover title-search covers stay low-res when the record has no ISBN |

### perf

| id | area | finding |
|---|---|---|
| `F-075496` | kobo | Every KEPUB download rewrites the whole zip on the request greenlet when embed_metadata is on |
| `F-5c2758` | kobo | EPUB layout cache does not remember failures, so a corrupt book re-parses and re-logs on every sync page |
| `F-652e6e` | kobo | Kobo store proxying blocks the request greenlet for up to 12s per non-GET call |
| `F-a6c145` | kobo | Kobo cover padding runs ImageMagick on the request greenlet on every cache miss |
| `F-c577f9` | kobo | Kobo sync re-parses every book's EPUB container+OPF from disk on every sync page |
| `F-c96bd6` | kobo | Kobo sync commits once per emitted book inside the request |

### test

| id | area | finding |
|---|---|---|
| `F-15d425` | spa | Follow-up board files reporter confirmations as "we owe a reply" (5 of 8 rows are not owed) |
| `F-2b5186` | spa | Magic-link disabled path (403) has no automated coverage |
| `F-4939f6` | ci | hidden-books.spec.ts:237 flakes on 'Response has been disposed' (shared route.fulfill pattern) |
| `F-9c44aa` | spa | Book-card action row: three gaps in the #1166 e2e coverage |
| `F-abdea7` | spa | Promote the SPA e2e gate to required once it has earned it |
| `F-d4107c` | ci | API tests bypass the HTTP boundary: routing, method and auth decorators aren't pinned |
| `F-7a530b` | ci | tag-merge-delete.spec.ts flakes on 'socket hang up' and can block a release cut |
| `F-d16bdf` | ci | Fork PR with CI stuck in action_required reads as green to the merge gate |

### docs

| id | area | finding |
|---|---|---|
| `F-df9d72` | docs | Install guides: add real-hardware-verified screenshots |

### chore

| id | area | finding |
|---|---|---|
| `F-0504c5` | shelves | Magic-shelf pagination: full cursor (magic_shelf_last_id residual) |
| `F-67982f` | admin | Account page: email body template field missing |
| `F-1a7b43` | i18n | cps/spa_strings.py AUTOGEN block has drifted ~57 msgids from the SPA source |


## Closed

| id | status | finding | resolved |
|---|---|---|---|
| `F-2ff8ac` | duplicate | KOSync furthest-wins is an unlocked read-compare-write, so a concurrent push can regress progress | 2026-08-07 |
| `F-0a2f72` | resolved | New UI: read/unread + favorite quick-actions on book cards (parity with classic cards) | 2026-08-06 |
