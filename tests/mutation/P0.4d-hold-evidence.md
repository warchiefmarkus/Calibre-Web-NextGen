P0.4d — HOLD follow-up: manual diagnostic stays out of CI

All Python commands use the repo virtualenv, denoted `$PYTHON`, from `$ROOT`.
Captured output redacts local paths. This follow-up changes no runtime behavior.

1. Deletion and docstring correction — OBSERVED.

   Command: `git diff 7fbfff4bf6 HEAD -- tests/unit/test_mutation_process_probe.py
   tests/mutation/mutate.py`.
   Output: the standalone 31-line test module is deleted; `_phase_members` now
   documents returned inspection gaps as blocking acceptance
   (`tests/mutation/mutate.py:205`). The source edit is committed in `bd899db09d`.

   Command: `git diff 7fbfff4bf6 HEAD -- tests/unit/test_mutation_harness.py
   tests/mutation/inspect_process_args.py`.
   Output: empty. Both files are byte-for-byte unchanged. An additional Python
   AST comparison after stripping docstrings observed that `mutate.py` is
   otherwise unchanged. No containment-verdict or timeout change was made.

   Removed coverage: the deleted test checked the manual script's printed
   INCONCLUSIVE line and EIO counter. No retained test asserts that manual CLI
   presentation/counter behavior. The product probe's uninspectable type,
   bounded attempts, boolean-coercion TypeError and no-ps tripwire remain
   covered by `test_darwin_unresolved_process_has_bounded_inspection`
   (`tests/unit/test_mutation_harness.py:1608`). The manual script remains
   unchanged, including its source-text pin (`tests/mutation/inspect_process_args.py:14`).

2. Collected count and full run — OBSERVED.

   Exactly one unparameterized test was deleted from a separate module. The
   previously combined count was 180: 179 harness cases plus that one test.
   The combined count therefore drops by one to 179. The requested harness-only
   run collected 179, unchanged from its previous count: a drop of zero within
   that file. No harness case was removed. The previous two-module count is
   recorded in [P0.4d-review-final-focused.txt](P0.4d-review-final-focused.txt).

   Command:

   ```sh
   "$PYTHON" -m pytest tests/unit/test_mutation_harness.py -o addopts= \
     -p no:rerunfailures -p no:flaky -p no:cacheprovider -v -s --tb=short
   ```

   [Full output](P0.4d-hold-full.txt):

   ```text
   collecting ... collected 179 items
   1 failed, 178 passed in 926.47s (0:15:26)
   exit: 1
   ```

   The failing case was
   `test_poison_suite_changes_a_shared_tree_verdict_but_not_an_isolated_one[ignored]`
   (`tests/unit/test_mutation_harness.py:456`). Its traceback reported:

   ```text
   mutate.IsolationError: provenance REJECTED: pytest probe execution failed (exit=-9, timeout=True, cleanup=phase processes remain or have not been reaped before cleanup deadline)
   ```

   This includes the cleanup-deadline failure text that the maintainer ruled
   out of scope (`tests/mutation/mutate.py:342`). The output also records a
   preflight timeout; it is not described here as an EIO failure. No causal
   diagnosis or new HEAD-versus-BASE timing measurement was performed. The
   single full invocation completed all 179 cases, with no skips or reruns.
   No timeout or containment code was changed in response to the failure.

3. No remaining executable reference to the manual diagnostic — OBSERVED.

   Commands:

   ```sh
   rg -n --glob '*.py' --glob '*.sh' --glob '*.yaml' --glob '*.yml' \
     'inspect_process_args|test_mutation_process_probe' tests .github
   rg -n 'inspect_process_args' tests --glob '!*.md' --glob '!*.txt'
   ```

   Both searches returned no output, exit 1 (no matches). The deleted module
   was the only executable reference found before the deletion. Remaining
   references in Markdown and text are historical reports and captured output.
   Nothing in the searched test/CI sources invokes the manual diagnostic.

4. Changelog guard — OBSERVED.

   Command, with repo virtualenv `python3`:
   `python3 scripts/check_changelog_diff.py origin/main HEAD`.

   ```text
   CHANGELOG integrity guard passed: the entry requirement is satisfied or every changed path is non-shipping, and no PR-authored release structure was lost.
   exit: 0
   ```

   All changed paths remain under exempt `tests/`; no fragment was added.

Scope: no other test was weakened or deleted. The manual diagnostic and
containment-verdict work were retained. No timeout was raised, no test retry or
skip was added, and no stash operation was used. The separately ruled
process-table inspection and cleanup-deadline failures are out of scope; this
follow-up will record any occurrence without changing those mechanisms.
