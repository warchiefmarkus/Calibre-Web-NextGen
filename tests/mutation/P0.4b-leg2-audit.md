# Salvage audit — deliverable 1

Scope: commit `9a62b49e69`, examined as unreviewed contributor code.
All execution evidence below is OBSERVED on Mac/APFS only; no Linux gate ran.
`$ROOT` means the project directory resolved two levels above this worktree.
Commands use the absolute interpreter obtained from `$ROOT/repo/.venv/bin/python`.
No import-provenance dependency was found. Parent deliverables 3–7 are untouched.

## Function and type verdicts (original salvage line numbers)

| Symbol in tests/mutation/mutate.py | Verdict | Finding |
| --- | --- | --- |
| PhaseResult:55 | correct as written | Passive result fields; does not itself enforce disposition. |
| _PHASE_GATE_WRAPPER:65 | defective | EOF releases execution, even if tracker setup failed. |
| _DarwinProcessTracker.__init__:74 | defective | Registration failure leaks the queue; caller leaks gated process and pipe. |
| _register:85 | defective | NOTE_FORK does not provide recursive tracking or child identity. |
| _table:95 | defective | Timeout/OSError is not converted into the tracker error contract. |
| _discover_children:111 | defective | A ps ancestry snapshot can miss a reparented escape; PID reuse is unchecked. |
| _watch:134 | defective | Unhandled discovery exceptions kill thread silently; fork notifications coalesce. |
| snapshot:147 | defective | Caller takes one stale snapshot before cleanup; later descendants are omitted. |
| close:151 | defective | Timed join can leave a thread using a closed queue. |
| _processes_with_phase_token:157 | defective | Linux-only scan is not ancestry: cleared environment loses ownership; permission errors are ignored. Darwin explicitly refuses. |
| _signal_group:183 | defective | Swallows permission failure. |
| _pid_is_running:192 | defective | Zombies count as gone, not reaped; Linux stat parsing breaks on spaces in comm; ps errors can imply absent. |
| _group_exists:215 | defective | Same zombie/stat problems; unsupported-platform permissions unhandled. |
| _wait_until_gone:249 | correct as written | Bounded polling helper, conditional on a bounded truthful predicate. |
| _terminate_phase_processes:258 | defective | Snapshot and PID reuse races; late forks missed; zombie exit is treated as reaping; leader may be mislabeled escaped; errors can bypass cleanup. |
| run_phase_process:317 | defective | Setup is outside finally; exception paths leak gate/tracker/child; only leader is waited; no scrub coupling; unsupported platforms may execute before rejecting. |

The process helpers are reachable from the four added tests, but have no caller
in the command-line mutation flow. That flow still uses the old shared checkout;
wiring its migration is parent deliverable 6 and is not started here.

## Test verdicts (tests/unit/test_mutation_harness.py, original lines)

| Test | Verdict | Observation |
| --- | --- | --- |
| test_phase_exit_kills_a_child_that_would_write_later:120 | defective | Passed, but no child-ready or PID/reaping witness; scheduler delay can make absence vacuous. |
| test_phase_escape_is_terminated_and_is_an_error:147 | defective | FAILED at escaped_pids; no handshake proves setsid happened before group kill. |
| test_phase_timeout_is_not_a_process_leak:174 | defective | Passed; tests only the leader result, launches no descendant and inspects no surviving process. |
| test_poison_suite_changes_a_shared_tree_verdict_but_not_an_isolated_one:188 | defective | Passed; only baseline.ignored affects the verdict. Claims about every poison are unsupported; delayed child has no readiness witness; post-phase scrub is manual. |

The .gitignore fixture change is correct as written: explicit .pyc is intentionally
ignored so git-clean coverage can exercise it. It supplies no provenance check.

## Real initial execution

Command (absolute resolved interpreter, retries 0, no maxfail):

```sh
"$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -k 'phase_exit or phase_escape or phase_timeout or poison_suite' -q -o addopts='' --tb=short
```

```text
.F..                                                                     [100%]
_______________ test_phase_escape_is_terminated_and_is_an_error ________________
tests/unit/test_mutation_harness.py:169: in test_phase_escape_is_terminated_and_is_an_error
    assert result.escaped_pids
E   AssertionError: assert ()
1 failed, 3 passed, 7 deselected in 19.85s
```

Which original tests have been seen red for the reason their names assert?
**None.** The escape test failed, but the original fixture did not establish that
an escape took place. The other three have only been seen passing. This statement
is specifically about the original four tests, before the leg-2 corrections.

## Design limit

OBSERVED in the local macOS SDK sys/event.h: NOTE_FORK does not pass child PID to
userspace, and NOTE_TRACK/NOTE_TRACKERR/NOTE_CHILD have been unsupported since 10.5.
Consequently kqueue plus ancestry polling cannot establish arbitrary process-tree
ownership. Environment scans also cannot establish ownership after an exec with
a replaced environment. A fix must not call either mechanism a complete proof.
Filesystem scrub does not isolate temporary directories, the venv, common Git
data/config/refs, home directories, services, network, ports, or shared caches.

## Resolution — deliverable 2

OBSERVED: the kqueue tracker, launch gate, Linux token scanner, boolean liveness
probes, and stale-snapshot cleanup have been deleted. Recursive tracking cannot
be repaired by adding another ancestry poll. The diagnostic replacement reads
Darwin exec environments, takes repeated group/token snapshots, uses process
birth times to avoid knowingly signalling a reused PID, kills immediately, and
waits for process disappearance including zombies. The leader is reaped directly;
orphan descendants are reaped by the OS and observed gone.

OBSERVED: setup now consists of context-managed output files and Popen without
an extra pipe/thread/queue. Launch failure leaks no descriptors. Interruption
runs cleanup. Inspection errors reach containment_error and still attempt group
termination. Group permission failures are no longer swallowed. Unsupported
platforms and the default arbitrary-descendant contract reject before launch.

The diagnostic API requires explicit `ownership_contract="inherited-token"`.
This requires inspectable exec environments, unchanged user identity, and token
retention by descendants. It is NOT the requested arbitrary-descendant contract.
There is no permission inferred to weaken that requirement: the default stays
closed, and the existing command-line flow does not use this diagnostic backend.
The check-identity-then-signal syscall pair is not atomic; this too prevents a
claim of complete OS containment. A real kernel ownership boundary is still
needed to close deliverable 3 of this leg (parent deliverable 2).

OBSERVED: the original tests are replaced with readiness handshakes, actual PID
and zombie disappearance checks, an ignoring-SIGTERM child, interruption and
inspection-error exercises, descriptor-leak coverage, and a strict-mode rejection
test. A finite tokenless detached child is also executed as an explicit positive
counterexample to the diagnostic contract's completeness.

The original four tests had no trustworthy red history. After correction, the
exit, escape, and timeout tests have all been observed red with cleanup disabled
by a test-only fixture that restores real cleanup in its teardown. The poison
oracle has been observed red separately for all seven channels. Its bytecode
fixture reads generated bytecode, not cps or any project import; parent import
provenance remains outside this work.

Commands use the resolved absolute interpreter, no maxfail, retries 0:

```sh
CWNG_PHASE_CLEANUP=disabled "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -k 'phase_exit or phase_escape or phase_timeout' -q -o addopts='' --tb=short
"$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -k 'not poison_suite' -q -o addopts='' --tb=short
```

Real output excerpts, Mac/APFS only:

```text
E   AssertionError: child invalidated the boundary after exit
E   AssertionError: assert 45376 in ()
E   AssertionError: assert None is not None
3 failed, 19 deselected in 8.29s
15 passed, 7 deselected in 23.35s
```

Additional controlled salvage reproduction: insert a 0.2-second delay before
_DarwinProcessTracker._table reads ancestry; the parent waits for a child-ready
file written AFTER setsid, then exits. Real output:

```text
delayed-discovery confirmed-escape: returncode=0 escaped_detected=False containment_error=None delayed_write=True
```

This is an observed false success in the salvage, not an assumption about a race.
The test child exits by itself after its finite delayed write.

## Phase lifecycle integration — deliverable 3 remains INCOMPLETE

OBSERVED: IsolatedSweep.run_phase now couples the diagnostic runner to scrub
before execution, process termination/disappearance, and scrub afterward. The
next call scrubs again. The phase itself asserts its group ID equals its PID;
two consecutive real executions use distinct leaders. A timeout or containment
error stops further phases on the sweep. If containment reports an error, the
wrapper does not scrub underneath potentially surviving writers.

OBSERVED: strict mode rejects before subprocess launch; it does not silently use
the diagnostic token contract. This is a deliberate design correction to the
salvage's unconditional success claim. The requested process-tree containment is
NOT closed. This Mac implementation cannot track arbitrary tokenless detached
descendants and does not provide an atomic kernel handle for process signalling.
Parent deliverables 3–7 have not been started to conceal or work around that gap.

ASSUMED / not implemented: a kernel-enforced process ownership boundary that
retains descendants across reparenting, session changes and environment changes
would be needed for the requested guarantee. No such backend has been established
here. The diagnostic poison result must not be presented as establishing it.

## Poison oracle — deliverable 4

OBSERVED: the corrected oracle is red in a shared tree independently for ignored
files, tracked content, collateral content, index, HEAD, executed stale bytecode,
and a delayed child. Every clean pre-poison selection returns 0; every shared
post-poison selection returns 1. For the child case the shared tree is fully
scrubbed and observed clean, then the mutant releases a waiting writer outside
that scrub boundary. That writer changes the verdict. This is not merely a file
left over for a later scrub to remove.

OBSERVED: the same seven assertions are green through IsolatedSweep.run_phase
under the explicit diagnostic token contract. All post-poison selections return
0. The negative control is selected ONLY in the test via CWNG_POISON_BOUNDARY;
the production runner has no cleanup-disable option. The process-test cleanup
negative control likewise lives only in a fixture and invokes real cleanup in
its teardown even after an expected assertion failure.

Real red/green output excerpts and exact argument lists are preserved in
P0.4b-leg2-evidence.txt. Full-file command, executed with the absolute interpreter:

```sh
PYTEST_ADDOPTS= "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short
```

```text
25 passed in 67.55s (0:01:07)
```

All timings are Mac/APFS only; Python 3.12.7. No retries, no maxfail, no Linux gate.
The full file also OBSERVED the tokenless-detached-child counterexample, printed
in the same evidence file. Thus the oracle is established for these seven
channels, but its diagnostic green result does NOT close arbitrary-descendant
containment. Deliverable 3 remains incomplete; no stronger isolated backend has
been implemented or demonstrated.

Deletion accounting: PhaseResult is retained. The formerly correct general
_wait_until_gone helper became dead after cleanup was replaced by the bounded,
repeated ownership scan and was deleted for that reason. The Linux-only scanner
was deleted rather than carried forward as an untested backend. No import
provenance feature or command-line lifecycle migration was added.

## Reconciliation against origin/main

The final `git diff --name-only origin/main..HEAD` is expected to contain exactly
these paths (checked after the deliverable-4 commit):

| Path | What the branch contains |
| --- | --- |
| tests/mutation/P0.4b-leg2-audit.md | This per-function/per-test audit, resolutions, limitations and completion status. |
| tests/mutation/P0.4b-leg2-evidence.txt | Real selected red and green output, full-file pass line, commands and Mac/APFS qualification. |
| tests/mutation/mutate.py | Parent leg's isolated sweep lifecycle; replacement of salvage tracking with a guarded Mac diagnostic runner; scrub/phase coupling. The old CLI remains unchanged. |
| tests/unit/test_mutation_harness.py | Parent lifecycle tests; corrected process and poison tests; negative controls; strict refusal and explicit limitation tests; diagnostic phase-order tests. |

No files outside the writable paths were changed. Parent deliverables 3–7 remain
out of scope and unstarted. Four leg-2 commits preserve the audit, corrections,
partial lifecycle integration, and oracle evidence separately. No push, PR,
merge, or change to another existing worktree was performed.
