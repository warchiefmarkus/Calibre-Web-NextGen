# Leg 8 judge HOLD corrections

All timings are Mac/APFS only. The interpreter is the absolute pinned venv
interpreter, resolved from cwd as "$ROOT/repo/.venv/bin/python". No real Linux
run is claimed. All pytest commands use -o addopts= -p no:rerunfailures
-p no:flaky, retries 0 and no maxfail.

## 1. Linux lane selection — corrected

Individual Darwin-dependent tests carry @DARWIN_ONLY, a skipif with the explicit
reason "requires macOS diagnostic process backend". Platform-independent tests
remain marked unit and continue running on Linux. No CI/quarantine edit needed.
A test-only CWNG_TEST_PLATFORM=linux proxy selects the Linux partition, changes
the harness's platform view, and rejects any attempted Darwin process syscall.

Before adding the decorators, the proxy selection -k phase_exit failed:
    1 failed, 115 deselected in 0.24s
    IsolationError: arbitrary descendant containment is unavailable

After, command:
    CWNG_TEST_PLATFORM=linux "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -m 'smoke or unit' -q -rs -o addopts= -p no:rerunfailures -p no:flaky --tb=short
    72 passed, 45 skipped in 14.20s

OBSERVED proxy only, not a Linux kernel run. ubuntu-latest will execute the
unmarked platform-independent tests and skip the 45 Darwin cases. The skip
reason and per-function decorators make the partition visible; -rs lists it.
Linux keeps Git lifecycle/scrub tests, strict-backend rejection, byte integrity,
collection/summary/exit accounting, xfail accounting, journal/spec validation,
result forgery rejection and the matrix presentation test. Native process
inspection/containment, real provenance/CLI/pytest phases and Darwin full-sync
ordering stay on macOS. A partition regression asserts native conditions remain
false for macOS, so these tests are not silently skipped there.

Native control command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k 'platform_partition or phase_exit' -q with the options above:
    2 passed, 115 deselected in 3.47s
OBSERVED Mac/APFS, no skips.

## 2. EIO mechanism — NOT ESTABLISHED; no errno fix

Command: pinned interpreter tests/mutation/inspect_process_args.py
Real output:
    OBSERVED scans=177 probes=182443 EIO=0 (Mac/APFS only)
The bounded, read-only probe instruments the exact raise in memory, recording PID,
privacy-filtered comm, size-query versus buffer-read stage and process identity.
It never signals a process or logs arguments/environments. Production errno
handling is unchanged. This sample does not refute the judge's EIO observation;
it cannot establish its mechanism. No claim of determinism under load is made.
Deliverable 2 remains open; widening tolerated errors would be a blind fix.

## 3. Shared Git prune — corrected

OBSERVED three broad prune sites, not two: stale reaping, failed creation, close.
All are removed. Normal and stale cleanup use only the owned worktree's exact
registration. Failed creation retains ownership metadata for later stale cleanup,
rather than deleting the metadata and globally pruning registrations.
Shared Git administration for the harness's own registration remains outside the
worktree scrub boundary; arbitrary selection writes to shared refs/config remain
unsupported. No unrelated real worktree was moved, removed or pruned.

Regression uses independent synthetic repositories; an unrelated registration's
directory is temporarily renamed, then restored in finally. Separate cases cover
create/stale reaping, close, and failed add (with stale reaping bypassed).
Command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k cleanup_preserves -q with standard options above.
Before, all three assertions lost the unrelated registration:
    3 failed, 117 deselected in 1.95s
After, -k 'cleanup_preserves or isolated_sweep or stale':
    5 passed, 115 deselected in 2.62s
OBSERVED Mac/APFS only. Failure trace paths omitted for privacy; pass/fail lines
are actual output. No test was deleted.

Second EIO probe, same command, concurrent with the full native suite:
    OBSERVED scans=189 probes=174289 EIO=0 (Mac/APFS only)
Total 356732 probes without EIO. This remains an unsuccessful reproduction, not
an established mechanism and not evidence of determinism under load.

## 4. Small correctness items — two corrected, one refuted

(a) Removed the documentation-text assertion, retained the independent Git
behaviour demonstration in test_git_writing_policy_names_shared_state_and_demonstrates_limit.
Control experiment: load the test with runpy.run_path, redirect its _HARNESS to a
synthetic directory containing ISOLATION.md with equivalent wording:
"Selections that alter common Git state are outside the supported boundary."
Call the test against that temporary directory. Before removing the assertion:
    RED: behavioral policy test rejects equivalent documentation wording
After:
    POLICY shared refs/common config survive scrub: UNSUPPORTED (Mac/APFS only)
    GREEN: behavioral policy test accepts equivalent documentation wording
No test deleted; only the source-text assertion removed. The operator's scope-note
correction was not edited.

(b) REFUTED the assertion that the SURVIVOR guard cannot fire. A negative-output
regression is expected to name a token absent from the correct implementation.
Controlled mutation in check_mutations.py: replace the status mapping
0: 'NO_TEST_FAILURE' with 0: 'SURVIVOR', keeping UNVERIFIED and exit 1.
Run pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k review_matrix_driver -q with the standard options above. Restore driver in
finally. Actual red output:
    FAILED tests/unit/test_mutation_harness.py::test_review_matrix_driver_is_diagnostic_and_nonzero
    1 failed, 120 deselected in 3.95s
    CONTROLLED MATRIX MUTATION EXIT 1
The unchanged guard detects precisely that regression. Driver and guard retained.

(c) _git now translates TimeoutExpired into IsolationError('git command timed out').
The regression injects TimeoutExpired into subprocess.run; checks the helper,
then calls the real CLI main path and requires exit 1 plus UNVERIFIED ERROR.
Command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k git_timeout -q with standard options. Before fix:
    FAILED tests/unit/test_mutation_harness.py::test_git_timeout_is_a_handled_isolation_error
    1 failed, 120 deselected in 0.19s
The documentation now distinguishes completed/handled diagnostics (1), argparse
errors (2), and other nonzero exits, rather than promising every path exits 1.

After fixes, focused command: pinned interpreter -m pytest
tests/unit/test_mutation_harness.py
-k 'git_timeout or git_writing_policy or review_matrix_driver or cleanup_preserves'
-q with standard options:
    6 passed, 115 deselected in 6.34s
OBSERVED Mac/APFS only. Full-suite run below also exercises the added CLI assertion.

## Branch diff reconciliation

Command: git diff --name-only origin/main..HEAD. Each line reconciled below.
All entries except the unit test are under tests/mutation/.

| File | Attribution |
| --- | --- |
| ISOLATION.md | Earlier architecture notes; leg 8 changes exit-code wording only |
| P0.4b-leg2-audit.md | Inherited salvage audit, unchanged this leg |
| P0.4b-leg2-evidence.txt | Inherited process/poison evidence, unchanged |
| P0.4b-leg3-evidence.md | Inherited provenance/labels evidence, unchanged |
| P0.4b-leg4-evidence.md | Inherited soundness evidence, unchanged |
| P0.4b-leg4-full-suite.txt | Inherited suite output, unchanged |
| P0.4b-leg4-mutations-initial.txt | Inherited mutation experiment, unchanged |
| P0.4b-leg4-mutations-resolved.txt | Inherited mutation dispositions, unchanged |
| P0.4b-leg5-evidence.md | Inherited CLI/journal/policy evidence, unchanged |
| P0.4b-leg5-full-suite.txt | Inherited suite output, unchanged |
| P0.4b-leg6-evidence.md | Inherited review-fix evidence, unchanged |
| P0.4b-leg6-full-suite.txt | Inherited suite output, unchanged |
| P0.4b-leg7-assessment.md | Inherited class-defeat assessment, unchanged |
| P0.4b-leg7-baseline.txt | Inherited suite output, unchanged |
| P0.4b-leg8-evidence.md | This leg's observations and unresolved EIO finding |
| check_mutations.py | Inherited diagnostic matrix; controlled mutation restored byte-for-byte |
| inspect_process_args.py | New bounded read-only EIO raise-site probe |
| leg7_probe.py | Inherited defeat-vector probe, unchanged |
| mutate.py | Inherited isolated harness; remove broad pruning, retain failed-add ownership, translate Git timeout |
| provenance_probe.py | Inherited three-shape package/target preflight, unchanged |
| pytest_evidence.py | Inherited accounting/frame witness, unchanged; known leg 7 limits remain |
| tests/unit/test_mutation_harness.py | Individual Darwin skips, proxy/partition regression, three Git registration regressions, timeout helper/CLI regression; remove prose assertion only |

## Final validation (Mac/APFS only)

OBSERVED command, absolute pinned interpreter resolved from cwd:
    ROOT=$(cd ../.. && pwd)
    "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -q -o addopts= -p no:rerunfailures -p no:flaky --tb=no
Actual final output:
    121 passed in 149.81s (0:02:29)
Zero skips, retries 0, no maxfail. Baseline 116 plus five new parameter cases;
no tests deleted. This is one passing native run, not deterministic-load proof.

OBSERVED final Linux proxy command:
    CWNG_TEST_PLATFORM=linux "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py -m 'smoke or unit' -q -rs -o addopts= -p no:rerunfailures -p no:flaky --tb=no
Actual final output:
    76 passed, 45 skipped in 12.37s
All 45 skips report "requires macOS diagnostic process backend". A real Linux
run was NOT performed. Expected ubuntu-latest selection: 76 portable cases,
45 explicitly skipped Darwin cases; this expectation is a proxy inference.

OBSERVED git diff --check: no output, exit 0. Branch inventory above has 22
paths, all within the allowed write scope. This leg changes only ISOLATION.md,
mutate.py, the unit test, and the two new leg 8 evidence/probe files.

Reference map: platform selection tests/unit/test_mutation_harness.py:23,
partition assertion :1193, Git preservation regression :1203, policy behaviour
:965, matrix guard :1180, timeout helper/CLI regression :1235. Production Git
wrapper tests/mutation/mutate.py:651, stale cleanup :698, failed-add ownership
:786, close :872; exit wording tests/mutation/ISOLATION.md:47. Instrumented
raise-site probe tests/mutation/inspect_process_args.py:14, callback :25.

HOLD remains justified by deliverable 2. No EIO mechanism or fix is claimed.
Known leg 7 execution-witness limitations and macOS process escapes remain
outside the demonstrated guarantees; all mutation outcomes remain diagnostic.
