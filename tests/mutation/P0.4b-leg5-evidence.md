# Leg 5 evidence

All observations and timings are Mac/APFS only. Commands use the absolute
interpreter resolved from cwd as "$ROOT/repo/.venv/bin/python".
All pytest runs use -o addopts= -p no:rerunfailures -p no:flaky, retries 0,
no maxfail. No Linux backend is implemented.

## 1. Load-sensitive timeouts — OBSERVED

tests/unit/test_mutation_harness.py:124 defines a 60-second startup watchdog.
The reported phase-exit and poison-suite subprocess timeouts use it; child-ready
handshakes allow 30 seconds instead of three. These are bounded hang detectors,
not timing assertions. The dedicated one-second timeout test is retained.
No retry or skip on timeout was introduced. No concurrent-load stress benchmark
was performed; immunity to arbitrary scheduling starvation is not claimed.

Commands: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-q -o addopts= -p no:rerunfailures -p no:flaky --tb=short, with:
- CWNG_PHASE_CLEANUP=disabled, -k phase_exit
- CWNG_POISON_BOUNDARY=shared, -k 'poison_suite and tracked', -s
- normal environment, -k 'phase_exit or (poison_suite and tracked)', -s

Real output excerpts (Mac/APFS only):
    E   AssertionError: child invalidated the boundary after exit
    1 failed, 95 deselected in 6.87s
    ORACLE boundary=shared channel=tracked clean=0 baseline=0 mutant=1 CONTAMINATING (Mac/APFS only)
    E   AssertionError: CONTAMINATING: tracked changed the verdict
    1 failed, 95 deselected in 2.67s
    ORACLE boundary=isolated-diagnostic channel=tracked clean=0 baseline=0 mutant=0 CLEAN (Mac/APFS only)
    2 passed, 94 deselected in 9.92s

The process negative-control fixture still invokes real cleanup in teardown.
Neither experiment weakens production cleanup.

## 2. Git-writing policy — OBSERVED

Chosen policy: explicitly unsupported, documented in ISOLATION.md:3.
Source scanning cannot reliably detect dynamic Git commands or mutation-induced
writes, so no heuristic detection is claimed. Private HEAD/index changes are
scrubbed; shared refs/common config/hooks and other-worktree writes are unsupported.
Independent temporary repositories created by tests are allowed.

The test test_git_writing_policy_names_shared_state_and_demonstrates_limit
changes only its own synthetic repository. It writes a shared ref and common
config from its linked worktree, scrubs, and observes both changes still present.
Command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k git_writing_policy -q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
Real output (Mac/APFS only):
    POLICY shared refs/common config survive scrub: UNSUPPORTED (Mac/APFS only)
    1 passed, 96 deselected in 0.88s

## 3. Journal migration — OBSERVED

The current branch had no journal implementation or --clear-journal option;
the reference P0.4a branch did. Detection now reproduces that reference's
canonical-source-path SHA-256 key and temporary cwng-mutation/<key>/active.json
location without creating it. Any existing entry, even malformed or a symlink,
refuses execution. No contents are interpreted as restore instructions.

Chosen policy: refuse with preservation/comparison/recovery instructions.
ISOLATION.md documents locating and resolving the old state, including the
original TMPDIR requirement. This avoids importing the retired recovery writer.
Tests preserve both valid and malformed journal bytes and reject --clear-journal.
Command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k 'legacy_journal or clear_journal' -q -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
Real output (Mac/APFS only):
    3 passed, 97 deselected in 0.17s

## 4. CLI migration — implementation and development evidence

OBSERVED: mutate.py main routes inline and JSON specifications through one
IsolatedSweep at an explicitly supplied --seed, resolved once. Each item calls
run_checked_mutation and present_checked_result. Source uncommitted state is
excluded and preserved. Durable evidence lives outside the source checkout.
An ERROR stops subsequent items. All terminal outcomes remain UNVERIFIED/nonzero.
The CLI prints the unsupported shared-Git-write policy and outside boundary.

The old run_mutant, _backup_name, backup/restore execution and
DiagnosticObservation type were removed. Their tests were migrated to the
isolated API: invalid anchors, source preservation, immutable labels and terminal
rejection remain checked. The backup-name test now asserts removal of the old
route. This is test migration, not a claim that the old test bodies are unchanged.

Real CLI tests use the actual harness script, pinned absolute interpreter,
independent committed synthetic cps repositories and the real installed console
launcher. They do not replace the sweep, phase runner, provenance or pytest.
Dirty source bytes are preserved. Test bodies assert local cps resolution and
clean collateral/ignored files before deliberately dirtying both. Evidence must
record collection/baseline/mutant exits [0,0,0] or [0,0,1], and worktree teardown
is asserted. This is an end-to-end synthetic-repository exercise, not a production
application mutation sweep.

Development failures: first migrated-test run produced
3 failed, 10 passed, 87 deselected in 7.56s (Mac/APFS only).
One fixture attempted a Git commit with no change; two CLI fixtures contained
a misescaped newline and were correctly rejected during provenance collection.
Fixed the fixture commit and string escaping. These are not claimed as deliberate
red controls or production defects.
Next focused command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k 'real_cli or isolated_invalid or clear_journal' -q -s -o addopts=
-p no:rerunfailures -p no:flaky --tb=short.
Real output excerpts (Mac/APFS only; seed replaced with a placeholder):
    UNVERIFIED TESTS_PASSED: execution checks passed; authority remains unverified
    CLI isolated phases=3 observed_exit=0 terminal_exit=1 source-preserved (Mac/APFS only)
    UNVERIFIED TEST_FAILURE: execution checks passed; authority remains unverified
    CLI isolated phases=3 observed_exit=1 terminal_exit=1 source-preserved (Mac/APFS only)
    5 passed, 95 deselected in 8.43s

CLI command inside that test:
    "$ROOT/repo/.venv/bin/python" tests/mutation/mutate.py --repo <fixture-repo> --seed <fixture-commit> --evidence-dir <fixture-evidence> --file victim.py --old 'VALUE = 1' --new 'VALUE = 2' --test test_probe.py

Additional tests exercise a two-item specification, an outside-root cps import
rejected by the actual CLI preflight, malformed/empty specifications, traversal
and pytest-option injection. Invalid specifications cannot allocate a sweep.

Outside the boundary: temporary directories, venv, home, shared Git data/config/
refs, Docker, databases, network, ports, caches, services and escaped processes.
The Linux strong backend remains unimplemented. No authoritative verdict is
claimed. No push, PR, merge, or change to another existing worktree is performed.

Further development observations (Mac/APFS only):
- First full-suite attempt stopped during collection: 1 error in 0.18s.
  A literal newline in the newly added assertion was fixed; no tests ran.
- Spec/provenance selection: 1 failed, 5 passed, 100 deselected in 8.78s.
  The nonexistent negative path caused an internal probe error instead of an
  outside-root witness. Changed the fixture to the real sweep metadata file
  outside the execution root; no production check was relaxed.
- Corrected selection, -k 'cli_spec and provenance_reject':
      CLI spec: UNVERIFIED exit=1 evidence-count=2 (Mac/APFS only)
      CLI provenance_reject: UNVERIFIED exit=1 evidence-count=1 (Mac/APFS only)
      2 passed, 104 deselected in 7.25s

Final implementation/test references:
1. STARTUP_TIMEOUT tests/unit/test_mutation_harness.py:125; phase exit :166;
   readiness :128; poison fixture :363 and phase watchdogs :411.
2. Policy ISOLATION.md:3; real shared-state test test_mutation_harness.py:925.
3. Legacy identity/refusal mutate.py:883/:889; tests :940/:955.
4. CLI spec parsing/main mutate.py:901/:929; checked API :579.
   Direct-route removal :446; isolated source checks :453/:466;
   label/terminal checks :546/:576; real CLI :584; spec/provenance :963;
   invalid-spec refusal before sweep allocation :995.

Reconciliation for git diff --name-only origin/main..HEAD after the final commit:
- tests/mutation/P0.4b-leg2-audit.md — inherited audit, unchanged this leg.
- tests/mutation/P0.4b-leg2-evidence.txt — inherited evidence, unchanged this leg.
- tests/mutation/P0.4b-leg3-evidence.md — inherited evidence, unchanged this leg.
- tests/mutation/P0.4b-leg4-evidence.md — inherited evidence, unchanged this leg.
- tests/mutation/P0.4b-leg4-full-suite.txt — inherited output, unchanged this leg.
- tests/mutation/P0.4b-leg4-mutations-initial.txt — inherited output, unchanged this leg.
- tests/mutation/P0.4b-leg4-mutations-resolved.txt — inherited output, unchanged this leg.
- tests/mutation/ISOLATION.md — new unsupported-use, legacy-recovery and CLI policy.
- tests/mutation/P0.4b-leg5-evidence.md — this leg's evidence and reconciliation.
- tests/mutation/P0.4b-leg5-full-suite.txt — final real full-suite output excerpts.
- tests/mutation/check_mutations.py — inherited mutation driver, unchanged this leg.
- tests/mutation/mutate.py — legacy journal refusal and isolated CLI; direct route deleted.
- tests/mutation/provenance_probe.py — inherited probe, unchanged this leg.
- tests/mutation/pytest_evidence.py — inherited observer, unchanged this leg.
- tests/unit/test_mutation_harness.py — watchdog adjustment, migrated old-route tests,
  shared-Git policy, journal refusal, real CLI and spec/provenance checks.

## Final full-suite result — OBSERVED

Command: "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py
-q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
PYTEST_ADDOPTS was empty; retries 0, no maxfail.
Real output (Mac/APFS only):
    106 passed in 116.51s (0:01:56)
    exit=0

P0.4b-leg5-full-suite.txt preserves actual safe output excerpts, including both
CLI mutation outcomes and their nonzero exits, shared-state policy, two-item
specification, and CLI provenance rejection. All four leg deliverables complete.
The old route's tests were migrated rather than keeping dead APIs to preserve
their bodies. Final count exceeds 96.

Deliverable commits: 1 = 5cd3936686; 2 = 4b52d6f278; 3 = f21f10b316;
4 = the commit containing this final record.
