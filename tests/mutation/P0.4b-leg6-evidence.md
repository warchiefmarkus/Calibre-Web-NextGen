# Leg 6 blind-review corrections

All timings and execution are Mac/APFS only. Commands use the absolute pinned
interpreter resolved from cwd as "$ROOT/repo/.venv/bin/python".
Review source: state/modernization/runs/p04b-blind-review/run.jsonl, including
the final report's exact reproduction fixtures. Reproductions below translate
those fixtures into regression tests using independent temporary repositories;
no existing source worktree is mutated. Raw private paths are excluded.

## 1. Preflight writes erase measured input — CONFIRMED, corrected

OBSERVED command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k 'review_preflight or review_post' -q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
Before:
    REVIEW1 DIRECT_EXIT=1 HARNESS_EXIT=1 SIGNAL=TESTS_PASSED PHASE_RETURNCODES=[0, 0, 0] (Mac/APFS only)
    3 failed, 106 deselected in 5.89s
An independent verification-only red selection removed the collateral poison:
    assert b'VALUE = 1\n' == b'VALUE = 2\n'
    1 failed, 108 deselected in 0.94s
After, selection additionally includes sweep_scrubs_after:
    REVIEW1 DIRECT_EXIT=1 HARNESS_EXIT=1 SIGNAL=TEST_FAILURE PHASE_RETURNCODES=[0, 0, 1] (Mac/APFS only)
    4 passed, 105 deselected in 9.35s

IsolatedSweep.run_phase now scrubs after all preflight subprocesses terminate,
reapplies the mutation and independently compares measured bytes immediately
before launch. The order test was corrected in place: three probe terminations,
then scrub, then measured process, then scrub. No test was deleted.
The regression separately poisons collateral state and injects an application
helper that returns without retaining the requested bytes; removing the scrub
or final verification makes the corresponding assertion fail.

The resulting TEST_FAILURE is an UNVERIFIED observation, not an authoritative
macOS verdict. Arbitrary escaped writers remain outside this boundary.

## 2. Actual target provenance — CONFIRMED, corrected

OBSERVED reproduction: review_actual_target copies the review's SOURCE_CHECKOUT
sys.path insertion and victim import. Its witness records SOURCE_CHECKOUT/OTHER
rather than persisting a private absolute path. It uses actual CLI execution.
Command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k review_actual_target -q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
Before:
    REVIEW2 IMPORTED_FROM=SOURCE_CHECKOUT SIGNAL=TESTS_PASSED HARNESS_EXIT=1 (Mac/APFS only)
    1 failed, 109 deselected in 3.93s
Independent missing-target regression, -k review_unobserved:
    assert 'TESTS_PASSED' == 'ERROR'
    1 failed, 110 deselected in 4.42s

Design correction: three-shape preflight witnesses cps roots only. It is retained
under that narrow claim. Actual measured baseline/mutant pytest now installs a
Python call profiler before collection; it records whether the target's canonical
source executes, whether a matching relative module path executes elsewhere, and
whether the profiler remains installed at session finish. The parent requires
seen=True, foreign=False and active=True. Missing/foreign/disabled witnesses
reject. No forced import manufactures a positive witness.

Python-source targets only are supported. Child-only imports, native code,
renamed foreign code, code loaded before observation and deliberate tampering
with instrumentation are not supported witnesses. This is not a general proof
that all imports or dependencies are confined. ISOLATION.md and the probe
docstring now state the narrower claims. No name/target identity fields were
added to the durable payload; the separate evidence-identity finding is unstarted.

Existing synthetic bodies that previously never imported the mutation target
now explicitly import it when exercising a supported passing/xfail/setup path.
The new missing-target regression separately proves that absence is rejected.
No tests were deleted. The campaign's baseline mutation anchor was updated to
match the new target argument.

An intermediate integration run had 1 failed, 14 passed, 96 deselected in 42.38s
(Mac/APFS only): Darwin process inspection returned errno 5 during a spec case.
That infrastructure rejection was retained, not ignored or converted to a pass.
No process-inspection fix is claimed. Before the next execution, path matching
was tightened from basename to full relative module suffix (avoids rejecting
every unrelated __init__.py when the target is a package initializer).
Next broader run: 1 failed, 6 passed, 104 deselected in 24.67s (Mac/APFS only);
the same process-inspection errno 5 rejected the critical reproduction's child
probe, with [0,0] phases. This is recorded as infrastructure failure, not proof
that the mutation was measured.

After adding positive tests for local top-level and package-initializer targets,
command selection -k 'review_actual or review_unobserved or review_target_witness':
    REVIEW2 IMPORTED_FROM=SOURCE_CHECKOUT SIGNAL=ERROR HARNESS_EXIT=1 (Mac/APFS only)
    4 passed, 109 deselected in 15.98s

## 3. Forged public results — CONFIRMED, corrected

OBSERVED: the review's object.__setattr__ reproduction and its SimpleNamespace
variant both returned zero before the fix. Command selection -k review_forged,
using the pinned interpreter and standard retry-disabled flags:
    REVIEW3 shape=setattr STATUS=SURVIVED AUTHORITATIVE=True EXIT_CODE=0 PRESENT_RETURN=0 (Mac/APFS only)
    REVIEW3 shape=duck STATUS=SURVIVED AUTHORITATIVE=True EXIT_CODE=0 PRESENT_RETURN=0 (Mac/APFS only)
    2 failed, 113 deselected in 0.20s

The presenter now validates concrete type and actual status, authority and exit
fields, retains signal/digest checks, and returns literal 1 rather than any
caller-controlled exit. Frozen types remain assignment conveniences only.
Two earlier tests were renamed to describe normal-assignment protection rather
than claiming unforgeable objects. Neither was deleted; the new tests exercise
the actual authority boundary.
After, selection -k 'review_forged or terminal_cannot or normal_assignment or durable_evidence':
    REVIEW3 shape=setattr PRESENT=REJECTED (Mac/APFS only)
    REVIEW3 shape=duck PRESENT=REJECTED (Mac/APFS only)
    7 passed, 108 deselected in 0.75s

## 4. Matrix executable presentation — CONFIRMED, corrected

OBSERVED exact executable reproduction wrapped by review_matrix: pinned
interpreter tests/mutation/check_mutations.py --only equivalent_integer_bound
--output <temporary-output>. The regression invokes that command and asserts
stdout, persisted output qualification and nonzero terminal status.
Before:
    equivalent_integer_bound: SURVIVOR; 49 passed, 67 deselected in 2.51s
    REVIEW4 EXIT=0 (Mac/APFS only)
    1 failed, 115 deselected in 4.00s
After:
    UNVERIFIED equivalent_integer_bound: NO_TEST_FAILURE; 49 passed, 67 deselected in 2.51s (Mac/APFS only)
    REVIEW4 EXIT=1 (Mac/APFS only)
    1 passed, 115 deselected in 3.92s
(All timings, including the original unqualified output, are Mac/APFS only.)

Regression command: pinned interpreter -m pytest tests/unit/test_mutation_harness.py
-k review_matrix -q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.

check_mutations now prints qualified observational names, qualifies persisted
output, returns 1 on completion and passes it through sys.exit. It no longer
emits the unqualified survivor vocabulary. The measured-application mutation
anchor was narrowed to the post-preflight application, because there are now
two application calls. OBSERVED: all 29 anchors remain unique and all 29 edited
variants compile. The full matrix was not re-executed in this leg; the review's
three survivor dispositions are not reinterpreted or claimed as newly measured.

## Final references and scope

1. mutate.py:817 (run_phase); regression tests :1010 and :1037;
   corrected existing lifecycle test :305.
2. pytest_evidence.py:11 (actual target witness), mutate.py:622 (required witness);
   regressions :1064, :1084, :1097. cps-only probe docstring at provenance_probe.py:2.
3. mutate.py:603 (presenter); regressions :1115; normal-assignment tests renamed
   at :546 and :562. Arbitrary object mutation remains possible, but cannot
   obtain a zero result from the presenter.
4. check_mutations.py (main/terminal); executable regression :1138.
Test references above are tests/unit/test_mutation_harness.py.

No test was deleted. The suite starts at 106 and adds ten cases. The name/target
evidence-identity finding and Linux backend are unstarted. Other review findings
outside the four requested ones (post-rename durability and changed-TMPDIR
legacy discovery) are not claimed fixed.

Outside the boundary: temporary directories, venv, home, shared Git data/config/
refs, Docker, databases, network, ports, caches, services, instrumentation
tampering and escaped processes. No authoritative macOS verdict is claimed.

Reconciliation of git diff --name-only origin/main..HEAD after final commit:
- tests/mutation/ISOLATION.md: this leg narrows import claims and explains the actual presentation boundary.
- tests/mutation/P0.4b-leg2-audit.md: inherited, unchanged this leg.
- tests/mutation/P0.4b-leg2-evidence.txt: inherited, unchanged this leg.
- tests/mutation/P0.4b-leg3-evidence.md: historical evidence, unchanged; its broad authority claims are superseded above.
- tests/mutation/P0.4b-leg4-evidence.md: historical evidence, unchanged.
- tests/mutation/P0.4b-leg4-full-suite.txt: inherited output, unchanged.
- tests/mutation/P0.4b-leg4-mutations-initial.txt: inherited output, unchanged.
- tests/mutation/P0.4b-leg4-mutations-resolved.txt: inherited output, unchanged.
- tests/mutation/P0.4b-leg5-evidence.md: historical evidence, unchanged.
- tests/mutation/P0.4b-leg5-full-suite.txt: inherited output, unchanged.
- tests/mutation/P0.4b-leg6-evidence.md: this correction ledger, before/after observations and scope.
- tests/mutation/P0.4b-leg6-full-suite.txt: actual final full-suite output excerpts.
- tests/mutation/check_mutations.py: diagnostic/nonzero presentation and adjusted anchors.
- tests/mutation/mutate.py: post-preflight scrub/reapplication/verification, target witness gate and presenter checks.
- tests/mutation/provenance_probe.py: docstring narrowed to cps-root witnessing.
- tests/mutation/pytest_evidence.py: measured Python target-call witness.
- tests/unit/test_mutation_harness.py: ten added cases, corrected lifecycle expectation,
  precise assignment-test names and explicit target imports in supported fixtures.

## Full-suite result — OBSERVED

Command: "$ROOT/repo/.venv/bin/python" -m pytest tests/unit/test_mutation_harness.py
-q -s -o addopts= -p no:rerunfailures -p no:flaky --tb=short.
PYTEST_ADDOPTS empty; retries 0; no maxfail.
Real output (Mac/APFS only):
    REVIEW1 DIRECT_EXIT=1 HARNESS_EXIT=1 SIGNAL=TEST_FAILURE PHASE_RETURNCODES=[0, 0, 1] (Mac/APFS only)
    REVIEW2 IMPORTED_FROM=SOURCE_CHECKOUT SIGNAL=ERROR HARNESS_EXIT=1 (Mac/APFS only)
    REVIEW3 shape=setattr PRESENT=REJECTED (Mac/APFS only)
    REVIEW3 shape=duck PRESENT=REJECTED (Mac/APFS only)
    UNVERIFIED equivalent_integer_bound: NO_TEST_FAILURE; 49 passed, 67 deselected in 2.39s (Mac/APFS only)
    REVIEW4 EXIT=1 (Mac/APFS only)
    116 passed in 150.63s (0:02:30)
    exit=0

All four requested findings are confirmed and closed within the stated
diagnostic boundary. No test deletion. This full run had no infrastructure
failures; the earlier targeted inspection errors remain recorded above.
Commits: finding 1 8151ca6982; finding 2 3e4ee85a31; finding 3 cc92089712;
finding 4 is the commit containing this final record. No push, PR or merge.
